---
phase: 25-v0-0-6-operator-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-v0-0-6-operator-debt-closure/25-TOKEN-LOCKDOWN.md
autonomous: false
requirements: [DEBT-07, DEBT-08]
user_setup:
  - service: npm (npmjs.com package settings)
    why: "npmjs.com token revocation and publishing-access lockdown have no CLI/API surface — only the npmjs.com web UI can perform them (release/SKILL.md Step 10 items 1-2)."
    dashboard_config:
      - task: "Revoke the granular access token used as the NPM_TOKEN fallback"
        location: "npmjs.com -> Access Tokens (account settings)"
      - task: "Set @jeremiewerner/motto publishing access to trusted-publisher-only (tokens disallowed)"
        location: "npmjs.com -> package @jeremiewerner/motto -> Settings -> Publishing access"

must_haves:
  truths:
    - "The granular npm access token used as the NPM_TOKEN fallback is revoked on npmjs.com and the GitHub Actions repo secret NPM_TOKEN is deleted, both confirmed absent (DEBT-07, D-06)"
    - "npm publishing access for @jeremiewerner/motto is set to trusted-publisher-only so a stolen token alone can no longer publish (DEBT-08, D-07)"
    - "A committed, leak-safe record documents both actions with command/verification/date and never any token value (D-08)"
  artifacts:
    - ".planning/phases/25-v0-0-6-operator-debt-closure/25-TOKEN-LOCKDOWN.md"
    - "npmjs.com publishing-access setting: trusted-publisher-only for @jeremiewerner/motto"
  key_links:
    - "gh secret list confirms NPM_TOKEN absent after gh secret delete (the scriptable verification half of DEBT-07)"
    - "the trusted-publisher-only lock must not break the already-working OIDC publish path proven live on 2026-07-05 (D-04)"
---

<objective>
Close the two credential-surface debts deferred from the v0.0.6 ship (release/SKILL.md Step 10 items 1-2), sequenced lock-first per D-03: revoke the granular npm token and delete the GitHub `NPM_TOKEN` secret (DEBT-07), and set npmjs.com publishing access to trusted-publisher-only (DEBT-08). Record both as a committed, leak-safe evidence artifact.

Purpose: The 2026-07-05 OIDC publish already proved trusted publishing works; until the fallback token is revoked and tokens are structurally disallowed, a stolen token could still publish. This plan makes zero-tokens a structural guarantee, not an incidental current state. It runs first so any patch release Plan 25-02 might trigger publishes through the locked config, live-proving the lock (D-02/D-03).
Output: A revoked token, a deleted `NPM_TOKEN` secret confirmed absent, a trusted-publisher-only publishing lock, and `25-TOKEN-LOCKDOWN.md` recording all of it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-v0-0-6-operator-debt-closure/25-CONTEXT.md
@.planning/phases/25-v0-0-6-operator-debt-closure/25-PATTERNS.md
@skills/release/SKILL.md
</context>

<artifacts_this_phase_produces>
Symbols and identifiers created by this plan (new here — exclude from source-drift acknowledgement):

- New file: `.planning/phases/25-v0-0-6-operator-debt-closure/25-TOKEN-LOCKDOWN.md` (leak-safe evidence record, structured after `22-SECRETS-SCAN.md`)
- npmjs.com publishing-access state change for `@jeremiewerner/motto`: trusted-publisher-only (tokens disallowed)
- Deletion (not creation) of GitHub Actions repo secret `NPM_TOKEN`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the NPM_TOKEN GitHub secret and confirm absent</name>
  <files>.planning/phases/25-v0-0-6-operator-debt-closure/25-TOKEN-LOCKDOWN.md</files>
  <read_first>
    - skills/release/SKILL.md (Step 10 item 1 — the exact `gh secret delete <name>` procedure and the "record that both actions were taken, never the token value itself" discipline)
    - .planning/milestones/v0.0.6-phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md (leak-safe record shape: opening paragraph stating what the file records and what it never contains, then one section per action with command/context/result fields)
    - .planning/STATE.md (Blockers/Concerns — confirms the secret was still present as of 2026-07-05 and names the two remaining operator actions)
  </read_first>
  <action>
    Run `gh secret list --repo jeremiewerner/motto` to observe the current state (the secret name from Phase 21's checkpoint is `NPM_TOKEN`). If `NPM_TOKEN` is present, run `gh secret delete NPM_TOKEN --repo jeremiewerner/motto`. If it is already absent, record that fact instead of running delete. Re-run `gh secret list --repo jeremiewerner/motto` and confirm `NPM_TOKEN` does not appear. Then create `.planning/phases/25-v0-0-6-operator-debt-closure/25-TOKEN-LOCKDOWN.md` following the `22-SECRETS-SCAN.md` shape: an opening paragraph stating the file is the committed, leak-safe record for DEBT-07/DEBT-08 and that it never contains any token value; a `## DEBT-07 — Token Revocation & Secret Deletion` section recording the `gh secret delete` command run (or "already absent"), the `gh secret list` verification output interpreted as present/absent (not the raw secret), and the date 2026-07-06; leave a placeholder line in that section for the npmjs.com token-revocation attestation that Task 2's checkpoint will fill. Record only the secret name and pass/fail — never any token value. Do not delete or recreate any git tag, and do not mint any new token.
  </action>
  <verify>
    <automated>gh secret list --repo jeremiewerner/motto 2>/dev/null | grep -v '^#' | grep -c 'NPM_TOKEN' | grep -qx 0 && echo ABSENT</automated>
  </verify>
  <done>`gh secret list --repo jeremiewerner/motto` shows no `NPM_TOKEN` entry, and `25-TOKEN-LOCKDOWN.md` exists with a DEBT-07 section recording the deletion/absence, verification, and date — no token value anywhere in the file.</done>
  <acceptance_criteria>
    - `gh secret list --repo jeremiewerner/motto` output contains no line matching `NPM_TOKEN`.
    - `25-TOKEN-LOCKDOWN.md` exists and contains a `## DEBT-07 — Token Revocation & Secret Deletion` heading.
    - `25-TOKEN-LOCKDOWN.md` opening paragraph states the file never records a token value.
    - No npm auth token or credential value appears anywhere in `25-TOKEN-LOCKDOWN.md` or the SUMMARY.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Maintainer revokes the npm token and locks publishing to trusted-publisher-only</name>
  <read_first>
    - skills/release/SKILL.md (Step 10 item 1 — revoke the granular access token on npmjs.com; Step 10 item 2 — set publishing access so tokens are disallowed and only the configured Trusted Publisher can publish; Step 8 item 4 — never mint a new NPM_TOKEN)
    - .planning/phases/25-v0-0-6-operator-debt-closure/25-CONTEXT.md (D-04 — if the lock breaks publishing, fix the Trusted Publisher config on npmjs.com and re-run the failed job; NEVER re-mint a token; D-07 — token-only publishing disallowed)
    - .planning/phases/25-v0-0-6-operator-debt-closure/25-TOKEN-LOCKDOWN.md (the record Task 1 created — Task 2's attestation fills the placeholder line and adds the DEBT-08 section)
  </read_first>
  <action>
    Blocking human checkpoint (no automated implementation — npmjs.com token revocation and publishing-access settings have no CLI/API surface): the maintainer performs two dashboard actions. (1) On npmjs.com Access Tokens, revoke the granular access token that was used as the `NPM_TOKEN` fallback. (2) On npmjs.com -> package `@jeremiewerner/motto` -> Settings -> Publishing access, set publishing access to trusted-publisher-only so tokens are disallowed and only the configured GitHub Actions Trusted Publisher (org `jeremiewerner`, repo `motto`, workflow `ci.yml`) can publish. Do NOT mint any replacement token. After performing both, append to `25-TOKEN-LOCKDOWN.md`: fill the DEBT-07 npmjs.com token-revocation attestation placeholder Task 1 left, and add a `## DEBT-08 — Trusted-Publisher-Only Lock` section recording the setting change (action, the npmjs.com Settings location, date 2026-07-06, result pass/fail) — never any token value.
  </action>
  <what-built>
    Task 1 deleted the GitHub `NPM_TOKEN` secret (the CI-side half). This checkpoint completes the registry-side half: revoking the actual token on npmjs.com and structurally disallowing token-based publishing so the deleted secret can never be re-added to a working publish path.
  </what-built>
  <verify>
    <human-check>npmjs.com's Access Tokens page no longer lists the revoked granular token, and @jeremiewerner/motto's package Settings -> Publishing access shows trusted-publisher-only (tokens disallowed).</human-check>
  </verify>
  <done>The granular npm token is revoked on npmjs.com, publishing access for @jeremiewerner/motto is locked to trusted-publisher-only, and both are recorded in `25-TOKEN-LOCKDOWN.md` with no token value present.</done>
  <how-to-verify>
    1. On npmjs.com, open account Access Tokens and confirm the granular token previously used for `NPM_TOKEN` is no longer listed (revoked).
    2. On npmjs.com, open package @jeremiewerner/motto -> Settings -> Publishing access and confirm it is set to trusted-publisher-only (token publishing disallowed).
    3. Reload both pages and confirm the states persist.
  </how-to-verify>
  <resume-signal>Type "approved — token revoked and publishing locked to trusted-publisher-only" once both npmjs.com pages confirm the states above, or describe what went wrong.</resume-signal>
  <acceptance_criteria>
    - Maintainer attests (recorded in `25-TOKEN-LOCKDOWN.md` and the SUMMARY) that the granular token is revoked and @jeremiewerner/motto publishing access is trusted-publisher-only.
    - `25-TOKEN-LOCKDOWN.md` contains a `## DEBT-08 — Trusted-Publisher-Only Lock` heading with action, verification, and date.
    - No npm auth token or credential value is recorded anywhere in the plan, `25-TOKEN-LOCKDOWN.md`, or the SUMMARY.
    - No replacement `NPM_TOKEN` was minted (D-04/Step 8 item 4).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stolen/leaked npm token → npm registry publish | Once publishing access is trusted-publisher-only, a token alone can no longer publish — the boundary is closed structurally, not just by rotation |
| GitHub Actions repo secret store → CI publish job | `NPM_TOKEN` deleted so no long-lived credential remains readable by any workflow or compromised action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | npm publish path still accepts a token after OIDC migration | high | mitigate | Set publishing access to trusted-publisher-only (DEBT-08) so a token alone cannot publish; revoke the specific fallback token (DEBT-07) |
| T-25-02 | Information Disclosure | Recording the token value into a committed phase artifact | high | mitigate | Leak-safe record discipline copied from 22-SECRETS-SCAN.md — record command/result/date only, explicit "never a token value" statement in the file's opening paragraph and every acceptance criterion |
| T-25-03 | Denial of Service | The lock breaks the publish path, tempting a token re-mint | medium | mitigate | D-04 stance recorded: fix the Trusted Publisher config on npmjs.com and re-run the failed job, never re-mint a token; the OIDC path was already proven live 2026-07-05 |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages; this plan makes registry/secret configuration changes and writes one Markdown record only |
</threat_model>

<verification>
- `gh secret list --repo jeremiewerner/motto` shows no `NPM_TOKEN`.
- Maintainer attestation (in `25-TOKEN-LOCKDOWN.md`) confirms the npmjs.com token is revoked and publishing access is trusted-publisher-only.
- `25-TOKEN-LOCKDOWN.md` contains DEBT-07 and DEBT-08 sections and no token value.
</verification>

<success_criteria>
- The granular npm token is revoked and `NPM_TOKEN` is deleted from GitHub secrets, confirmed absent (DEBT-07).
- npm publishing for @jeremiewerner/motto is locked to trusted-publisher-only (DEBT-08).
- All actions are captured in a committed, leak-safe record with no credential values (D-08).
</success_criteria>

<output>
Create `.planning/phases/25-v0-0-6-operator-debt-closure/25-01-SUMMARY.md` when done (record only the secret-list result, the maintainer attestations, and the record file's presence — never any credential value).
</output>
