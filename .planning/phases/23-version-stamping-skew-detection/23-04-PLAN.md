---
phase: 23-version-stamping-skew-detection
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - src/lint.js
  - src/build.js
  - bin/motto.js
  - test/lint.test.js
  - test/build.test.js
  - test/cli.test.js
  - test/dogfood.test.js
autonomous: true
requirements: [VER-02, VER-04, VER-06]
must_haves:
  truths:
    - "lintProject/buildProject return an additive warnings[] array; a version-skewed project yields a skew warning while ok/exit code stay unchanged"
    - "A project with no mottoVersion stamp (incl. this repo's own tree) lints/builds with warnings [] — no skew warning, no crash"
    - "renderResult prints ⚠ warning lines to stderr independent of ok and --quiet; --format json includes warnings verbatim with zero code change"
    - "lintProject/buildProject never rewrite motto.yaml (byte-identical before/after), guarded by a test"
  artifacts:
    - src/lint.js
    - src/build.js
    - bin/motto.js
    - test/lint.test.js
    - test/build.test.js
    - test/cli.test.js
  key_links:
    - "lint.js/build.js call checkSkew(config.mottoVersion, getOwnVersion()) and push to warnings[], gated on no config error already reported for mottoVersion"
    - "bin/motto.js renderResult renders result.warnings as ⚠ stderr lines without touching exitCode"
---

<objective>
Wire the skew advisory end-to-end: `lintProject` and `buildProject` gain an additive `warnings[]` array populated by `checkSkew(config.mottoVersion, getOwnVersion())`; `bin/motto.js`'s `renderResult` prints `⚠` lines to stderr independent of `ok`/`quiet`; and integration tests cover the skew-both-directions, no-stamp (VER-04), non-blocking (VER-02), and never-rewrite (VER-06) behaviors. `--format json` requires zero rendering change (warnings serialize verbatim).

Purpose: This is the user-facing surface of the feature — the direction-aware advisory that never changes exit code or `ok` (VER-02), stays silent for pre-stamp projects (VER-04), and never mutates motto.yaml (VER-06). It depends on version.js (checkSkew/getOwnVersion) and config.js (the mottoVersion-error gate).
Output: modified src/lint.js, src/build.js, bin/motto.js; extended test/lint.test.js, test/build.test.js, test/cli.test.js; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md
@.planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md
@src/version.js
@src/config.js
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04 slice)

Modified `src/lint.js`:
- Imports `checkSkew`, `getOwnVersion` from `./version.js`
- `lintProject` builds a local `warnings` array and returns it as a new additive field on every return path
- Skew check gated on config.mottoVersion having no reported error

Modified `src/build.js`:
- Imports `checkSkew`, `getOwnVersion` from `./version.js`
- `buildProject` computes the same skew warning at STEP 2 (config already valid via lint gate) and returns `warnings[]`

Modified `bin/motto.js`:
- `renderResult` renders `result.warnings` as `⚠ <skill>: <message>` stderr lines, independent of ok/quiet; exitCode logic unchanged

Modified test files:
- `test/lint.test.js`, `test/build.test.js`: fixtures for skewed (both directions), no-stamp, malformed-stamp (no double-report), and never-rewrite (VER-06) cases
- `test/cli.test.js`: spawned-CLI assertion that ⚠ prints to stderr and exit code is unaffected
- `test/dogfood.test.js`: extend the live lintProject(REPO_ROOT) test to assert no skew warning (success criterion 3 proof)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire checkSkew into lintProject and buildProject warnings[] (VER-02, VER-04)</name>
  <files>src/lint.js, src/build.js</files>
  <read_first>
    - src/lint.js (processConfig lines 43-61 — needs to expose the parsed config; lintProject lines 320-357 — all return paths get the additive warnings field)
    - src/build.js (buildProject STEP 1 lint gate lines 89-95, STEP 2 config re-read lines 97-99, STEP 7 return lines 206-214; plugin.json version stamping around line 199 which must stay sourced from config.version)
    - src/version.js (checkSkew, getOwnVersion signatures)
    - src/config.js (the mottoVersion validation block — to know when a mottoVersion error is already present so skew is gated)
    - .planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md (Pattern 3 lines 243-257; Anti-Patterns lines 259-265 gating rule; D-R1 double-report avoidance lines 340-350)
    - .planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md (src/lint.js section lines 118-157 — processConfig widening + return-shape extension; src/build.js section lines 160-181)
  </read_first>
  <behavior>
    - lintProject on a project whose mottoVersion is OLDER than the live tool → return.warnings contains one {skill:'motto.yaml', message} matching /check the upgrade ledger/; return.ok is governed solely by errors.length===0 (unchanged); the same project with zero real errors stays ok:true.
    - lintProject on a project whose mottoVersion is NEWER than the live tool → warnings message matches /upgrade motto/.
    - lintProject on a project with NO mottoVersion → return.warnings is [] (empty, present); ok/count unchanged; no crash.
    - lintProject on a project with a malformed mottoVersion → the malformed value is an error (ok:false via config.js), and skew is NOT additionally reported as a warning (no double-report).
    - buildProject mirrors the same: warnings[] present on all return paths; a skewed but otherwise-valid project builds (ok:true) with the skew warning; plugin.json's version key still equals config.version, never mottoVersion.
    - Every lintProject/buildProject return path includes a warnings array (defaults to []).
  </behavior>
  <action>
    In src/lint.js: import { checkSkew, getOwnVersion } from './version.js'. CHOSEN APPROACH (committed, not optional): re-run loadConfig inline in lintProject to obtain the parsed config — do NOT widen processConfig's return shape. This mirrors the codebase's existing accepted precedent in src/build.js:98-99 (`const configText = await readFile(join(projectRoot, 'motto.yaml'), 'utf8'); const { config } = loadConfig(configText);` — the RESEARCH-blessed "Option A — re-read" convention). Verified against live source: processConfig (src/lint.js:43-61) destructures only `{ errors: configErrors }` from loadConfig and returns nothing, so it exposes no config object; re-reading is the reuse pattern already sanctioned in build.js. Concretely: after processConfig has run (so any motto.yaml read/parse/validation errors are already in errors[]), re-read motto.yaml and call loadConfig to get `config` — inside a try/catch that collapses any read failure to a null config (never throw; if the file is unreadable, processConfig already pushed the error, and a null config simply means no skew check). Build a local `warnings = []` near the top of lintProject.

    GATING (Issue 1 — explicit, see acceptance criteria): compute skew ONLY when `config` is non-null AND `parseVersion(config.mottoVersion)` is truthy — i.e. the stamp is present and well-formed. Because config.js reports a malformed mottoVersion as an errors[] entry (Plan 23-02) and parseVersion returns null for every malformed/absent value, this single guard (`const parsed = parseVersion(config?.mottoVersion); if (parsed) { const w = checkSkew(config.mottoVersion, getOwnVersion()); if (w) warnings.push(w); }`) guarantees a malformed stamp is reported exactly once (as an error) and NEVER also as a skew warning — no double-report. Import parseVersion from './version.js' alongside checkSkew/getOwnVersion. Add `warnings` to EVERY return object in lintProject (the two early returns at src/lint.js:338 and :344, and the final return at :356) — all must carry the field so consumers never see undefined. Keep `ok: errors.length === 0` exactly as-is; warnings never affect ok.

    In src/build.js: import { checkSkew, getOwnVersion } from './version.js'. At STEP 2 (config already valid because the lint gate passed), compute the skew warning directly via checkSkew(config.mottoVersion, getOwnVersion()) into a local warnings array (do NOT re-derive from lintResult's internal variables — avoids coupling to lint.js internal naming). Add `warnings` to the STEP 7 success return (lines 207-213) and to the early lint-fail return (line 94) — for the lint-fail path, warnings may be [] or forwarded from lintResult, but must be present. Do NOT touch the plugin.json write at line 199: its version key must remain `config.version`, never mottoVersion (regression guard — asserted in Task 3).
  </action>
  <verify>
    <automated>node --test test/lint.test.js test/build.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/lint.js and src/build.js both import checkSkew and getOwnVersion from './version.js'.
    - Every return statement in lintProject includes a `warnings` field (verifiable: `grep -c 'warnings' src/lint.js` > 0 and each return object lists it).
    - `ok: errors.length === 0` in lint.js is unchanged — warnings do not enter the ok computation.
    - build.js STEP 7 return and the lint-fail early return both include `warnings`.
    - lintProject uses the re-read approach (re-runs loadConfig inline to get `config`), NOT a widened processConfig return — mirroring src/build.js:98-99. Verifiable: processConfig's signature/return is unchanged (`grep -n 'return { config }' src/lint.js` finds nothing inside processConfig) and lintProject contains a second `loadConfig(` call (`grep -c 'loadConfig' src/lint.js` >= 1 in lint.js, from a re-read inside lintProject).
    - EXPLICIT gate (Issue 1): the skew call is guarded by `parseVersion(config?.mottoVersion)` being truthy — a malformed mottoVersion (parseVersion → null, already reported as an error by config.js) does NOT reach checkSkew and therefore produces NO skew warning. Verifiable: for a fixture whose mottoVersion is malformed (e.g. bare number `7`), lintProject returns exactly one mottoVersion error in errors[] and warnings[] contains no skew entry (a malformed stamp is reported once, as an error, never also as a warning). The guard condition (`if (parseVersion(config.mottoVersion)) { ... checkSkew ... }`, or equivalent truthy-parsed check) appears literally in src/lint.js.
    - `node --test test/lint.test.js test/build.test.js` passes (with the fixtures added in Task 3).
  </acceptance_criteria>
  <done>lintProject/buildProject return additive warnings[]; skew computed direction-aware and gated; ok/exit contract untouched; plugin.json version untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Render warnings[] as ⚠ stderr lines in renderResult (VER-02)</name>
  <files>bin/motto.js</files>
  <read_first>
    - bin/motto.js (renderResult lines 193-206 and its doc comment lines 181-192 — the exact structure to extend; note quiet only suppresses the success line and process.exit() is never used)
    - .planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md (D-R2 lines 352-356 — warnings to stderr independent of ok/quiet; json needs no change)
    - .planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md (bin/motto.js section lines 184-211 — the exact ⚠ loop and the keep-exitCode-unchanged rule)
  </read_first>
  <behavior>
    - Text mode: for any result with a non-empty warnings[], renderResult writes one `⚠ <skill>: <message>` line per warning to stderr, whether ok is true or false, and whether or not --quiet is set (quiet only suppresses the success line).
    - The existing success-line and ✗-error-line behavior is unchanged.
    - JSON mode: renderResult already serializes the whole result (including warnings) verbatim — no code change to the json branch; a result with warnings round-trips through JSON.stringify.
    - exitCode is set to 1 iff !result.ok — warnings never change it.
  </behavior>
  <action>
    In renderResult, add a loop over `result.warnings ?? []` writing `⚠ ${w.skill}: ${w.message}\n` to process.stderr, placed so it runs UNCONDITIONALLY relative to the existing ok/quiet if/else (i.e. warnings print regardless of which branch fired and regardless of quiet). Leave the `format === 'json'` branch untouched (it already includes warnings via JSON.stringify(result)) — but ensure the warnings loop does NOT also fire in json mode to avoid duplicate output (guard the ⚠ loop to text mode only, mirroring how the ✗ error loop is text-mode-only). Keep `if (!result.ok) process.exitCode = 1;` exactly as-is. Update the renderResult doc comment to note warnings rendering to stderr independent of ok/quiet.
  </action>
  <verify>
    <automated>node --test test/cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/motto.js renderResult contains a loop emitting `⚠ ` lines to process.stderr from result.warnings.
    - The ⚠ loop runs in text mode independent of quiet and independent of result.ok, and does NOT fire in json mode (no duplicate warning output alongside JSON).
    - `if (!result.ok) process.exitCode = 1;` is unchanged.
    - `node --test test/cli.test.js` passes.
  </acceptance_criteria>
  <done>renderResult prints ⚠ warnings to stderr independent of ok/quiet; json mode unchanged; exitCode logic untouched; cli tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration + never-rewrite + no-stamp fixtures (VER-02, VER-04, VER-06)</name>
  <files>test/lint.test.js, test/build.test.js, test/cli.test.js, test/dogfood.test.js</files>
  <read_first>
    - test/lint.test.js, test/build.test.js (existing lintProject/buildProject mkdtemp fixture conventions; existing result-shape assertions)
    - test/cli.test.js (existing spawned-CLI assertion pattern for stdout/stderr/exit code)
    - test/dogfood.test.js (the live `lintProject(REPO_ROOT)` assertion at lines 35-44 — currently asserts ok:true/count:3/errors:[]; extend it to also assert no skew warning per Issue 4. REPO_ROOT anchored via import.meta.url at line 16. This is the live no-stamp fixture and never-hardcode-version convention)
    - src/init.js (scaffoldProject — used to produce a stamped fixture, then hand-edit the stamp for skew fixtures)
    - .planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md (Code Examples: missing-stamp lines 378-398, direction pair lines 427-450, never-rewrite lines 452-471; D-R4 lines 368-374 — use a dedicated temp-dir no-stamp fixture, keep REPO_ROOT unstamped)
  </read_first>
  <behavior>
    - No-stamp (VER-04): a motto.yaml with no mottoVersion (scaffold-then-strip, or a hand-written fixture, OR the live REPO_ROOT/dogfood tree) → lintProject/buildProject return warnings [] (or undefined-safe empty), ok unchanged, no throw.
    - Dogfood no-stamp proof for success criterion 3 (Issue 4): the existing live `lintProject(REPO_ROOT)` assertion in test/dogfood.test.js:35-44 is extended to ALSO assert that the result carries no skew warning — i.e. `(result.warnings ?? []).length === 0` (empty/absent warnings), explicitly tying Motto's own unstamped tree to success criterion 3 (lints with no skew warning, no crash). This complements the dedicated temp-dir no-stamp fixture; it is the live-tree proof.
    - Malformed stamp, no double-report (Issue 1 integration proof): a fixture whose mottoVersion is malformed (e.g. bare number `7`, or `mottoVersion: [0,0,7]`) → lintProject returns ok:false with EXACTLY ONE mottoVersion error in errors[] (matching /mottoVersion/i) AND warnings[] contains no skew entry (no /check the upgrade ledger/ or /upgrade motto/ message) AND no throw. This proves at the integration layer what Plan 23-02 proves at the loadConfig unit layer: a malformed stamp is an error, never additionally a warning.
    - Skew, tool newer (project behind): a fixture whose mottoVersion is a fake older version than the live tool → warnings contains a /check the upgrade ledger/ message; ok/exit unchanged.
    - Skew, project newer: a fixture whose mottoVersion is a fake NEWER version (e.g. '99.0.0') than the live tool → warnings contains /upgrade motto/; ok/exit unchanged.
    - Non-blocking (VER-02): a skewed-but-otherwise-valid project → result.ok is true and (via cli.test.js spawned run) process exit code is 0 while a ⚠ line appears on stderr.
    - Never-rewrite (VER-06): capture motto.yaml bytes before and after lintProject AND buildProject → byte-identical; assert equality.
    - plugin.json regression: after buildProject, the emitted plugin.json version equals the project version, not mottoVersion.
  </behavior>
  <action>
    Add fixtures to test/lint.test.js and test/build.test.js using the mkdtemp convention. For skew fixtures, scaffold via scaffoldProject then hand-edit motto.yaml's mottoVersion line to a fake version guaranteed older ('0.0.1') and a fake version guaranteed newer ('99.0.0') than any real Motto release — never assert against the live literal; assert the message direction (/check the upgrade ledger/ vs /upgrade motto/) and that ok is unchanged. For the no-stamp case, use a DEDICATED temp-dir fixture with mottoVersion stripped (per D-R4 — do NOT rely on REPO_ROOT being unstamped for the assertion, though a dogfood.test.js assertion that REPO_ROOT lints with warnings [] is also acceptable as a complementary live proof). MALFORMED-STAMP INTEGRATION FIXTURE (Issue 1 / no double-report): scaffold, then hand-edit motto.yaml's mottoVersion line to a malformed value (bare number `mottoVersion: 7`, or `mottoVersion: [0,0,7]`); run lintProject and assert (a) ok is false, (b) errors[] contains exactly one entry matching /mottoVersion/i, (c) warnings[] contains NO entry matching /check the upgrade ledger/ or /upgrade motto/, and (d) assert.doesNotThrow around the lintProject call. Add the never-rewrite test (VER-06): scaffold, read motto.yaml bytes, run lintProject, re-read, assert.strictEqual(after, before); mirror for buildProject. Add a plugin.json regression assertion in build.test.js that the built plugin.json version equals config.version. In test/cli.test.js, add a spawned-CLI test: run lint/build against a skewed fixture, assert the child process exit code is 0 (or unchanged) AND a ⚠ line is present on stderr. DOGFOOD EXTENSION (Issue 4 / success criterion 3): in test/dogfood.test.js, extend the existing `lintProject(REPO_ROOT)` test (lines 35-44) to also assert `(result.warnings ?? []).length === 0` — proving Motto's own unstamped tree lints with no skew warning and no crash. Do NOT stamp the repo's own motto.yaml (it stays unstamped per D-R4). Use fake fixture versions throughout; grep-clean of real version literals.
  </action>
  <verify>
    <automated>node --test test/lint.test.js test/build.test.js test/cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/lint.test.js test/build.test.js test/cli.test.js` passes with zero failures.
    - A no-stamp fixture test asserts warnings is empty and no throw (VER-04).
    - The dogfood test (test/dogfood.test.js:35-44) explicitly asserts `(result.warnings ?? []).length === 0` for lintProject(REPO_ROOT), tying Motto's own unstamped tree to success criterion 3 (no skew warning, no crash) (Issue 4).
    - A malformed-stamp integration fixture test (Issue 1) asserts lintProject returns ok:false with exactly one /mottoVersion/i error in errors[], NO skew warning in warnings[], and no throw — proving no double-report at the integration layer.
    - Two skew-direction tests exist: one matches /check the upgrade ledger/, one matches /upgrade motto/ (VER-02 direction-aware).
    - A never-rewrite test asserts motto.yaml bytes are identical before/after both lintProject and buildProject (VER-06).
    - A spawned-CLI test asserts exit code is unaffected by a skew warning while ⚠ appears on stderr.
    - A build regression test asserts plugin.json version equals config.version (not mottoVersion).
    - `grep -nE '"0\.0\.[0-9]"' test/lint.test.js test/build.test.js test/cli.test.js` shows no new hardcoded live-Motto-version literals in assertions (fake '0.0.1'/'99.0.0' fixtures are allowed as intentional skew fixtures, not live-version assertions).
    - Full suite `npm test` is green.
  </acceptance_criteria>
  <done>Integration fixtures prove skew both directions (VER-02), no-stamp silence (VER-04), and never-rewrite (VER-06); plugin.json regression guarded; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motto.yaml → lintProject/buildProject | mottoVersion read from an untrusted file feeds checkSkew; must be gated so malformed values never reach an unguarded comparison |
| result.warnings → renderResult (CLI stderr) | Warning message text is written to the terminal; it is display-only, never used as a path/shell argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-08 | Denial of Service | checkSkew called on a malformed stamp during lint/build | high | mitigate | Skew check gated on "no mottoVersion error reported"; parseVersion also null-guards internally so a malformed value collapses to null (no throw, no double-report) |
| T-23-09 | Tampering (data integrity) | lint/build accidentally rewriting motto.yaml | high | mitigate | No write path added to lint/build; VER-06 never-rewrite test asserts byte-identical motto.yaml before/after both operations |
| T-23-10 | Elevation / contract break | skew warning flipping exit code or ok | high | mitigate | warnings kept strictly additive; `ok = errors.length === 0` unchanged; exitCode logic in renderResult unchanged; cli.test.js asserts exit code unaffected (VER-02) |
| T-23-11 | Tampering (data integrity) | mottoVersion leaking into plugin.json version key | medium | mitigate | build.js line-199 write left sourcing config.version; build.test.js regression asserts plugin.json version equals config.version |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages this phase (D-06) — no install task; nothing to audit |
</threat_model>

<verification>
- `node --test test/lint.test.js test/build.test.js test/cli.test.js` green.
- Full suite `npm test` green (husky pre-commit).
- REPO_ROOT/dogfood tree lints clean with warnings [] (no-stamp live proof, D-R4) — the dogfood test (test/dogfood.test.js:35-44) explicitly asserts `(result.warnings ?? []).length === 0` for lintProject(REPO_ROOT), tying it to success criterion 3; this repo's own motto.yaml stays unstamped (`grep -v '^#' motto.yaml | grep -c mottoVersion` = 0).
- Never-rewrite: motto.yaml byte-identical before/after lint and build.
</verification>

<success_criteria>
- Direction-aware skew advisory surfaces on lint/build without changing ok or exit code (VER-02).
- No-stamp projects (incl. this repo's own tree) stay silent and crash-free (VER-04).
- lint/build never rewrite motto.yaml (VER-06), guarded by a test.
</success_criteria>

<output>
Create `.planning/phases/23-version-stamping-skew-detection/23-04-SUMMARY.md` when done
</output>
