---
phase: 23-version-stamping-skew-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/version.js
  - test/version.test.js
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "getOwnVersion() returns the running tool's package.json version string, or null on any read/parse failure — never throws"
    - "parseVersion() returns {major,minor,patch} for a leading X.Y.Z string (v-prefix tolerated) and null for every non-string or non-matching value"
    - "checkSkew() returns null for equal/absent/unparsable versions, and a direction-aware {skill,message} descriptor otherwise"
  artifacts:
    - src/version.js
    - test/version.test.js
  key_links:
    - "src/version.js exports getOwnVersion, parseVersion, checkSkew — consumed by config.js, init.js, lint.js, build.js in later plans"
---

<objective>
Create the pure foundation module `src/version.js` — the only file every later plan in this phase depends on. It provides three exports: `getOwnVersion()` (reads Motto's own package.json version, never throws), `parseVersion()` (pure 3-segment version parser), and `checkSkew()` (pure direction-aware skew comparator producing a warning descriptor).

Purpose: Isolate all version-parsing and skew-comparison logic in one pure, exhaustively-unit-tested module so downstream orchestrators (config.js, init.js, lint.js, build.js) consume a stable, never-throw interface (VER-03).
Output: `src/version.js` (new) and `test/version.test.js` (new), fully green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md
@.planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 slice)

New file `src/version.js` exporting:
- `getOwnVersion()` — function, `() => string|null`
- `parseVersion(v)` — function, `(unknown) => {major:number,minor:number,patch:number}|null`
- `checkSkew(stampedVersion, toolVersion)` — function, `(string|undefined, string|null) => {skill:string, message:string}|null`
- `VERSION_RE` — module-internal regex constant (not necessarily exported)
- `compareParsed(a,b)` — module-internal helper (not necessarily exported)

New file `test/version.test.js` — unit tests for the above.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/version.js pure module (getOwnVersion, parseVersion, checkSkew)</name>
  <files>src/version.js</files>
  <read_first>
    - .planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md (Pattern 1, lines 132-216 — the full target implementation is specified verbatim; D-R3 message wording lines 358-366)
    - .planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md (src/version.js section, lines 22-54 — doc-comment + never-throw conventions)
    - src/config.js (doc-comment header lines 1-9 and defensive try/catch lines 44-52 — the house style to mirror)
  </read_first>
  <behavior>
    - getOwnVersion: returns a non-empty version string equal to the value read from the repo's own package.json "version" key; returns null if the read or JSON.parse fails; result is memoized (cached) across calls; never throws for any reason.
    - parseVersion('1.2.3') → {major:1,minor:2,patch:3}; parseVersion('v1.2.3') → {major:1,minor:2,patch:3}; parseVersion('0.0.7-beta.1') → {major:0,minor:0,patch:7} (suffix ignored by design); parseVersion for a number, array, object, boolean, null, undefined, '', '0.7', 'latest' → null; never throws.
    - checkSkew equal ('1.2.3','1.2.3') → null; checkSkew absent (undefined,'1.2.3') → null; checkSkew unparsable ('garbage','1.2.3') → null; checkSkew(null-tool) ('1.2.3', null) → null.
    - checkSkew stamped older than tool ('1.2.3','1.3.0') → {skill:'motto.yaml', message} where message contains both '1.2.3' and '1.3.0' and the phrase 'check the upgrade ledger'.
    - checkSkew stamped newer than tool ('1.3.0','1.2.3') → {skill:'motto.yaml', message} where message contains both versions and the phrase 'upgrade motto'.
  </behavior>
  <action>
    Create src/version.js as a pure module (single fs read in getOwnVersion, everything else pure). Mirror src/config.js's JSDoc header convention: a top-of-file block comment stating the module purpose, that getOwnVersion is the ONE function touching the filesystem (a single readFileSync), and that parseVersion/checkSkew are pure string-in/object-out and never throw. Implement per 23-RESEARCH.md Pattern 1 (lines 137-216) as the starting point.

    getOwnVersion: memoize via a module-level `cached` variable initialized undefined; on first call resolve package.json via `new URL('../package.json', import.meta.url)`, readFileSync utf8, JSON.parse, set cached to the version only when `typeof pkg.version === 'string'` else null; wrap in try/catch that sets cached to null on any failure. Do NOT use `process.env.npm_package_version` and do NOT use `import ... with { type: 'json' }` (both rejected in RESEARCH Alternatives / Don't Hand-Roll for concrete reasons: env var absent under npx/direct invocation, import-assertion emits ExperimentalWarning on Node 20.0.0–20.18.2).

    parseVersion: define `VERSION_RE = /^v?(\d+)\.(\d+)\.(\d+)/` (deliberately NOT end-anchored so trailing prerelease/build suffixes are ignored — add a one-line code comment stating this is an intentional narrowing, not full semver precedence, per Pitfall 5 and the V5-input-validation security control). Guard `typeof v !== 'string'` returning null before any string method; trim, exec, return null on no-match, else return {major,minor,patch} via Number() on the three capture groups.

    compareParsed(a,b): module-internal (not exported) numeric tuple comparator returning -1/0/1 across major then minor then patch.

    checkSkew(stampedVersion, toolVersion): parseVersion both; if either is null return null (nothing to compare — never an error here); compareParsed; return null on equal; for stamped < tool return the "check the upgrade ledger" message, for stamped > tool return the "upgrade motto" message. Use exactly the two message templates from 23-RESEARCH.md D-R3 (lines 361-363), each naming both version strings and the {skill:'motto.yaml'} descriptor shape. Treat this wording as a working draft per D-R3 (VER-F1 defers final wording validation to magma) — do not add structured JSON skew fields (VER-F1 deferred).
  </action>
  <verify>
    <automated>node -e "import('./src/version.js').then(m => { const ok = m.parseVersion('v1.2.3').patch === 3 && m.parseVersion(7) === null && m.checkSkew('1.2.3','1.2.3') === null && /check the upgrade ledger/.test(m.checkSkew('1.2.3','1.3.0').message) && /upgrade motto/.test(m.checkSkew('1.3.0','1.2.3').message) && typeof m.getOwnVersion() === 'string'; process.exit(ok ? 0 : 1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `src/version.js` exports named functions getOwnVersion, parseVersion, checkSkew.
    - `getOwnVersion()` returns a string equal to package.json's version field (currently "0.0.6", read live — never hardcoded).
    - `parseVersion` returns null (not a throw) for each of: number 7, array [0,0,7], object, boolean true, null, undefined, '', 'latest', '0.7'.
    - Source contains the exact regex `/^v?(\d+)\.(\d+)\.(\d+)/` with no trailing `$` anchor, plus a comment noting the intentional-narrowing / non-full-semver simplification.
    - `checkSkew` message for stamped<tool contains substring `check the upgrade ledger`; for stamped>tool contains substring `upgrade motto`; both contain both version strings; equal/absent/unparsable return null.
    - The verify command exits 0.
  </acceptance_criteria>
  <done>src/version.js exists with the three exports; the inline verify command exits 0; no throw reachable from any input shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/version.test.js — exhaustive pure unit tests (VER-03 + parse matrix)</name>
  <files>test/version.test.js</files>
  <read_first>
    - src/version.js (the module under test — read the just-created exports and signatures)
    - .planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md (Code Examples: direction-aware skew test pair lines 427-450; Pitfall 6 version-literal brittleness lines 325-330)
    - .planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md ("Never hardcode version literals in tests" lines 257-259)
    - test/config.test.js (existing describe/it + node:assert conventions to mirror)
  </read_first>
  <behavior>
    - parseVersion cases: equal-shape parses for '1.2.3', 'v1.2.3'; independent major/minor/patch extraction; suffix-ignoring '0.0.7-beta.1' → patch 7; null for number, array, object, boolean, null, undefined, '', '0.7', '0.0.7.1' (via leading match this yields {0,0,7} — assert the documented behavior, whichever the regex produces), 'latest'.
    - checkSkew: equal → null; stamped older → message matches /check the upgrade ledger/ and contains both versions; stamped newer → message matches /upgrade motto/ and contains both versions; unparsable stamp → null; null tool version → null.
    - getOwnVersion: returns a truthy string; equals the version read directly from package.json at test time (read live, never compared to a literal).
  </behavior>
  <action>
    Create test/version.test.js using node:test (`describe`/`it`) and node:assert, matching test/config.test.js style. Cover the full parseVersion matrix, both checkSkew directions plus equal/absent/unparsable, and getOwnVersion. For any test needing two DIFFERENT fixture versions use clearly-fake strings like '1.2.3' vs '1.2.4' / '1.3.0' — never Motto's real current version. For getOwnVersion, assert it equals a live read of package.json's version (e.g. read package.json in the test and compare), and assert.ok it is a non-empty string — do NOT assert.strictEqual against a literal '0.0.6' or '0.0.7' (Pitfall 6). Include an explicit assert.doesNotThrow around parseVersion for each adversarial non-string input to prove the never-throw boundary at the unit layer.
  </action>
  <verify>
    <automated>node --test test/version.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/version.test.js` passes with zero failing tests.
    - `grep -nE '"0\.0\.[0-9]"' test/version.test.js` returns no hits inside assertion calls (only fake fixtures '1.2.3'/'1.2.4'/'1.3.0' or live package.json reads).
    - Test file contains at least one `assert.doesNotThrow` wrapping parseVersion on a non-string input.
    - Both skew directions asserted: one test matches /check the upgrade ledger/, another matches /upgrade motto/.
  </acceptance_criteria>
  <done>test/version.test.js green under `node --test`; version literals not hardcoded; never-throw asserted at unit layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → getOwnVersion | Tool reads its own on-disk package.json; a corrupt/unreadable file must degrade to null, not crash |
| version string args → parseVersion/checkSkew | Values ultimately originate from hand-edited motto.yaml (untrusted); must be type-guarded before any string operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Denial of Service | parseVersion/checkSkew on malformed input | high | mitigate | `typeof v !== 'string'` guard returns null before any `.trim`/`.exec`; unit tests assert.doesNotThrow across the full adversarial matrix (VER-05 pure-layer coverage) |
| T-23-02 | Denial of Service | getOwnVersion reading a corrupt package.json | medium | mitigate | try/catch collapses any read/parse failure to null; caller treats null as "skew check unavailable" |
| T-23-03 | Tampering / Injection | future dynamic use of raw version string | low | accept | Not present in this design — version string used only for comparison/display; one-line code comment documents the constraint for future maintainers (V5 note) |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages this phase (D-06) — no install task exists; nothing to audit |
</threat_model>

<verification>
- `node --test test/version.test.js` green.
- Inline node -e smoke check (Task 1 verify) exits 0.
- No version literals hardcoded in test assertions (grep clean).
</verification>

<success_criteria>
- src/version.js provides getOwnVersion, parseVersion, checkSkew as a never-throw pure interface.
- Direction-aware messages (VER-03) proven by unit tests in both directions.
- Foundation ready for consumption by plans 02, 03, 04.
</success_criteria>

<output>
Create `.planning/phases/23-version-stamping-skew-detection/23-01-SUMMARY.md` when done
</output>
