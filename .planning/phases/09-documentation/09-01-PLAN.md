---
phase: 09-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["README.md"]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "README.md exists at the repo root (none existed before)."
    - "README documents installing the CLI from npm with the verbatim command `npm i -g @jeremiewerner/motto`, then invoking `motto lint` / `motto build` (DOC-01)."
    - "README documents adding the marketplace with `/plugin marketplace add jeremiewerner/motto` and installing skills with `/plugin install motto@motto` (DOC-02)."
    - "README documents Claude Desktop usage: the `~/.claude/skills/` symlink one-liner and the web-upload zip one-liner, using the concrete example skill `author-skill` (DOC-03)."
    - "README contains one compact end-to-end example (scaffold -> lint -> build -> install) per D-03, in addition to per-path copy-paste blocks."
    - "README follows author-first ordering (D-02): what-is-Motto intro -> authoring quickstart -> distribution -> consumption -> Claude Desktop -> brief dev/contributing."
    - "README uses only current identifiers per D-06; the stale pre-rename hyphenated plugin identifier appears ZERO times anywhere in README.md."
  artifacts:
    - "README.md"
  key_links:
    - "README.md is auto-included in the npm tarball via the package.json `files` allowlist, so it becomes the npmjs.com package page and must read well there."
    - "Every install identifier in README matches `.claude-plugin/marketplace.json` (marketplace `motto`, plugin `motto`, source `@jeremiewerner/motto`) and `package.json` (`bin.motto`, scoped name) verbatim."
---

<objective>
Author the project `README.md` at the repo root — Motto's front-door doc and, because it is auto-included in the npm tarball, the npmjs.com package page. It states what Motto is and documents every supported install/use path: the npm CLI (DOC-01), the self-hosted marketplace + skill install (DOC-02), and Claude Desktop usage via the `~/.claude/skills/` symlink and web-upload zip one-liners (DOC-03).

Ordering is author-first (D-02): what-is-Motto intro -> authoring quickstart (npm CLI -> scaffold -> lint -> build -> publish) -> distribution (marketplace) -> consumption (install skills) -> Claude Desktop -> brief contributing/dev section. Per-path copy-paste command blocks PLUS one compact end-to-end example (D-03). Every command uses CURRENT identifiers verbatim (D-06) — the stale pre-rename hyphenated plugin id must never appear.

Purpose: A new user can install the CLI, install the skills, and use the built skills in Claude Desktop without guesswork.
Output: `README.md` at the repo root (this phase's only file deliverable).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-documentation/09-CONTEXT.md

# Source-of-truth for identifiers and the authoring/publish narrative
@.claude-plugin/marketplace.json
@package.json
@motto.yaml
@skills/setup-project/SKILL.md
@skills/author-skill/SKILL.md
@skills/release/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with intro, authoring quickstart (DOC-01), and the compact end-to-end example (D-03)</name>
  <files>README.md</files>
  <read_first>
    - package.json — scoped name `@jeremiewerner/motto`, `bin.motto`, description ("Framework for authoring, validating, and packaging Claude Code Agent Skills"), keywords, homepage. Seed the title/tagline/intro from these.
    - motto.yaml — project config example (name/version/description/plugins.public=motto) to show in the quickstart.
    - skills/setup-project/SKILL.md — directory layout + motto.yaml walkthrough; align the scaffold step and point users at `/motto:setup-project`.
    - skills/author-skill/SKILL.md — how to write a conforming SKILL.md; align the authoring section and point users at `/motto:author-skill`.
    - skills/release/SKILL.md — the real publish flow (`npm version` -> dogfood -> tarball verify -> `npm publish`, plus the v0.0.4-onward note); align the publish step, do not duplicate the whole checklist.
    - .planning/phases/09-documentation/09-CONTEXT.md — locked decisions D-01..D-06.
  </read_first>
  <action>
    Create README.md at the repo root. Open with an H1 title and a one-line tagline drawn from the package.json description, then a short "What is Motto" section (2-4 sentences) explaining that Motto is a framework for authoring, validating, and packaging Claude Code Agent Skills, and that its core value is the strict schema + linter that guarantees skills conform before they ship (per D-01). Briefly note the build output is plain, standard Agent Skills that any agent loads with no knowledge of Motto.

    Then write the authoring quickstart (author-first ordering per D-02), as per-path copy-paste command blocks:
    1. Install the CLI from npm using the verbatim command `npm i -g @jeremiewerner/motto`, then show it is invoked as `motto` (`motto lint`, `motto build`). This satisfies DOC-01.
    2. Scaffold a project: point the reader at the `/motto:setup-project` skill (the Claude Code slash command) for the guided directory-layout + motto.yaml walkthrough, and show the fixed source tree (`skills/<name>/SKILL.md`, `shared/references/`, `motto.yaml`, generated `dist/`) aligned with skills/setup-project/SKILL.md. Include the small motto.yaml example from motto.yaml (name/version/description/plugins.public: motto).
    3. Author a skill: point the reader at the `/motto:author-skill` skill and summarize the three required frontmatter fields (name, description, audience) and the body spine (H1 title + `**Role:**` line) at a glance — do not reproduce the full schema; defer to the skill.
    4. Validate + build: `motto lint` (reports all errors in one run) then `motto build` (runs lint first; wipes and rebuilds `dist/`, emitting `dist/public/` and, when private skills exist, `dist/private/`).
    5. Publish: summarize the release flow aligned with skills/release/SKILL.md — `npm version X.Y.Z` (auto-syncs motto.yaml) -> dogfood lint+build -> tarball verify -> `npm publish` + `git push --follow-tags`. Keep it brief and reference the `release` skill for the full maintainer checklist. Include the note that `npm version` creates the tag from v0.0.4 onward.

    After the quickstart, add ONE compact end-to-end example block (per D-03) that walks scaffold -> lint -> build -> install in sequence, clearly distinguishing shell commands (`npm i -g @jeremiewerner/motto`, `motto lint`, `motto build`) from Claude Code slash commands (`/motto:setup-project`, `/plugin marketplace add jeremiewerner/motto`, `/plugin install motto@motto`). This example previews the distribution/consumption identifiers that Task 2 documents in full.

    Use ONLY current identifiers verbatim (D-06). The public plugin was renamed mid-milestone; the stale pre-rename hyphenated plugin identifier must never appear anywhere in the file. Do not paste any "do not use X" style note naming the old identifier — simply use the correct ones. Write so the file also reads well as the npmjs.com package page (the package.json `files` allowlist auto-includes README in the tarball).
  </action>
  <verify>
    <automated>test -f README.md && grep -qF 'npm i -g @jeremiewerner/motto' README.md && grep -qF 'motto lint' README.md && grep -qF 'motto build' README.md && grep -qF '/motto:setup-project' README.md && grep -qF '/motto:author-skill' README.md && echo PASS</automated>
  </verify>
  <done>README.md exists at the repo root with an H1 title, a "what is Motto" intro, and an author-first authoring quickstart. The verbatim CLI install command `npm i -g @jeremiewerner/motto` is present (DOC-01), the `/motto:setup-project` and `/motto:author-skill` skills are referenced, and one compact end-to-end example block (scaffold -> lint -> build -> install) is present (D-03).</done>
</task>

<task type="auto">
  <name>Task 2: Append distribution (DOC-02), Claude Desktop usage (DOC-03), and a brief dev/contributing section</name>
  <files>README.md</files>
  <read_first>
    - .claude-plugin/marketplace.json — marketplace name `motto`, plugin `motto`, `source: npm` -> `@jeremiewerner/motto`, skills override `./dist/public/`, `strict: false`. Source-of-truth for the marketplace/install identifiers.
    - .planning/REQUIREMENTS.md §Documentation — DOC-02, DOC-03, and the exact Claude Desktop one-liners plus the `--zip` out-of-scope rationale.
    - .planning/phases/09-documentation/09-CONTEXT.md — D-04 (public GitHub-form add as PRIMARY path), D-05 (repo-public action executed at ship), D-06 (identifier correctness), and the <specifics> block with the exact symlink/zip one-liners and the Claude Desktop nuance.
    - README.md — the file created in Task 1; append to it, keep the author-first ordering intact.
  </read_first>
  <action>
    Append to README.md, continuing the author-first ordering (D-02): distribution -> consumption -> Claude Desktop -> brief dev/contributing.

    Distribution + consumption (DOC-02): document adding Motto's self-hosted marketplace with the verbatim command `/plugin marketplace add jeremiewerner/motto` as the primary add path (per D-04), then installing the skills with `/plugin install motto@motto`. State that this loads Motto's public skills (`author-skill`, `setup-project`) into Claude Code and exposes them as `/motto:author-skill` and `/motto:setup-project`. These identifiers must match .claude-plugin/marketplace.json verbatim (marketplace `motto`, plugin `motto`). Add a short execution note that the public GitHub-form add requires the `jeremiewerner/motto` repo to be public (the repo-visibility action per D-05 is performed at ship, not part of this README) — surface it as a one-line prerequisite, not a section.

    Claude Desktop (DOC-03): explain the nuance first — Claude Desktop's Code tab IS Claude Code, so it is already covered by the marketplace path above; the symlink and zip one-liners serve the Chat / web-upload path. Then give the two copy-paste one-liners using the concrete example skill `author-skill`:
    - Symlink built skills into the personal skills dir, using an absolute source so it resolves from `~/.claude/skills/`: `ln -s "$(pwd)/dist/public/author-skill" ~/.claude/skills/author-skill`
    - Web-upload zip one-liner: `cd dist/public && zip -r author-skill.zip author-skill/`
    Note both operate on the built `dist/public/<skill>` output (produced by `motto build`), and generalize `<skill>` for other skills.

    Brief dev/contributing section (per D-01, writer's discretion whether inline or a short stub): a few lines covering how to run the project locally — Node >= 20, `npm install`, `node --test` for the test suite, and `node bin/motto.js lint` / `node bin/motto.js build` to dogfood. Keep it brief; a full CONTRIBUTING.md is explicitly deferred (do not create one).

    Continue to use ONLY current identifiers verbatim (D-06). Do not introduce the stale pre-rename hyphenated plugin identifier anywhere, and do not add any "not X" note that names it.
  </action>
  <!-- planner-discipline-allow: motto-skills -->
  <verify>
    <automated>grep -qF '/plugin marketplace add jeremiewerner/motto' README.md && grep -qF '/plugin install motto@motto' README.md && grep -qF '~/.claude/skills/author-skill' README.md && grep -qF 'zip -r author-skill.zip author-skill/' README.md && grep -qF 'ln -s' README.md && { grep -v '^#' README.md | grep -qi 'motto-skills' && { echo 'FAIL: stale identifier present'; exit 1; }; :; } && echo PASS</automated>
  </verify>
  <done>README.md documents the marketplace add (`/plugin marketplace add jeremiewerner/motto`) and skill install (`/plugin install motto@motto`) satisfying DOC-02, the Claude Desktop symlink and zip one-liners for `author-skill` satisfying DOC-03, and a brief dev/contributing section. The stale pre-rename hyphenated plugin identifier does not appear anywhere in README.md (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase produces a static README.md only. No runtime code, network, auth, or data handling is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | Documented shell one-liners (symlink/zip) | low | accept | The only reviewable concern is that the `ln -s` and `zip -r` one-liners are benign and correct. They operate only on the build output under `dist/public/` and the user's own `~/.claude/skills/`; no elevation, no remote fetch. Verified by grep against the exact strings from CONTEXT <specifics>. |

Note: This phase installs no packages (npm/pip/cargo), so no supply-chain (T-09-SC) checkpoint applies. No new runtime attack surface is added.
</threat_model>

<verification>
Phase-level checks (all automated, run against README.md at repo root):
- DOC-01: `grep -qF 'npm i -g @jeremiewerner/motto' README.md`
- DOC-02: `grep -qF '/plugin marketplace add jeremiewerner/motto' README.md` AND `grep -qF '/plugin install motto@motto' README.md`
- DOC-03: `grep -qF '~/.claude/skills/author-skill' README.md` AND `grep -qF 'zip -r author-skill.zip author-skill/' README.md`
- D-03: one compact end-to-end example present (scaffold -> lint -> build -> install commands appear in sequence).
- D-06 (hard gate): `grep -v '^#' README.md | grep -i 'motto-skills'` returns NOTHING — the stale pre-rename hyphenated plugin identifier appears zero times.
</verification>

<success_criteria>
1. README.md exists at the repo root and documents installing the CLI from npm (`npm i -g @jeremiewerner/motto`) — DOC-01.
2. README documents adding the marketplace (`/plugin marketplace add jeremiewerner/motto`) and installing Motto's skills (`/plugin install motto@motto`) into Claude Code — DOC-02.
3. README documents Claude Desktop usage — the `~/.claude/skills/` symlink one-liner and the web-upload zip one-liner using `author-skill` — DOC-03.
4. README follows author-first ordering (D-02), includes per-path blocks plus one compact end-to-end example (D-03), and uses only current identifiers verbatim (D-06) with zero occurrences of the stale hyphenated plugin id.
</success_criteria>

<output>
Create `.planning/phases/09-documentation/09-01-SUMMARY.md` when done.

Artifacts this phase produces: `README.md` at the repo root.
Execution note (dependency, not a README section): the public GitHub-form marketplace add (D-04) requires the `jeremiewerner/motto` repo to be public — the `gh repo edit jeremiewerner/motto --visibility public` action (D-05) is performed at ship, outside this file deliverable.
</output>
