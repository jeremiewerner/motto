---
phase: 08-marketplace-distribution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
autonomous: false
requirements: [MKT-01, MKT-02, MKT-03]

must_haves:
  truths:
    - ".claude-plugin/marketplace.json exists at the repo root and is valid JSON"
    - "Manifest declares source: npm resolving to @jeremiewerner/motto, a skills override to ./dist/public/, and strict: false (MKT-03)"
    - "claude plugin validate . exits 0 (schema valid, no duplicate names, no path-traversal)"
    - "Marketplace name is 'motto'; the single plugin entry name is 'motto-skills' and matches dist/public/.claude-plugin/plugin.json name"
    - "A maintainer can run /plugin marketplace add jeremiewerner/motto then /plugin install motto-skills@motto and the public skills author-skill + setup-project load (MKT-01, MKT-02 — human-verified live)"
  artifacts:
    - ".claude-plugin/marketplace.json"
  key_links:
    - "plugins[0].name 'motto-skills' == dist/public/.claude-plugin/plugin.json name (mismatch → /plugin install motto-skills@motto fails with 'Plugin not found')"
    - "plugins[0].skills './dist/public/' == the actual skills directory inside the published npm tarball (mismatch → zero skills discovered)"
    - "plugins[0].source.package '@jeremiewerner/motto' == the package published by Phase 7 (mismatch/typo → users install unintended code)"
---

<objective>
Author the single new file `.claude-plugin/marketplace.json` at the Motto repo root so Motto's public skills become installable in Claude Code through a self-hosted marketplace that resolves the plugin from the published `@jeremiewerner/motto` npm package. This is a configuration-only phase: one JSON file, no code, no build changes, no new dependencies.

Purpose: Close MKT-01/MKT-02/MKT-03 — register the `motto` marketplace and the `motto-skills` plugin (`source: npm`, skills override to `dist/public/`, `strict: false`) so `/plugin marketplace add jeremiewerner/motto` + `/plugin install motto-skills@motto` load `author-skill` and `setup-project`.

Output: `.claude-plugin/marketplace.json` (new), schema-validated with `claude plugin validate .`.

Depends on Phase 7 (complete): this marketplace's `source: npm` entry resolves against the already-published `@jeremiewerner/motto@0.0.3` package whose tarball ships `dist/public/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-marketplace-distribution/08-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts This Phase Produces

| Artifact | Kind | Identifiers | Consumed by |
|----------|------|-------------|-------------|
| `.claude-plugin/marketplace.json` | New JSON config at repo root | marketplace name `motto`; plugin entry `motto-skills` | Claude Code `/plugin marketplace add jeremiewerner/motto` → `/plugin install motto-skills@motto`; Phase 9 README docs (DOC-02) |

**Downstream consumers must reference these exact identifiers:**
- Marketplace name: `motto` (install suffix `@motto`)
- Plugin name: `motto-skills` (install command `/plugin install motto-skills@motto`)
- Loaded skill namespaces: `/motto-skills:author-skill`, `/motto-skills:setup-project`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author .claude-plugin/marketplace.json at the repo root</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    - .planning/phases/08-marketplace-distribution/08-RESEARCH.md — "Code Examples" section (the exact, verbatim JSON to author) and "Pattern 2: Complete marketplace.json for Motto" (field-by-field justification). Copy the JSON exactly from RESEARCH; do not improvise field names or values.
    - .planning/ROADMAP.md §"Phase 8: Marketplace Distribution" — the 3 locked success criteria this file must satisfy.
  </read_first>
  <action>
    Create the directory `.claude-plugin/` at the repo root (it does not exist yet — verified) and write `.claude-plugin/marketplace.json` by copying the exact JSON from 08-RESEARCH.md "Code Examples" / "Pattern 2" verbatim. Do NOT retype from memory or reformat the schema — read the block and reproduce it.

    The manifest MUST contain these exact identifiers (copy them from RESEARCH, do not alter):
    - Top-level marketplace `name`: `motto` (per MKT-02; not a reserved name).
    - `owner.name`: `Jérémie Werner`; `owner.email`: `jeremiew@pm.me`.
    - A single entry in `plugins`:
      - `name`: `motto-skills` (per MKT-02; must match `dist/public/.claude-plugin/plugin.json` name).
      - `source`: object `{ "source": "npm", "package": "@jeremiewerner/motto" }` (per MKT-03; the scoped package Phase 7 published).
      - `description`: `Motto skill authoring and packaging tools`.
      - `skills`: `./dist/public/` (per MKT-03; path override — a directory path string, NOT a glob).
      - `strict`: `false` (per MKT-03; the npm package has no plugin.json at its root, so the marketplace entry is the full definition).
    - Do NOT add `source.version` — omitting it means always-latest from npm, per the RESEARCH anti-pattern note (pinning adds per-release manual burden; the npm package version is the source of truth).

    Do not create any `plugin.json` at the repo root and do not touch `dist/` — the nested `dist/public/.claude-plugin/plugin.json` (name `motto-skills`, emitted by `motto build`) already exists and stays as-is. Adding a root-level plugin.json would trigger the `strict: false` conflict described in RESEARCH Pitfall 1.
  </action>
  <verify>
    <automated>node -e "const m=require('./.claude-plugin/marketplace.json'); const p=m.plugins[0]; const ok = m.name==='motto' && p.name==='motto-skills' && p.source.source==='npm' && p.source.package==='@jeremiewerner/motto' && p.skills==='./dist/public/' && p.strict===false && !('version' in p.source); if(!ok){console.error('FIELD MISMATCH',JSON.stringify(m,null,2));process.exit(1);} const pj=require('./dist/public/.claude-plugin/plugin.json'); if(pj.name!==p.name){console.error('plugin name mismatch: marketplace='+p.name+' plugin.json='+pj.name);process.exit(1);} console.log('marketplace.json OK; plugin name matches dist/public/.claude-plugin/plugin.json');"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/marketplace.json` exists at the repo root and parses as valid JSON.
    - `name === "motto"`.
    - `plugins[0].name === "motto-skills"` AND equals `dist/public/.claude-plugin/plugin.json` `name`.
    - `plugins[0].source` is `{ source: "npm", package: "@jeremiewerner/motto" }` with no `version` key.
    - `plugins[0].skills === "./dist/public/"`.
    - `plugins[0].strict === false`.
    - `owner.name === "Jérémie Werner"`, `owner.email === "jeremiew@pm.me"`.
  </acceptance_criteria>
  <done>The manifest exists at the repo root, parses, and the automated field/name-match assertion exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Schema-validate the manifest with claude plugin validate (with JSON fallback)</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    - .planning/phases/08-marketplace-distribution/08-RESEARCH.md — "Validation (run from repo root)", "Environment Availability" (the `claude plugin validate .` fallback note), and "Assumptions Log" (A1: the nested `plugin.json` / `strict: false` no-conflict claim).
  </read_first>
  <action>
    From the repo root, run `claude plugin validate .` to validate the marketplace.json schema, duplicate names, and source-path integrity. It must exit 0.

    Fallback (only if the Claude Code CLI is not installed in the execution environment, per RESEARCH Environment Availability): treat the JSON-parse + exact-field assertion from Task 1 as the validation gate and record in the SUMMARY that `claude plugin validate .` was unavailable and the schema was checked by JSON assertion instead. Do not fail the task solely because the CLI is absent.

    Carry the A1 assumption forward explicitly in the SUMMARY: the claim that `dist/public/.claude-plugin/plugin.json` (2 levels deep) is NOT discovered by Claude Code's plugin-root `plugin.json` lookup and therefore does not conflict with `strict: false`. Note the documented fallback if A1 proves wrong (RESEARCH Pitfall 1 / Critical Note): either switch to `strict: true` with a root-level `.claude-plugin/plugin.json` that delegates via `skills`, or remove the nested `plugin.json` from `motto build`'s public output. Do not implement either fallback now — only record the trigger and remedy.
  </action>
  <verify>
    <automated>if command -v claude >/dev/null 2>&1; then claude plugin validate . && echo "claude plugin validate: PASS"; else echo "claude CLI unavailable — falling back to JSON assertion"; node -e "const m=require('./.claude-plugin/marketplace.json'); const p=m.plugins[0]; if(!(m.name==='motto'&&p.name==='motto-skills'&&p.source.source==='npm'&&p.source.package==='@jeremiewerner/motto'&&p.skills==='./dist/public/'&&p.strict===false)){process.exit(1);} console.log('JSON-assertion fallback: PASS');"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `claude plugin validate .` exits 0; OR, if the CLI is unavailable, the JSON-assertion fallback passes and the SUMMARY records the CLI absence.
    - The SUMMARY records the A1 assumption and its documented fallback path (not implemented, just noted).
  </acceptance_criteria>
  <done>The manifest passes `claude plugin validate .` (or the documented JSON-assertion fallback), and A1 is carried forward in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-install human verification (MKT-01, MKT-02, A1)</name>
  <action>Human-verification checkpoint — the executor cannot install a plugin into a live Claude Code session. Follow the steps in <how-to-verify> and report the result.</action>
  <what-built>
    `.claude-plugin/marketplace.json` registering the `motto` marketplace with the `motto-skills` plugin (`source: npm` → `@jeremiewerner/motto`, skills override `./dist/public/`, `strict: false`). This confirms MKT-01, MKT-02, and the A1 assumption — none of which can be proven headless, because the executor cannot install a plugin into a live Claude Code session.
  </what-built>
  <how-to-verify>
    In a live Claude Code session (maintainer account, `gh auth` credentials present for the private `jeremiewerner/motto` repo):
    1. Run `/plugin marketplace add jeremiewerner/motto` — expect the `motto` marketplace to resolve and list the `motto-skills` plugin (confirms MKT-01).
    2. Run `/plugin install motto-skills@motto` — expect install to succeed, pulling `@jeremiewerner/motto` from the public npm registry (confirms MKT-02).
    3. Confirm the two public skills load and are invocable as `/motto-skills:author-skill` and `/motto-skills:setup-project` (confirms MKT-02 + validates A1 — the nested `dist/public/.claude-plugin/plugin.json` does NOT trigger a `strict: false` conflict).
    Expected outcome: marketplace adds cleanly, plugin installs, both slash commands appear. If install succeeds but the `motto-skills:*` commands do not appear (A1 wrong / conflict), apply the RESEARCH Pitfall 1 fallback (switch to `strict: true` + root plugin.json, or drop the nested plugin.json from the build).
  </how-to-verify>
  <resume-signal>Type "approved" once both skills load, or describe the error (e.g. conflict, "Plugin not found", skills missing) so the A1 fallback can be planned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Claude Code user | The marketplace.json a user adds directs Claude Code to download and install code from an npm package. A wrong pointer = a supply-chain redirection. |
| npm registry → user machine | `/plugin install` downloads `@jeremiewerner/motto` and loads its skills into the user's Claude Code. |

No authentication, no session state, no code execution, and no package installs are introduced in this phase (configuration-only, single static JSON file). RESEARCH "Package Legitimacy Audit" is N/A — no npm/pip/cargo installs occur, so no `T-08-SC` install checkpoint is required.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `plugins[0].source.package` / `owner` / marketplace `name` | high | mitigate | The exact package string `@jeremiewerner/motto`, marketplace name `motto`, and plugin name `motto-skills` are copied verbatim from 08-RESEARCH (Pattern 2, VERIFIED against official docs). Task 1's automated assertion pins the exact `source.package` string and requires `plugins[0].name` to equal the emitted `dist/public/.claude-plugin/plugin.json` name. Task 2's `claude plugin validate .` gates the manifest schema before commit. A typo'd package/owner therefore fails an automated gate, not silently ships. |
| T-08-02 | Tampering | `strict: false` masking a real manifest/plugin.json error (A1) | medium | mitigate | A1 (nested `plugin.json` at `dist/public/.claude-plugin/` not found by the plugin-root lookup, so no `strict: false` conflict) is surfaced as an explicit assumption in Task 2's SUMMARY with its documented fallback (RESEARCH Pitfall 1), and is confirmed by the Task 3 live-install human checkpoint rather than assumed silently. |
| T-08-03 | Spoofing | Marketplace name `motto` (impersonation / name-squatting) | low | accept | `motto` is not a reserved name and does not impersonate an official Anthropic marketplace (verified against the reserved-names list in RESEARCH). Maintainer authoring their own marketplace for their own scoped package; no external-facing squatting surface. |
| T-08-04 | Information Disclosure | Private `jeremiewerner/motto` repo blocking external `marketplace add` | low | accept | Out of scope for Phase 8 (RESEARCH Pitfall 3): MKT-01 only requires the maintainer (with `gh auth` credentials) to add the marketplace. Making the repo public is a Phase 9 / future concern, not a security defect of this manifest. |

No high-severity threat is left unmitigated. T-08-01 (the only high) is mitigated by verbatim-copy + automated string assertion + schema validation gate.
</threat_model>

<verification>
Phase-level checks (run from repo root):
1. `test -f .claude-plugin/marketplace.json` — file exists at repo root.
2. Task 1 automated assertion exits 0 — all exact field values present; `plugins[0].name` matches `dist/public/.claude-plugin/plugin.json` name.
3. `claude plugin validate .` exits 0 (or documented JSON-assertion fallback passes).
4. Human checkpoint approved: `/plugin marketplace add jeremiewerner/motto` + `/plugin install motto-skills@motto` load `author-skill` and `setup-project` (MKT-01, MKT-02; validates A1).
</verification>

<success_criteria>
- `.claude-plugin/marketplace.json` exists at the repo root, parses, and declares `source: npm` → `@jeremiewerner/motto`, `skills: ./dist/public/`, `strict: false`, marketplace name `motto`, plugin name `motto-skills` (MKT-03).
- `claude plugin validate .` exits 0 (or JSON-assertion fallback with recorded CLI absence).
- Live `/plugin marketplace add jeremiewerner/motto` + `/plugin install motto-skills@motto` load both public skills (MKT-01, MKT-02), human-approved.
- A1 assumption + fallback carried forward in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/08-marketplace-distribution/08-01-SUMMARY.md` when done. Record: the created file, `claude plugin validate .` result (or CLI-unavailable fallback), the A1 assumption + documented fallback, and the human-checkpoint outcome for MKT-01/MKT-02.
</output>
