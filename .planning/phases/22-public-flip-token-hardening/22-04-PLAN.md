---
phase: 22-public-flip-token-hardening
plan: 04
type: execute
wave: 2
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md
autonomous: false
requirements: [OPEN-01, OPEN-03]

must_haves:
  truths:
    - "A final gitleaks rescan runs as the literal last pre-flip step, at the exact HEAD SHA that gets flipped to public — no unscanned trailing-commit window (D-08)"
    - "jeremiewerner/motto is public, and the flip only happened after the final scan recorded 0 unresolved findings"
  artifacts:
    - ".planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md (Scan #2 — Final section)"
    - "jeremiewerner/motto repo visibility = PUBLIC"
  key_links:
    - "the HEAD SHA recorded in Scan #2 equals the HEAD SHA at the moment Task 2's flip executes — verified by running both in immediate succession with no intervening commit"
---

<objective>
Run the FINAL, literal-pre-flip gitleaks rescan (D-08) after every other content-changing plan in this phase (22-01, 22-02) has landed, then flip `jeremiewerner/motto` to public as a deliberate, human-performed one-way-door action.

Purpose: D-08 requires the recorded scan HEAD to equal the flipped HEAD, closing the "scan now, one more commit later, flip on unscanned content" failure mode RESEARCH.md explicitly warns against. The flip itself is kept a human action per CONTEXT.md's Claude's Discretion framing, even though `gh repo edit --visibility public` is technically scriptable — the one-way-door gravity warrants a deliberate click, not an autonomous one.
Output: A `## Scan #2 — Final` record appended to 22-SECRETS-SCAN.md; `jeremiewerner/motto` flipped to public.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-public-flip-token-hardening/22-CONTEXT.md
@.planning/phases/22-public-flip-token-hardening/22-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols and identifiers created by this plan (new here — exclude from source-drift acknowledgement):

- New heading in `.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md`: `## Scan #2 — Final (pre-flip)`
- Repo visibility change: `jeremiewerner/motto` PRIVATE -> PUBLIC
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the FINAL pre-flip gitleaks rescan</name>
  <files>.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md</files>
  <read_first>
    - .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md (created in plan 22-01 — append a new section, do not modify Scan #1 or the PII Sweep section)
    - .planning/phases/22-public-flip-token-hardening/22-CONTEXT.md (D-08 — the rescan is the literal last pre-flip step; the recorded HEAD SHA must equal the flipped HEAD, no unscanned trailing-commit window)
  </read_first>
  <action>
    Confirm no further content-changing commits are pending from this phase (plans 22-01 and 22-02's edits are already committed; plan 22-03 makes no file changes). Run `gitleaks version` and `git rev-parse HEAD`, then `gitleaks git . --report-format json --report-path <scratch-tmp-path> --exit-code 0` again from repo root. Append a `## Scan #2 — Final (pre-flip)` section to 22-SECRETS-SCAN.md with the same fields as Scan #1 (command, version, HEAD SHA, commits-scanned count, exit code, findings count), explicitly stating this HEAD SHA is the one that Task 2 will flip to public — do not make any further content-changing commit after this task and before Task 2 runs. Delete the scratch JSON report as in plan 22-01 (never committed).
  </action>
  <verify>
    <automated>grep -c '^## Scan #2' .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md</automated>
  </verify>
  <done>The final, literal-pre-flip gitleaks scan is recorded with the exact HEAD SHA that Task 2 flips to public.</done>
  <acceptance_criteria>
    - 22-SECRETS-SCAN.md contains a `## Scan #2 — Final (pre-flip)` heading whose recorded HEAD SHA matches `git rev-parse HEAD` at the moment Task 2 begins (i.e., no intervening commit between the two tasks).
    - Findings count is 0, or every finding has a `rotate` triage row (D-06 — never a purge).
    - No raw JSON report file is staged or committed.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Flip `jeremiewerner/motto` to public</name>
  <read_first>
    - .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md (Task 1's Scan #2 result — must show 0 unresolved findings before proceeding)
    - .planning/phases/22-public-flip-token-hardening/22-CONTEXT.md (D-08, D-09 — the flip is the one-way door; the stranger walkthrough happens only after)
  </read_first>
  <action>
    Blocking human checkpoint (one-way door — deliberately kept a human action, even though `gh repo edit --visibility public --accept-visibility-change-consequences` is technically available): the maintainer flips `jeremiewerner/motto` to public, via that `gh` command or GitHub Settings -> General -> Danger Zone -> Change visibility, only after confirming Task 1's Scan #2 shows 0 unresolved findings at the current HEAD.
  </action>
  <what-built>
    Task 1 recorded a clean final scan at the current HEAD; this checkpoint performs the actual one-way visibility change.
  </what-built>
  <verify>
    <human-check>`gh repo view jeremiewerner/motto --json visibility` reports PUBLIC.</human-check>
  </verify>
  <done>jeremiewerner/motto is public; the recorded final-scan HEAD SHA equals the HEAD SHA at the moment of the flip.</done>
  <how-to-verify>
    1. Confirm 22-SECRETS-SCAN.md's Scan #2 section shows 0 findings (or all findings triaged and rotated) at the current HEAD SHA.
    2. Flip visibility: `gh repo edit jeremiewerner/motto --visibility public --accept-visibility-change-consequences` (or the GitHub Settings UI equivalent).
    3. Confirm: `gh repo view jeremiewerner/motto --json visibility` reports `"visibility":"PUBLIC"`.
  </how-to-verify>
  <resume-signal>Type "approved" once `gh repo view jeremiewerner/motto --json visibility` reports PUBLIC, or describe what went wrong.</resume-signal>
  <acceptance_criteria>
    - `gh repo view jeremiewerner/motto --json visibility` reports `PUBLIC`.
    - The visibility change happened only after Task 1's Scan #2 recorded 0 unresolved findings at that same HEAD SHA.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private git history → public git history | The one-way visibility flip; every commit ever made becomes world-readable at the moment Task 2 executes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Information Disclosure | Full git history, at the literal moment of the flip | high | mitigate | Scan #2 (Task 1) is the final content check before the flip (D-08); the flip (Task 2) only proceeds after confirming 0 unresolved findings at that same HEAD |
| T-22-06 | Information Disclosure | A commit landing between the final scan and the flip, reopening the unscanned window | high | mitigate | Task 2's action explicitly forbids any intervening commit; the checkpoint verifies the same HEAD SHA recorded in Task 1 |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages; this plan runs `gitleaks` and a `gh`/GitHub Settings visibility change only |
</threat_model>

<verification>
- 22-SECRETS-SCAN.md's `## Scan #2 — Final` section shows 0 unresolved findings at a HEAD SHA that matches the flip's HEAD.
- `gh repo view jeremiewerner/motto --json visibility` reports `PUBLIC`.
</verification>

<success_criteria>
- The full-history secrets scan gate is genuinely final — no unscanned trailing-commit window (OPEN-01, D-08 fully closed).
- `jeremiewerner/motto` is public (OPEN-03's flip precondition satisfied; the stranger walkthrough in plan 22-05 depends on this).
</success_criteria>

<output>
Create `.planning/phases/22-public-flip-token-hardening/22-04-SUMMARY.md` when done.
</output>
