---
phase: 22-public-flip-token-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [OPEN-03, PUB-05]
user_setup:
  - service: npm (Trusted Publisher / OIDC)
    why: "npmjs.com's Trusted Publisher record has no CLI/API surface — only the npmjs.com web UI can create it. Plan 22-02's `id-token: write` change is inert without a matching record."
    dashboard_config:
      - task: "Add a GitHub Actions Trusted Publisher for @jeremiewerner/motto"
        location: "npmjs.com -> package @jeremiewerner/motto -> Settings -> Trusted Publisher -> Add GitHub Actions publisher"

must_haves:
  truths:
    - "GitHub branch protection on `main` requires the five real CI status checks to pass, with `enforce_admins: false` so the existing solo-maintainer `git push --follow-tags` release flow is not broken (D-12, Pitfall 1)"
    - "An npmjs.com Trusted Publisher record exists for @jeremiewerner/motto matching org=jeremiewerner, repo=motto, workflow=ci.yml, no environment (D-13, Pitfall 4)"
  artifacts:
    - "GitHub branch protection rule on jeremiewerner/motto:main"
    - "npmjs.com Trusted Publisher record for @jeremiewerner/motto"
  key_links:
    - "the required-status-check context strings match the live CI job/matrix names exactly (\"test (20)\", not \"test\")"
    - "the Trusted Publisher's workflow filename matches ci.yml exactly (the actual file plan 22-02 edits)"
---

<objective>
Configure the two repo/registry-level settings that make OPEN-03's "unbypassable CI gate" (D-12) and PUB-05's OIDC exchange (D-13) actually work: GitHub branch protection on `main` (scripted directly via `gh api`, since repo-admin access is confirmed) and the npmjs.com Trusted Publisher record (a genuine maintainer-only web-UI action with no CLI/API surface).

Purpose: Branch protection has no ordering dependency on the flip itself (per RESEARCH.md) and can be configured immediately, with `enforce_admins: false` deliberately preserving the already-shipped `release` skill's direct-push flow (Pitfall 1). The Trusted Publisher record must exist before the OIDC-authenticated publish job (plan 22-02) can ever succeed, but its own live proof is deferred to the v0.0.6 ship tag (D-13).
Output: A live branch-protection rule on `main`; a confirmed npmjs.com Trusted Publisher record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-public-flip-token-hardening/22-CONTEXT.md
@.planning/phases/22-public-flip-token-hardening/22-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols and identifiers created by this plan (new here — exclude from source-drift acknowledgement):

- GitHub branch protection rule: `jeremiewerner/motto` branch `main` (required status checks: `test (20)`, `test (22)`, `test (24)`, `dogfood`, `pack-install-e2e`; `enforce_admins: false`)
- npmjs.com Trusted Publisher record for package `@jeremiewerner/motto` (GitHub Actions publisher: org `jeremiewerner`, repo `motto`, workflow `ci.yml`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Configure GitHub branch protection on `main`</name>
  <files></files>
  <read_first>
    - .planning/phases/22-public-flip-token-hardening/22-RESEARCH.md (Common Pitfall 1 — enforcing on administrators breaks the release skill's direct push; Code Examples — the exact `gh api` PUT payload; Assumption A3 — `npm-drift`'s job-level `if:` makes it report skipped/passing on PRs, so it is deliberately excluded from the required list)
    - .planning/phases/22-public-flip-token-hardening/22-CONTEXT.md (D-12 — branch protection lands in this phase, folded into the flip's repo-settings action)
    - skills/release/SKILL.md (Step 4 — `git push --follow-tags` direct-to-main flow that `enforce_admins: false` must preserve)
  </read_first>
  <action>
    Run `gh api repos/jeremiewerner/motto/commits/main/check-runs --jq '.check_runs[].name'` (or `gh run view <latest-run-id> --json jobs --jq '.jobs[].name'` against the most recent CI run) to reconfirm the exact live check-run/job names before applying — expected: `test (20)`, `test (22)`, `test (24)`, `dogfood`, `pack-install-e2e` (verified live during planning against run 28685021049). Then apply `gh api repos/jeremiewerner/motto/branches/main/protection --method PUT` with a JSON payload setting: `required_status_checks.strict: true`, `required_status_checks.checks` to exactly those 5 context names (as `{"context": "<name>"}` objects), `enforce_admins: false` (deliberate — preserves the release skill's direct `git push --follow-tags` to `main`), `required_pull_request_reviews: null`, `restrictions: null`. Deliberately exclude `npm-drift` from the required-checks list (its job-level `if: github.ref == 'refs/heads/main'` means it never reports on a PR-triggered run, which would leave a required check permanently pending). Record the exact command run and the resulting JSON in this plan's SUMMARY.
  </action>
  <verify>
    <automated>gh api repos/jeremiewerner/motto/branches/main/protection --jq '{enforce_admins: .enforce_admins.enabled, checks: [.required_status_checks.checks[].context]}'</automated>
  </verify>
  <done>`main` requires the 5 real CI status checks to pass for merges, with `enforce_admins: false` so the existing solo-maintainer release push flow is unaffected.</done>
  <acceptance_criteria>
    - `gh api repos/jeremiewerner/motto/branches/main/protection` (GET) returns HTTP 200 (not 404).
    - The returned JSON's `enforce_admins.enabled` is `false`.
    - The returned JSON's `required_status_checks.checks[].context` array contains exactly `test (20)`, `test (22)`, `test (24)`, `dogfood`, `pack-install-e2e` and does not contain `npm-drift`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Maintainer configures the npm Trusted Publisher</name>
  <read_first>
    - .planning/phases/22-public-flip-token-hardening/22-RESEARCH.md (Pattern 1 — exact Trusted Publisher fields, established by the Phase 21-03 `NPM_TOKEN` checkpoint precedent; Common Pitfall 4 — filename/org/repo must match exactly, or the ship-time publish silently 404s/401s)
    - .github/workflows/ci.yml (post-plan-22-02 state — confirms the workflow filename is exactly `ci.yml`, no separate `publish.yml`)
  </read_first>
  <action>
    Blocking human checkpoint (no automated implementation — npmjs.com's Trusted Publisher config has no CLI/API surface): the maintainer goes to npmjs.com -> package `@jeremiewerner/motto` -> Settings -> Trusted Publisher -> Add GitHub Actions publisher, and fills in exactly: Organization/user = `jeremiewerner`, Repository = `motto`, Workflow filename = `ci.yml` (filename only, not a path), Environment name = left blank (no GitHub Environment is declared anywhere in ci.yml), Allowed actions = `npm publish`.
  </action>
  <what-built>
    Plan 22-02 added `id-token: write` and `npm publish --provenance` to the publish job — the OIDC handshake is inert until npmjs.com has a matching Trusted Publisher record.
  </what-built>
  <verify>
    <human-check>npmjs.com's package Settings page for @jeremiewerner/motto shows a Trusted Publisher record for jeremiewerner/motto, workflow ci.yml, no environment.</human-check>
  </verify>
  <done>An npmjs.com Trusted Publisher record exists and matches the ci.yml publish job's identity exactly; live proof of the OIDC handshake itself is deferred to the v0.0.6 ship tag per D-13.</done>
  <how-to-verify>
    1. On npmjs.com, open package @jeremiewerner/motto -> Settings -> Trusted Publisher.
    2. Add a GitHub Actions publisher with org `jeremiewerner`, repo `motto`, workflow filename `ci.yml`, blank environment, allowed action `npm publish`.
    3. Save, then reload the Settings page and confirm the record is listed with those exact values.
  </how-to-verify>
  <resume-signal>Type "approved" once the Trusted Publisher record is visible on the npmjs.com Settings page with the exact fields above, or describe what went wrong.</resume-signal>
  <acceptance_criteria>
    - Maintainer attests (recorded in the SUMMARY) the Trusted Publisher record exists with org=jeremiewerner, repo=motto, workflow=ci.yml, no environment, allowed action=npm publish.
    - No npm auth token or credential value is recorded anywhere in the plan or SUMMARY.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External contributor PR → `main` | Branch protection gates merges on 5 required, exactly-named green CI checks |
| GitHub Actions OIDC token → npm Trusted Publisher record | npm authorizes the publish exchange only if org/repo/workflow-filename match exactly; no wildcard/fuzzy match |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Tampering / Denial of Service | Branch protection on `main` (over-broad `enforce_admins`, or guessed check-context strings) | high | mitigate | `enforce_admins` pinned `false` (preserves the release skill's direct push, Pitfall 1); required-check context strings pulled live from a real CI run's job names immediately before applying, not guessed or copied from a template |
| T-22-04 | Spoofing | npmjs.com Trusted Publisher record mismatched against org/repo/workflow filename | medium | mitigate | Exact fields specified from the live `ci.yml` filename; maintainer confirms via the Settings page reload before resuming (Task 2) |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages; this plan makes API/UI configuration changes only |
</threat_model>

<verification>
- `gh api repos/jeremiewerner/motto/branches/main/protection` returns 200 with `enforce_admins.enabled: false` and the exact 5-check required list.
- Maintainer attestation (SUMMARY) confirms the npmjs.com Trusted Publisher record's fields.
- The live OIDC publish exchange itself is not exercised by this plan — deferred to the v0.0.6 ship tag per D-13.
</verification>

<success_criteria>
- `main` is protected with the real CI gate, without breaking the existing solo-maintainer release flow (OPEN-03/D-12).
- The npmjs.com Trusted Publisher record exists and exactly matches the workflow it must authorize (PUB-05 static half, D-13).
</success_criteria>

<output>
Create `.planning/phases/22-public-flip-token-hardening/22-03-SUMMARY.md` when done (record only the branch-protection JSON result and the Trusted Publisher field attestation — never any credential value).
</output>
