---
phase: 17-docs-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/references/skill-schema.md
  - test/doc-sync.test.js
  - skills/build-skill/SKILL.md
autonomous: true
requirements: [DOC-06]
must_haves:
  truths:
    - "skill-schema.md header cites src/schema.js as source of truth and carries NO version number (D-04)."
    - "skill-schema.md §6 documents the real template cascade (never the false 'NOT validated' claim); §7 outputs (both layers), §8 dependencies, §9 allowed-tools exist with verbatim lint-error tables — exact error strings kept, canonical-rule-source role (D-02)."
    - "§2 lists the previously-missing 'description must be a string (got <typeof>)' error with the array-special-case note."
    - "node --test doc-sync.test.js fails if any live error string in src/schema.js or src/lint.js stops appearing verbatim in skill-schema.md (drift breaks the suite)."
    - "skills/build-skill/SKILL.md Step 2 has no supersede clause and no field delta — it points to the bundled reference for the full rule set (D-05)."
    - "Full suite green; motto lint clean; grep 'letter-start kebab-case' and grep 'Common Lint Errors' in skills/build-skill/SKILL.md each return 0."
  artifacts:
    - shared/references/skill-schema.md
    - test/doc-sync.test.js
    - skills/build-skill/SKILL.md
  key_links:
    - "doc-sync.test.js reads src/schema.js + src/lint.js source text and asserts each static error segment is contained in skill-schema.md (direction: code → doc, never reverse)."
    - "motto build bundles skill-schema.md verbatim into dist/public/build-skill/references/ — build-skill Step 2 trusts that bundled copy with no local delta once the reference is current."
---

<objective>
Bring `shared/references/skill-schema.md` current with the settled v0.0.5 validator, add a `node:test` doc-sync guard that breaks the suite on drift, and remove the now-obsolete supersede/delta prose from `build-skill` Step 2. This is the DOC-06 half of the phase: make the canonical schema reference tell the truth and mechanize its freshness.

Purpose: The doc currently makes an actively FALSE claim (§6: "template/dependencies are NOT validated in Motto v0.0.2") — they are both fully validated as of Phases 14/15. The stale "v0.0.2" header is the motivating example for D-04. Once the reference is correct and bundled, build-skill's Step 2 stopgap delta (Phase 16) has nothing left to say.
Output: A rewritten schema reference, one new test file, and a trimmed build-skill Step 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-docs-audit/17-CONTEXT.md
@.planning/phases/17-docs-audit/17-RESEARCH.md
@.planning/phases/17-docs-audit/17-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite skill-schema.md current (header + §2 fix + §6 rewrite + new §7/§8/§9 + §4 note + §10 renumber)</name>
  <files>shared/references/skill-schema.md</files>
  <read_first>
    - shared/references/skill-schema.md (the file being rewritten — §1/§3/§5/§7 verified current, §2 has a drift, §6 is false, header stale)
    - .planning/phases/17-docs-audit/17-RESEARCH.md → "Validator Ground Truth Catalog" (every verbatim error string with line citations) and "Doc-Sync Test Design"
    - .planning/phases/17-docs-audit/17-PATTERNS.md → "shared/references/skill-schema.md (doc rewrite)" (house-style skeleton, placeholder-token convention, exact target lines)
    - src/schema.js (ground truth: template cascade ~283-329, outputs ~355-375, dependencies ~385-425, allowed-tools ~435-455)
    - src/lint.js (ground truth: checkOutputsFs error strings ~209-238)
    - src/templates.js (SECTIONS/TEMPLATES registry — `procedure` waives nothing today)
  </read_first>
  <action>
    Surgically patch skill-schema.md per D-01 (keep the existing per-field section skeleton and placeholder-token convention `<typeof>`/`N`/`X` from §1/§3). Do NOT restructure §1, §3, §5, §7 content — only renumber §7 as noted below.

    HEADER (D-04): In line 3, delete the parenthetical "(v0.0.2)" so it reads "the canonical rule source for the Motto skill schema." Keep the "bundled verbatim by `motto build`" sentence and the "All claims are derived from `src/schema.js`, `src/config.js`, `src/frontmatter.js`, and `src/lint.js`." sentence unchanged. Add NO version number anywhere.

    §2 DRIFT FIX: In the `**Lint errors:**` list under `## 2. \`description\` Field`, add a bullet for the non-string error, verbatim static text: `description must be a string (got <typeof>)`. Add one prose sentence noting that Array values render as the literal word `"array"` (not `typeof`) in this message — the one field with an array special-case.

    §4 CROSS-REFERENCE: Under `## 4. Body Spine`, append one sentence stating that the H1-title and Role checks are each individually skippable when the resolved template's `waives` set contains `"title"`/`"role"`, that this is controlled by the `template` field (see §6), and that no shipped template uses `waives` today (currently inert). Do not change the two existing Check statements or their error strings.

    §6 REWRITE (replaces the false block at current lines 126-136 entirely): retitle to `## 6. \`template\` Field`. State the gate (an absent `template` key skips all template checks; an explicitly-present falsy value like `template: ""` is treated as unknown and errors — never silently skipped). Give a YAML example (`template: procedure`). Add the cascade error table with these verbatim static segments (placeholders as tokens): `template must be a string (got <typeof>)`; `unknown template "X" (available: procedure)`; `template "X" requires <section>…</section> section — ...`. Document `hasClosedSection` semantics in prose: line-start-anchored open/close tags, fenced code blocks (3+ backticks or 3+ tildes, ≤3 leading spaces, closing fence same char and length ≥ opener) excluded from scanning, open-before-close ordering required, bare tags only (no attributes). Note the current registry ships exactly one template (`procedure`, requiring `<process>` and `<success_criteria>`) and that adding a template is a pure `src/templates.js` data edit.

    §7 NEW `outputs` (per D-01 renumbering): title `## 7. \`outputs\` Field`. State the gate (absent key skips; empty map `{}` is a valid no-op). MUST document TWO layers with a "Layer" distinction (Pitfall 2). Layer 1 (schema.js, lexical): verbatim segments `outputs must be a map of name -> file (got <typeof>)` (note array renders as `"array"`), `outputs.<key> must be a non-empty string path (got <typeof>)`, `outputs.<key> path "X" is unsafe (must not be absolute or contain ".." traversal)`. Note `isOutputPathLexicallySafe` is root-independent (never calls resolve, no cwd dependence) and that `normalize()` collapses interior traversal. Layer 2 (lint.js, fs, runs only inside `motto lint`/`lintProject`, never a bare `validateSkill()`): verbatim segments `outputs.<key> file "X" does not exist`, `outputs.<key> "X" is not a file`, `outputs.<key> file "X" escapes the skill directory via symlink`, `outputs.<key> file "X" could not be resolved: <message>`. Add the `{var}` note: the existence check is literal — `{var}` placeholders belong inside a declared output file's CONTENT, never in the `outputs:` path string.

    §8 NEW `dependencies`: title `## 8. \`dependencies\` Field`. Gate (absent skips; empty array `[]` valid no-op). Per-entry cascade table with verbatim segments: `dependencies must be an array (got <typeof>)`, `dependencies entry must be a non-empty string (got <typeof>)`, `dependencies entry "X" is not valid "plugin:skill" format`, `dependencies entry "X" is a self-dependency`, `dependency "X" not found (available: ...)`, `dependencies entry "X" is private but this skill is public (audience-direction guard)`. Document the THREE load-bearing rules exactly: (1) self-dependency check runs BEFORE the not-found membership check; (2) namespaced `plugin:skill` entries are format-checked ONLY (exempt from self-dep, not-found, and audience guard); (3) the audience guard fails in exactly one direction — public→private only. Preserve, do not "fix", the wording asymmetry between the plural "dependencies entry ... is a self-dependency" and the singular "dependency ... not found".

    §9 NEW `allowed-tools`: title `## 9. \`allowed-tools\` Field`. Gate (absent skips; accepts BOTH a string and an array; empty array `[]` passes; empty/whitespace-only string errors). Format-only (Option A — no shape regex; `"Bash(git add *)"` passes trivially as one non-empty rule). Error table verbatim: `allowed-tools must be a non-empty string or array (got an empty string)`, `allowed-tools[<i>] must be a non-empty string (got <typeof>)`, `allowed-tools must be a string or array (got <typeof>)`. State explicitly there is no tokenizing / parenthesized-pattern parsing.

    §10 RENUMBER: change the current `## 7. Frontmatter Envelope` heading to `## 10. Frontmatter Envelope`. No content change to that section. Confirm no in-doc cross-reference to "§7" needs updating (none found at research time).
  </action>
  <verify>
    <automated>test -f shared/references/skill-schema.md && grep -q 'canonical rule source for the Motto skill schema' shared/references/skill-schema.md && grep -qv 'v0.0.2' shared/references/skill-schema.md; grep -c 'v0.0.2' shared/references/skill-schema.md | grep -qx 0 && grep -q '## 10. Frontmatter Envelope' shared/references/skill-schema.md && grep -q 'audience-direction guard' shared/references/skill-schema.md && grep -q 'outputs must be a map of name -> file' shared/references/skill-schema.md && grep -q 'allowed-tools must be a non-empty string or array' shared/references/skill-schema.md && ! grep -q 'NOT validated in Motto' shared/references/skill-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'v0.0.2' shared/references/skill-schema.md` → 0 (header carries no version number, D-04).
    - Source assertion: the substring `NOT validated in Motto` is absent from the file (the false §6 claim is gone).
    - Source assertion: headings `## 6. \`template\` Field`, `## 7. \`outputs\` Field`, `## 8. \`dependencies\` Field`, `## 9. \`allowed-tools\` Field`, `## 10. Frontmatter Envelope` all present.
    - Source assertion: every verbatim static segment listed in the action for §2/§6/§7/§8/§9 appears in the file (proven exhaustively by Task 2's doc-sync test).
    - Behavior assertion: `outputs` documentation carries an explicit Layer 1 (schema.js) vs Layer 2 (lint.js) distinction.
  </acceptance_criteria>
  <done>skill-schema.md documents template/outputs/dependencies/allowed-tools with verbatim error tables, the §2 drift is fixed, the header has no version number, §6's false claim is removed, and Frontmatter Envelope is renumbered §10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test/doc-sync.test.js — code→doc drift guard (D-03)</name>
  <files>test/doc-sync.test.js</files>
  <behavior>
    - For a curated array of STATIC error segments (literal text lying between `${...}` interpolation boundaries in the source), assert each segment is present in src/schema.js OR src/lint.js source text (self-check: the curated list is still real live source).
    - For each same segment, assert it is present in shared/references/skill-schema.md (the doc still quotes it).
    - Direction is code → doc ONLY: expected set derived from source, asserted `includes` in the doc. Never iterate the doc's tables and check the code (Pitfall 3 — that direction masks a doc going stale).
    - If a source message changes and the doc keeps the old string, the doc-assertion fails; if the source changes and the curated list is not updated, the self-check fails. Either way the suite goes red.
  </behavior>
  <read_first>
    - test/dogfood.test.js (house style: node:test, node:assert/strict, REPO_ROOT via import.meta.url — NEVER process.cwd(), real repo files not fixtures, assertion messages carry the diagnostic)
    - .planning/phases/17-docs-audit/17-RESEARCH.md → "Doc-Sync Test Design" (Option 1 recommended) and "Code Examples" (static-segment extraction sketch) and Pitfall 3
    - .planning/phases/17-docs-audit/17-PATTERNS.md → "test/doc-sync.test.js (NEW, test / static-analysis)"
    - src/schema.js, src/lint.js (the source text being read at test time)
  </read_first>
  <action>
    Create test/doc-sync.test.js following test/dogfood.test.js house style. Import `describe`, `it` from node:test; `assert` from node:assert/strict; `readFile` from node:fs/promises; `resolve`/`dirname`/`join` from node:path; `fileURLToPath` from node:url. Anchor `const REPO_ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '..')` verbatim — do NOT use process.cwd() (this is the cwd-path-bug convention from dogfood.test.js and the Security Domain note).

    At the top of the describe block, read all three files as UTF-8 text: `src/schema.js`, `src/lint.js`, `shared/references/skill-schema.md` (join under REPO_ROOT).

    Define a `staticSegments` array of pure-literal substrings (each chosen to NOT span a `${...}` boundary so it survives verbatim in both source and doc). Use exactly these segments (they are live-verified against src/schema.js and src/lint.js):
      'name is required'
      'name must be a string (got '
      'name must be letter-start kebab-case'
      'name must not exceed 64 characters'
      'must not contain the reserved substrings "anthropic" or "claude"'
      'description is required'
      'description must be a string (got '
      'description must not exceed 1024 characters'
      'description must not contain XML tags (e.g. <example>)'
      'audience must be public|private'
      'template must be a string (got '
      'unknown template "'
      'requires <'
      'outputs must be a map of name -> file'
      'must be a non-empty string path (got '
      'is unsafe (must not be absolute or contain'
      'does not exist'
      'is not a file'
      'escapes the skill directory via symlink'
      'could not be resolved: '
      'dependencies must be an array'
      'dependencies entry must be a non-empty string'
      'is not valid "plugin:skill" format'
      'is a self-dependency'
      'not found (available: '
      'is private but this skill is public (audience-direction guard)'
      'allowed-tools must be a non-empty string or array'
      'allowed-tools[' 
      'allowed-tools must be a string or array (got '

    Write one `describe('doc-sync (DOC-06)', ...)` block. Inside, loop the segments in a single `it` (or one `it` per segment) with TWO asserts per segment, each carrying a diagnostic message:
      1. `assert.ok(schemaSrc.includes(seg) || lintSrc.includes(seg), \`stale curated segment (not in schema.js/lint.js source): "${seg}"\`)` — the self-check.
      2. `assert.ok(docText.includes(seg), \`skill-schema.md missing expected error string: "${seg}"\`)` — the doc containment.

    Add NO new dependencies. Do NOT re-implement any validation cascade inside the test (anti-pattern — that would create a second divergeable source of truth).
  </action>
  <verify>
    <automated>node --test test/doc-sync.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `node --test test/doc-sync.test.js` passes (proves every curated segment is in both live source and the rewritten doc).
    - Behavior assertion: deleting the substring `audience-direction guard` from skill-schema.md and re-running the test makes it FAIL (drift is caught) — this is the guard's whole purpose; the executor may spot-check this manually but must restore the doc after.
    - Source assertion: `grep -c 'process.cwd' test/doc-sync.test.js` → 0 (REPO_ROOT anchored via import.meta.url only).
    - Source assertion: the test adds no import outside node: stdlib (no new package.json dependency).
  </acceptance_criteria>
  <done>test/doc-sync.test.js exists, passes as part of `node --test`, derives its expected set from src/schema.js/lint.js source text, and asserts containment in skill-schema.md (never the reverse).</done>
</task>

<task type="auto">
  <name>Task 3: Trim build-skill Step 2 — remove supersede clause + field delta (D-05)</name>
  <files>skills/build-skill/SKILL.md</files>
  <read_first>
    - skills/build-skill/SKILL.md (Step 2 at lines 24-31 carries the supersede clause + 4-bullet field delta; Step 5 restates the NAME cascade but is OUT OF BOUNDARY — do not touch it)
    - .planning/phases/17-docs-audit/17-RESEARCH.md → "build-skill Step 2 Cleanup Detail" (exact target text and DOC-06 grep invariants)
    - .planning/phases/17-docs-audit/17-PATTERNS.md → "skills/build-skill/SKILL.md (Step 2 prose trim)" (before/after target form)
  </read_first>
  <action>
    Replace the entire body of Step 2 (the paragraph beginning "Consult the bundled `skill-schema` reference for the base frontmatter..." through the 4 delta bullets ending "...the Bash invocation patterns the skill genuinely runs.") with a single sentence, no delta and no supersede clause, per D-05:

    "Consult the bundled `skill-schema` reference for the full frontmatter and body rules (`name`, `description`, `audience`, the Title + Role spine, `template`, `outputs`, `dependencies`, `allowed-tools`, `shared_references`)."

    Keep the `## Step 2 — Map onto the schema` heading unchanged. Do NOT touch Step 5's inline NAME-cascade guard (out of phase boundary per RESEARCH.md — it is a live behavioral check, not a lint-string reference, and does not contain the grepped substring). Do NOT reintroduce any lint-error string or regex into build-skill's prose. Do NOT touch Step 7 check 3's existing "supersedes ... for descriptions you author" wording — that is a deliberate content-quality directive, not the schema-reference supersede clause D-05 targets.
  </action>
  <verify>
    <automated>grep -c 'letter-start kebab-case' skills/build-skill/SKILL.md | grep -qx 0 && grep -c 'Common Lint Errors' skills/build-skill/SKILL.md | grep -qx 0 && ! grep -q 'it supersedes the bundled reference' skills/build-skill/SKILL.md && ! grep -q 'not validated' skills/build-skill/SKILL.md && grep -q 'for the full frontmatter and body rules' skills/build-skill/SKILL.md && node bin/motto.js lint && node --test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'letter-start kebab-case' skills/build-skill/SKILL.md` → 0 (DOC-06 invariant preserved).
    - Source assertion: `grep -c 'Common Lint Errors' skills/build-skill/SKILL.md` → 0 (DOC-06 invariant preserved).
    - Source assertion: the substrings `it supersedes the bundled reference` and `not validated` are absent from the file (delta + supersede removed).
    - Source assertion: `for the full frontmatter and body rules` present (the new single-sentence Step 2).
    - CLI output: `node bin/motto.js lint` reports clean; `node --test` full suite green (including the dogfood build test that bundles skill-schema.md into dist/public/build-skill/references/).
  </acceptance_criteria>
  <done>build-skill Step 2 is a single reference-pointing sentence with no delta/supersede clause, both DOC-06 grep invariants still return 0, motto lint is clean, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → local filesystem | doc-sync.test.js reads repo source files (src/schema.js, src/lint.js, skill-schema.md) as text; no untrusted/network input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | test path resolution in doc-sync.test.js | low | mitigate | Anchor REPO_ROOT via `import.meta.url`, never `process.cwd()` — avoids the cwd-dependent path-resolution bug class (Phase 15 CR-01). Enforced by acceptance criterion `grep -c process.cwd → 0`. |
| T-17-02 | Information Disclosure | doc-sync.test.js file reads | low | accept | Reads only in-repo, already-tracked text files; no secrets, no network, no user input. No new attack surface introduced by a docs+test phase (RESEARCH.md Security Domain). |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase — node: stdlib only (node:test/node:fs/node:assert). No package legitimacy checkpoint required. |
</threat_model>

<verification>
- `node --test` full suite green (includes new test/doc-sync.test.js and the existing dogfood build/lint tests).
- `node bin/motto.js lint` clean.
- `grep -c 'v0.0.2' shared/references/skill-schema.md` → 0.
- `! grep -q 'NOT validated in Motto' shared/references/skill-schema.md`.
- `grep -c 'letter-start kebab-case' skills/build-skill/SKILL.md` → 0 and `grep -c 'Common Lint Errors' skills/build-skill/SKILL.md` → 0.
</verification>

<success_criteria>
skill-schema.md is current (source-cited header, no version number, all four new/rewritten field sections with verbatim error tables, §2 drift fixed, §10 renumbered); a doc-sync test guards the reference against future drift; build-skill Step 2 is a single reference-pointing sentence with the DOC-06 grep invariants intact; motto lint clean and full suite green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

Files edited:
- `shared/references/skill-schema.md` — header D-04 (drop "(v0.0.2)"); §2 add `description must be a string (got <typeof>)` + array note; §4 append `waives` cross-reference sentence; §6 full rewrite to `## 6. \`template\` Field` (real cascade, `hasClosedSection` semantics); §7 renumbered Frontmatter Envelope → §10.
- `skills/build-skill/SKILL.md` — Step 2 body replaced with one reference-pointing sentence (no delta/supersede).

Files created:
- `test/doc-sync.test.js` — new node:test file; `describe('doc-sync (DOC-06)')`; curated static-segment array; two asserts per segment (source self-check + doc containment).

New prose sections in skill-schema.md:
- `## 7. \`outputs\` Field` (Layer 1 schema.js + Layer 2 lint.js tables, `{var}` note).
- `## 8. \`dependencies\` Field` (per-entry cascade, three ordering rules, wording-asymmetry note).
- `## 9. \`allowed-tools\` Field` (Option A format-only table).
</artifacts_produced>

<output>
Create `.planning/phases/17-docs-audit/17-01-SUMMARY.md` when done.
</output>
