---
phase: 04-self-hosted-skill-tree-gap-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - motto.yaml
  - shared/references/skill-schema.md
  - skills/authoring-a-skill/SKILL.md
  - skills/motto-project-setup/SKILL.md
  - skills/motto-release/SKILL.md
autonomous: false
requirements:
  - SELF-01
  - SELF-02
  - SELF-03
  - SELF-04
  - SELF-05
  - DOG-01
  - DOG-02

must_haves:
  truths:
    - "Running `node bin/motto.js lint` at the repo root exits 0 and prints `✓ 3 skills OK` with no errors (DOG-01)."
    - "`motto.yaml` declares name=motto, version \"0.0.2\", a description, plugins.public=motto-skills, plugins.private=motto-private (SELF-01)."
    - "Three skills exist — authoring-a-skill (public), motto-project-setup (public), motto-release (private) — and no skill `name` field contains the substring claude or anthropic (SELF-02..04)."
    - "`node bin/motto.js build` produces dist/public/ and dist/private/, each skill copied verbatim, skill-schema bundled into each public skill's references/, and a per-bucket .claude-plugin/plugin.json carrying version 0.0.2 (DOG-02, SELF-05)."
    - "shared/references/skill-schema.md exists and is declared via `shared_references: [skill-schema]` by both public skills (SELF-05)."
    - "`node --test` still passes with 53 tests green and zero new dependencies added."
  artifacts:
    - motto.yaml
    - shared/references/skill-schema.md
    - skills/authoring-a-skill/SKILL.md
    - skills/motto-project-setup/SKILL.md
    - skills/motto-release/SKILL.md
  key_links:
    - "`shared_references: [skill-schema]` in each public skill ↔ shared/references/skill-schema.md (LINT-05 resolution at lint + bundling at build)."
    - "`audience: private` in motto-release ↔ `plugins.private: motto-private` in motto.yaml (build private-contradiction gate, src/build.js:135)."
    - "Each skill's folder name ↔ its frontmatter `name` field (must be identical, src/schema.js:89)."
---

<objective>
Author Motto's own skills tree in the real source layout and prove the shipped v0.0.1 `motto lint` + `motto build` pipeline runs clean on it: a root `motto.yaml`, a `shared/references/skill-schema.md` reference, and three skills (two public, one private) — all passing lint and building the expected `dist/` artifacts.

Purpose: Self-hosting (dogfood). This is the first time the validators and builder run against real authored content rather than synthetic fixtures, proving the pipeline and exercising the full schema surface (public + private buckets + a shared reference).
Output: motto.yaml, shared/references/skill-schema.md, three SKILL.md files; a clean `motto lint` (`✓ 3 skills OK`); a `dist/` tree matching the Build Output Contract; 53 existing tests still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Primary source — exact, validator-passing content blocks + 9-item gap scan + mandatory authoring order
@.planning/phases/04-self-hosted-skill-tree-gap-fixes/04-RESEARCH.md
@.planning/REQUIREMENTS.md

# Body-section outlines for each skill
@.planning/research/FEATURES.md

# The validators the authored content must satisfy — read to confirm exact rules; DO NOT modify unless a real gap surfaces
@src/schema.js
@src/config.js
@src/lint.js
@src/build.js
@src/frontmatter.js
@bin/motto.js
</context>

## Phase Goal

**As a** Motto maintainer, **I want to** author Motto's own skills tree and have it pass `motto lint` and `motto build` clean, **so that** the shipped lint→build pipeline is proven on real input and Motto ships as a self-hosted, distributable plugin.

> Derived from the ROADMAP Phase 4 `**Goal:**` line (which is in prose, not user-story form — see plan return note). The dogfood actor is the Motto maintainer.

## MVP Slice Framing

This is a content-authoring vertical slice, not a layered build. The "failing end-to-end test" already exists as an observable baseline: today `node bin/motto.js lint` at the repo root exits 1 with `✗ (project): no skills found` (no `motto.yaml`, no `skills/`). Task 1 turns that RED baseline GREEN end-to-end (lint + build pass on a minimal-but-real tree). Task 2 refines the same files into genuinely useful teaching content while keeping the pipeline green. Task 3 is human verification of output and content quality. No automated test files are authored here — the permanent dogfood test is Phase 5 (DOG-03).

**Mandatory authoring order (from 04-RESEARCH.md §Authoring Order — load-bearing, prevents cascade failures):**
1. `shared/references/skill-schema.md` FIRST (else both public skills fail LINT-05 "shared_references not found").
2. `motto.yaml` WITH `plugins.private` set BEFORE the private skill (else `motto build` fails the private-contradiction gate after lint passes — gap G7).
3. The three `SKILL.md` files.
4. `motto lint` → clean. 5. `motto build` → inspect dist/. 6. `node --test` → still 53 green.

<tasks>

<task type="auto">
  <name>Task 1: Thinnest end-to-end slice — author the tree in order, achieve clean lint + build</name>
  <files>shared/references/skill-schema.md, motto.yaml, skills/authoring-a-skill/SKILL.md, skills/motto-project-setup/SKILL.md, skills/motto-release/SKILL.md</files>
  <read_first>
    - 04-RESEARCH.md §Exact Content Blocks (the verbatim, validator-traced frontmatter + body-spine blocks for motto.yaml and all three SKILL.md files, and the skill-schema.md outline)
    - 04-RESEARCH.md §Authoring Order (the load-bearing creation sequence)
    - 04-RESEARCH.md §Body Authoring Constraints (BA1 fenced-code-block rule, BA2 Role-line-must-be-a-real-sentence rule)
    - src/schema.js (name cascade, audience, body H1 + Role checks, shared_references safe-basename + membership), src/config.js (truthy fields + NAME_KEBAB on plugin names), src/frontmatter.js (opening/closing `---`, stray-delimiter detection)
  </read_first>
  <action>
    Create the five files in the mandatory authoring order from 04-RESEARCH.md §Authoring Order.

    Step 1 — shared/references/skill-schema.md: a plain reference file with NO frontmatter (do not start it with `---`; start with an H1). Cover the schema sections listed in 04-RESEARCH.md §`shared/references/skill-schema.md` (name, description, audience, body spine, shared_references, template/dependencies, frontmatter envelope). Minimal-but-real content is fine in this task; Task 2 expands it.

    Step 2 — motto.yaml: use the exact block from 04-RESEARCH.md §`motto.yaml` (SELF-01). Set name: motto, version: "0.0.2" (quoted), a short single-sentence description, plugins.public: motto-skills, plugins.private: motto-private. plugins.private MUST be present now (before the private skill) per gap G7.

    Step 3 — skills/authoring-a-skill/SKILL.md (SELF-02): folder name MUST equal the `name` field exactly. Use the exact frontmatter from 04-RESEARCH.md §`skills/authoring-a-skill/SKILL.md` (name: authoring-a-skill, audience: public, shared_references: [skill-schema] as a bare basename — no .md, no path). Body: first non-blank line is the `# ` H1 title; include a complete-sentence `**Role:**` line (BA2). One useful section is enough for this task.

    Step 4 — skills/motto-project-setup/SKILL.md (SELF-03): same pattern; name: motto-project-setup, audience: public, shared_references: [skill-schema].

    Step 5 — skills/motto-release/SKILL.md (SELF-04): name: motto-release, audience: private, NO shared_references key (the release flow cites no schema content; an empty refs array runs zero shared-ref checks). H1 + complete-sentence Role line.

    Constraints: keep every `description` short, single-sentence, and free of `<`/`>` (gap G1 — length/XML are NOT validator-enforced in v0.0.2; author within the CLAUDE.md spec anyway). No skill `name` field may contain the substrings claude or anthropic (descriptions and bodies may freely say "Claude Code"). Do NOT modify any file under src/ in this task — the validators are consumed as-is.
  </action>
  <verify>
    <automated>node bin/motto.js lint; test $? -eq 0 && node bin/motto.js lint | grep -qx "✓ 3 skills OK"</automated>
    <automated>grep -rhE '^name:' skills/*/SKILL.md | grep -Eiq 'claude|anthropic' && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `node bin/motto.js lint` exits 0 and stdout contains exactly `✓ 3 skills OK`.
    - shared/references/skill-schema.md exists, has no frontmatter, and begins with an H1.
    - motto.yaml has name, version "0.0.2", description, plugins.public=motto-skills, plugins.private=motto-private.
    - Both public skills declare `shared_references: [skill-schema]`; motto-release declares none and is audience: private.
    - Scoped grep over `name:` lines of every SKILL.md matches neither claude nor anthropic (the SKILL.md `name` fields contain no reserved substring; "Claude Code" in bodies/descriptions is allowed).
    <!-- planner-discipline-allow: claude -->
    <!-- planner-discipline-allow: anthropic -->
  </acceptance_criteria>
  <done>Lint exits 0 with `✓ 3 skills OK`; all five files exist in the correct layout; no skill name contains a reserved substring.</done>
</task>

<task type="auto">
  <name>Task 2: Refine the slice — production content + build + regression check</name>
  <files>shared/references/skill-schema.md, skills/authoring-a-skill/SKILL.md, skills/motto-project-setup/SKILL.md, skills/motto-release/SKILL.md</files>
  <read_first>
    - 04-RESEARCH.md §Exact Content Blocks → the "Proposed body sections" lists for each skill, and the skill-schema.md "Sections to include" list
    - .planning/research/FEATURES.md (richer body-section outlines per skill)
    - 04-RESEARCH.md §Build Output Contract (the exact dist/ layout to verify)
    - 04-RESEARCH.md §Pre-emptive Gap Scan (G1 description, G5/BA1 fenced example blocks)
  </read_first>
  <action>
    Expand each SKILL.md body and skill-schema.md from the minimal Task-1 content into the full, genuinely useful section outlines in 04-RESEARCH.md §Exact Content Blocks and FEATURES.md. Keep all frontmatter unchanged — only bodies and the shared reference grow.

    For authoring-a-skill: cover required frontmatter (each field's rule + the exact lint error text from src/schema.js), the body spine contract, optional frontmatter, shared_references usage (bare basenames, how build bundles them), and an annotated minimal example SKILL.md. CRITICAL (BA1/G5): wrap any example SKILL.md that contains `---` delimiters inside a fenced code block so the example cannot be mistaken for stray frontmatter by src/frontmatter.js.

    For motto-project-setup: cover the source-tree layout, every motto.yaml field, running lint (clean vs error output), running build (what dist/ contains, per-bucket plugin.json), and installing the built plugin.

    For motto-release: cover the maintainer release steps in order (tests green, version bump in package.json + motto.yaml, dogfood lint+build, tag+commit, publish — leave publish as a clearly-marked stub since npm packaging is not yet decided, post-release housekeeping).

    Keep descriptions short and XML-free (G1). Then run `motto build` and confirm the dist/ tree matches the Build Output Contract, and confirm the 53 existing tests are still green.

    GAP-FIX NOTE: Per the 9-item gap scan, NO src/ change is expected — all predicted gaps are authorable-around. ONLY if `motto lint` or `motto build` actually misbehaves against this real content (a genuine validator/builder bug, NOT the deferred description length/XML gap G1) do you patch src/. If you do, re-run `node --test` and keep all tests green; if the fix needs a new test, add the test and the fix in the SAME commit (the husky pre-commit hook runs `npm test` and will block a standalone failing-test commit).
  </action>
  <verify>
    <automated>node bin/motto.js build && test -f dist/public/.claude-plugin/plugin.json && test -f dist/private/.claude-plugin/plugin.json && test -f dist/public/authoring-a-skill/references/skill-schema.md && test -f dist/public/motto-project-setup/references/skill-schema.md && test -f dist/public/authoring-a-skill/SKILL.md && test -f dist/private/motto-release/SKILL.md && test ! -e dist/private/motto-release/references && grep -q '"version": "0.0.2"' dist/public/.claude-plugin/plugin.json && grep -q '"version": "0.0.2"' dist/private/.claude-plugin/plugin.json</automated>
    <automated>node bin/motto.js lint | grep -qx "✓ 3 skills OK"</automated>
    <automated>node --test 2>&1 | grep -qE '^# fail 0$' && node --test 2>&1 | grep -qE '^# pass 53$'</automated>
  </verify>
  <acceptance_criteria>
    - `node bin/motto.js build` emits dist/public/ and dist/private/ exactly per 04-RESEARCH.md §Build Output Contract: verbatim SKILL.md copies, skill-schema.md bundled into BOTH public skills' references/, motto-release has NO references/ dir, and per-bucket .claude-plugin/plugin.json carries `"version": "0.0.2"`.
    - `node bin/motto.js lint` still exits 0 with `✓ 3 skills OK` after content expansion.
    - `node --test` reports `# pass 53` and `# fail 0` (no regressions; zero new dependencies — package.json dependencies unchanged).
    - Any example SKILL.md containing `---` in a skill body is inside a fenced code block (lint still clean proves no stray-delimiter false positive).
    - If and only if a real src/ bug surfaced: it is fixed, all tests green, and the fix+test ship in one commit.
  </acceptance_criteria>
  <done>Build produces the expected dist/ tree with bundled refs and versioned per-bucket plugin.json; lint still clean; 53 tests green; no new dependencies.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — lint output, dist structure, content quality</name>
  <read_first>
    - 04-RESEARCH.md §Build Output Contract (expected dist/ tree)
    - 04-RESEARCH.md §Body Authoring Constraints BA2 (Role lines must be real sentences — the validator cannot judge this)
  </read_first>
  <action>Pause for human verification. Present the lint output, the built dist/ tree, and a brief content-quality summary, then wait for the resume signal before marking the phase complete. Make no further file changes until the maintainer responds.</action>
  <what-built>
    A self-hosted Motto skills tree: motto.yaml, shared/references/skill-schema.md, and three SKILL.md files (authoring-a-skill, motto-project-setup public; motto-release private). `motto lint` passes clean and `motto build` emits dist/public/ + dist/private/ with bundled shared refs and per-bucket plugin.json.
  </what-built>
  <how-to-verify>
    1. From the repo root, run `node bin/motto.js lint` — confirm it prints `✓ 3 skills OK` and exits 0.
    2. Run `node bin/motto.js build`, then `find dist -type f` — confirm the tree matches 04-RESEARCH.md §Build Output Contract: dist/public/{authoring-a-skill,motto-project-setup}/SKILL.md + each with references/skill-schema.md; dist/private/motto-release/SKILL.md with NO references/ dir; both .claude-plugin/plugin.json files present.
    3. Open each plugin.json and confirm name (motto-skills / motto-private), version 0.0.2, and description.
    4. Skim each SKILL.md body — confirm the content is genuinely useful (real Role sentences, accurate schema rules), not placeholder text.
    5. Confirm `node --test` still reports 53 passing tests.
  </how-to-verify>
  <resume-signal>Type "approved" to finish the phase, or describe any content/output issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → Motto validators | Untrusted authored frontmatter/body crosses into validateSkill/loadConfig/parseFrontmatter. |
| source tree → dist/ (build copy) | Files are copied verbatim into a distributable plugin; a crafted shared_references entry could attempt path traversal out of shared/references/. |
| private bucket → distribution | Maintainer-only content (motto-release) must not leak into the public plugin bucket. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | shared_references → build copy (src/build.js cp) | medium | mitigate | Author shared_references as the bare basename `skill-schema` (no `/`, no `.`). src/schema.js:133 safe-basename check rejects path-escaping entries at lint; our content conforms. Verified by clean lint in Task 1. |
| T-04-02 | Spoofing | skill `name` field | medium | mitigate | No skill name contains `claude`/`anthropic` (RESERVED ban, src/schema.js:84). Enforced by lint + the scoped name-line grep in Task 1 acceptance. |
| T-04-03 | Information Disclosure | private skill routing (audience) | medium | mitigate | motto-release is audience: private and routed only to dist/private/ (src/build.js:159,183); the private-contradiction gate (build.js:135) requires plugins.private to be set. Verified by Task 2 dist inspection (private content under dist/private/ only). |
| T-04-04 | Tampering | SKILL.md body example with `---` (frontmatter.js stray detection) | low | mitigate | Wrap any example SKILL.md containing `---` in a fenced code block (BA1/G5); H1-first body already breaks YAML-mapping detection. Clean lint confirms no stray-delimiter false positive. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — zero new dependencies per the milestone constraint; package.json dependencies are unchanged and verified by `node --test` staying green. Nothing to slopcheck. |
</threat_model>

<verification>
- `node bin/motto.js lint` exits 0 and prints `✓ 3 skills OK`.
- `node bin/motto.js build` emits dist/public/ + dist/private/ matching the Build Output Contract; both plugin.json carry version 0.0.2; skill-schema.md bundled into both public skills; motto-release has no references/ dir.
- `node --test` reports 53 pass / 0 fail.
- No skill `name` field contains claude or anthropic.
- package.json dependencies unchanged (zero new deps).
</verification>

<success_criteria>
- All seven phase requirements satisfied: SELF-01 (motto.yaml), SELF-02/03 (public skills), SELF-04 (private skill), SELF-05 (shared reference declared + bundled), DOG-01 (clean lint), DOG-02 (expected build output).
- The five ROADMAP success criteria are observably TRUE (see must_haves.truths).
- Human verification approved.
</success_criteria>

<output>
Create `.planning/phases/04-self-hosted-skill-tree-gap-fixes/04-01-SUMMARY.md` when done.
</output>
