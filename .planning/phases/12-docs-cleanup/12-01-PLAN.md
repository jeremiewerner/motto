---
phase: 12-docs-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-04
  - DOC-05
must_haves:
  truths:
    - "README's scaffold section leads with `motto init my-project` and shows the scaffolded tree (skills/hello-world, shared/references/, motto.yaml, .gitignore, .claude-plugin/marketplace.json) — no manual create-motto.yaml prose (DOC-05, D-05)"
    - "README documents the end-to-end ship-your-plugin flow: motto build, commit dist/public/ and push the repo public (prose), consumer /plugin marketplace add <owner>/<repo> then /plugin install (DOC-04, D-01)"
    - "README motto.yaml example shows the scaffolded output verbatim (name: my-project, version: \"0.1.0\", description: A skills project scaffolded by motto init.) not Motto's own config (D-07)"
    - "Install-the-CLI section lists init, lint, build in that order and mentions motto --help once (D-12, D-13)"
    - "No setup-project reference remains anywhere in README (D-11)"
  artifacts:
    - "README.md (rewritten scaffold section + new Ship your plugin section + CLI touch-ups)"
  key_links:
    - "README scaffold + motto.yaml example ↔ src/init.js inline templates (must mirror verbatim; guarded end-to-end by test/init-dogfood.test.js)"
    - "README CLI command list + [path] annotations ↔ bin/motto.js GLOBAL_HELP / LINT_HELP / BUILD_HELP"
---

<objective>
Rewrite README.md so the scaffold path is built around `motto init` and a new "Ship your plugin" section documents the full commit-and-distribute flow. Remove every setup-project reference from the README in the same rewrite.

Purpose: DOC-04 (document the ship-your-plugin flow) and the documentation half of DOC-05 (scaffold section rewritten around `motto init`; README no longer mentions the skill that Plan 02 deletes). This is Commit 1 of the two-commit sequence locked by ROADMAP SC3 — the README that replaces `setup-project` must land before the skill is deleted.

Output: A rewritten README.md. No source code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-docs-cleanup/12-CONTEXT.md
@.planning/phases/12-docs-cleanup/12-PATTERNS.md
</context>

## Artifacts this phase produces

This phase (both plans) creates no new code symbols — no functions, classes, CLI flags, or config keys. Plan 01 produces:

- A new README section: `## Ship your plugin` (heading + Contents/ToC entry).
- A rewritten `## Scaffold a project` section, `## Install the CLI` section, `## End-to-end example` section, and `## Install Motto's skills` table (setup-project row removed).

No new source files, exports, or CLI surface. `motto init`, `motto lint [path]`, `motto build [path]`, `motto --help`, and the scaffolded `marketplace.json` all already exist (shipped in Phases 10-11); this plan only documents them.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Install-the-CLI + Scaffold sections around motto init, add [path]/--help touch-ups</name>
  <files>README.md</files>
  <read_first>
    - README.md (current §Install the CLI at ~26-37, §Scaffold a project at ~41-75 — see the current state before editing)
    - src/init.js (ground truth for scaffold output: HELLO_WORLD_SKILL_MD ~38-47, SCAFFOLD_DESCRIPTION ~51, writeScaffold write-order + file set ~127-179, motto.yaml template ~144, .gitignore template ~152, marketplace.json shape ~159-176)
    - bin/motto.js (GLOBAL_HELP ~44-54, LINT_HELP / BUILD_HELP usage strings ~66/75 — canonical CLI surface the README must not drift from)
  </read_first>
  <action>
    Rewrite §Install the CLI (D-13): after the `npm i -g @jeremiewerner/motto` block, replace the two-command fenced block with a three-command list in init/lint/build order mirroring GLOBAL_HELP — `motto init` (scaffold a skills project in the current directory), `motto lint [path]` (validate skills against the schema), `motto build [path]` (package skills into dist/ plugins). Add one inline sentence after the block noting `motto --help` (or `-h`) prints usage on stdout (D-12). Do NOT add a full CLI reference section (deferred idea — out of scope).

    Rewrite §Scaffold a project (D-05, D-06, D-07): lead with `motto init my-project`. Show the resulting tree mirroring src/init.js writeScaffold exactly — skills/hello-world/SKILL.md, shared/references/, motto.yaml, .gitignore, .claude-plugin/marketplace.json (use hello-world as the scaffolded skill name, NOT a generic my-skill/). Drop the manual "Create motto.yaml at the project root" prose entirely. Replace the motto.yaml example block with the SCAFFOLDED output verbatim from src/init.js: name: my-project / version: "0.1.0" / description followed by the exact SCAFFOLD_DESCRIPTION string `A skills project scaffolded by motto init.` / plugins: public: my-project (no plugins.private — init omits it). Keep the existing motto.yaml field table (current README ~68-75) as reference below the example. Document guards as two one-liners (D-06): the kebab-case name rule is the same rule `motto lint` enforces (single source: schema.js NAME_KEBAB), and `--force` scaffolds into a non-empty directory.

    Append `[path]` to `motto lint` and `motto build` where they appear as bare commands in prose or command fences (D-12), matching bin/motto.js LINT_HELP/BUILD_HELP usage strings (`motto lint [path]`, `motto build [path]`). The internal dev commands in the Development section that use `node bin/motto.js lint`/`build` may be left as-is (Claude's discretion).

    Keep all edits consistent with the documented never-throw behavior of init — do not describe `motto init` as throwing or crashing on bad input; it reports and returns.
  </action>
  <verify>
    <automated>grep -q 'motto init my-project' README.md && grep -q 'hello-world' README.md && grep -q 'A skills project scaffolded by motto init' README.md && grep -q 'name: my-project' README.md && grep -q '.claude-plugin' README.md && grep -q 'motto --help' README.md && test $(grep -c '\[path\]' README.md) -ge 2</automated>
  </verify>
  <done>
    §Scaffold a project leads with `motto init my-project`, shows the scaffolded tree (hello-world + .claude-plugin + .gitignore) and the scaffolded motto.yaml example (SCAFFOLD_DESCRIPTION string), documents the name + --force guards, and keeps the field table. §Install the CLI lists init/lint/build and mentions `motto --help`. `[path]` annotates lint/build usages.
  </done>
  <acceptance_criteria>
    - `grep -q 'motto init my-project' README.md` succeeds (scaffold section leads with the CLI command).
    - `grep -q 'A skills project scaffolded by motto init' README.md` succeeds (motto.yaml example uses the exact SCAFFOLD_DESCRIPTION from src/init.js:51, not Motto's own description).
    - `grep -q 'name: my-project' README.md` succeeds AND `grep -q 'version: "0.1.0"' README.md` succeeds (scaffolded values, not Motto's `name: motto` / `version: "0.0.3"`).
    - README scaffold tree lists `skills/hello-world/SKILL.md`, `.gitignore`, and `.claude-plugin/marketplace.json` (behavior: tree mirrors src/init.js writeScaffold file set).
    - `grep -q 'motto --help' README.md` succeeds and `grep -c '\[path\]' README.md` is ≥ 2.
    - No manual "Create motto.yaml at the project root" instruction remains in the scaffold section (behavior: manual-tree prose dropped, D-05).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Ship your plugin section, update E2E example, remove setup-project from README, fix ToC</name>
  <files>README.md</files>
  <read_first>
    - README.md (current §Validate and build ~105-137, §Publish to npm ~141-152, §End-to-end example ~156-195, §Add the marketplace ~199-207, §Install Motto's skills table ~219-223, Contents/ToC ~11-22 — after Task 1's edits are applied)
    - src/init.js (marketplace.json shape ~159-176: bare relative source `./dist/public/`, name, owner.name — the D-04 one-liner must not contradict this)
  </read_first>
  <action>
    Add a new `## Ship your plugin` section placed AFTER `## Validate and build` and BEFORE `## Publish to npm` (D-01). Section flow (D-01 through D-04): after `motto build` succeeds, commit `dist/public/` and push the repo public — write this as brief prose (a sentence), NOT literal git command fences (D-03; this section deliberately breaks the exact-command convention used elsewhere). Add a one-line note that `motto init` already created `.claude-plugin/marketplace.json` pointing at `./dist/public/`, so nothing needs configuring (D-04 — no schema walkthrough table; matches src/init.js marketplace.json shape). Then give the consumer one-liner `/plugin marketplace add <owner>/<repo>` followed by `/plugin install <plugin>@<repo>`, with a substitution line stating `<owner>/<repo>` is a placeholder for the user's own GitHub repository (D-02 — do NOT hardcode `jeremiewerner/motto` here; that name stays only in the existing §Add the marketplace / §Install Motto's skills sections). Reuse the existing blockquote prerequisite style (current README ~201) for the public-repo requirement, generalized to `<owner>/<repo>`.

    Update §End-to-end example (D-08): change step 2 from the `/motto:setup-project` slash command to `motto init my-project` (CLI), renumber the steps accordingly, and make the example END with the ship steps (motto build → commit/push prose → `/plugin marketplace add <owner>/<repo>` → `/plugin install`), replacing the current terminal marketplace/install steps. Fix the example's final install-output comment so it lists `/motto:author-skill` only (drop the setup-project mention, D-11).

    Remove the `setup-project` row from the §Install Motto's skills table (D-11), leaving only the author-skill row. Add NO migration/removal note (D-11).

    Update the Contents/ToC list so it includes the new "Ship your plugin" entry and every anchor stays accurate (renumber as needed).
    <!-- planner-discipline-allow: setup-project -->
  </action>
  <verify>
    <automated>grep -q '^## Ship your plugin' README.md && grep -q '<owner>/<repo>' README.md && test $(grep -c 'Ship your plugin' README.md) -ge 2 && grep -q 'motto init my-project' README.md && ! grep -q 'setup-project' README.md</automated>
  </verify>
  <done>
    A `## Ship your plugin` section sits after §Validate and build with a placeholder `<owner>/<repo>` consumer one-liner, prose (not fenced) git steps, and the marketplace.json one-line reassurance. The E2E example uses `motto init my-project` and ends with the ship flow. No setup-project reference remains anywhere in README. The Contents list includes the new section and all anchors resolve.
  </done>
  <acceptance_criteria>
    - `grep -q '^## Ship your plugin' README.md` succeeds (section heading exists).
    - `grep -c 'Ship your plugin' README.md` is ≥ 2 (heading + ToC entry).
    - `grep -q '<owner>/<repo>' README.md` succeeds (placeholder consumer one-liner, D-02).
    - `! grep -q 'setup-project' README.md` succeeds (zero setup-project references remain: scaffold section, E2E step 2, install comment, and skills-table row all cleared).
    - Ship section contains no fenced `git commit` / `git push` code block (behavior: git steps are prose, D-03).
    - The E2E example's last steps are the ship flow (`/plugin marketplace add <owner>/<repo>` then `/plugin install`), and step 2 is `motto init my-project` (behavior: D-08).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README instructions → user's public GitHub repo | Users follow the Ship-your-plugin guidance to commit built output and push their repo public |

## STRIDE Threat Register

ASVS level 1; blocking threshold: high. No package-manager installs occur in this phase, so the supply-chain threat (T-12-SC) is not applicable.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Ship-your-plugin guidance (commit + push public) | medium | mitigate | README ship steps reference only `dist/public/` — never `dist/private/` or the whole `dist/`. The scaffolded `.gitignore` (INIT-06) already ignores `dist/private/`, so private skills are not pushed. README must not instruct committing `dist/private/`. |
| T-12-02 | Tampering | Documentation drift — scaffold/CLI examples diverge from real init/CLI output | low | mitigate | Scaffold example mirrors src/init.js templates verbatim (guarded end-to-end by test/init-dogfood.test.js); CLI command list + [path] annotations mirror bin/motto.js GLOBAL_HELP/LINT_HELP/BUILD_HELP. |
</threat_model>

<verification>
- `grep -q '^## Ship your plugin' README.md` (DOC-04 section exists)
- `grep -q 'motto init my-project' README.md` (DOC-05 scaffold rewritten around init)
- `! grep -q 'setup-project' README.md` (README carries no reference to the skill Plan 02 deletes)
- `grep -q '<owner>/<repo>' README.md` (placeholder consumer one-liner, D-02)
- Contents/ToC anchors resolve to real headings (manual read confirms).
</verification>

<success_criteria>
- README documents the end-to-end ship-your-plugin flow: build, commit `dist/public/` + push public (prose), consumer `/plugin marketplace add <owner>/<repo>` and `/plugin install` (ROADMAP SC1, DOC-04).
- README scaffold section is rewritten around `motto init` with no manual tree instructions (ROADMAP SC2, DOC-05).
- Zero `setup-project` references remain in README (prepares the clean delete in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/12-docs-cleanup/12-01-SUMMARY.md` when done.
</output>
