---
phase: 12-docs-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
gap_closure: true
requirements:
  - DOC-04
  - DOC-05
must_haves:
  truths:
    - "README.md's Validate-and-build clean-lint example shows the current skill count (2), matching live `node bin/motto.js lint` output — closes gap #1 (DOC-05)."
    - "README.md's ship-your-plugin install one-liner uses the marketplace placeholder in BOTH occurrences (lines 164, 226), and the prerequisite note explains that placeholder is marketplace.json's `name` field (== project name), not the repo name — closes gap #2 / WR-02 (DOC-04)."
    - "README.md's `--force` description discloses that the five scaffold paths are overwritten if they already exist — closes gap #3 / WR-04 (DOC-05)."
  artifacts:
    - "README.md (three line-level copy corrections; no new symbols)"
  key_links:
    - "README `✓ 2 skills OK` example ↔ live `node bin/motto.js lint` output"
    - "README `@<marketplace>` install placeholder ↔ src/init.js:159-176 marketplace.json top-level `name` field (== project name)"
---

<objective>
Close the three accuracy gaps in README.md that Phase 12 verification (12-VERIFICATION.md) flagged in phase-introduced content. All three are copy-only edits to a single file — no source changes, no test changes.

Purpose: The phase goal ("README documents the full ship-your-plugin path built around `motto init`") implies the documented path is accurate. Two of the three defects are in text this phase itself authored; one is a documentation-drift regression from the setup-project deletion.
Output: README.md with three corrected lines (129; 164/167/226; 89). Nothing else changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-docs-cleanup/12-VERIFICATION.md
@.planning/phases/12-docs-cleanup/12-REVIEW.md
</context>

## Artifacts this phase produces

| Artifact | Change | New symbols |
|----------|--------|-------------|
| `README.md` line 129 | Clean-lint example skill count 3 → 2 | none |
| `README.md` lines 164, 226 | Install one-liner suffix placeholder → `<marketplace>` | none |
| `README.md` line 167 | Prerequisite note gains `<marketplace>` explanation | none |
| `README.md` line 89 | `--force` bullet reworded to disclose overwrite behavior | none |

No new files, no new functions, no test changes, no source changes.

<tasks>

<!-- planner-discipline-allow: 3 skills OK -->
<!-- planner-discipline-allow: @<repo> -->
<!-- planner-discipline-allow: only skips the empty-directory check -->

<task type="auto">
  <name>Task 1: Correct three README accuracy defects (skill count, install placeholder, --force disclosure)</name>
  <files>README.md</files>
  <read_first>
    - README.md (target file; lines 89, 129, 163-167, 221-227 are the edit sites; lines 83 and 173 are OUT OF SCOPE and must stay byte-for-byte unchanged)
    - .planning/phases/12-docs-cleanup/12-VERIFICATION.md (the three `gaps:` entries are the source of truth)
    - .planning/phases/12-docs-cleanup/12-REVIEW.md (WR-02 supplies the exact replacement text for the install note; WR-04 supplies the exact replacement text for the `--force` bullet)
    - src/init.js lines 159-176 (marketplace.json is written with top-level `name` == the project name passed to `motto init`, confirming `<marketplace>` == project name, not repo name)
  </read_first>
  <action>
Make exactly three copy corrections in README.md. Do not touch any other line. In particular, leave line 83 (the `plugins.public` reserved-word row, WR-01) and line 173 (`/motto:release`, WR-03) unchanged — the verifier ruled both out of Phase 12 scope.

EDIT A — gap #1, README.md line 129 (inside the "Validate and build" clean-lint fenced block).
The example currently shows a skill count of three, but live `node bin/motto.js lint` reports two (the setup-project deletion in Plan 02 dropped the count). Change the count from three to two so the fenced line reads exactly:
`✓ 2 skills OK`

EDIT B — gap #2 / WR-02, README.md lines 164 and 226 (the `/plugin install` one-liners in the "Ship your plugin" section and in End-to-end example step 8).
On BOTH lines, change the token after `@` from the repo placeholder to the marketplace placeholder, so each line reads exactly:
`/plugin install <plugin>@<marketplace>`
Leave the two `/plugin marketplace add <owner>/<repo>` lines (163 and 221) exactly as they are — the marketplace-add form correctly uses the owner/repo pair; only the install suffix is wrong.
Then update the prerequisite blockquote at README.md line 167 to add the WR-02 explanation. Replace that blockquote line with exactly:
`> **Prerequisite:** substitute `<owner>/<repo>` with your own public GitHub repository, and `<plugin>` with the `plugins.public` name from your `motto.yaml`. `<marketplace>` is the `name` field of the scaffolded `.claude-plugin/marketplace.json` — `motto init` sets it to your project name, so for a project named `my-project` the install command is `/plugin install my-project@my-project`.`

EDIT C — gap #3 / WR-04, README.md line 89 (the `--force` bullet in the scaffold "Two guards" list).
The current second sentence understates data-loss risk; `writeScaffold` (src/init.js:127-180) overwrites the five scaffold paths with plain `writeFile`. Replace the whole bullet with exactly:
`- **`--force`:** lets `motto init` write into a non-empty directory. It never deletes files, but the five scaffold paths (`motto.yaml`, `.gitignore`, `skills/hello-world/SKILL.md`, `shared/references/.gitkeep`, `.claude-plugin/marketplace.json`) are **overwritten** if they already exist.`

These are the ONLY three edits. Do not renumber sections, do not reflow other prose, do not touch test/dogfood.test.js or any source file.
  </action>
  <acceptance_criteria>
    - Gap #1 positive: `grep -q '✓ 2 skills OK' README.md` succeeds.
    - Gap #1 negative: `! grep -q '3 skills OK' README.md` (the stale count is gone).
    - Gap #2 positive: `[ "$(grep -c '@<marketplace>' README.md)" = "2" ]` (both install lines updated).
    - Gap #2 negative: `! grep -q '@<repo>' README.md` (no install line still uses the repo suffix).
    - Gap #2 note: `grep -q 'name. field of the scaffolded' README.md` succeeds (prerequisite note explains the marketplace placeholder).
    - Gap #2 regression guard: `[ "$(grep -c 'marketplace add <owner>/<repo>' README.md)" = "2" ]` (the correct marketplace-add lines are untouched).
    - Gap #3 positive: `grep -q 'are \*\*overwritten\*\* if they already exist' README.md` succeeds.
    - Gap #3 negative: `! grep -q 'only skips the empty-directory check' README.md` (misleading claim removed).
    - Out-of-scope guard (WR-01): `grep -q 'must not contain .anthropic. or .claude.' README.md` still succeeds (line 83 untouched).
    - Out-of-scope guard (WR-03): `grep -q '/motto:release' README.md` still succeeds (line 173 untouched).
    - No debt markers introduced: `grep -nE 'TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER' README.md` returns nothing.
    - Full suite still green: `node --test` reports 131/131 pass, 0 fail (copy-only edits must not affect tests).
  </acceptance_criteria>
  <verify>
    <automated>grep -q '✓ 2 skills OK' README.md && ! grep -q '3 skills OK' README.md && [ "$(grep -c '@<marketplace>' README.md)" = "2" ] && ! grep -q '@<repo>' README.md && [ "$(grep -c 'marketplace add <owner>/<repo>' README.md)" = "2" ] && grep -q 'are \*\*overwritten\*\* if they already exist' README.md && ! grep -q 'only skips the empty-directory check' README.md && grep -q '/motto:release' README.md && node --test 2>&1 | tail -3</automated>
  </verify>
  <done>All three gaps closed: README.md:129 reads `✓ 2 skills OK`; both `/plugin install` lines use `@<marketplace>` with the prerequisite note explaining the placeholder; the `--force` bullet discloses the overwrite behavior. WR-01 (line 83) and WR-03 (line 173) are unchanged. `node --test` still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only change. No new code, no trust boundary crossed, no package installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02 | Tampering (doc drift) | README.md examples vs real CLI output | low | mitigate | This plan is itself the mitigation for a materialized instance of T-12-02 (stale skill count); the `<verify>` grep gates re-assert README matches live `motto lint` / init behavior, and `node --test` guards against collateral breakage. |
</threat_model>

<verification>
- `grep -q '✓ 2 skills OK' README.md && ! grep -q '3 skills OK' README.md` — gap #1 closed.
- `[ "$(grep -c '@<marketplace>' README.md)" = "2" ] && ! grep -q '@<repo>' README.md` — gap #2 closed on both lines.
- `grep -q 'are \*\*overwritten\*\* if they already exist' README.md && ! grep -q 'only skips the empty-directory check' README.md` — gap #3 closed.
- `grep -q 'must not contain .anthropic. or .claude.' README.md && grep -q '/motto:release' README.md` — deferred WR-01/WR-03 lines untouched.
- `node --test` — 131/131 pass.
</verification>

<success_criteria>
README.md's Validate-and-build example, ship-your-plugin install one-liner (both occurrences + prerequisite note), and `--force` description all match reality. The two deferred out-of-scope lines are byte-for-byte unchanged. Full test suite green. DOC-04 and DOC-05 accuracy caveats from verification are resolved.
</success_criteria>

<output>
Create `.planning/phases/12-docs-cleanup/12-03-SUMMARY.md` when done.
</output>
