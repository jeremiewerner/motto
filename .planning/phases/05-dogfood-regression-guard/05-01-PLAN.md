---
phase: 05-dogfood-regression-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/dogfood.test.js
  - src/config.js
autonomous: true
requirements: [DOG-03, DOG-04]
must_haves:
  truths:
    - "test/dogfood.test.js lints the repo root in-place (read-only, anchored via import.meta.url — not process.cwd()) and asserts clean lint with skill count 3 (DOG-03)"
    - "The same test builds an isolated mkdtemp copy of skills/ + shared/ + motto.yaml (never the repo's own dist/) and asserts the expected dist/ artifacts: public + private buckets, bundled shared ref, per-bucket plugin.json (DOG-03)"
    - "A self-test asserts the NAME_KEBAB regex is identical (.source + .flags) between src/schema.js and src/config.js, failing if the two definitions ever drift (DOG-04)"
    - "npm test passes green (54 existing + new dogfood tests) and the husky pre-commit hook (already running npm test) runs the dogfood test on every commit; package.json dependencies unchanged"
  artifacts:
    - test/dogfood.test.js
    - "src/config.js — export keyword added to the NAME_KEBAB const at line 35"
  key_links:
    - "src/config.js export NAME_KEBAB -> test/dogfood.test.js import of configKebab (without the export the import throws SyntaxError at module load, failing ALL tests in the file)"
    - "REPO_ROOT anchor (resolve(dirname(fileURLToPath(import.meta.url)), '..')) -> lintProject reads the real authored tree read-only"
    - "mkdtemp copy -> buildProject(tempDir) so the destructive dist/ wipe never touches the repo's own dist/"
---

<objective>
Wire a permanent dogfood regression guard into the test suite and close the NAME_KEBAB sync tech debt left from v0.0.1.

Phase 5 has two requirements:
- DOG-03 — a `node:test` dogfood test that lints the repo root in-place (read-only) and builds an isolated `mkdtemp` copy, asserting clean lint + the expected `dist/` artifact set, running on every commit via the existing husky `npm test` pre-commit hook.
- DOG-04 — a self-test asserting the `NAME_KEBAB` regex stays identical between `src/schema.js` and `src/config.js`, requiring one src change (add `export` to the `config.js` const so it is importable).

Purpose: Prove the shipped v0.0.1 lint→build pipeline on Motto's own real skill tree forever, and make the documented "these two regexes must stay identical" invariant machine-verifiable.
Output: `test/dogfood.test.js` (3 describe blocks), one-character `export` change in `src/config.js`. Zero new dependencies.
</objective>

<phase_goal>
**As a** Motto maintainer, **I want to** have an automated guard run on every commit that lints my real skill tree, builds an isolated copy, and checks the two NAME_KEBAB regexes have not drifted, **so that** a broken or removed skill, a malformed build output, or a silent regex divergence fails the commit before it can ship.
</phase_goal>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dogfood-regression-guard/05-RESEARCH.md

# Existing patterns to mirror (before()/after() describe-level setup, mkdtemp, stat() presence checks, readFile+JSON.parse for plugin.json)
@test/lint.test.js
@test/build.test.js
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | Permanent automated test guards the self-hosted tree on every commit + NAME_KEBAB debt closed | Plan 05-01 (both tasks) |
| REQ | DOG-03 — node:test dogfood test: lint REPO_ROOT in-place + build mkdtemp copy; clean lint + dist/ artifacts; runs every commit | Plan 05-01 Task 2 (lint+build), husky already runs npm test |
| REQ | DOG-04 — self-test: NAME_KEBAB identical between schema.js and config.js | Plan 05-01 Task 1 |
| RESEARCH | Exact test skeleton, REPO_ROOT anchor, brittleness checklist (no version hardcoding, assert specific paths, .source/.flags compare) | Plan 05-01 Tasks 1 & 2 |
| RESEARCH | Required src change: add export to src/config.js:35 | Plan 05-01 Task 1 |
| CONTEXT | (no CONTEXT.md — has_context=false) | n/a |

All items COVERED. No unplanned items. No split required.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Export NAME_KEBAB from config.js and add the DOG-04 parity guard</name>
  <files>src/config.js, test/dogfood.test.js</files>
  <read_first>
    - src/config.js:13-35 — the documented "intentional duplicate" comment block and the private `const NAME_KEBAB` at line 35
    - src/schema.js:33 — the already-exported `NAME_KEBAB`
    - .planning/phases/05-dogfood-regression-guard/05-RESEARCH.md §"DOG-04 Strategy" and §"DOG-04 describe block" — the exact import and assertion shape
    - test/config.test.js:4 — confirms current tests import only `loadConfig`, not `NAME_KEBAB`
  </read_first>
  <action>
    First run `grep -rn "from.*config" test/` and confirm the only match is `loadConfig` in test/config.test.js (no direct NAME_KEBAB import exists) — this proves adding the export breaks nothing. Expected: one match, `loadConfig`.

    Then make the single src change required by DOG-04: add the `export` keyword to the `NAME_KEBAB` const declaration in src/config.js (line 35), turning the module-private const into a named export. Do NOT change the regex pattern, the surrounding JSDoc, or any other line — schema.js remains the documented source of truth; the export only makes the duplicate importable for the parity test. Leave the existing cross-reference comment intact.

    Create test/dogfood.test.js with the imports from the research skeleton (node:test describe/it/before/after, node:assert/strict, node:fs/promises, node:path, node:os, node:url) and the REPO_ROOT anchor `resolve(dirname(fileURLToPath(import.meta.url)), '..')`. Per the research brittleness checklist, NEVER use process.cwd() for the anchor. Import `NAME_KEBAB as schemaKebab` from ../src/schema.js and `NAME_KEBAB as configKebab` from ../src/config.js.

    Add the first describe block "NAME_KEBAB parity (DOG-04)" with one synchronous `it` that asserts `schemaKebab.source` strictEquals `configKebab.source` AND `schemaKebab.flags` strictEquals `configKebab.flags`. Compare `.source` and `.flags` separately — never compare the RegExp objects with strictEqual directly, because RegExp equality is by object identity and would always fail even for identical patterns. Include clear assertion messages naming schema.js as the source of truth.
  </action>
  <verify>
    <automated>grep -rn "from.*config" test/ | grep -v NAME_KEBAB | grep -q loadConfig && node -e "import('./src/config.js').then(m=>{if(!m.NAME_KEBAB)throw new Error('NAME_KEBAB not exported');})" && node --test test/dogfood.test.js 2>&1 | grep -qE 'pass[[:space:]]+[1-9]' && node --test 2>&1 | grep -qE 'fail[[:space:]]+0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from.*config" test/` shows only the `loadConfig` import (no pre-existing NAME_KEBAB import to disturb)
    - `src/config.js` line 35 now reads `export const NAME_KEBAB = ...`; the regex literal and JSDoc are byte-for-byte unchanged otherwise
    - `import { NAME_KEBAB } from './src/config.js'` resolves (no SyntaxError)
    - test/dogfood.test.js exists; the DOG-04 describe block passes GREEN on first run (the current regexes already match)
    - The DOG-04 test would FAIL if either regex's `.source` or `.flags` diverged (it compares both, not object identity)
    - Full `node --test` still reports `fail 0`
    - package.json dependencies unchanged
  </acceptance_criteria>
  <done>NAME_KEBAB is exported from config.js, the DOG-04 parity self-test passes, and the full suite stays green with zero new dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add the DOG-03 in-place lint guard and isolated mkdtemp build guard</name>
  <files>test/dogfood.test.js</files>
  <read_first>
    - .planning/phases/05-dogfood-regression-guard/05-RESEARCH.md §"DOG-03 lint describe block", §"DOG-03 build describe block", §"Assertions: Robust vs Brittle", §"Brittleness Checklist" — the exact assertions and the must-not-do list
    - src/lint.js:183-207 — `lintProject` returns `{ ok, errors, count }` where count = number of skill dirs
    - src/build.js:149-150, 207-213 — `buildProject` returns `{ ok, outDir, errors, skillCount, bucketCount }` and wipes `projectRoot/dist` at step 4
    - test/lint.test.js:76-85 — the before()/after() describe-level setup/teardown pattern
    - test/build.test.js:92-110, 200-222 — mkdtemp + stat() presence checks + readFile/JSON.parse for plugin.json
    - motto.yaml — public plugin name `motto-skills`, private plugin name `motto-private`
  </read_first>
  <action>
    Extend the existing test/dogfood.test.js (created in Task 1) by appending two more describe blocks.

    Block "dogfood lint (DOG-03)": one async `it` that calls `lintProject(REPO_ROOT)` and asserts `result.ok === true` (with a message that dumps result.errors on failure), `result.count === 3`, and `result.errors` deepStrictEquals []. This reads the real authored tree in place; lint performs no filesystem mutation so no setup/teardown is needed. The `count === 3` assertion is the real guard — it fails deliberately if a skill is added, removed, or a stray fixture lands in skills/.

    Block "dogfood build (DOG-03)": use describe-level `before()`/`after()`, mirroring test/lint.test.js. In `before()`: mkdtemp a temp dir under os.tmpdir() (prefix like `motto-dogfood-`), then `cp` (recursive) ONLY `skills/`, `shared/`, and `motto.yaml` from REPO_ROOT into the temp dir — never copy dist/. No verbatimSymlinks option is needed on this cp (the real tree has no symlinks). Then call `buildProject(tempDir)` and store the result. CRITICAL: build ONLY against tempDir, NEVER buildProject(REPO_ROOT) — buildProject wipes projectRoot/dist and would destroy the repo's own dist/. In `after()`: guard with `if (tempDir)` then `rm(tempDir, { recursive: true, force: true })` so the temp dir is cleaned even when a test fails.

    Add `it` blocks asserting against the built tempDir output: buildResult.ok === true (dump errors on failure), skillCount === 3, bucketCount === 2, errors deepStrictEquals []. Assert specific artifact paths exist via `stat()` (stat throws ENOENT if missing): dist/public/authoring-a-skill/SKILL.md, dist/public/motto-project-setup/SKILL.md, the bundled references/skill-schema.md inside each of those two public skills, and dist/private/motto-release/SKILL.md. Assert motto-release has NO references/ directory using `assert.rejects` expecting code ENOENT (it declares no shared_references). For the per-bucket manifests, readFile + JSON.parse: public .claude-plugin/plugin.json has name === 'motto-skills' with version and description present (assert.ok on version/description — do NOT hardcode the version string 0.0.2, so the test survives a version bump); private .claude-plugin/plugin.json has name === 'motto-private' with version present.

    Per the research brittleness checklist: do not assert exact version values, do not do full-file content equality, do not depend on filesystem ordering, and tolerate the gitignored repo-level dist/ (you only ever stat paths under tempDir).
  </action>
  <verify>
    <automated>node --test 2>&1 | grep -qE 'fail[[:space:]]+0' && node --test 2>&1 | grep -qE 'tests[[:space:]]+6[0-9]'</automated>
  </verify>
  <acceptance_criteria>
    - test/dogfood.test.js now has three describe blocks: NAME_KEBAB parity (DOG-04), dogfood lint (DOG-03), dogfood build (DOG-03)
    - The lint block asserts lintProject(REPO_ROOT) returns ok:true, count:3, errors:[] — and fails if a skill is broken or removed (real guard)
    - The build block builds only the mkdtemp copy; the repo's own dist/ is never wiped (no buildProject(REPO_ROOT) call anywhere)
    - Build assertions check skillCount:3, bucketCount:2, specific SKILL.md paths, bundled references/skill-schema.md in both public skills, motto-release with no references/, and per-bucket plugin.json name + version present
    - No version string is hardcoded; no full-file equality; no reliance on filesystem ordering
    - after() removes the temp dir even on failure (guarded by `if (tempDir)`)
    - `node --test` reports `fail 0` with a total in the 60s (54 prior + ~10 new)
    - package.json dependencies unchanged
  </acceptance_criteria>
  <done>The dogfood test lints the real tree in place and builds an isolated copy, asserting the full expected dist/ artifact set; the suite is green and runs on every commit via the existing husky npm test hook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner -> repo filesystem | The dogfood test reads the real repo tree (lint) and writes only to an OS temp dir (build); the boundary that matters is build's destructive dist/ wipe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | buildProject dist/ wipe (src/build.js:149-150) | high | mitigate | Build ONLY against the mkdtemp copy — `buildProject(tempDir)`, never `buildProject(REPO_ROOT)`. REPO_ROOT is used read-only for lint. Asserted by Task 2 acceptance criteria. |
| T-05-02 | Denial of Service | temp-dir leak on test failure | low | mitigate | `after()` hook removes tempDir with `rm(..., {recursive,force})`, guarded by `if (tempDir)` so it runs even when before() or an it() throws. |
| T-05-03 | Tampering | NAME_KEBAB drift between schema.js and config.js | medium | mitigate | DOG-04 parity self-test compares `.source` + `.flags` of both exports; any divergence fails the commit. |
| T-05-04 | Spoofing | wrong skill silently substituted (count stays 3) | medium | mitigate | Assert specific artifact paths (authoring-a-skill, motto-project-setup, motto-release) by name via stat(), not just counts. |

No package-manager installs in this phase (zero new dependencies) — no supply-chain (T-*-SC) checkpoint required.
</threat_model>

<verification>
- `node --test` exits 0 with `fail 0`; total test count rises from 54 into the 60s
- `grep -rn "from.*config" test/` shows only the pre-existing `loadConfig` import plus the new dogfood import (no broken imports)
- `git diff --stat` shows exactly two files touched: src/config.js (one-line export) and test/dogfood.test.js (new)
- package.json `dependencies` unchanged (still single dep `yaml`)
- Negative check (manual sanity, not committed): the dogfood test would fail if a skill dir were renamed/removed or if one NAME_KEBAB regex were edited — confirming it is a real guard, not a tautology
</verification>

<success_criteria>
- DOG-03: test/dogfood.test.js lints REPO_ROOT in-place (anchored via import.meta.url) asserting clean lint + count 3, and builds an isolated mkdtemp copy asserting the full dist/ artifact set across public + private buckets with bundled shared ref and per-bucket plugin.json
- DOG-04: NAME_KEBAB is exported from config.js and a self-test asserts .source + .flags parity with schema.js
- npm test green; husky pre-commit (already `npm test`) runs the dogfood test on every commit
- Zero new dependencies; the repo's own dist/ is never wiped by the test
</success_criteria>

<output>
Create `.planning/phases/05-dogfood-regression-guard/05-01-SUMMARY.md` when done
</output>
