---
phase: 24-upgrade-path-ledger-policy
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - skills/release/SKILL.md
  - .claude/CLAUDE.md
autonomous: false
requirements: [UPG-02]
must_haves:
  truths:
    - "A contributor planning a structure/schema change sees the upgrade-path rule at plan time in .claude/CLAUDE.md Constraints (any agent session, not just GSD flows)."
    - "The release skill has a blocking Ledger Gate step that diffs schema-bearing files since the last tag and forces a UPGRADING.md entry or an explicit 'not breaking' verdict before tag/push."
    - "The release skill stays lint-clean (template: procedure conformance) and dogfood counts stay intact."
    - "magma is stamped by walking it through the documented v0.0.7 UPGRADING.md steps alone — proving the prose is sufficient (success criterion 1)."
    - "Motto's own motto.yaml stays unstamped; dogfood.test.js zero-skew-warning assertion stays green (D-07 / Phase 23 D-R4)."
  artifacts:
    - "skills/release/SKILL.md (Ledger Gate step + success-criteria bullet)"
    - ".claude/CLAUDE.md (Constraints upgrade-path bullet)"
  key_links:
    - "release/SKILL.md Ledger Gate -> UPGRADING.md (blocks tag/push pending an entry-or-verdict)."
    - ".claude/CLAUDE.md Constraints -> the release skill's Ledger Gate (plan-time rule points at ship-time enforcement)."
    - "magma/motto.yaml mottoVersion stamp -> the v0.0.7 UPGRADING.md entry (live validation of the prose)."
---

<objective>
Make the standing upgrade-path constraint operational: insert a blocking Ledger Gate step into `skills/release/SKILL.md` (diff heuristic + entry-or-verdict, D-01/D-02), surface the plan-time rule in `.claude/CLAUDE.md` Constraints (D-03), and validate the v0.0.7 ledger entry live by stamping magma from the documented steps alone (D-07, success criterion 1).

Purpose: UPG-01 (Plan 01) makes the ledger *exist*; this plan makes a documented *process* require every future structure/schema change to add a ledger entry before ship, discoverable both where a contributor plans a change and at the release choke point. Without this, the ledger is a one-time doc with no forcing function — the exact gap that stranded the v0.0.5 `<role>` break (UPG-02 / success criterion 3).
Output: an edited `skills/release/SKILL.md`, an edited `.claude/CLAUDE.md`, and a stamped `~/Projects/magma/motto.yaml` (external repo — not tracked by this repo's git).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-upgrade-path-ledger-policy/24-CONTEXT.md
@.planning/phases/24-upgrade-path-ledger-policy/24-RESEARCH.md
@.planning/phases/24-upgrade-path-ledger-policy/24-PATTERNS.md
@skills/release/SKILL.md
@skills/changelog/SKILL.md
@.claude/CLAUDE.md
@UPGRADING.md
</context>

<artifacts_this_phase_produces>
See Plan 01's "artifacts this phase produces" section for the full Phase 24 symbol list. This plan specifically produces:
- `skills/release/SKILL.md` — a new "Ledger Gate" step (inserted after Step 3 Dogfood Check, renumbering Steps 4-9 to 5-10) plus one new `<success_criteria>` bullet describing the gate's guarantee. No new code symbols.
- `.claude/CLAUDE.md` §Constraints — a new `- **Upgrade path**: ...` bullet synced from PROJECT.md line 142. No new code symbols.
- `~/Projects/magma/motto.yaml` — a `mottoVersion` key added (external repo, not this repo's changeset).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Insert the blocking Ledger Gate step into skills/release/SKILL.md</name>
  <files>skills/release/SKILL.md</files>
  <read_first>
    - skills/release/SKILL.md (full file — the existing 9-step process, Step 1's blocking-gate idiom lines 21-29, Step 3 Dogfood Check lines 50-62, Step 4 Push lines 64-72, and the `<success_criteria>` list lines 159-171; also the `template: procedure` frontmatter — the skill MUST stay lint-clean)
    - skills/changelog/SKILL.md (Steps 1-2, the `git describe --tags --abbrev=0` tag-resolution + git-range pattern to adapt into a path-scoped diff)
    - .planning/phases/24-upgrade-path-ledger-policy/24-RESEARCH.md (Pattern 2: Release-Skill Gate Step — the drafted step text, D-01/D-02/D-09 language; Pitfall 2 — document the file-list rationale inline)
    - UPGRADING.md (the file the gate step requires future entries in — confirm the filename the step points at matches what Plan 01 created)
  </read_first>
  <action>
    Insert a new step into `skills/release/SKILL.md`'s `<process>` block between the existing Step 3 (Dogfood Check) and Step 4 (Push). Renumber the existing Steps 4-9 to 5-10 (clean sequential renumbering — the file uses no letter-suffixed steps; RESEARCH.md A2 prefers this over a "Step 3.5"). Update every internal cross-reference to the renumbered steps (e.g. references to "Step 6", "Step 8", "Step 9" inside Steps 5-7 and the success-criteria list) so no reference points at a now-wrong number.

    The new step is `## Step 4 — Ledger Gate`. It must mirror Step 1's blocking idiom exactly: heading → one-line rationale → fenced command → explicit "do NOT proceed until" blocking clause (same strength as Step 1's test gate, per D-01).

    Command (fenced): diff schema-bearing files since the last tag —
    `git diff $(git describe --tags --abbrev=0)..HEAD -- src/schema.js src/templates.js src/config.js src/init.js`

    Blocking clause: if this diff is non-empty, do NOT proceed to the Push step until either (1) a new entry for this version has been added to `UPGRADING.md`, or (2) an explicit "not breaking, no entry needed" verdict has been recorded in this release conversation — silence is not a verdict, state it out loud (D-01 escape hatch). Add the D-09 borderline default in the step body: when in doubt, write the entry — entries are cheap; the v0.0.5 `<role>` stranding (before this policy existed) is the cautionary tale.

    Add one inline sentence documenting the four-file list's rationale and its staleness risk (Pitfall 2): the list is the current home of schema logic; a future maintainer adding a new schema-adjacent file should add it here — do NOT make the list dynamically derived (e.g. `git ls-files src/`), which would over-trigger on every commit. Frame the human/agent verdict as the real backstop, the file list as the mechanical trigger (D-02).

    Scope note (D-08): the gate concerns behavior-defined breaking changes — a change that makes a previously-passing project fail lint/build OR requires editing project files (motto.yaml keys, SKILL.md structure, dist layout consumers depend on). New warnings/wording/flags do not trigger an entry. State the pre-1.0 norm briefly (D-10): a hard break + ledger entry is sufficient and the norm; dual-accept compatibility windows stay case-by-case, not mandated.

    Add one new bullet to the `<success_criteria>` list mirroring the existing bullet style: the Ledger Gate blocks tag/push on a non-empty schema-file diff pending either a new UPGRADING.md entry or a recorded "not breaking" verdict.

    Comment-text discipline: do NOT introduce any literal string into the skill body that a lint check negative-greps for; this is prose guidance, not code. Keep all edits inside the `template: procedure` shape so `motto lint` stays green.
  </action>
  <verify>
    <automated>node bin/motto.js lint 2>&1 | tail -3 && grep -q 'Ledger Gate' skills/release/SKILL.md && grep -q 'git diff' skills/release/SKILL.md && grep -q 'not breaking, no entry needed' skills/release/SKILL.md && node --test test/dogfood.test.js 2>&1 | grep -E '# (pass|fail)'</automated>
  </verify>
  <acceptance_criteria>
    - `skills/release/SKILL.md` contains a `## Step 4 — Ledger Gate` heading positioned between the Dogfood Check step and the Push step.
    - The gate step contains the fenced diff command over `src/schema.js src/templates.js src/config.js src/init.js` and an explicit "do NOT proceed until" blocking clause naming both escape paths (UPGRADING.md entry OR recorded "not breaking, no entry needed" verdict).
    - Existing Steps 4-9 are renumbered 5-10 with all internal cross-references updated (no reference points at a stale step number).
    - The `<success_criteria>` list gains one bullet describing the Ledger Gate guarantee.
    - `node bin/motto.js lint` still reports the skill tree OK (release skill stays lint-clean; template: procedure conformance intact).
    - `node --test test/dogfood.test.js` reports `# fail 0` (dogfood skill counts intact).
    - The inline file-list rationale sentence (Pitfall 2) is present.
  </acceptance_criteria>
  <done>The release skill has a blocking Ledger Gate step matching Step 1's strength, renumbered cleanly, lint-clean, with the file-list rationale documented inline.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the upgrade-path rule in .claude/CLAUDE.md Constraints (D-03)</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - .claude/CLAUDE.md (lines 1-18 — the `<!-- GSD:project-start source:PROJECT.md -->` / `<!-- GSD:project-end -->` managed block and the existing `### Constraints` bullet list lines 11-16)
    - .planning/PROJECT.md (§Constraints line 142 — the upgrade-path constraint's exact locked wording, which is the source-of-truth this bullet syncs from)
    - .planning/phases/24-upgrade-path-ledger-policy/24-PATTERNS.md (Shared Patterns → "CLAUDE.md is machine-managed" — edit-point guidance; the bullet style `- **Label**: sentence.`)
  </read_first>
  <action>
    Add an upgrade-path bullet to the `### Constraints` list in `.claude/CLAUDE.md`, inside the `<!-- GSD:project-start source:PROJECT.md -->` … `<!-- GSD:project-end -->` managed block, immediately after the existing "No content stripping" bullet (making CLAUDE.md's Constraints list match PROJECT.md §Constraints, which already carries this constraint at line 142 but CLAUDE.md is currently stale/out of sync).

    Use the established `- **Label**: sentence.` bullet shape. Bullet content: any change to project structure or an existing feature ships with a way for existing Motto projects to upgrade (documented steps at minimum); breaking changes need an `UPGRADING.md` entry — see the release skill's Ledger Gate. Keep it to one bullet, concise, pointing at both the ledger artifact and the ship-time gate so a plan-time reader knows where enforcement lives.

    This is a one-time manual sync of the CLAUDE.md managed block to match its PROJECT.md source (PROJECT.md line 142 already has the constraint; do NOT re-derive or reword it beyond fitting the CLAUDE.md bullet style). Do not touch any content outside the Constraints list.
  </action>
  <verify>
    <automated>grep -qi 'upgrade path' .claude/CLAUDE.md && grep -q 'UPGRADING.md' .claude/CLAUDE.md && grep -q 'GSD:project-start' .claude/CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/CLAUDE.md` `### Constraints` list contains a new bullet whose label is the upgrade-path rule and whose body names `UPGRADING.md` and the release skill's Ledger Gate.
    - The new bullet sits inside the `<!-- GSD:project-start ... -->` / `<!-- GSD:project-end -->` managed block, after the "No content stripping" bullet.
    - The bullet follows the `- **Label**: sentence.` style of the surrounding bullets.
    - No content outside the Constraints list is modified.
  </acceptance_criteria>
  <done>Any agent session reading .claude/CLAUDE.md sees the upgrade-path constraint at plan time, pointing at both the ledger and the release-skill gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live-validate the v0.0.7 ledger entry by stamping magma from the docs alone</name>
  <files>~/Projects/magma/motto.yaml</files>
  <action>Human-verify checkpoint (no autonomous code change in this repo). Walk `~/Projects/magma` through UPGRADING.md's v0.0.7 stamp-adoption entry using ONLY the documented prose, then report whether the stamp succeeded from the docs alone and feed any friction back as a Plan 01 correction. Details in the checkpoint fields below. Do NOT touch this repo's own motto.yaml.</action>
  <what-built>
    UPGRADING.md's v0.0.7 entry (Plan 01) claims a pre-v0.0.7 project can adopt the `mottoVersion` stamp from documented steps alone. This checkpoint proves that claim against a real project — magma at `~/Projects/magma` — which currently has `<role>` tags (past the v0.0.5 break) but zero `mottoVersion` key (a clean v0.0.7-only validation target, live-confirmed in RESEARCH.md).
  </what-built>
  <read_first>
    - UPGRADING.md (the v0.0.7 entry — the ONLY instructions permitted for this walkthrough; if the steps do not work from the prose alone, the doc is wrong, not the reader — D-07 / success criterion 1)
    - ~/Projects/magma/motto.yaml (the target file — confirm it has no mottoVersion key before starting)
  </read_first>
  <how-to-verify>
    Follow ONLY the numbered steps in UPGRADING.md's `## v0.0.7 — Adopt the mottoVersion stamp` entry — do not use Motto internals, a helper script, or `motto upgrade` (deferred UPG-F1; using a script defeats the "from documented steps alone" validation goal, RESEARCH.md anti-pattern).

    1. Open `~/Projects/magma/motto.yaml`.
    2. Determine the currently-installed motto version as the entry instructs (`motto --version` or `npm ls @jeremiewerner/motto`).
    3. Add the `mottoVersion` key exactly where and how the entry's before/after snippet shows (quoted string, near the `version` key).
    4. Run `motto lint` inside `~/Projects/magma` and confirm behavior matches the entry's Verify line (no new errors; the skew check is now active for magma).

    Report: (a) did the stamp adoption succeed using ONLY the ledger prose? (b) any friction, ambiguity, or wrong detail in the entry that had to be worked around — that friction is a defect in UPGRADING.md's v0.0.7 entry and must be fed back as a Plan 01 correction, not silently patched over. (c) confirm `~/Projects/magma/motto.yaml` now has a `mottoVersion` key.

    CRITICAL — do NOT stamp Motto's own `motto.yaml` at this repo's root. Motto's own tree stays the living unstamped fixture (D-07 / Phase 23 D-R4); `test/dogfood.test.js` asserts zero skew warnings on REPO_ROOT. A `git diff` on this repo touching `motto.yaml` is an immediate red flag (Pitfall 4). The stamp happens entirely inside `~/Projects/magma`, external to this repo.
  </how-to-verify>
  <resume-signal>Type "approved" if magma was stamped from the docs alone with no undocumented friction; otherwise describe the friction/wrong detail so the v0.0.7 UPGRADING.md entry can be corrected before this phase closes.</resume-signal>
  <verify><human-check>magma's motto.yaml gains a mottoVersion key using only UPGRADING.md's v0.0.7 prose; no edit to this repo's own motto.yaml; friction reported for feedback into Plan 01.</human-check></verify>
  <done>magma is stamped from the documented steps alone; any prose defect is captured for correction; Motto's own tree remains unstamped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor/agent → release process | The Ledger Gate is a conversational/procedural control, not a code-enforced one; its strength is human/agent adherence at the single release choke point (D-01, deliberately not CI). |
| .claude/CLAUDE.md managed block ↔ PROJECT.md source | A GSD-synced block; the manual sync here must stay within the markers so a future regeneration is a no-op, not a conflict. |
| this repo ↔ ~/Projects/magma | An external repo edited during validation; magma's changes are NOT part of this repo's changeset and must not leak into it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04 | Repudiation | Ledger Gate skipped silently at a future release | medium | mitigate | The gate mandates an explicit spoken verdict ("not breaking, no entry needed") — silence is defined as non-compliance in the step body; the diff command makes the trigger mechanical, removing the "didn't notice" excuse (D-01/D-02). |
| T-24-05 | Tampering | Editing Motto's own motto.yaml during magma validation | high | mitigate | Checkpoint explicitly forbids touching REPO_ROOT motto.yaml; dogfood.test.js zero-skew assertion is the automated tripwire (Pitfall 4); `git diff --name-only` must not include motto.yaml. |
| T-24-06 | Denial of Service | Gate file-list going stale, missing a future break | medium | mitigate | Inline rationale sentence (Pitfall 2) prompts maintainers to extend the list; human/agent verdict is the real backstop, not the list alone (D-02). |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable — zero new dependencies). |
</threat_model>

<verification>
- `node bin/motto.js lint` reports the skill tree OK (release skill stays lint-clean after the gate insertion).
- `node --test` full suite reports `# fail 0` — critically `test/dogfood.test.js` still asserts zero skew warnings on REPO_ROOT (own tree unstamped, D-07).
- `git diff --name-only` for this plan's commits does NOT include `motto.yaml` (Pitfall 4).
- `~/Projects/magma/motto.yaml` has a `mottoVersion` key after the checkpoint (external repo, verified manually, not by this repo's tests).
- `.claude/CLAUDE.md` and PROJECT.md Constraints lists both carry the upgrade-path bullet.
</verification>

<success_criteria>
- The release skill has a blocking Ledger Gate step (UPG-02 ship-time enforcement) that is lint-clean and cleanly renumbered.
- `.claude/CLAUDE.md` Constraints carries the upgrade-path rule (UPG-02 plan-time discoverability, success criterion 3).
- magma is stamped from the documented v0.0.7 steps alone, with any friction fed back into the entry (success criterion 1).
- Motto's own tree stays unstamped; dogfood zero-skew assertion stays green.
</success_criteria>

<output>
Create `.planning/phases/24-upgrade-path-ledger-policy/24-02-SUMMARY.md` when done.
</output>
