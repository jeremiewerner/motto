---
phase: 24-upgrade-path-ledger-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UPGRADING.md
  - src/version.js
  - test/version.test.js
  - test/upgrading-ledger.test.js
autonomous: true
requirements: [UPG-01]
must_haves:
  truths:
    - "A reader who just saw the skew warning can open UPGRADING.md and find both the v0.0.5 <role> migration and the v0.0.7 mottoVersion stamp-adoption entry (D-06: seed scope is exactly these two entries — no retro-audit of other v0.0.5 validation additions)."
    - "Each entry has a one-line what-breaks statement, numbered steps, one before/after snippet, and a motto lint verify command — no rationale essays."
    - "The skew warning names UPGRADING.md by filename so a first-time reader knows where to look."
    - "node --test stays green: the version.test.js skew-message assertions match the new wording."
  artifacts:
    - "UPGRADING.md (repo root)"
    - "test/upgrading-ledger.test.js"
  key_links:
    - "src/version.js checkSkew() message string -> UPGRADING.md filename (skew remedy resolves to a real file)."
    - "test/version.test.js skew assertions -> the exact new message wording (updated in lockstep)."
---

<objective>
Author the breaking-change ledger `UPGRADING.md` at repo root, seeded with exactly the two UPG-01 entries (v0.0.5 `<role>` migration; v0.0.7 `mottoVersion` stamp adoption), make the Phase 23 skew warning point at the ledger by filename, and add a minimal backstop test guarding the ledger's existence and seed headings.

Purpose: This is the concrete artifact the Phase 23 skew warning's "check the upgrade ledger" remedy resolves to. Without it, the remedy points at nothing (UPG-01 unmet). This plan owns the ledger *content* [UPG-01]; the standing *process* that keeps it current [UPG-02] is Plan 02.
Output: `UPGRADING.md` (2 entries), an edited `src/version.js` skew message, an updated `test/version.test.js` assertion, and a new `test/upgrading-ledger.test.js` backstop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-upgrade-path-ledger-policy/24-CONTEXT.md
@.planning/phases/24-upgrade-path-ledger-policy/24-RESEARCH.md
@.planning/phases/24-upgrade-path-ledger-policy/24-PATTERNS.md
@src/version.js
@src/init.js
@test/version.test.js
@test/doc-sync.test.js
</context>

<artifacts_this_phase_produces>
Symbols and files created or modified across Phase 24 (both plans), so downstream drift verification excludes them:
- `UPGRADING.md` (NEW, repo root) — the breaking-change ledger, seeded with `## v0.0.5 ...` and `## v0.0.7 ...` entry headings.
- `test/upgrading-ledger.test.js` (NEW) — backstop test asserting the ledger exists and contains both seed-entry headings.
- `src/version.js` `checkSkew()` message string — edited to name `UPGRADING.md` (no new symbols; wording-only change inside the existing template literal).
- `test/version.test.js` skew-message assertions — regex updated in lockstep with the new wording.
- `skills/release/SKILL.md` — new "Ledger Gate" step (Plan 02).
- `.claude/CLAUDE.md` §Constraints — new upgrade-path bullet synced from PROJECT.md (Plan 02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author UPGRADING.md with the two seed entries</name>
  <files>UPGRADING.md</files>
  <read_first>
    - .planning/phases/24-upgrade-path-ledger-policy/24-RESEARCH.md (Pattern 1: Ledger Entry Template, lines 125-201 — the two seed entries are drafted verbatim there against verified sources)
    - .planning/phases/24-upgrade-path-ledger-policy/24-PATTERNS.md (UPGRADING.md section — content depth contract D-05, heading scheme `## vX.Y.Z — <short break description>`)
    - src/init.js (lines ~150-163 — the exact scaffold key order `name`, `version`, `mottoVersion`, `description`, `plugins`; the v0.0.7 before/after YAML snippet MUST match this order and quoting, per Pitfall 1)
  </read_first>
  <action>
    Create `UPGRADING.md` at repo root (sibling to README.md — NOT under docs/, which does not exist and must not be created; D-04/D-05 lightweight principle).

    Open with a short one-paragraph intro stating: this file lists breaking changes to Motto's project structure/schema and the steps an existing Motto-scaffolded project needs to cross each one; entries are keyed by the version where the break landed; note that versions 0.0.4 and 0.0.5 never hit npm, so a project coming from 0.0.3 crosses both entries on its way to 0.0.6+ (D-11).

    Then two entries, newest-first ordering (v0.0.7 above v0.0.5), each keyed `## vX.Y.Z — <short break description>` (D-11: keyed by the version where the break landed, not by date):

    Entry `## v0.0.7 — Adopt the mottoVersion stamp`: what-breaks line stating nothing breaks (this is not a lint error) — pre-v0.0.7 projects have no `mottoVersion` key so the skew check is silently skipped; adopting the stamp turns skew detection on. Numbered steps: open `motto.yaml`; add a `mottoVersion` key set to the currently-installed motto version (check with `motto --version` or `npm ls @jeremiewerner/motto`); place it near the existing `version` key, noting they are distinct fields (`version` = the project's own version; `mottoVersion` = the tool version the project was last verified against). One before/after YAML snippet with the EXACT scaffold key order from src/init.js (`name`, `version`, `mottoVersion`, `description`, `plugins`), `mottoVersion` as a quoted string. Verify line: `motto lint` — no change in errors; a future skew now surfaces as a warning instead of silently passing.

    Entry `## v0.0.5 — \`**Role:**\` bold line → \`<role>\` section tag`: what-breaks line naming the concrete lint error the reader will see (`motto lint` reports a missing-role error, or an empty-role error if a `<role>` tag exists but is empty) for any SKILL.md still using the old `**Role:**` bold-line convention — the bold line is no longer recognized and becomes inert body text. Numbered steps: in every `skills/<name>/SKILL.md`, find the line starting with the old bold role marker; replace it with a `<role>` … `</role>` block moving the same sentence(s) inside; run `motto lint` and confirm the role error is gone for that skill. One before/after snippet showing the old bold-marker line becoming a `<role>` … `</role>` block. Verify line: `motto lint` → no role errors for the migrated skill(s).

    Content discipline (D-05, hard): each entry is exactly what-breaks + numbered steps + one before/after snippet + verify command. No rationale essays, no design-doc cross-references, no "why we did this" prose. Copy the RESEARCH.md Pattern 1 drafts as the primary source text, adjusting only heading scheme and ordering.
  </action>
  <verify>
    <automated>test -f UPGRADING.md && grep -q '## v0.0.7' UPGRADING.md && grep -q '## v0.0.5' UPGRADING.md && grep -q 'mottoVersion' UPGRADING.md && grep -qi 'role' UPGRADING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `UPGRADING.md` exists at repo root (not under docs/).
    - File contains a heading matching `## v0.0.7` and a heading matching `## v0.0.5`.
    - The v0.0.7 entry's before/after YAML shows keys in the order `name`, `version`, `mottoVersion`, `description`, `plugins` with `mottoVersion` quoted, matching src/init.js's scaffold order.
    - Each entry contains numbered steps, one before/after snippet, and a `motto lint` verify line.
    - No entry contains a "why/rationale" essay paragraph or a link to a design doc (D-05 depth contract).
    - The intro notes 0.0.4/0.0.5 never hit npm (D-11).
  </acceptance_criteria>
  <done>UPGRADING.md exists with both seed entries conforming to the D-05 depth contract and D-11 version-keying.</done>
</task>

<task type="auto">
  <name>Task 2: Point the skew warning at UPGRADING.md and update its test in lockstep</name>
  <files>src/version.js, test/version.test.js</files>
  <read_first>
    - src/version.js (checkSkew(), lines ~92-108 — the pure-function, never-throw invariant stated in the module doc comment lines 1-15; the exact current message template literal on line ~101)
    - test/version.test.js (lines ~102-123 — the two `assert.match(warning.message, /check the upgrade ledger/)` assertions that pin the current wording)
    - .planning/phases/24-upgrade-path-ledger-policy/24-RESEARCH.md (Open Question 1 — recommendation to name the file: `check UPGRADING.md for changes since ${stampedVersion}`)
  </read_first>
  <action>
    In `src/version.js` `checkSkew()`, edit only the older-than-tool branch message string (the `cmp < 0` branch). Change the remedy phrase from the generic "check the upgrade ledger for changes since ${stampedVersion}" to name the file: "check UPGRADING.md for changes since ${stampedVersion}". Keep the rest of the message identical (still names both `stampedVersion` and `toolVersion`). This is a wording-only change inside the existing template literal — do NOT add try/catch, filesystem I/O, or any throw path; the module's never-throw / pure-function invariant is a standing project rule.

    In `test/version.test.js`, update both assertions that match `/check the upgrade ledger/` (the test at ~line 102 and the minor-segment test at ~line 120) to match the new wording `/check UPGRADING\.md/`. Leave the version-substring assertions (`/1\.2\.3/`, `/1\.3\.0/`, etc.) unchanged. Grep the file for `check the upgrade ledger` first to confirm exactly two occurrences and update all of them — no occurrence may remain matching the old phrase.

    Do NOT change the newer-than-tool branch ("upgrade motto") message or its test.
  </action>
  <verify>
    <automated>node --test test/version.test.js 2>&1 | grep -E '# (pass|fail)' && ! grep -q 'check the upgrade ledger' src/version.js && ! grep -q 'check the upgrade ledger' test/version.test.js && grep -q 'UPGRADING.md' src/version.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/version.js` `checkSkew()` older-than-tool branch message contains the substring `UPGRADING.md` and no longer contains `check the upgrade ledger`.
    - `checkSkew()` has no `try`/`catch`, no filesystem call, and no `throw` (never-throw invariant preserved).
    - `test/version.test.js` has zero occurrences of `check the upgrade ledger`; both former occurrences now assert on `UPGRADING.md`.
    - `node --test test/version.test.js` reports `# fail 0`.
    - The newer-than-tool ("upgrade motto") branch message and its test are unchanged.
  </acceptance_criteria>
  <done>The skew remedy names UPGRADING.md; version.test.js passes against the new wording with zero stale-phrase matches.</done>
</task>

<task type="auto">
  <name>Task 3: Add the ledger backstop test</name>
  <files>test/upgrading-ledger.test.js</files>
  <read_first>
    - test/doc-sync.test.js (imports pattern lines 1-11, REPO_ROOT anchoring via import.meta.url, the `assert.ok(docText.includes(...))` idiom — the exact structural template to copy)
    - .planning/phases/24-upgrade-path-ledger-policy/24-PATTERNS.md (test/upgrading-ledger.test.js section — the drafted describe/it shape; Open Question 2 recommends existence + heading check only, NOT source-text parity)
    - UPGRADING.md (the file this test guards — confirm the exact seed headings `## v0.0.5` and `## v0.0.7` exist before asserting on them)
  </read_first>
  <action>
    Create `test/upgrading-ledger.test.js` as a minimal existence + seed-heading backstop, mirroring `test/doc-sync.test.js`'s structure. Anchor `REPO_ROOT` via `resolve(dirname(fileURLToPath(import.meta.url)), '..')` — never `process.cwd()` (repo-wide convention).

    One `describe('upgrading-ledger backstop', ...)` with one `it('UPGRADING.md exists and contains both seed entries', ...)`: read `UPGRADING.md` from REPO_ROOT via `readFile(..., 'utf8')`, then assert the text includes `## v0.0.5` and `## v0.0.7`, with a descriptive message on each assertion (e.g. missing v0.0.5 <role> migration entry heading / missing v0.0.7 mottoVersion stamp entry heading).

    Do NOT attempt source-text parity against any code file — there is no source-of-truth code the ledger mirrors, so a parity check would just re-encode content with no behavioral assertion (Open Question 2). Do NOT parse git tags or attempt to verify that *future* entries exist — that judgment is deliberately human/agent-side (D-01), not mechanized.
  </action>
  <verify>
    <automated>node --test test/upgrading-ledger.test.js 2>&1 | grep -E '# (pass|fail)'</automated>
  </verify>
  <acceptance_criteria>
    - `test/upgrading-ledger.test.js` exists and anchors REPO_ROOT via `import.meta.url` (no `process.cwd()`).
    - The test reads `UPGRADING.md` and asserts it includes both `## v0.0.5` and `## v0.0.7`.
    - `node --test test/upgrading-ledger.test.js` reports `# fail 0`.
    - `node --test` (full suite) reports `# fail 0`.
    - The test does NOT reference git tags or attempt source-text parity against a code file.
  </acceptance_criteria>
  <done>The backstop test passes and guards the ledger's existence and both seed headings; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project motto.yaml → checkSkew() | Untrusted `mottoVersion` string crosses into the skew comparator; already handled by the never-throw parseVersion contract (Phase 23, VER-05). This plan's message-wording edit does not widen that surface. |
| UPGRADING.md → human reader | Static doc content; no code parses it except the backstop test's substring check. No injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | src/version.js checkSkew() message edit | low | mitigate | Wording-only change inside the existing template literal; no new input handling, no I/O, no throw path added — never-throw invariant preserved and asserted by Task 2's acceptance criteria (no try/catch/throw). |
| T-24-02 | Denial of Service | checkSkew() on malformed mottoVersion | low | accept | Pre-existing parseVersion never-throw guard (Phase 23) already collapses any malformed string to null; unchanged by this plan. |
| T-24-03 | Information Disclosure | UPGRADING.md content | low | accept | Public doc containing only migration steps and non-secret before/after snippets; no credentials or internal-only paths. |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable — zero new dependencies). |
</threat_model>

<verification>
- `node --test` full suite reports `# fail 0` (includes version.test.js and the new upgrading-ledger.test.js).
- `grep -c 'check the upgrade ledger' src/version.js test/version.test.js` returns 0 for both files.
- `UPGRADING.md` exists at repo root with `## v0.0.5` and `## v0.0.7` headings.
- Repo's own `motto.yaml` is NOT touched (Pitfall 4 — the own tree stays unstamped; `git diff --name-only` for this plan must not include `motto.yaml`).
</verification>

<success_criteria>
- UPGRADING.md exists with exactly the two seed entries, each conforming to the D-05 depth contract (what-breaks + numbered steps + one before/after + verify command).
- The skew warning names UPGRADING.md; both version.test.js assertions updated in lockstep and passing.
- The backstop test guards the ledger and passes; full suite green.
- Success criterion 2 (v0.0.5 `<role>` entry findable with the steps to cross it) is satisfiable by reading UPGRADING.md.
</success_criteria>

<output>
Create `.planning/phases/24-upgrade-path-ledger-policy/24-01-SUMMARY.md` when done.
</output>
