# Milestone v0.0.1: Core lint + build CLI

**Status:** ✅ SHIPPED 2026-06-30
**Phases:** 1-3
**Total Plans:** 5 (3 + 1 + 1)

## Overview

The initial release of Motto: author Agent Skills in a fixed source tree (`skills/<name>/` + `shared/references/`), lint them against a strict schema with `motto lint`, and package lint-passing skills into self-contained, standard Agent Skill plugin folders with `motto build`. Output is portable — loadable by any agent with no Motto present. Built on Node ≥20, plain ESM, `node --test`, single runtime dependency `yaml`.

## Phases

### Phase 1: Pure Core

**Goal**: The parsing and validation foundation produces correct, deterministic results on all valid and malformed skill inputs.
**Depends on**: Nothing (first phase)
**Requirements**: PARSE-01..04, LINT-01..05, CONF-01..03
**Plans**: 3 plans

Plans:
- [x] 01-01: Scaffold (package.json + yaml) and `parseFrontmatter` (PARSE-01..04)
- [x] 01-02: `validateSkill` skill-schema validator (LINT-01..05)
- [x] 01-03: `loadConfig` core config validator (CONF-01..03)

**Details:**
Three pure, side-effect-free, never-throw functions returning a uniform `errors[]` model. `parseFrontmatter` normalizes input (BOM strip + CRLF→LF) first, parses with `YAML.parseDocument` (errors mapped, never thrown), and structurally detects a stray inner `---`. `validateSkill` enforces letter-start kebab `name` = folder, no `anthropic`/`claude` substring, `audience` ∈ {public,private}, `# Title` + `**Role:**` body spine, and safe-basename `shared_references`. `loadConfig` validates `motto.yaml` identity fields and plugin names with the same kebab regex. Verified 16/16 must-haves.

### Phase 2: motto lint

**Goal**: Developers can run `motto lint` against a project and get a complete, deterministic error report or a clean exit.
**Depends on**: Phase 1
**Requirements**: LINT-06, LINT-07, CLI-01
**Plans**: 1 plan

Plans:
- [x] 02-01: `lintProject` orchestrator + `motto` CLI binary (LINT-06, LINT-07, CLI-01)

**Details:**
`src/lint.js` `lintProject(cwd) → {ok, errors, count}` wires the Phase 1 validators with cwd-based discovery (immediate subdirs of `skills/`, dotfiles ignored), alphabetical sorting, per-file error isolation, shared-ref scanning from `shared/references/`, and config-first error reporting. `bin/motto.js` is a thin `parseArgs` shell using `process.exitCode`. Missing SKILL.md and zero-skills are hard errors. Verified 7/7 must-haves; all 4 success criteria empirically confirmed.

### Phase 3: motto build

**Goal**: Developers can run `motto build` to produce self-contained, distributable Claude Code plugin folders ready for installation.
**Depends on**: Phase 2
**Requirements**: BUILD-01..06, CLI-02
**Plans**: 1 plan

Plans:
- [x] 03-01: `buildProject` packager (lint gate, verbatim copy, shared-ref bundling, conditional buckets, plugin.json) + CLI wiring (BUILD-01..06, CLI-02)

**Details:**
`src/build.js` `buildProject(cwd) → {ok, outDir, errors}` runs lint + collision + private-contradiction checks BEFORE wiping `./dist`, then copies each skill verbatim with `fs.cp({recursive:true, verbatimSymlinks:true})` (preserves relative symlinks — the default rewrites them to absolute), bundles declared `shared_references` into each skill's `references/`, and emits per-bucket `.claude-plugin/plugin.json` = `{name: plugins.<audience>, version, description}`. `dist/public/` always emitted; `dist/private/` only when a private skill exists AND `plugins.private` set. Verified 7/7 must-haves; all 5 success criteria empirically confirmed.

## Milestone Summary

**Decimal Phases:** None.

**Key Decisions (validated):**
- Output = standard Agent Skills, copied verbatim (no content stripping / no compiler).
- Uniform never-throw `errors[]` model across the whole core.
- `YAML.parseDocument` (errors accumulated) over `YAML.parse` (throws).
- Binary `audience` (public|private); private bucket optional and conditional.
- `name` = letter-start kebab regex, shared between schema + config (D-16).
- CLI: `node:util parseArgs`, cwd-only, `process.exitCode` (never `process.exit(1)`).

**Stats:** 3 phases · 5 plans · 41 commits · ~2192 LOC (src/test/bin) · 53 tests · single dep `yaml` · 2026-06-29 → 2026-06-30.

**Tech Debt (carried forward):**
- `NAME_KEBAB` duplicated in `src/schema.js` (export) and `src/config.js` (intentional copy, D-16, cross-ref comment) — manual sync point; add an equality self-test or import the export.
- `02-01`/`03-01` SUMMARY frontmatter `requirements_completed` left empty (reqs verified satisfied via VERIFICATION + traceability).
- CLAUDE.md states `yaml` license MIT; actual is ISC.
- ROADMAP Phase-1 SC#1 wording says missing-frontmatter "throws"; locked design + code are never-throw.

**Not validated (no real consumer yet):** Motto authors zero skills of its own — the tool has not been dogfooded on a real `skills/` tree. Candidate for the next milestone.

**Audit:** `.planning/v0.0.1-MILESTONE-AUDIT.md` — PASSED (22/22 reqs, 3/3 phases, integration clean, 6/6 E2E flows, 53/53 tests).
