---
phase: 22-public-flip-token-hardening
plan: 05
type: execute
wave: 3
depends_on: ["22-04"]
files_modified:
  - README.md
autonomous: false
requirements: [OPEN-03]

must_haves:
  truths:
    - "A logged-out stranger can follow README links, install the CLI via npm, add the marketplace, install the plugin, and see the skill in Claude Code's skill list (D-09, D-10)"
    - "README.md carries a CI badge and an npm version badge, and its publish-flow section describes the actual current flow (tests -> bump -> push -> CI publishes), not the stale pre-Phase-21 local `npm publish` step (D-11, Pitfall 3)"
  artifacts:
    - "README.md (badges + corrected Publish to npm section)"
    - "Recorded stranger-walkthrough checklist results"
  key_links:
    - "README's publish-flow description matches skills/release/SKILL.md's actual current local flow (tests -> bump -> push only)"
---

<objective>
Have the maintainer walk the logged-out-stranger path end to end (README, npm install, marketplace add + install, skill-list appearance) now that the repo is public, then fix README.md's known-stale "Publish to npm" section and add CI/npm badges, plus anything else the walkthrough surfaces.

Purpose: OPEN-03 requires the public repo to actually work for a stranger, not just exist. D-09/D-10 scope the walkthrough to the distribution path only (not skill invocation, which is already v0.0.5-verified); D-11 scopes the README fix to badges + accuracy, bounded by findings — no rewrite.
Output: A recorded walkthrough checklist result; an accurate, badged README.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-public-flip-token-hardening/22-CONTEXT.md
@.planning/phases/22-public-flip-token-hardening/22-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols and identifiers created by this plan (new here — exclude from source-drift acknowledgement):

- New README.md badges: GitHub Actions CI badge, npm version badge (both linked images, placed directly under the H1 title)
- Rewritten README.md "Publish to npm" section content (heading text and TOC anchor unchanged, body content replaced)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Stranger walkthrough checklist</name>
  <read_first>
    - .planning/phases/22-public-flip-token-hardening/22-CONTEXT.md (D-09 — checklist scope: README links, `npm i -g` install, marketplace add + install + skill list; D-10 — distribution path only, no skill invocation)
    - README.md (current content the walkthrough follows)
    - .claude-plugin/marketplace.json (the marketplace entry the walkthrough exercises)
  </read_first>
  <action>
    Blocking human checkpoint (requires a genuinely logged-out browser session and a clean shell — cannot be simulated in this environment): the maintainer walks the following checklist from a logged-out browser and a clean shell/terminal, recording pass/fail per item: (1) open https://github.com/jeremiewerner/motto logged out and confirm the README renders with every link resolving; (2) run `npm i -g @jeremiewerner/motto` in a clean shell and confirm it installs and `motto --help` runs; (3) in a Claude Code session, run `/plugin marketplace add jeremiewerner/motto` then `/plugin install motto@motto`; (4) confirm the installed skill (`build-skill`) appears in Claude Code's skill list. This is a distribution-path check only — do not invoke the skill's behavior (D-10; skill behavior is already v0.0.5-verified). Record each item's pass/fail and any inaccuracy found (e.g., stale instructions, broken link) in the resume signal so Task 2 can act on it.
  </action>
  <what-built>
    The repo is now public (plan 22-04); this checkpoint proves the actual stranger-facing distribution path works end to end.
  </what-built>
  <verify>
    <human-check>All 4 checklist items pass from a logged-out browser + clean shell; any failures or inaccuracies are reported in the resume signal.</human-check>
  </verify>
  <done>The logged-out stranger path (README, npm install, marketplace add+install, skill-list appearance) is walked and its results recorded.</done>
  <how-to-verify>
    See the four checklist items in the action field above; record pass/fail for each.
  </how-to-verify>
  <resume-signal>Type "approved — all 4 items pass" or list which item(s) failed and what was inaccurate (e.g., stale README instructions, a broken link).</resume-signal>
  <acceptance_criteria>
    - All 4 checklist items are explicitly recorded pass/fail in the SUMMARY.
    - Any reported inaccuracy is carried forward as a concrete input to Task 2.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: README accuracy pass — badges + publish-flow fix + walkthrough findings</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content — the "Publish to npm" section, currently describing a local `npm publish` step, is the known-stale target per RESEARCH.md Pitfall 3)
    - skills/release/SKILL.md (the current, accurate local flow: tests -> `npm version` bump -> `git push --follow-tags`; CI publishes via OIDC per plan 22-02)
    - package.json (`name`/`version` fields — `@jeremiewerner/motto` — for the npm badge target)
    - .github/workflows/ci.yml (workflow filename `ci.yml`, for the CI badge target)
    - .planning/phases/22-public-flip-token-hardening/22-CONTEXT.md (D-11 — badges + accuracy fixes only, bounded by findings, no rewrite, no contributing-section expansion)
  </read_first>
  <action>
    Add two badges directly under the README's H1 title (`# Motto`), before the existing bold framework-description line: a GitHub Actions CI badge image linking to `https://github.com/jeremiewerner/motto/actions/workflows/ci.yml` with badge source `https://github.com/jeremiewerner/motto/actions/workflows/ci.yml/badge.svg`, and an npm version badge image linking to `https://www.npmjs.com/package/@jeremiewerner/motto` with badge source `https://img.shields.io/npm/v/@jeremiewerner/motto`. Replace the body of the existing "Publish to npm" section (keep its heading text and TOC anchor unchanged) so it describes the actual current flow: run tests, `npm version X.Y.Z` (bumps package.json + motto.yaml, creates the tag), `git push --follow-tags` — CI then publishes via OIDC trusted publishing — pointing readers to `skills/release/SKILL.md` for the full runbook. Remove the stale local `npm publish` line entirely. Then apply any additional accuracy fix reported in Task 1's resume signal, bounded strictly to what was reported — no unrelated rewrite, no contributing-section expansion (D-11).
  </action>
  <verify>
    <automated>grep -c 'actions/workflows/ci.yml/badge.svg' README.md</automated>
    <automated>grep -c 'img.shields.io/npm/v/@jeremiewerner/motto' README.md</automated>
    <automated>grep -c 'fires prepublishOnly' README.md</automated>
  </verify>
  <done>README.md carries CI + npm badges and an accurate publish-flow description; any stranger-walkthrough-reported inaccuracy is fixed.</done>
  <acceptance_criteria>
    - README.md contains both the CI badge image markdown and the npm version badge image markdown within the first 10 lines after the H1 title.
    - README.md's "Publish to npm" section no longer contains the stale `npm publish # fires prepublishOnly` local-publish line (`grep -c 'fires prepublishOnly' README.md` returns 0).
    - The "Publish to npm" section now describes tests -> `npm version` bump -> `git push --follow-tags` -> CI publishes.
    - Any inaccuracy reported by Task 1 is fixed and no longer present.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public README/npm/marketplace → logged-out stranger | The stranger's first-contact experience; inaccurate instructions erode trust in the project even without a direct security vulnerability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-07 | Information Disclosure (of a misleading process, not a credential) | README instructions describing a retired local-publish flow | low | mitigate | Badges + accuracy fixes driven by a real logged-out walkthrough (Task 1), not assumption; the stale local `npm publish` step is explicitly removed (Task 2) |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages; the walkthrough's `npm i -g` step installs only the already-published, already-audited `@jeremiewerner/motto` package |
</threat_model>

<verification>
- Walkthrough checklist results are recorded (4/4 items, pass/fail).
- README.md's badges and publish-flow section pass the automated grep checks above.
</verification>

<success_criteria>
- A logged-out stranger can successfully follow README -> npm install -> marketplace add/install -> see the skill in Claude Code's skill list (OPEN-03, fully closed).
- README.md is accurate and carries CI + npm badges (D-11).
</success_criteria>

<output>
Create `.planning/phases/22-public-flip-token-hardening/22-05-SUMMARY.md` when done.
</output>
