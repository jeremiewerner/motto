---
phase: 22-public-flip-token-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md
  - .planning/PROJECT.md
autonomous: true
requirements: [OPEN-01, OPEN-02]

must_haves:
  truths:
    - "gitleaks git . has been run against full history and the result (version, HEAD SHA, exit code, findings count) is recorded in a committed leak-safe markdown file, per D-05"
    - "A mechanical PII sweep (working tree grep + git log -p + commit author/committer email extraction) has run across full history and every distinct finding is triaged in the same record, per D-03/D-04"
    - "The `.planning/` public-as-is visibility decision is an explicit, dated row in PROJECT.md's Key Decisions table, not a default, per D-01/D-02"
  artifacts:
    - ".planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md"
    - "PROJECT.md Key Decisions table row for `.planning/` visibility"
  key_links:
    - "the PROJECT.md decision row cites the PII sweep's commit-metadata-email finding so the maintainer's acceptance is informed, not blind"
---

<objective>
Run the first (triage-lead-time) full-history gitleaks secrets scan and a mechanical PII sweep, recording both leak-safely in a new phase artifact, then record the `.planning/` visibility decision (public as-is, D-01) as an explicit row in PROJECT.md's Key Decisions table.

Purpose: OPEN-01 requires a recorded, auditable secrets-scan result before the one-way public flip; OPEN-02 requires the `.planning/` visibility choice to be an explicit decision, not a silent default. This plan produces both records early so there is triage lead time before the phase's later final rescan (plan 22-04) and flip.
Output: `.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md` (Scan #1 + PII Sweep sections) and one new PROJECT.md Key Decisions row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-public-flip-token-hardening/22-CONTEXT.md
@.planning/phases/22-public-flip-token-hardening/22-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols and identifiers created by this plan (new here — exclude from source-drift acknowledgement):

- New file: `.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md`
- New markdown headings in that file: `## Scan #1 — Triage`, `## PII Sweep`
- New row in `.planning/PROJECT.md`'s `## Key Decisions` table (Decision column contains `.planning/` and `public as-is`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the initial full-history gitleaks scan and record it leak-safely</name>
  <files>.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md</files>
  <read_first>
    - .planning/phases/22-public-flip-token-hardening/22-RESEARCH.md (Pattern 2 — Leak-Safe Recorded Verification, D-05 record shape; the live-verified scan example: `gitleaks git . --exit-code 0` on HEAD `97c71fd`, gitleaks 8.30.1, "403 commits scanned... no leaks found", exit 0)
    - .planning/phases/22-public-flip-token-hardening/22-CONTEXT.md (D-05 — exact command, version, HEAD SHA, exit code, findings count, per-hit triage table; never the matched value; D-06 — real secrets are rotated, never purged; D-07 — false positives triaged in the record only, no `.gitleaks.toml`)
  </read_first>
  <action>
    From the repo root, run `gitleaks version` and `git rev-parse HEAD` to capture the tool version and the exact commit being scanned. Run `gitleaks git . --report-format json --report-path <scratch-tmp-path> --exit-code 0` (write the JSON report to the session scratch directory, never under the repo tree) and capture gitleaks' own stdout summary line (commits scanned count, leak count). Read the JSON report only to extract structured fields if findings are non-zero (rule ID, short commit SHA, file path — never the matched secret string), then delete the scratch JSON file. Create `.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md` with a `## Scan #1 — Triage` section recording: the exact command run, gitleaks version, HEAD SHA scanned, commits-scanned count, exit code, findings count. If findings is 0, state that plainly. If findings > 0, add a table with columns `commit SHA (short) | file | rule ID | maintainer action` — real secrets get `action = rotate` (D-06, no history purge), false positives get `action = false positive (triaged, no .gitleaks.toml per D-07)` — and no column ever holds the matched secret text.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md && grep -c '^## Scan #1' .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md</automated>
    <automated>git status --porcelain .planning/phases/22-public-flip-token-hardening/ | grep -c '\.json$' || true</automated>
  </verify>
  <done>22-SECRETS-SCAN.md exists with a complete, leak-safe Scan #1 record (command, version, HEAD SHA, commits scanned, exit code, findings count, per-hit triage if any); no raw gitleaks JSON report is staged or committed.</done>
  <acceptance_criteria>
    - `.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md` exists and contains a `## Scan #1 — Triage` heading with a gitleaks-version line, a HEAD-SHA line, an exit-code line, and a findings-count line.
    - `git status --porcelain .planning/phases/22-public-flip-token-hardening/` shows no untracked or staged `.json` file (the raw report is never committed).
    - If findings count > 0, the record contains a per-hit table with commit/file/rule/action columns and no matched-secret-text column.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the mechanical PII sweep across full history and append findings</name>
  <files>.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md</files>
  <read_first>
    - .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md (created by Task 1 — append a new section, do not modify Scan #1)
    - .planning/phases/22-public-flip-token-hardening/22-RESEARCH.md (Common Pitfall 2 — gitleaks does not scan commit author/committer headers; live-verified finding `jeremie@studiometa.fr` across recent commits, distinct from the intentionally-public `jeremiew@pm.me`; Standard Stack "Supporting" row for `git log -p --all` / `git log --all --format='%ae|%ce'`)
    - .planning/phases/22-public-flip-token-hardening/22-CONTEXT.md (D-03 — targeted prose sweep, not a full agent audit; D-04 — sweep scope is working tree + one mechanical history pass)
    - .claude-plugin/marketplace.json (the already-intentionally-public `jeremiew@pm.me` address, for comparison)
  </read_first>
  <action>
    Run three mechanical passes from the repo root and append a `## PII Sweep` section to 22-SECRETS-SCAN.md: (1) `git log --all --format='%ae|%ce' | sort -u` to list every distinct commit author/committer email across full history; (2) a working-tree grep across `.planning/` for email and internal-URL-looking patterns (e.g. `grep -rEn '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' .planning/ --include='*.md'`); (3) `git log -p --all | grep -Eo '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' | sort -u` to catch emails that only ever appeared in diff content. Record every distinct email/URL found. For each one, state whether it is already intentionally public (compare against `jeremiew@pm.me` in marketplace.json). Call out `jeremie@studiometa.fr` by name if present in the commit-metadata pass, noting explicitly: gitleaks does not scan commit headers (so this address was invisible to Task 1's scan), and D-01/D-06 already forbid a history rewrite to remove it — so this finding is presented as an explicit item for the maintainer to knowingly accept, feeding directly into Task 3's decision record.
  </action>
  <verify>
    <automated>grep -c '^## PII Sweep' .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md</automated>
  </verify>
  <done>PII sweep results (commit-metadata emails, working-tree grep, diff-content grep) are appended to 22-SECRETS-SCAN.md with an explicit disposition per distinct finding.</done>
  <acceptance_criteria>
    - 22-SECRETS-SCAN.md contains a `## PII Sweep` heading listing every distinct email address returned by `git log --all --format='%ae|%ce' | sort -u`.
    - If `jeremie@studiometa.fr` is present in that output, the record names it explicitly as a commit-metadata-only (gitleaks blind spot) finding requiring maintainer acceptance, not a fix.
    - The record distinguishes the already-public `jeremiew@pm.me` from any other address found.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Record the `.planning/` visibility decision in PROJECT.md's Key Decisions table</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (`## Key Decisions` table — match the existing three-column `| Decision | Rationale | Outcome |` row format exactly)
    - .planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md (Task 2's PII Sweep section — cite its finding in the rationale)
    - .planning/phases/22-public-flip-token-hardening/22-CONTEXT.md (D-01, D-02 — public as-is, no history rewrite, no dedicated flip doc)
  </read_first>
  <action>
    Add exactly one new row to PROJECT.md's `## Key Decisions` table, matching the existing row format. Decision column: "`.planning/` ships public as-is (no history rewrite)". Rationale column: reference D-01's reasoning (tags stay valid, history stays honest, the planning record is a feature for a dev-tooling repo) and explicitly cite the PII sweep's commit-metadata-email finding from Task 2 (name the finding if present) as an accepted, informed exposure since D-06 forbids purge-by-rewrite. Outcome column: "✓ Accepted — Phase 22". Do not create any separate standalone flip document (D-02 explicitly rejects one) — the row plus this phase's 22-SECRETS-SCAN.md are the complete record.
  </action>
  <verify>
    <automated>grep -c 'public as-is' .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md's Key Decisions table carries one new, dated row recording the `.planning/` public-as-is decision, with rationale referencing the PII sweep finding; no separate flip document was created.</done>
  <acceptance_criteria>
    - PROJECT.md's `## Key Decisions` table has exactly one new row whose Decision column contains both `.planning/` and `public as-is`.
    - The row's Rationale column references the commit-metadata PII finding from Task 2.
    - No new standalone markdown file was created for this decision.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private git history → future public git history | Every commit ever made becomes permanently world-readable once the phase's later flip (plan 22-04) executes; this plan is the first gate |
| `.planning/` content → public repo | Planning docs, STATE.md, decisions, and commit metadata become world-readable; PII (author emails) is a separate category gitleaks does not cover |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Information Disclosure | Full git history (all commits, all branches) | high | mitigate | `gitleaks git .` full-history scan (Task 1) with a committed, leak-safe record; any real hit is rotated (D-06), never purged; this is the first of two scans — the final gate is plan 22-04's Scan #2 at the literal pre-flip HEAD |
| T-22-02 | Information Disclosure (privacy, not credential) | Commit author/committer email metadata (`jeremie@studiometa.fr`) — invisible to gitleaks | medium | accept | No technical mitigation available without a history rewrite, which D-01/D-06 explicitly reject; mitigation is informed maintainer consent, recorded in PROJECT.md's Key Decisions row (Task 3) |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages; this plan runs only `gitleaks` (already-installed standalone binary) and `git`/`grep` |
</threat_model>

<verification>
- `.planning/phases/22-public-flip-token-hardening/22-SECRETS-SCAN.md` exists with both `## Scan #1 — Triage` and `## PII Sweep` sections, leak-safe (no raw JSON, no matched secret text, no committed report file).
- PROJECT.md's Key Decisions table has a new row for the `.planning/` public-as-is decision referencing the PII sweep finding.
- This is triage-lead-time work only — the D-08 FINAL rescan (which must equal the literal pre-flip HEAD) happens later, in plan 22-04, after every other content-changing plan in this phase has landed.
</verification>

<success_criteria>
- A committed, auditable, leak-safe record of the first full-history secrets scan exists (OPEN-01, triage pass).
- The `.planning/` visibility decision is an explicit, dated, rationale-backed row in PROJECT.md — not a default (OPEN-02, fully closed by this plan).
- The commit-metadata PII finding (if present) is surfaced for informed maintainer acceptance, not silently absorbed.
</success_criteria>

<output>
Create `.planning/phases/22-public-flip-token-hardening/22-01-SUMMARY.md` when done.
</output>
