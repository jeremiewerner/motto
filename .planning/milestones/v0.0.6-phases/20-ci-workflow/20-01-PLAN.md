---
phase: 20-ci-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/prepare.mjs
  - scripts/prepare-guard-check.mjs
autonomous: true
requirements: [CIW-05]
must_haves:
  truths:
    - "Running `npm ci` in a `.git`-less copy of the repo exits 0 and does NOT invoke husky (the prepare guard no-ops)"
    - "Running `npm install`/`npm ci` at a real checkout root (with `.git` present) still runs husky and installs git hooks"
    - "scripts/ is absent from a tarball consumer's install, and that never breaks the consumer (prepare never fires for a nested dependency)"
  artifacts:
    - "scripts/prepare.mjs — .git-existence guard wrapping `npx husky`"
    - "scripts/prepare-guard-check.mjs — explicit .git-less proof (git archive -> tmp -> npm ci -> assert exit 0)"
    - "package.json prepare field = `node scripts/prepare.mjs`"
  key_links:
    - "package.json `prepare` script -> scripts/prepare.mjs"
    - "scripts/prepare.mjs -> existsSync('.git') decides no-op vs `npx husky`"
    - "scripts/prepare-guard-check.mjs -> git archive HEAD -> npm ci in .git-less tmp tree"
---

<objective>
Guard the husky `prepare` lifecycle script so it no-ops in any `.git`-less context (tarball extraction, `npm ci` in a git-less tree) while still running loudly in a real dev checkout (CIW-05). Then close the flagged D-15 gap with an explicit proof script, because research verified (live npm experiments) that the D-06 pack-install-e2e flow does NOT actually invoke a nested dependency's `prepare` — so D-15's "implicit proof via pack-E2E" premise is false as written.

Purpose: The v0.0.4/v0.0.5 install path must never fail on a missing `.git`, and CIW-05 must be genuinely exercised, not assumed.
Output: `scripts/prepare.mjs`, `scripts/prepare-guard-check.mjs`, and the `package.json` `prepare` field pointed at the guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ci-workflow/20-CONTEXT.md
@.planning/phases/20-ci-workflow/20-RESEARCH.md
@.planning/phases/20-ci-workflow/20-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols/paths created by Phase 20 (across all plans) — exclude from drift/new-symbol flagging:
- `scripts/prepare.mjs`, `scripts/prepare-guard-check.mjs`, `scripts/pack-install-e2e.mjs`, `scripts/npm-drift-check.mjs`
- `.github/workflows/ci.yml` (jobs: `test`, `dogfood`, `pack-install-e2e`, `npm-drift`)
- `package.json` `prepare` field value changes from `husky` to `node scripts/prepare.mjs`
This plan produces: `scripts/prepare.mjs`, `scripts/prepare-guard-check.mjs`, and the `package.json` `prepare` field change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add scoped husky prepare guard (CIW-05, D-13/D-14)</name>
  <files>scripts/prepare.mjs, package.json</files>
  <read_first>
    - package.json (current `prepare: "husky"` at line 20; `files: ["bin/","src/","dist/public/"]` allowlist excludes scripts/; `devDependencies.husky`)
    - .planning/phases/20-ci-workflow/20-RESEARCH.md (Code Examples "scripts/prepare.mjs guard (D-13/D-14)" — verified skeleton; Pitfall 2 verified correction)
    - .planning/phases/20-ci-workflow/20-PATTERNS.md ("scripts/prepare.mjs" section — process.exit(0) caveat: legitimate for a standalone lifecycle script, NOT the bin/motto.js process.exitCode-only rule)
    - bin/motto.js (exit-code discipline convention, lines ~44-48 — for contrast only)
  </read_first>
  <action>
    Create scripts/prepare.mjs as a standalone ESM lifecycle script (D-13, D-14): import existsSync from node:fs and execSync from node:child_process. If `.git` does not exist in cwd, call `process.exit(0)` immediately (no-op — tarball install / git-less `npm ci` / ZIP download). Otherwise call `execSync('npx husky', { stdio: 'inherit' })` so a genuinely broken husky install in a real checkout still fails loudly. Do NOT use `husky || true` (D-13 explicitly rejects always-succeed). The early `process.exit(0)` is correct here and MUST NOT be rewritten to `process.exitCode` — that rule is specific to bin/motto.js's CLI dispatch loop, not standalone scripts (per 20-PATTERNS caveat).

    Then update package.json: change the `prepare` script value from `husky` to `node scripts/prepare.mjs`. Leave `test`, `prepublishOnly`, `version`, `files`, and all other fields untouched. Note `scripts/` stays out of the `files` allowlist by design (D-14) — this is safe because `prepare` never fires for a package installed as a nested dependency (Pitfall 2 verified finding), so the file's absence in a consumer's node_modules never matters at runtime.
  </action>
  <verify>
    <automated>node scripts/prepare.mjs && echo "GUARD-OK exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/prepare.mjs` in this repo (`.git` present) runs `npx husky` and exits 0
    - `grep -c '"prepare": "node scripts/prepare.mjs"' package.json` returns 1
    - scripts/prepare.mjs contains an `existsSync('.git')` check that early-exits before invoking husky
    - `package.json` still lists `files: ["bin/", "src/", "dist/public/"]` unchanged (scripts/ NOT added)
  </acceptance_criteria>
  <done>The husky prepare step is git-existence-scoped: real checkout runs husky, git-less context no-ops with exit 0, `husky || true` is not used.</done>
</task>

<task type="auto">
  <name>Task 2: Explicit .git-less guard proof (closes D-15 gap, CIW-05)</name>
  <files>scripts/prepare-guard-check.mjs</files>
  <read_first>
    - scripts/prepare.mjs (the guard being proven — created in Task 1)
    - .planning/phases/20-ci-workflow/20-RESEARCH.md (Open Questions #1 recommendation: git archive HEAD | tar -x into tmp dir, then npm ci, assert exit 0; Pitfall 2 — why the pack-E2E flow does NOT exercise the guard)
    - test/init-dogfood.test.js (mkdtemp/rm `{ recursive: true, force: true }` tmpdir lifecycle pattern — lines 1-30)
    - test/cli.test.js (spawnSync `{ encoding: 'utf8' }` subprocess pattern + failure surfacing, lines 24-33)
    - package.json (confirms package-lock.json is committed so `npm ci` works in the archived tree)
  </read_first>
  <action>
    Create scripts/prepare-guard-check.mjs — the honest replacement for D-15's false implicit-proof premise. Using node:child_process spawnSync and node:fs/promises (mkdtemp/rm), node:os tmpdir, node:path join: (1) mkdtemp a tmp dir; (2) run `git archive HEAD` piped into `tar -x -C <tmpdir>` (or spawnSync `git` with `archive --format=tar HEAD` capturing a buffer and extracting via `tar -x -C <tmpdir>`) to materialize a `.git`-LESS copy of the committed tree — this is exactly the context the guard must handle; (3) run `npm ci` with cwd set to the tmp dir; (4) assert the `npm ci` exit status is 0 — proving `scripts/prepare.mjs`'s `.git`-absent branch ran and no-oped rather than failing; (5) rm the tmp dir with `{ recursive: true, force: true }` in a finally-style cleanup; (6) print a success line and exit 0, or on failure echo the captured stdout/stderr root cause (per test/cli.test.js convention) and exit 1.

    This runs correctly in CI's pack-install-e2e job (checkout provides `.git`, so `git archive HEAD` works; the extracted tmp tree has no `.git`). It requires network for `npm ci` (installs yaml + husky from the committed lockfile). Note: because the archived tree contains `scripts/prepare.mjs` (once committed), `npm ci` there invokes the guard, which sees no `.git` and exits 0.
  </action>
  <verify>
    <automated>node scripts/prepare-guard-check.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/prepare-guard-check.mjs` prints a success line and exits 0 (requires network for `npm ci`)
    - The script materializes a `.git`-less tree via `git archive HEAD` before running `npm ci`
    - On `npm ci` non-zero exit the script echoes captured stdout+stderr and exits 1 (no bare exit)
    - The tmp dir is removed with `rm(..., { recursive: true, force: true })` regardless of outcome
  </acceptance_criteria>
  <done>CIW-05 is genuinely exercised: a `.git`-less `npm ci` succeeds because the prepare guard no-ops, proven by a committed script (not the false D-15 implicit path).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm lifecycle -> shell (`execSync('npx husky')`) | prepare guard shells out; no external/user input reaches the command string |
| committed tree -> tmp extraction (`git archive` -> tar) | guard-check extracts and runs `npm ci` in a throwaway tmp dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | scripts/prepare.mjs `execSync('npx husky')` | low | mitigate | Fixed literal command string; no interpolation of env/user input; runs only when `.git` present |
| T-20-02 | Denial of Service | scripts/prepare-guard-check.mjs tmp dir | low | mitigate | `rm(..., {recursive:true,force:true})` cleanup in finally-style path; unique `mkdtemp` prefix avoids collision |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A); `npm ci` installs only committed-lockfile deps (yaml, husky) |
</threat_model>

<verification>
- `node scripts/prepare.mjs` exits 0 in this repo (husky runs)
- `node scripts/prepare-guard-check.mjs` exits 0 (git-less `npm ci` succeeds via guard no-op)
- `grep -c '"prepare": "node scripts/prepare.mjs"' package.json` returns 1
- `npm test` still green (no regression to existing suite)
</verification>

<success_criteria>
- CIW-05: husky `prepare` no-ops outside a git checkout without failing, and still runs in a real checkout
- The D-15 gap is closed by an explicit, committed proof rather than a false implicit claim
</success_criteria>

<output>
Create `.planning/phases/20-ci-workflow/20-01-SUMMARY.md` when done
</output>
