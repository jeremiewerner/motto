---
phase: 20-ci-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pack-install-e2e.mjs
  - scripts/npm-drift-check.mjs
autonomous: true
requirements: [CIW-03, CIW-04]
must_haves:
  truths:
    - "Running `node scripts/pack-install-e2e.mjs` locally performs npm pack -> tmp consumer install -> npx motto init -> lint --format json -> build --format json, asserting exit codes and parsed JSON, and exits 0 on success"
    - "Every pack-E2E failure branch echoes the failing command plus raw stdout/stderr before exiting 1 — never a bare exit"
    - "Running `node scripts/npm-drift-check.mjs` always exits 0 — on version mismatch it prints a GitHub Actions warning annotation; on registry failure it prints an inconclusive warning annotation"
  artifacts:
    - "scripts/pack-install-e2e.mjs — tarball consumer-path E2E (CIW-03, D-05/D-06)"
    - "scripts/npm-drift-check.mjs — never-red registry drift warning (CIW-04, D-09..D-12)"
  key_links:
    - "npm pack --json output `[0].filename` -> tarball path fed to `npm install <tarball>` (never reconstructed from name+version — Pitfall 4)"
    - "npx motto lint/build --format json stdout -> JSON.parse -> `.ok === true` + count-field assertions (Phase 19 contract: lint {ok,count}, build {ok,outDir,skillCount,bucketCount})"
    - "package.json version + registry dist-tags.latest -> strict inequality -> ::warning:: annotation"
---

<objective>
Create the two CI helper scripts that plan 20-03's workflow jobs will invoke: the pack-install E2E proving what npm actually ships works standalone (CIW-03), and the never-red npm registry drift check that makes the v0.0.4/v0.0.5 silent-drift failure mode visible (CIW-04). Both are plain Node stdlib — zero new dependencies.

Purpose: Prove the shipped artifact on the real consumer path, and surface publish drift as a yellow banner instead of a months-later discovery.
Output: `scripts/pack-install-e2e.mjs`, `scripts/npm-drift-check.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ci-workflow/20-CONTEXT.md
@.planning/phases/20-ci-workflow/20-RESEARCH.md
@.planning/phases/20-ci-workflow/20-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols/paths created by Phase 20 (across all plans) — exclude from drift/new-symbol flagging:
- `scripts/prepare.mjs`, `scripts/prepare-guard-check.mjs`, `scripts/pack-install-e2e.mjs`, `scripts/npm-drift-check.mjs`
- `.github/workflows/ci.yml` (jobs: `test`, `dogfood`, `pack-install-e2e`, `npm-drift`)
- `package.json` `prepare` field value changes from `husky` to `node scripts/prepare.mjs`
This plan produces: `scripts/pack-install-e2e.mjs` and `scripts/npm-drift-check.mjs` (including their internal `run()`/`main()` helpers).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pack-install E2E script (CIW-03, D-05/D-06/D-07)</name>
  <files>scripts/pack-install-e2e.mjs</files>
  <read_first>
    - .planning/phases/20-ci-workflow/20-RESEARCH.md (Code Examples "pack-install-e2e.mjs skeleton" — treat as reference implementation; Pitfall 3 — echo raw stdout in JSON failure messages; Pitfall 4 — read `filename` from npm pack --json output, never reconstruct from `@scope/name`)
    - .planning/phases/20-ci-workflow/20-PATTERNS.md ("scripts/pack-install-e2e.mjs" section — analogs and JSON contract field names)
    - test/cli.test.js (runCli spawnSync pattern, lines 24-33; failure-message convention embedding stderr/stdout)
    - test/init-dogfood.test.js (mkdtemp/rm tmpdir lifecycle, lines 1-30; in-process assertions on ok/count/skillCount/bucketCount fields, lines ~65-77)
    - .planning/phases/19-cli-ergonomics-build-skill-verification/19-CONTEXT.md (consumed contract: `--format json` emits exactly one compact JSON doc on stdout — lint `{ok, count}`, build `{ok, outDir, skillCount, bucketCount}`; errors on stderr)
    - bin/motto.js (renderResult — the JSON shapes being asserted; do not modify)
  </read_first>
  <action>
    Create scripts/pack-install-e2e.mjs following the RESEARCH.md skeleton: a `run(cmd, args, opts)` helper wrapping spawnSync with `{ encoding: 'utf8' }` that, on non-zero status, prints the command line plus raw stdout and stderr and exits 1 (per D-05: every step's exit code checked; per Pitfall 3: root cause always included). Flow per D-06 (real consumer path): (1) mkdtemp a pack destination dir; (2) `npm pack --json --pack-destination <packDir>` at the repo root, JSON.parse stdout and read `data[0].filename` directly — NEVER assemble the filename from package.json name+version, since scoped `@jeremiewerner/motto` flattens to `jeremiewerner-motto-<version>.tgz` (Pitfall 4); (3) mkdtemp a consumer dir, run `npm init -y` there, then `npm install <tarballPath>` there — exercising bin linking, the `files` allowlist, and dependency resolution exactly as a stranger would; (4) `npx motto init e2e-project` in the consumer dir; (5) in the created e2e-project dir, `npx motto lint --format json`, JSON.parse stdout, assert `.ok === true` and `.count >= 1`; (6) `npx motto build --format json`, JSON.parse stdout, assert `.ok === true` and `.skillCount >= 1` (D-05: parsed-JSON assertions, no file-tree diffing); (7) after JSON steps, also invoke the guard proof: run `node scripts/prepare-guard-check.mjs` from the repo root as a final step OR leave that to the workflow (Claude's discretion in 20-03 wiring — do NOT duplicate the guard logic here; the guard proof lives in its own script from plan 20-01); (8) rm both tmp dirs with `{ recursive: true, force: true }`; (9) print a single success line (e.g. `pack-install-e2e: OK`) and exit 0.

    Field names must match the Phase 19 contract exactly: lint uses `count` (NOT `skillCount`); build uses `skillCount` and `bucketCount`. Structure the assertion section so Phase 21's D-05 tarball-leak assertion (PUB-03) can slot in as one more step cleanly — e.g. keep the pack manifest (`data[0].files`) in a variable, but do NOT add the leak assertion now (deferred by design). JSON-parse failure branches must include the raw stdout in the error message so a future `--format json` regression is debuggable from CI logs alone (Pitfall 3).
  </action>
  <verify>
    <automated>node scripts/pack-install-e2e.mjs && echo "E2E-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/pack-install-e2e.mjs` exits 0 locally and prints a success line (requires network for `npm install`)
    - The script reads the tarball filename from `npm pack --json` output index 0, not from a template string built from package name/version
    - lint assertion checks `.ok === true` and `.count >= 1`; build assertion checks `.ok === true` and `.skillCount >= 1`
    - Every failure branch prints the raw stdout and stderr of the failing step before exiting 1
    - Both tmp dirs are removed with `rm(..., { recursive: true, force: true })`
    - The script does not modify anything in the repo working tree (pack destination is a tmp dir)
  </acceptance_criteria>
  <done>The full consumer path (pack -> install tarball -> init -> lint -> build) is proven by one committed script with exit-code + parsed-JSON assertions and descriptive failures.</done>
</task>

<task type="auto">
  <name>Task 2: npm registry drift check script (CIW-04, D-09..D-12)</name>
  <files>scripts/npm-drift-check.mjs</files>
  <read_first>
    - .planning/phases/20-ci-workflow/20-RESEARCH.md (Pattern 3 "Never-red advisory check" — verified skeleton; Anti-Pattern: an unhandled fetch rejection outside try/catch fails the job; verified registry endpoint and response shape)
    - .planning/phases/20-ci-workflow/20-PATTERNS.md ("scripts/npm-drift-check.mjs" section — whole-body try/catch, never-throw boundary discipline)
    - package.json (the `version` field being compared — currently 0.0.3, matching registry, so the check is silent today by design)
  </read_first>
  <action>
    Create scripts/npm-drift-check.mjs per the RESEARCH.md Pattern 3 skeleton, committed as a script file (chosen over inline `node -e` for readability, per Claude's-discretion note in CONTEXT.md). Behavior: read `version` from package.json via readFileSync + JSON.parse; inside a single try/catch wrapping the ENTIRE network+parse body, `fetch('https://registry.npmjs.org/@jeremiewerner%2Fmotto')` (D-12 — stdlib fetch, URL-encoded scope slash); if `!res.ok`, print `::warning::` with an "inconclusive (registry returned <status>)" message and return (D-10); parse JSON, read `data['dist-tags']?.latest`; if missing, print an inconclusive `::warning::` and return; if `latest !== localVersion` (strict inequality, any mismatch — D-11, no semver parsing), print a `::warning::` naming both versions (D-09 — annotation is the only signal carrier); catch block prints `::warning::npm-drift check inconclusive (<err.message>)`. No branch ever sets a non-zero exit code — the script must be structurally incapable of exiting non-zero on any handled path (D-10: warn-and-pass, never red; a flaky registry must never block the push gate). No retry/backoff logic (Don't-Hand-Roll table: failure is just another inconclusive branch).
  </action>
  <verify>
    <automated>node scripts/npm-drift-check.mjs; test $? -eq 0 && echo "DRIFT-EXIT0-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/npm-drift-check.mjs` exits 0 on every run (match, mismatch, or network failure)
    - With package.json at 0.0.3 and registry latest 0.0.3, the script prints no warning annotation (silent-match branch)
    - The mismatch branch's output line starts with the GitHub Actions warning workflow-command prefix and names both the registry version and the package.json version
    - The fetch call and all response parsing sit inside one try/catch covering the whole async body (no partial coverage)
    - The script contains no retry loop and no `process.exitCode`/`process.exit` assignment to a non-zero value
  </acceptance_criteria>
  <done>Registry drift is surfaced as a non-blocking yellow annotation; the check itself can never turn a CI run red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> registry.npmjs.org (read-only fetch) | untrusted external HTTP response parsed by drift check |
| repo root -> tmp consumer project (`npm install <tarball>`) | tarball's own manifest drives install; no third-party packages beyond committed lockfile deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Tampering | drift-check registry response | low | mitigate | Response only compared as a string against local version; never eval'd, never interpolated into a shell command; malformed JSON lands in the catch -> inconclusive warning |
| T-20-04 | Denial of Service | drift-check network hang blocking CI | low | accept | Job is advisory (D-10); a hang delays only the non-blocking npm-drift job; GitHub's default job timeout bounds it |
| T-20-05 | Information Disclosure | pack-E2E failure logs echoing stdout/stderr | low | accept | Repo is private this phase; output is CLI/npm diagnostics only, no secrets in scope (no tokens exist until Phase 21) |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A); E2E installs only the locally-packed tarball + its committed-lockfile dep (yaml) |
</threat_model>

<verification>
- `node scripts/pack-install-e2e.mjs` exits 0 and prints its success line
- `node scripts/npm-drift-check.mjs` exits 0 and prints nothing today (0.0.3 == 0.0.3)
- `npm test` still green (no regression; scripts live outside test/ so `node --test` discovery is unaffected)
</verification>

<success_criteria>
- CIW-03: the shipped-tarball consumer path is proven end-to-end by a committed script with exit-code + parsed-JSON assertions
- CIW-04: registry drift produces a visible, non-blocking warning; check failures are inconclusive warnings, never red
</success_criteria>

<output>
Create `.planning/phases/20-ci-workflow/20-02-SUMMARY.md` when done
</output>
