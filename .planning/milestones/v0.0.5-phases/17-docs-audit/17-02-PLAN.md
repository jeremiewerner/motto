---
phase: 17-docs-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-07]
must_haves:
  truths:
    - "grep -c author-skill README.md → 0 — no lingering author-skill reference anywhere in README (D-06/D-07)."
    - "The '## Author a skill' section BODY describes build-skill's real flow (any input → one-batch gap-fill → write → lint loop → quality gate → compact receipt), not author-skill's old interview behavior."
    - "The '## Author a skill' heading text is unchanged, so the #author-a-skill Contents anchor still resolves (no Contents-list edit needed)."
    - "Every author-skill sample name in install/symlink/zip examples and the skills table is now build-skill — commands stay copy-paste runnable against this repo (build-skill actually exists in dist/public/ after motto build)."
  artifacts:
    - README.md
  key_links:
    - "README examples reference dist/public/build-skill/ — the real public skill this repo's own `motto build` produces (asserted by test/dogfood.test.js)."
    - "#author-a-skill anchor ↔ '## Author a skill' heading text (kept identical to preserve the Contents link)."
---

<objective>
Update README.md so no `author-skill` reference remains and the `build-skill` flow is documented (DOC-07). This is the README half of the phase: replace author-skill's retired interview narrative with build-skill's real behavior, and make every sample-skill command runnable against this repo.

Purpose: `author-skill` was retired atomically in Phase 16 — the README is the only tracked source still naming it (8 sites). A mechanical rename alone would misdescribe build-skill's actual flow, so the "Author a skill" section body is rewritten, while the other 7 sites are mechanical sample-name swaps.
Output: A README with zero author-skill references and a build-skill-accurate authoring narrative.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-docs-audit/17-CONTEXT.md
@.planning/phases/17-docs-audit/17-RESEARCH.md
@.planning/phases/17-docs-audit/17-PATTERNS.md
@skills/build-skill/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the "Author a skill" section body + End-to-end Step 3 + skills-table row around build-skill's real flow (D-06)</name>
  <files>README.md</files>
  <read_first>
    - README.md (line 95 "Author a skill" body; line 203 End-to-end Step 3; lines 252-254 skills table; line 15 Contents anchor `[Author a skill](#author-a-skill)`)
    - skills/build-skill/SKILL.md (the real flow to describe: ingest → map-to-schema → one-batch gap-fill → auto-detect template → guard+write → ≤3-attempt filtered lint loop → content-quality gate → compact receipt; description line 3 is the trigger-style wording to mirror)
    - .planning/phases/17-docs-audit/17-RESEARCH.md → "README Audit — Author-Skill Reference Sites" (each site's target) and "Anchor Stability"
    - .planning/phases/17-docs-audit/17-PATTERNS.md → "README.md (section rewrite + 8-site replace)"
  </read_first>
  <action>
    Keep the `## Author a skill` heading text EXACTLY as-is (do not rename to "Build a skill") — per RESEARCH.md Anchor Stability, the #author-a-skill anchor and the Contents link at line 15 depend on it. Rewrite only the section BODY.

    Replace the current line-95 paragraph ("Use the `/motto:author-skill` skill in Claude Code for a step-by-step guide...") with a build-skill flow description per D-06: hand `/motto:build-skill` ANY input (pasted text, a document, or a conversation transcript); it extracts what is already given and asks only about genuinely-missing schema slots in one batched message; auto-detects whether the skill needs `template: procedure`; writes `skills/<name>/SKILL.md` (plus any declared output-template files); runs the real `motto lint` in a loop (up to 3 attempts, self-fixing); self-reviews against a content-quality gate; then emits a compact receipt. Keep the existing frontmatter-spine example block and the four field bullets and the "Run `motto lint`..." line that follow (lines 97-116) unchanged — they document the schema, not author-skill, and stay accurate. The slash command in the rewritten prose is `/motto:build-skill`.

    End-to-end example Step 3 (line 203): change `/motto:author-skill` to `/motto:build-skill`. Update the surrounding comment "# 3 — Author your first skill (Claude Code slash command)" only if it names author-skill (it does not — leave the comment text).

    Skills table (lines 252-254): change the row `| \`author-skill\` | \`/motto:author-skill\` | Step-by-step guide for writing a conforming \`SKILL.md\` |` to a build-skill row: `| \`build-skill\` | \`/motto:build-skill\` | Structures freeform input — pasted text, a document, or a transcript — into a conforming, lint-clean skill |` (a trigger-style description mirroring build-skill's own `description`, NOT a restatement of internal steps).
  </action>
  <verify>
    <automated>grep -q '## Author a skill' README.md && grep -q '(#author-a-skill)' README.md && grep -q '/motto:build-skill' README.md && grep -q '| `build-skill` | `/motto:build-skill`' README.md && ! grep -q '/motto:author-skill' README.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: heading `## Author a skill` still present verbatim; Contents link `(#author-a-skill)` still present (anchor preserved).
    - Source assertion: `grep -c '/motto:author-skill' README.md` → 0 (all three slash-command sites in this task converted).
    - Behavior assertion: the "Author a skill" body names `/motto:build-skill` and describes the one-batch gap-fill → write → lint-loop → quality-gate → receipt flow (not "step-by-step guide"/interview).
    - Source assertion: the skills table contains a `build-skill` row with a trigger-style description, no `author-skill` row.
  </acceptance_criteria>
  <done>The "Author a skill" section body, End-to-end Step 3, and skills-table row all describe/name build-skill; the #author-a-skill anchor is preserved; no `/motto:author-skill` slash command remains.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical author-skill → build-skill sample-name swaps (dist tree, symlink, zip, prose) (D-07)</name>
  <files>README.md</files>
  <read_first>
    - README.md (line 143 dist tree `author-skill/`; line 267 symlink one-liner; line 270 prose sentence; line 275 zip one-liner; line 278 trailing substitution note)
    - .planning/phases/17-docs-audit/17-RESEARCH.md → "README Audit" table rows for lines 143, 267, 270, 275, 278 (exact current text and target)
  </read_first>
  <action>
    Replace `author-skill` with `build-skill` at these five remaining sites (D-07 — build-skill is the sample name because it actually exists in dist/public/ after `motto build`, keeping every command copy-paste runnable against this repo):

    - Line ~143 (dist tree diagram): `    author-skill/` → `    build-skill/`. Leave the sibling `secret-skill/` private-bucket placeholder untouched (it is a generic illustrative name, not an author-skill reference, and out of scope).
    - Line ~267 (symlink one-liner): `ln -s "$(pwd)/dist/public/author-skill" ~/.claude/skills/author-skill` → `ln -s "$(pwd)/dist/public/build-skill" ~/.claude/skills/build-skill`.
    - Line ~270 (prose): `The absolute source path (\`$(pwd)/dist/public/author-skill\`)...` → same sentence with `build-skill`.
    - Line ~275 (zip one-liner): `cd dist/public && zip -r author-skill.zip author-skill/` → `cd dist/public && zip -r build-skill.zip build-skill/`.
    - Line ~278 (trailing note): `Substitute \`author-skill\` with any other skill name...` → same sentence with `build-skill`.

    After this task, `grep -c author-skill README.md` MUST be 0. Do not alter the `#author-a-skill` anchor or `## Author a skill` heading (those contain "author-a-skill"/"Author a skill", not the substring "author-skill", so they do not match the grep and must survive).
  </action>
  <verify>
    <automated>grep -c 'author-skill' README.md | grep -qx 0 && grep -q 'dist/public/build-skill' README.md && grep -q 'zip -r build-skill.zip build-skill/' README.md && grep -q '(#author-a-skill)' README.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c author-skill README.md` → 0 (the phase-defining DOC-07 acceptance check — no author-skill reference remains anywhere).
    - Source assertion: `dist/public/build-skill` present in both the symlink one-liner and the dist tree diagram.
    - Source assertion: `zip -r build-skill.zip build-skill/` present.
    - Source assertion: `(#author-a-skill)` Contents anchor and `## Author a skill` heading still present (the grep-surviving anchor/heading were not damaged).
  </acceptance_criteria>
  <done>All five mechanical sites now name build-skill, `grep -c author-skill README.md` returns 0, and every install/symlink/zip example is runnable against this repo's own `motto build` output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits a single Markdown documentation file (README.md). No code, no input parsing, no new execution path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Tampering | README example commands | low | mitigate | Sample name (`build-skill`) is chosen (D-07) so every symlink/zip/install command resolves against a skill that actually exists in `dist/public/` after `motto build` — no copy-paste command points at a non-existent path. |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase — documentation-only edit. No package legitimacy checkpoint required. |
</threat_model>

<verification>
- `grep -c author-skill README.md` → 0.
- `grep -c '/motto:author-skill' README.md` → 0.
- `## Author a skill` heading and `(#author-a-skill)` Contents anchor both still present.
- `dist/public/build-skill` referenced in symlink one-liner and dist tree; `build-skill` row present in skills table.
</verification>

<success_criteria>
README.md contains zero `author-skill` references, the "Author a skill" section body documents build-skill's real one-batch-gap-fill → write → lint-loop → quality-gate → receipt flow, the #author-a-skill anchor is preserved, and every sample-skill command names build-skill (runnable against this repo's build output).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Files edited:
- `README.md` — "Author a skill" section body rewritten around build-skill's flow (heading + frontmatter-spine example kept); End-to-end Step 3 slash command → `/motto:build-skill`; skills-table row → `build-skill` with trigger-style description; dist tree diagram, symlink one-liner + prose, zip one-liner, and trailing substitution note all swapped `author-skill` → `build-skill`.

No new files, no new prose sections (section headings unchanged to preserve anchors). No test changes.
</artifacts_produced>

<output>
Create `.planning/phases/17-docs-audit/17-02-SUMMARY.md` when done.
</output>
