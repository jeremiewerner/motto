---
phase: 15-field-validation-integrity-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schema.js
  - test/schema.test.js
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-05, VAL-06]
must_haves:
  truths:
    - "motto lint rejects an outputs entry that is absolute or contains .. traversal (lexical check, before any fs access)"
    - "A public skill listing a private skill in dependencies fails lint; a skill listing itself fails; a bare dep naming no in-tree skill fails with an inline available-skills hint"
    - "A namespaced plugin:skill dependency is format-checked only (exempt from resolution and audience guard)"
    - "allowed-tools accepts a non-empty string or an array of non-empty strings including Bash(git add *); wrong container type, non-string entry, or empty string fails"
    - "Every new schema-layer field check returns accumulated errors without throwing on adversarial malformed input (throwing toString/Symbol.toPrimitive, null, arrays, numbers)"
  artifacts:
    - "src/schema.js exports isOutputPathLexicallySafe(skillDirAbs, entry)"
    - "src/schema.js validateSkill accepts skillNames (Set) and audienceMap (Map) parameters"
    - "test/schema.test.js contains adversarial + happy-path cases for outputs, dependencies, allowed-tools"
  key_links:
    - "isOutputPathLexicallySafe uses resolve()+sep containment with a trailing separator on both sides (sibling-prefix false-positive guard)"
    - "self-dependency (entry === dirName) check runs BEFORE the skillNames.has(entry) membership check"
    - "each new field is hasOwnProperty-gated, not truthiness-gated (wrong type still errors; empty container is a valid no-op)"
---

<objective>
Add the three new optional frontmatter fields — `outputs:`, `dependencies:`, `allowed-tools` — to the pure validator `validateSkill` in `src/schema.js`, plus the exported lexical-safety predicate the fs layer (Plan 02) will reuse. This plan covers every check that needs NO filesystem access: outputs lexical path-safety, dependencies namespaced-format / self-dependency / in-tree resolution / audience-direction guard, and allowed-tools format-only validation. All checks are never-throw and proven by adversarial malformed-input tests.

Purpose: `src/schema.js` is Motto's pure-validator tier (no fs, synchronous, never throws — D-01). It is the correct home for all lexical/format guards. The fs-dependent existence + symlink-escape checks (VAL-01 second half) live in Plan 02's `lint.js` layer and reuse the predicate this plan exports.
Output: Extended `validateSkill` (outputs lexical, dependencies guards, allowed-tools format), a new exported `isOutputPathLexicallySafe` predicate, an updated function signature accepting `skillNames`/`audienceMap`, an updated docblock, and a full adversarial + happy-path test suite.
</objective>

<artifacts_this_phase_produces>
Symbols created across the whole of Phase 15 (this plan + Plan 02). Listed so the plan-review source-grounding pass excludes them from drift verification:

- `isOutputPathLexicallySafe(skillDirAbs, entry)` — new exported pure function in `src/schema.js` (this plan)
- New `validateSkill` parameters: `skillNames` (Set), `audienceMap` (Map) — `src/schema.js` (this plan)
- Validation of three new frontmatter fields: `outputs`, `dependencies`, `allowed-tools` (this plan)
- `loadSkillAudiences(skillsDir, skillNames)` — new helper in `src/lint.js` (Plan 02)
- `checkOutputsFs(skillsDir, dirName, data, errors)` — new async helper in `src/lint.js` (Plan 02)
- `allowed-tools` field added to `skills/release/SKILL.md` (Plan 02)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-field-validation-integrity-guards/15-CONTEXT.md
@.planning/phases/15-field-validation-integrity-guards/15-RESEARCH.md
@.planning/phases/15-field-validation-integrity-guards/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: outputs lexical block + exported predicate + signature/docblock update</name>
  <files>src/schema.js</files>
  <read_first>
    - src/schema.js — the whole file. Study: the `NAME_KEBAB` export style (line 37), the `import { SECTIONS, TEMPLATES } from "./templates.js"` line (line 15, the only current import), the TEMPLATE `hasOwnProperty`-gated cascade (lines 227-251), the `shared_references` per-entry cascade (lines 281-292), the NAME type-guard-before-method cascade (lines 165-186), the `validateSkill` signature (lines 147-151), and the docblock at lines 128-130 that claims `dependencies` is a passthrough key.
    - .planning/phases/15-field-validation-integrity-guards/15-RESEARCH.md — Pattern 1 (hasOwnProperty-gated field), Pattern 2 (outputs lexical predicate), Pitfall 1 (schema.js purity — node:path OK, node:fs NEVER), Pitfall 4 (typeof null === 'object' + arrays are objects), Pitfall 6 (stale D-14 docblock).
    - .planning/phases/15-field-validation-integrity-guards/15-CONTEXT.md — "outputs: existence & path-safety" and "Empty-field policy" decision blocks.
  </read_first>
  <action>
    In src/schema.js add its first-ever `node:path` import: `resolve`, `sep`, `isAbsolute` from "node:path" (pure string math — does NOT violate the "no filesystem" purity contract; node:fs / node:fs/promises must NEVER be imported here).

    Add a new EXPORTED pure function `isOutputPathLexicallySafe(skillDirAbs, entry)` modeled on the `NAME_KEBAB` export style. It returns false for a non-string or empty-string entry, false for an absolute entry (`isAbsolute`), and otherwise resolves both `skillDirAbs` and the joined target and returns whether the target is contained — using a trailing `sep` appended to BOTH the resolved root and the resolved target before the `startsWith` comparison, so a sibling directory whose name is a prefix (e.g. a `build-skill-evil` sibling of `build-skill`) is NOT a false positive. Give it a JSDoc noting it is pure (node:path only, no fs) and shared with the lint.js fs layer (Plan 02).

    Extend the `validateSkill` signature to add two new trailing optional parameters after `templatesRegistry`: `skillNames = new Set()` and `audienceMap = new Map()`. Update the function's JSDoc `@param` list and the top-of-file Exports summary line (currently `validateSkill(skill, sharedRefs?, templatesRegistry?)`) to include the two new params. These params are unused by THIS task's code (they are consumed by the dependencies block in Task 2) but must be added now so the signature is stable for both tasks.

    Add the `outputs:` validation block inside `validateSkill`, placed after the `shared_references` loop (near the end, before `return errors`). Gate it with `Object.prototype.hasOwnProperty.call(data, "outputs")` (NOT truthiness — matches the TEMPLATE/D-07 precedent and CONTEXT.md's Empty-field policy). When the key is present: if the value is `null`, or `typeof !== "object"`, or `Array.isArray(...)`, push one "outputs must be a map of name -> file" error reporting the observed type (guard covers the `typeof null === "object"` trap and arrays-are-objects trap — Pitfall 4). Otherwise iterate `Object.entries(outputs)` as an independent per-entry cascade: for each `[key, value]`, if value is a non-string or empty string push an `outputs.<key> must be a non-empty string path` error and `continue`; else if `!isOutputPathLexicallySafe(skillDirAbs, value)` push an `outputs.<key> path "<value>" is unsafe (must not be absolute or contain ".." traversal)` error and `continue`; else do nothing (fs existence + symlink-escape are deferred to Plan 02's lint.js). An empty `{}` correctly produces zero errors (the loop body never runs).

    `skillDirAbs` inside `validateSkill` must be computed from the skill's directory. `validateSkill` receives `dirName` but not an absolute path; compute `skillDirAbs` via `resolve(dirName)` for the lexical containment math ONLY — the lexical check is relative-vs-absolute + traversal detection and does not touch the filesystem, so resolving `dirName` against the process cwd is acceptable for the purely lexical predicate (the real filesystem-anchored containment is re-checked against the true skills dir in Plan 02). Keep the whole block never-throw: every branch guards `typeof`/`Array.isArray` before any string/coercion, so a value with a throwing `toString`/`Symbol.toPrimitive` short-circuits before interpolation.

    Update the docblock at lines 128-130: remove the claim that `dependencies` "is accepted without error (D-14)" — this becomes false once Task 2 lands the dependencies block. Reword so only genuinely unknown frontmatter keys remain passthrough; note that `dependencies`, `outputs`, and `allowed-tools` are now validated fields.
  </action>
  <verify>
    <automated>node --test test/schema.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/schema.js contains `export function isOutputPathLexicallySafe(`
    - src/schema.js imports `resolve`, `sep`, `isAbsolute` from "node:path" and imports NOTHING from "node:fs" or "node:fs/promises" (grep confirms zero node:fs* imports in this file)
    - src/schema.js `validateSkill(` signature includes `skillNames` and `audienceMap` parameters with `new Set()` / `new Map()` defaults
    - src/schema.js contains an `outputs` block gated by `Object.prototype.hasOwnProperty.call(data, "outputs")`
    - The lines-128-130 docblock no longer states that `dependencies` is accepted without error
    - `node --test test/schema.test.js` exits 0 (existing 131+ tests still green — regression guard: skills without the new fields validate identically)
  </acceptance_criteria>
  <done>The exported lexical predicate, the outputs lexical block, the extended signature, and the corrected docblock are in place; all existing schema tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: dependencies guards + allowed-tools format block</name>
  <files>src/schema.js</files>
  <read_first>
    - src/schema.js — as edited by Task 1. Study again: `NAME_KEBAB` (line 37) for namespaced-half reuse, the `shared_references` per-entry cascade (now near the outputs block) for the loop shape, and the newly-added `skillNames`/`audienceMap` params.
    - .planning/phases/15-field-validation-integrity-guards/15-RESEARCH.md — Pattern 3 (dependencies: namespaced format / self-dep / bare resolution / audience guard), Pattern 4 (allowed-tools format-only), Pitfall 2 (self-dependency check MUST precede membership check), Pitfall 7 (adversarial throwing-toString objects).
    - .planning/phases/15-field-validation-integrity-guards/15-CONTEXT.md — "dependencies guards & error UX", "allowed-tools accepted shapes", "Empty-field policy" decision blocks.
  </read_first>
  <action>
    Add the `dependencies:` validation block inside `validateSkill`, gated by `Object.prototype.hasOwnProperty.call(data, "dependencies")`. When present: if `!Array.isArray(deps)` push a `dependencies must be an array (got <type>)` error. Otherwise iterate entries as an independent per-entry cascade. For each entry: if non-string or empty push `dependencies entry must be a non-empty string (got <type>)` and `continue`. If the entry `includes(":")` it is a namespaced `plugin:skill` entry — split on `:`, and if it does not split into exactly two parts OR either half fails `NAME_KEBAB.test(...)`, push a `dependencies entry "<entry>" is not valid "plugin:skill" format` error; then `continue` (namespaced entries are EXEMPT from resolution and the audience guard per D-05 — external, audience unknowable). For a bare (no-colon) entry, check `entry === dirName` FIRST and if so push `dependencies entry "<entry>" is a self-dependency` and `continue` (this ordering is mandatory — dirName is always a member of skillNames, so a membership-first check would silently misclassify a self-dep as resolved; Pitfall 2 / VAL-04-before-VAL-02). Next, if `!skillNames.has(entry)`, push `dependency "<entry>" not found (available: <sorted comma-joined skillNames>)` and `continue` (mirrors the Phase 14 unknown-template inline-available-list error shape). Finally the audience-direction guard: ONLY when `data.audience === "public"` AND `audienceMap.get(entry) === "private"`, push `dependencies entry "<entry>" is private but this skill is public (audience-direction guard)`. All other audience directions (private→private, private→public, public→public, or an unknown/absent target audience) pass.

    Add the `allowed-tools` validation block, gated by `Object.prototype.hasOwnProperty.call(data, "allowed-tools")` (bracket access — the field name contains a hyphen). Option A is locked (CONTEXT.md, resolves STACK.md Q2): format-only, NO shape regex, NO tokenizing. If the value is a string, push a `allowed-tools must be a non-empty string or array (got an empty string)` error only when it `trim()`s to empty; a non-empty string passes trivially (a parenthesized rule like `Bash(git add *)` is one valid permission rule by construction). If the value is an array, check each entry: non-string or empty pushes `allowed-tools[<i>] must be a non-empty string (got <type>)`; an empty array `[]` passes (coherent "zero tools" per Empty-field policy). Otherwise (neither string nor array) push `allowed-tools must be a string or array (got <type>)`.

    Every branch in both blocks must guard `typeof`/`Array.isArray` before any `.includes`/`.split`/`.test`/template-literal interpolation of the raw value, preserving the never-throw invariant against throwing `toString`/`Symbol.toPrimitive` objects.
  </action>
  <verify>
    <automated>node --test test/schema.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/schema.js contains a `dependencies` block gated by `Object.prototype.hasOwnProperty.call(data, "dependencies")`
    - In the dependencies bare-entry cascade, the `entry === dirName` self-dependency check textually precedes the `skillNames.has(entry)` membership check
    - Namespaced `plugin:skill` entries reuse `NAME_KEBAB` on both halves and are not subjected to resolution or the audience guard (they `continue` after the format check)
    - src/schema.js contains an `allowed-tools` block gated by `Object.prototype.hasOwnProperty.call(data, "allowed-tools")` accessed via bracket notation
    - The allowed-tools block contains no shape regex and no tokenizing/splitting of tool strings (Option A: format-only)
    - `node --test test/schema.test.js` exits 0
  </acceptance_criteria>
  <done>dependencies (namespaced format, self-dep, in-tree resolution, audience-direction guard) and allowed-tools (Option A format-only, string-or-array) validation blocks are in place; all schema tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: adversarial + happy-path schema tests for all three fields (VAL-06)</name>
  <files>test/schema.test.js</files>
  <read_first>
    - test/schema.test.js — the whole file. Study the existing `describe("validateSkill — template cascade ...")` block, the B20 "does not throw for truthy non-string names" test (~line 421), and the T-14-03 "template: object with a throwing toString/Symbol.toPrimitive never throws" test (~line 677) — reuse that exact throwing-object fixture pattern.
    - src/schema.js — as edited by Tasks 1-2, for the exact error-message strings and the `validateSkill(skill, sharedRefs, templatesRegistry, skillNames, audienceMap)` call signature (tests must pass `skillNames`/`audienceMap` explicitly to exercise dependency resolution and the audience guard).
    - .planning/phases/15-field-validation-integrity-guards/15-RESEARCH.md — Pitfall 7 (adversarial throwing-toString objects mandatory for all three fields), Pitfall 2 (assert the SPECIFIC self-dependency error string, not just error presence).
  </read_first>
  <behavior>
    outputs (VAL-01 lexical):
    - `outputs: {}` → zero errors (valid no-op)
    - `outputs: null` / `outputs: "x"` / `outputs: [1,2,3]` → one "must be a map" error, never throws
    - entry with an absolute path value → "unsafe" error
    - entry with a `../escape` traversal value → "unsafe" error
    - entry with a non-string/empty value → "must be a non-empty string path" error
    - entry value with a throwing toString/Symbol.toPrimitive → error, never throws
    dependencies (VAL-02/03/04):
    - `dependencies: []` → zero errors
    - `dependencies: "nope"` → "must be an array" error
    - bare entry equal to dirName → "self-dependency" error (assert the exact self-dep message, not merely non-empty errors)
    - bare entry not in skillNames → "not found (available: ...)" error with sorted names
    - bare entry present in skillNames, public skill depending on a private target (via audienceMap) → audience-direction error
    - private→private, private→public, public→public → zero dependency errors
    - namespaced `good-plugin:good-skill` → zero errors; `Foo:bar`, `a::b`, `:x`, `x:` → format error; namespaced entry NOT subjected to resolution/audience guard
    - entry that is a throwing-toString object → error, never throws
    allowed-tools (VAL-05):
    - non-empty string incl. `Bash(git add *)` → zero errors
    - `""` (empty string) → error
    - `[]` → zero errors
    - `["Read", "Bash(git add *)", "mcp__github__*"]` → zero errors
    - array with a non-string/empty entry → indexed error
    - non-string/non-array (number, object, null) → "must be a string or array" error, never throws
  </behavior>
  <action>
    Add new `describe(...)` blocks to test/schema.test.js covering the behaviors above for `outputs`, `dependencies`, and `allowed-tools`. Construct each `validateSkill` call with a minimal otherwise-valid skill object (`dirName`, `data` with valid name/description/audience, `body` with an H1 + `**Role:**` line so unrelated checks stay green) so the assertions isolate the field under test. Pass `skillNames` (a `Set`) and `audienceMap` (a `Map`) explicitly where dependency resolution or the audience guard is exercised. For every field, include at least one adversarial test that constructs a value (or entry) whose `toString`/`Symbol.toPrimitive` throws, wrapped in `assert.doesNotThrow(...)`, reusing the T-14-03 fixture shape. For the self-dependency case, assert the returned errors include the SPECIFIC self-dependency message (Pitfall 2 — do not let the audience guard's error mask a missing dedicated self-dep error).
  </action>
  <verify>
    <automated>node --test test/schema.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/schema.test.js contains new describe blocks referencing `outputs`, `dependencies`, and `allowed-tools`
    - At least one adversarial throwing-toString/Symbol.toPrimitive test exists for each of the three fields, each asserting no throw (VAL-06)
    - The self-dependency test asserts the specific self-dependency error string
    - `node --test test/schema.test.js` exits 0 with the new tests included
    - `node --test` (full suite) exits 0
  </acceptance_criteria>
  <done>All three new fields have adversarial + happy-path coverage; every new validator is proven never-throw; the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md frontmatter → validator | Author-controlled `outputs:`/`dependencies:`/`allowed-tools` values are untrusted input crossing into `validateSkill` |
| dependencies declaration → dist packaging | A public skill's `dependencies:` list can name a private skill, leaking a private-scoped identifier toward the public `dist/` bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering / Information Disclosure | `outputs:` path values (lexical) | high | mitigate | `isOutputPathLexicallySafe` rejects absolute paths and `..` traversal via resolve()+sep containment BEFORE any fs access; unsafe entries never reach the Plan 02 fs layer |
| T-15-03 | Information Disclosure | `dependencies:` audience-direction | high | mitigate | Audience-direction guard: a public skill depending on a private skill fails lint (public→private only), blocking a private-name leak into public dist before `motto build` runs |
| T-15-04 | Denial of Service | all three new field validators | medium | mitigate | Type-guard-before-method cascade (typeof/Array.isArray before any coercion) keeps every branch never-throw; adversarial throwing-toString tests (Task 3) prove it (VAL-06) |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs zero packages (RESEARCH.md Package Legitimacy Audit: N/A) — no supply-chain surface introduced |
</threat_model>

<verification>
- `node --test test/schema.test.js` — all schema tests (existing + new) pass.
- `node --test` — full suite green (regression guard: skills WITHOUT the new fields validate byte-identically; the dogfood `lintProject(REPO_ROOT)` count/ok assertion is unaffected because Plan 01 changes are additive and the live skills declare none of the three new fields yet).
- grep confirms `src/schema.js` imports nothing from `node:fs`/`node:fs/promises` (purity invariant, Pitfall 1).
</verification>

<success_criteria>
- `outputs:` lexical safety (absolute / traversal / non-string) errors correctly; safe entries defer fs checks to Plan 02.
- `dependencies:` namespaced-format, self-dependency, in-tree resolution, and public→private audience guard all enforce per CONTEXT.md.
- `allowed-tools` Option A format-only (string or array, non-empty) passes real spec forms including `Bash(git add *)`.
- Every new validator is never-throw, proven by adversarial malformed-input tests (VAL-06).
- `isOutputPathLexicallySafe` is exported for Plan 02 reuse; `validateSkill` accepts `skillNames`/`audienceMap`.
</success_criteria>

<output>
Create `.planning/phases/15-field-validation-integrity-guards/15-01-SUMMARY.md` when done.
</output>
