---
phase: 16-build-skill-author-skill-retirement
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - skills/build-skill/SKILL.md
autonomous: true
gap_closure: true
requirements: [BSKL-02, BSKL-03, BSKL-05]

must_haves:
  truths:
    - "Step 5 guard 1 rejects the proposed name pre-write for ALL name-cascade classes: not kebab-case / not lowercase-start, longer than 64 characters, and containing the words \"anthropic\" or \"claude\" — mirroring the src/schema.js NAME cascade (218-229) so a realistic name like a claude-* skill is caught before it is written, not after (WR-01 / gap #1 closed, BSKL-02)"
    - "Step 6 contains an authorized name-cascade recovery clause (delete skills/<name>/, return to Step 5 with a corrected name — the one permitted whole-directory operation) as a backstop so a name error that reaches lint is recoverable rather than burning all 3 attempts unfixably (BSKL-03)"
    - "Step 6 lint-loop fallback falls through ONLY when the command itself cannot be found or executed; a run that executes and reports lint errors is treated as the resolved linter and its output is used — never falling through to stale npx @jeremiewerner/motto lint on a real lint failure (WR-02 / gap #2 closed, BSKL-03)"
    - "Step 2 carries a supersede statement making build-skill's own field delta authoritative over the bundled skill-schema reference wherever they disagree, including its stale claim that template/dependencies are not validated (WR-03, BSKL-02)"
    - "Step 7 check 2 is scoped to template: procedure skills, and the Step 7 to Step 6 loop-back specifies a fresh 3-attempt budget with at most one quality-gate rewrite cycle total (IN-01, IN-02, BSKL-05)"
    - "build-skill still lints clean (node bin/motto.js lint reports OK) and node --test passes green; DOC-06 preserved — grep -F 'letter-start kebab-case' and grep -F 'Common Lint Errors' both return 0 matches"
  artifacts:
    - skills/build-skill/SKILL.md
  key_links:
    - "Step 5 name guard <-> src/schema.js NAME cascade (kebab + lowercase-start + <=64 chars + no anthropic/claude), described behaviorally not by copying error-message strings (DOC-06)"
    - "Step 6 fallback semantics <-> avoiding a version-skewed verdict from the stale published npx registry version on a genuine local lint failure"
    - "Step 7 quality-gate loop-back <-> Step 6 attempt budget (bounded total work, deterministic attempts-used receipt)"
---

<objective>
Close the two verification gaps and the three carried code-review anti-patterns in `skills/build-skill/SKILL.md` — all prose fixes to a single already-shipped file. This makes the "any input → complete, lint-clean skill" promise hold for the realistic `claude-*` / >64-char name input class (WR-01, the blocker), removes the stale-npx false-verdict risk in the lint loop (WR-02), and clears the three cheaper prose-consistency issues (WR-03, IN-01, IN-02) from the same unremediated review.

Purpose: Phase 16 verified `status: gaps_found` (5/7 must-haves). The root cause is a formal code review (16-REVIEW.md) whose 3 warnings + 2 info findings never got remediated before the phase was submitted. This plan applies exactly those documented fixes. No runtime/CLI/linter code changes — build-skill's body is the only surface.

Output: an edited `skills/build-skill/SKILL.md` whose Step 5 name guard mirrors the full NAME cascade, whose Step 6 lint loop distinguishes exec failure from lint failure and authorizes name-cascade recovery, and whose Steps 2/7 carry the supersede/scoping/attempt-budget clarifications. build-skill stays lint-clean and the dogfood suite stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-build-skill-author-skill-retirement/16-CONTEXT.md
@.planning/phases/16-build-skill-author-skill-retirement/16-VERIFICATION.md
@.planning/phases/16-build-skill-author-skill-retirement/16-REVIEW.md

# The file being edited, and the validator its guard must mirror
@skills/build-skill/SKILL.md
@src/schema.js
@shared/references/skill-schema.md
</context>

## Artifacts this phase produces

This plan modifies one existing file only — no new symbols, files, or CLI surface. Edited prose clauses added to `skills/build-skill/SKILL.md`:

- Step 5 guard 1 (currently line ~49): extended name validation — adds the >64-character rejection and the `anthropic`/`claude` substring rejection to the existing kebab / lowercase-start check.
- Step 6 (currently lines ~55-63): (a) reworded fallback semantics — fall through only on command-not-found / exec failure, never on a run that reports lint errors; (b) new authorized name-cascade recovery clause (delete `skills/<name>/`, return to Step 5 with a corrected name).
- Step 2 (currently line ~26): new supersede sentence making build-skill's field delta authoritative over the bundled `skill-schema` reference, including its stale `template`/`dependencies` "not validated" claim.
- Step 7 check 2 (currently line ~71): scoped to `template: procedure` generated skills.
- Step 7 loop-back (currently line ~79): attempt-budget clause — fresh 3-attempt budget, at most one quality-gate rewrite cycle total.

No frontmatter changes. `<process>`/`<success_criteria>` section structure and DOC-06 invariant are preserved.

<tasks>

<task type="auto">
  <name>Task 1: Close the two structured gaps — full NAME-cascade guard + name-recovery clause (WR-01) and exec-vs-lint-failure fallback semantics (WR-02)</name>
  <files>skills/build-skill/SKILL.md</files>
  <read_first>
    - skills/build-skill/SKILL.md — the file being edited; Step 5 guard 1 is at line ~49, Step 6 (lint loop) is at lines ~55-63. Read the current wording so the edits replace the exact existing clauses and preserve the surrounding numbered-guard / fallback-chain structure.
    - src/schema.js lines 207-234 — the real NAME cascade the guard must mirror: kebab-case regex `/^[a-z][a-z0-9]*(-[a-z0-9]+)*$/`, then `name.length > 64`, then `RESERVED.some(r => name.includes(r))` where `RESERVED = ["anthropic", "claude"]` (line 149), then name-equals-dirName. The guard must describe these rules BEHAVIORALLY and must NOT copy any error-message string from this file.
    - .planning/phases/16-build-skill-author-skill-retirement/16-REVIEW.md — WR-01 (the blocker) and WR-02 findings, including the reviewer's exact suggested wording for both fixes.
    - .planning/phases/16-build-skill-author-skill-retirement/16-VERIFICATION.md — gaps #1 and #2 (the `missing:` items define exactly what must be added); DOC-06 constraint that the guard must not restate lint error-message strings or regexes; human-verification item #3 (the fix must be behaviorally confirmable, guard-rejects-pre-write OR authorized Step 6 recovery).
    - .planning/phases/16-build-skill-author-skill-retirement/16-RESEARCH.md — Pitfall 7 (a global/npx install reports an older CLI surface than the local linter) — the reason WR-02's fallthrough-to-npx on a real lint failure is dangerous.
  </read_first>
  <action>
    Edit `skills/build-skill/SKILL.md` in two places. Preserve DOC-06: describe validator behavior, never restate an error-message string or regex from src/schema.js. Do NOT introduce the phrase that DOC-06's acceptance grep forbids; keep the existing phrasing "kebab-case, starting with a lowercase letter" for the shape rule.

    WR-01 fix, part A — extend Step 5 guard 1 (line ~49). The guard currently validates only that `name` is kebab-case starting with a lowercase letter. Add two more pre-write rejection conditions so it mirrors the full NAME cascade in src/schema.js:218-229: reject a name longer than 64 characters, and reject a name that contains the word "anthropic" or the word "claude" (the substrings the validator forbids — note this tool's own domain, Claude Code skill authoring, makes a claude-* name a highly plausible input, which is exactly why the pre-write guard must catch it). Keep the existing "stop and re-prompt for a valid name instead of silently sanitizing it" failure behavior and the path-segment rationale. State all conditions as a single validation clause; do not copy the linter's error text.

    WR-01 fix, part B — add an authorized name-cascade recovery clause to Step 6 as a backstop for any name error that still reaches lint. State that if the linter rejects the `name` itself (as opposed to a body/frontmatter issue inside the file), the agent may delete `skills/<name>/` and return to Step 5 with a corrected name — and that this delete-and-recreate is the ONE permitted whole-directory operation, an explicit exception to the "never edit files outside `skills/<name>/`" rule which otherwise still holds. This resolves the current dead-end where Step 6 authorizes no recovery for a name-only failure.

    WR-02 fix — reword Step 6's fallback-chain sentence (line ~57, currently "trying each of the following in order until one resolves"). Change the fall-through condition so the agent falls through the chain (local `node_modules/.bin/motto` → `motto` on PATH → `npx @jeremiewerner/motto`) ONLY when the command itself cannot be found or executed. Make explicit that a command which runs and reports lint errors IS the resolved linter: use its output and do NOT fall through. This prevents falling through to the published (stale) npx version on a genuine local lint failure, which would produce a version-skewed verdict (per RESEARCH Pitfall 7). Keep the three-invocation ordering and the existing dirName-filtering / read-only-for-other-skills / 3-attempt-cap behavior intact.

    Do not add fenced code blocks that restate lint messages. After editing, confirm build-skill still lints clean and the suite is green, then commit (green by construction — this edits only build-skill's own already-tracked file).
  </action>
  <verify>
    <automated>cd /Users/jeremie/Projects/motto && node bin/motto.js lint && node --test 2>&1 | tail -5 && grep -F 'letter-start kebab-case' skills/build-skill/SKILL.md; test $? -ne 0 && grep -F 'Common Lint Errors' skills/build-skill/SKILL.md; test $? -ne 0 && echo GAP1_GAP2_OK</automated>
  </verify>
  <acceptance_criteria>
    - Step 5 guard 1 now names three rejection conditions for the proposed name: not kebab-case / not lowercase-start (existing), longer than 64 characters (new), and containing "anthropic" or "claude" (new). Verify: `grep -c 'anthropic' skills/build-skill/SKILL.md` returns >=1 and `grep -c '64' skills/build-skill/SKILL.md` returns >=1, both within the Step 5 guard region.
    - Step 6 contains a name-cascade recovery clause authorizing deletion of `skills/<name>/` and a return to Step 5 with a corrected name, described as the one permitted whole-directory exception to the "never edit outside skills/<name>/" rule. Verify by reading the Step 6 prose: the words "delete" and "corrected name" (or an equivalent recovery phrasing) appear in Step 6.
    - Step 6's fallback wording no longer contains "until one resolves"; it now falls through only on command-not-found / exec failure and treats a run that reports lint errors as the resolved linter. Verify: `grep -F 'until one resolves' skills/build-skill/SKILL.md` returns 0 matches.
    - DOC-06 preserved: `grep -F 'letter-start kebab-case' skills/build-skill/SKILL.md` returns 0 matches and `grep -F 'Common Lint Errors' skills/build-skill/SKILL.md` returns 0 matches.
    - `node bin/motto.js lint` reports OK (build-skill still lints clean after the edits).
    - `node --test` passes green (zero failures) — template: procedure sections still matched, dogfood swap intact.
  </acceptance_criteria>
  <done>build-skill's Step 5 guard rejects all three name-cascade failure classes before writing, Step 6 authorizes name-recovery and falls through only on exec failure — the "any input → complete lint-clean skill" promise now holds for claude-* / >64-char names, and version-skewed lint verdicts are ruled out. build-skill lints clean and the suite is green. (BSKL-02, BSKL-03)</done>
</task>

<task type="auto">
  <name>Task 2: Close the three carried review prose fixes — Step 2 supersede (WR-03), Step 7 check-2 scoping (IN-01), Step 7 attempt-budget (IN-02)</name>
  <files>skills/build-skill/SKILL.md</files>
  <read_first>
    - skills/build-skill/SKILL.md — Step 2 is at line ~26, Step 7 check 2 at line ~71, Step 7 loop-back sentence at line ~79. Read the current wording so edits replace the exact existing clauses.
    - shared/references/skill-schema.md §6 (lines ~126-134) — the bundled reference's stale claim that `template` and `dependencies` "are NOT validated in Motto v0.0.2" (false since Phases 14/15). This is the §6 conflict Step 2's supersede sentence must neutralize.
    - .planning/phases/16-build-skill-author-skill-retirement/16-REVIEW.md — WR-03, IN-01, IN-02 findings with the reviewer's exact suggested wording for each.
    - .planning/phases/16-build-skill-author-skill-retirement/16-VERIFICATION.md — Anti-Patterns rows for lines 26, 71, 79 confirming these three carried fixes.
  </read_first>
  <action>
    Edit `skills/build-skill/SKILL.md` in three places. All are prose-consistency fixes; preserve DOC-06 (no restated lint strings) and the `<process>`/`<success_criteria>` structure.

    WR-03 fix — Step 2 (line ~26). The current sentence "For the newer fields it does not yet cover, apply this delta yourself:" understates the conflict: the bundled skill-schema reference §6 affirmatively claims `template`/`dependencies` are not validated, which is false as of the current linter. Replace with wording that mirrors the existing Step 7 check-3 supersede pattern already in the file — make build-skill's own delta authoritative over the bundled reference wherever they disagree, and call out specifically that this supersedes the reference's claim that `template` and `dependencies` are "not validated" (they are, as of the current linter). Keep the delta bullet list that follows.

    IN-01 fix — Step 7 check 2 (line ~71). The current check audits "`<success_criteria>` (or equivalent)", but "(or equivalent)" is undefined for a base-schema (non-procedure) generated skill that has no required `<success_criteria>` section. Scope the check: it applies when the GENERATED skill declares `template: procedure` — in that case its `<success_criteria>` must hold checkable conditions, not restated step or heading titles. Remove the undefined "(or equivalent)" escape hatch.

    IN-02 fix — Step 7 loop-back (line ~79). The current sentence "If fixing a check meant editing the file, re-run Step 6's lint loop before continuing." does not state whether the 3-attempt budget resets. Add that the re-run uses a fresh 3-attempt budget and that at most one quality-gate rewrite cycle occurs in total — so the receipt's "attempts used" is deterministic and the edit/lint loop is bounded.

    After editing, confirm build-skill still lints clean and the suite is green, then commit (edits only build-skill's own file).
  </action>
  <verify>
    <automated>cd /Users/jeremie/Projects/motto && node bin/motto.js lint && node --test 2>&1 | tail -5 && grep -F 'letter-start kebab-case' skills/build-skill/SKILL.md; test $? -ne 0 && echo GAP_CARRIED_OK</automated>
  </verify>
  <acceptance_criteria>
    - Step 2 now contains a supersede statement making build-skill's field delta authoritative over the bundled reference, explicitly covering the `template`/`dependencies` "not validated" claim. Verify: the words "supersede" (or "supersedes") and "validated" both appear in Step 2's prose; `grep -F 'For the newer fields it does not yet cover, apply this delta yourself:' skills/build-skill/SKILL.md` returns 0 matches (old wording replaced).
    - Step 7 check 2 is scoped to `template: procedure` generated skills and no longer contains the undefined "(or equivalent)" phrasing. Verify: `grep -F '(or equivalent)' skills/build-skill/SKILL.md` returns 0 matches, and `template: procedure` appears in the Step 7 check-2 region.
    - Step 7 loop-back specifies a fresh 3-attempt budget and at most one quality-gate rewrite cycle. Verify by reading Step 7: the loop-back sentence references a fresh attempt budget and a single rewrite cycle (e.g. "fresh 3-attempt budget", "at most one").
    - DOC-06 preserved: `grep -F 'letter-start kebab-case' skills/build-skill/SKILL.md` returns 0 matches.
    - `node bin/motto.js lint` reports OK and `node --test` passes green.
  </acceptance_criteria>
  <done>Step 2 supersedes the stale bundled reference, Step 7 check 2 is scoped to procedure skills, and the quality-gate loop-back has a bounded attempt budget — the three carried review findings are resolved. build-skill lints clean and the suite is green. (BSKL-02, BSKL-05)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inferred/gap-filled skill name → filesystem path segment | The `name` becomes `skills/<name>/` on disk; unsafe or reserved values must be rejected before the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Tampering (path traversal / unfixable name) | Step 5 GUARD (name → path segment) | high | mitigate | Task 1 extends guard 1 to reject not-kebab, >64-char, and anthropic/claude names BEFORE any write — the full NAME cascade — plus a Step 6 delete-and-recover backstop. Closes the WR-01 dead-end where a bad name was written then failed lint unrecoverably |
| T-16-06 | Tampering (version-skewed verdict) | Step 6 LINT LOOP fallback | medium | mitigate | Task 1 rewords the fallback to fall through only on exec failure, never on a real lint failure — preventing the stale published npx version from producing a false-clean/spurious verdict (RESEARCH Pitfall 7) |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this plan — prose edits to one already-tracked markdown file reusing in-repo imports vetted in Phases 14/15. No install task exists to gate |
</threat_model>

<verification>
Phase-level checks (run after both tasks):

- `node bin/motto.js lint` reports OK and `node --test` passes green (build-skill's template: procedure cascade still lints clean; dogfood swap intact; count===2).
- Step 5 guard 1 rejects >64-char and anthropic/claude names pre-write: `grep -c 'anthropic' skills/build-skill/SKILL.md` >= 1 and the guard region references a 64-character limit.
- Step 6 no longer contains "until one resolves" (`grep -F 'until one resolves'` → 0) and contains a name-recovery clause (delete + corrected-name).
- Step 2 contains a supersede statement; old "apply this delta yourself:" sentence is gone; "(or equivalent)" is gone (IN-01); Step 7 loop-back references a fresh attempt budget (IN-02).
- DOC-06 honored: `grep -F 'letter-start kebab-case'` and `grep -F 'Common Lint Errors'` both return 0 matches.
</verification>

<success_criteria>
- build-skill's Step 5 name guard mirrors the full src/schema.js NAME cascade (kebab + lowercase-start + <=64 chars + no anthropic/claude), catching the realistic claude-* / long-name input class before writing (WR-01 / gap #1 closed).
- Step 6 authorizes name-cascade recovery and falls through only on exec failure, never on a genuine lint failure (WR-01 backstop + WR-02 / gap #2 closed).
- Step 2 supersede statement, Step 7 check-2 scoping, and Step 7 attempt-budget clause resolve the three carried review findings (WR-03, IN-01, IN-02).
- DOC-06 preserved from day one — no lint error strings or regexes duplicated in build-skill's prose.
- build-skill lints clean and the full suite is green after every commit.
- BSKL-02, BSKL-03, and BSKL-05 are restored to fully-satisfied (the human-verification items — live gap-fill fidelity, quality-gate fidelity, post-fix WR-01 behavioral confirmation — remain human, as scoped by verification).
</success_criteria>

<output>
Create `.planning/phases/16-build-skill-author-skill-retirement/16-02-SUMMARY.md` when done.
</output>
