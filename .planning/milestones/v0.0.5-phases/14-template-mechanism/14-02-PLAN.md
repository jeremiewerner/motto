---
phase: 14-template-mechanism
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - skills/release/SKILL.md
  - test/dogfood.test.js
autonomous: true
requirements: [TMPL-01, TMPL-03]

must_haves:
  truths:
    - "The live `release` skill declares `template: procedure` and lints clean on the real tree (dogfood proof, not just fixtures)."
    - "The `release` body carries line-anchored `<process>`/`</process>` and `<success_criteria>`/`</success_criteria>` sections that satisfy the procedure template."
    - "`lintProject(REPO_ROOT)` still returns ok:true with count 2 and zero errors after the edit (no regression — Pitfall 4)."
    - "The built `dist/private/release/SKILL.md` retains the template tags verbatim (no content stripping)."
  artifacts:
    - "skills/release/SKILL.md — gains `template: procedure` frontmatter + `<process>`/`<success_criteria>` body sections"
    - "test/dogfood.test.js — new assertion that live template enforcement is exercised and tags survive the build"
  key_links:
    - "release SKILL.md `template: procedure` is enforced by the Plan 01 cascade — the whole mechanism proven end-to-end on the live tree"
    - "dogfood build copies SKILL.md verbatim, so the tags appear unchanged in dist/private/release/SKILL.md"
---

<objective>
Dogfood the Plan 01 mechanism on Motto's own skill tree: the `release` skill (a procedural checklist — a natural fit, locked in CONTEXT.md) adopts `template: procedure`, gaining `<process>` and `<success_criteria>` body sections, and the dogfood test suite gains an assertion proving enforcement is live on the real tree and the tags survive the verbatim build copy. This is the in-phase live proof that the mechanism works beyond unit fixtures (TMPL-01/TMPL-03).

Purpose: Prove `template: procedure` enforcement on a shipping skill without regressing the existing dogfood assertions (count 2, errors []).
Output: Edited `skills/release/SKILL.md` and an extended `test/dogfood.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-template-mechanism/14-CONTEXT.md
@.planning/phases/14-template-mechanism/14-PATTERNS.md
@skills/release/SKILL.md
@test/dogfood.test.js
</context>

<artifacts_produced>
## Artifacts this phase produces

New symbols/content introduced by this plan (exclude from source-drift acknowledgement):

- `skills/release/SKILL.md` gains a `template: procedure` frontmatter key and new body sections: `<process>`/`</process>` (wrapping the existing Step 1–6 checklist) and `<success_criteria>`/`</success_criteria>` (net-new content).
- `test/dogfood.test.js` gains one new `it(...)` assertion verifying the built `release` SKILL.md retains the template tags verbatim; existing assertions are unchanged.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Adopt template: procedure on the live release skill</name>
  <files>skills/release/SKILL.md</files>
  <read_first>
    - skills/release/SKILL.md (current structure: H1 `# Releasing Motto` at line 7, `**Role:**` line at line 9, `## Step 1`–`## Step 6` prose; frontmatter keys name/description/audience)
    - test/dogfood.test.js (Pitfall 4 coupling: lines 35-44 assert `count: 2` and `errors: []`; lines 103-113 assert `release` has NO `references/` dir — so do not introduce a `shared_references` key)
    - .planning/phases/14-template-mechanism/14-PATTERNS.md ("skills/release/SKILL.md (dogfood edit)" — the exact required edit and preservation constraints)
  </read_first>
  <action>
    Edit `skills/release/SKILL.md` to adopt the `procedure` template (locked dogfood target per CONTEXT.md):

    1. Add `template: procedure` to the frontmatter, placed after `audience: private`. Do NOT add any other key (specifically NOT `shared_references` — the dogfood test asserts `release` has no `references/` dir).
    2. Wrap the procedural body in a matched, line-anchored `<process>` / `</process>` pair: an opening `<process>` line at the start of its own line just before the `## Step 1` heading, and a `</process>` line on its own line after the end of `## Step 6`. The tags must satisfy the `^<process>` / `^</process>` anchors from Plan 01's scanner (each alone at line start).
    3. Add a matched `<success_criteria>` / `</success_criteria>` section (net-new content — no existing content maps to it). Place it after `</process>`, containing a short checklist of release done-conditions (e.g. tests pass, version bumped, tarball clean, published, tag pushed). Both tags line-anchored on their own lines.
    4. Preserve EXACTLY, unchanged: the H1 title line `# Releasing Motto`, the `**Role:**` line, and the existing `name` / `description` / `audience` frontmatter keys. A dropped or misplaced element here cascades into unrelated dogfood failures (Pitfall 4).

    After editing, run the FULL `node --test` suite (not just template tests) — the existing 131+ tests are the regression guard for exactly this edit (Pitfall 4).
  </action>
  <verify>
    <automated>node bin/motto.js lint 2>&1 | tail -3 ; node --test</automated>
  </verify>
  <acceptance_criteria>
    - `skills/release/SKILL.md` frontmatter contains `template: procedure` and still contains `name`, `description`, `audience: private` unchanged, with no `shared_references` key.
    - The body contains a line-anchored `<process>` opening line and a `</process>` closing line, wrapping the Step 1–6 checklist.
    - The body contains a line-anchored `<success_criteria>` opening line and a `</success_criteria>` closing line with net-new checklist content.
    - The H1 `# Releasing Motto` line and the `**Role:**` line are preserved verbatim.
    - `node bin/motto.js lint` reports the skill tree clean; `node --test` (full suite) is green, including the existing dogfood `count: 2` / `errors: []` assertions.
  </acceptance_criteria>
  <done>release/SKILL.md carries `template: procedure` with both required sections, lints clean, and the full test suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Assert live template enforcement + verbatim tag survival in dogfood test</name>
  <files>test/dogfood.test.js</files>
  <read_first>
    - test/dogfood.test.js (existing assertions: `lintProject(REPO_ROOT)` ok:true/count:2/errors:[] at lines 35-44; the built-artifact `it`s at lines 103-113 that stat `dist/private/release/SKILL.md`)
    - skills/release/SKILL.md (the tags added in Task 1 that must survive the build copy)
    - .planning/phases/14-template-mechanism/14-PATTERNS.md ("test/dogfood.test.js (extended — verify-only)")
  </read_first>
  <action>
    Extend `test/dogfood.test.js` to prove `template: procedure` enforcement is live on the real tree and the tags survive Motto's verbatim build copy (the project's "no content stripping" constraint):

    - Add one `it(...)` inside the existing dogfood build describe block that reads the built `dist/private/release/SKILL.md` (from the existing `tempDir` build output) and asserts its content contains the line-anchored `<process>`, `</process>`, `<success_criteria>`, and `</success_criteria>` tags — proving the tags are copied through unchanged.
    - Do NOT weaken or remove the existing `lintProject(REPO_ROOT)` assertions (ok:true, count 2, errors []) — those already prove the live tree (now including `release`'s `template: procedure`) lints clean, which is the enforcement proof. Keep them intact.

    Use the existing `readFile` import and `tempDir` build path already present in the file; do not restructure the `before`/`after` build harness.
  </action>
  <verify>
    <automated>node --test test/dogfood.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test/dogfood.test.js` has a new `it(...)` asserting the built `dist/private/release/SKILL.md` contains `<process>`, `</process>`, `<success_criteria>`, and `</success_criteria>` (verbatim tag survival).
    - The existing `lintProject(REPO_ROOT)` assertions (ok:true, count 2, errors []) remain unchanged and still pass.
    - `node --test test/dogfood.test.js` is green.
  </acceptance_criteria>
  <done>The dogfood suite proves live `template: procedure` enforcement and verbatim tag survival, and passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill author → linter/build | The edited `release/SKILL.md` body and frontmatter are processed by the same `validateSkill()` + verbatim build copy as any skill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | verbatim build copy of `release/SKILL.md` into `dist/private/` | low | accept | Verbatim copy with no content stripping is the project's locked invariant (v0.0.2). The new dogfood assertion positively verifies the tags survive unchanged; no transformation surface is introduced. |
| T-14-05 | Denial of Service | dogfood build harness reading the edited live skill | low | mitigate | Task builds against an isolated `mkdtemp` copy (existing harness), never the repo's own `dist/`; `buildProject` is lint-gated and fails fast with the root cause. No change to the harness. |

No `npm/pip/cargo` installs in this plan, so no supply-chain (T-14-SC) threat applies. No threat is rated `high`; plan remains autonomous.
</threat_model>

<verification>
- `node bin/motto.js lint` reports the real skill tree clean with `release` now under `template: procedure`.
- `node --test` full suite green — existing dogfood `count: 2` / `errors: []` assertions unregressed (Pitfall 4).
- The built `dist/private/release/SKILL.md` contains the four template tags verbatim.
</verification>

<success_criteria>
- Live dogfood proves `template: procedure` enforcement on the `release` skill (TMPL-01, TMPL-03) end-to-end, satisfying roadmap SC1 on the real tree.
- No regression to the existing 131+ test suite.
</success_criteria>

<output>
Create `.planning/phases/14-template-mechanism/14-02-SUMMARY.md` when done.
</output>
