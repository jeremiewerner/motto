---
phase: 14-template-mechanism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates.js
  - src/schema.js
  - test/schema.test.js
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-03, TMPL-04, TMPL-05]

must_haves:
  truths:
    - "A skill with `template: procedure` missing `<process>` or `<success_criteria>` fails lint with a named-section error that includes the registry description."
    - "A skill with an unknown template name fails lint with one line listing available templates, alphabetically."
    - "Section-tag matching ignores `<process>`/`</process>` lines that appear inside fenced code blocks."
    - "A skill with no `template:` key returns byte-for-byte identical errors to v0.0.4 (regression-guarded)."
    - "A malformed `template:` value (non-string, empty string, throwing object) produces an error and never throws (D-01)."
    - "The `waives` merge logic exists and is proven via an injected test-fixture template — adding a template is a data-only edit (TMPL-02)."
  artifacts:
    - "src/templates.js — pure-data SECTIONS + TEMPLATES exports, no functions, no imports"
    - "src/schema.js — exported hasClosedSection(body, tagName) + template cascade inside validateSkill"
    - "test/schema.test.js — scanner unit tests, cascade tests, adversarial tests, waives-fixture test, split B13"
  key_links:
    - "src/templates.js SECTIONS/TEMPLATES imported by src/schema.js"
    - "hasClosedSection consumed by the template cascade for each requiredSection"
    - "waivedSections resolved by the cascade BEFORE the Title/Role body-spine checks so waives can gate them"
    - "validateSkill's third templatesRegistry param defaults to {SECTIONS, TEMPLATES}, injectable for the waives fixture"
---

<objective>
Activate the `template:` frontmatter field as Motto's first data-driven validation profile. Create `src/templates.js` as a pure-data registry (TMPL-02) and extend `validateSkill()` in `src/schema.js` with a fence-aware section-tag scanner plus a template-field cascade (TMPL-01/03/04/05). In v0.0.4 `template` was an inert passthrough key (D-14); this plan promotes it to a validated key while keeping template-less skills byte-for-byte identical (TMPL-05).

Purpose: Deliver the entire lint mechanism so that adding a future template is a data-only edit to `src/templates.js` — no linter code change.
Output: `src/templates.js`, extended `src/schema.js` (exported `hasClosedSection` + template cascade), and a fully guarded `test/schema.test.js` (including the split of the B13 landmine).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-template-mechanism/14-CONTEXT.md
@.planning/phases/14-template-mechanism/14-RESEARCH.md
@.planning/phases/14-template-mechanism/14-PATTERNS.md
@.planning/superpowers/specs/2026-07-02-skill-builder-design.md
@src/schema.js
@src/lint.js
@test/schema.test.js
</context>

<artifacts_produced>
## Artifacts this phase produces

New symbols introduced by this plan (exclude from source-drift acknowledgement):

- `SECTIONS` — export in `src/templates.js` (tag-name → description map). Keys: `process`, `success_criteria`.
- `TEMPLATES` — export in `src/templates.js` (template-name → rules map). Keys: `procedure` with `requiredSections`.
- `hasClosedSection(body, tagName)` — new exported function in `src/schema.js`.
- `templatesRegistry` — new optional third parameter of `validateSkill(skill, sharedRefs, templatesRegistry)`, defaulting to `{ SECTIONS, TEMPLATES }`.
- `waivedSections` — internal local `Set` in `validateSkill()` (not exported).
- `FIXTURE_TEMPLATES` — test-only registry constant in `test/schema.test.js` (never added to `src/templates.js`).
- Test IDs `B13a` / `B13b` — replace the old `B13` test.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create src/templates.js pure-data registry (TMPL-02)</name>
  <files>src/templates.js</files>
  <read_first>
    - .planning/superpowers/specs/2026-07-02-skill-builder-design.md (§1 — the verbatim SECTIONS/TEMPLATES data shape this file must match)
    - src/schema.js (the `RESERVED` module-level constant at line 47 — the pure-data-export analog; also the header comment "Pure object validator — no filesystem, no YAML parsing")
    - .planning/phases/14-template-mechanism/14-RESEARCH.md ("Code Examples" → "src/templates.js (verbatim from the design spec)")
  </read_first>
  <action>
    Create `src/templates.js` as a pure-data ES module — no functions, no imports, no filesystem or YAML access — mirroring the `RESERVED` export style in `src/schema.js` (TMPL-02: adding a template must be a data-only edit).

    Export two named consts:
    - `SECTIONS`: an object mapping body-tag name to a human description. Include exactly two entries, using the descriptions from the design spec §1: `process` → "Numbered steps the agent executes, in order." and `success_criteria` → "Checkable conditions that define done." These descriptions double as the schema doc source (D-07) and are quoted back to authors in lint errors.
    - `TEMPLATES`: an object mapping template name to its rules. Include exactly one entry: `procedure` with `requiredSections: ["process", "success_criteria"]`. Leave a comment noting future keys (`requiredFields`, `waives`) exist in the mechanism but are unused by `procedure`.

    Add a file header comment stating the file is pure data (no functions, no I/O) per TMPL-02. Do NOT add a `waives` key to `procedure` — `procedure` waives nothing; the waives mechanism is exercised only by the injected test fixture in Task 3.
  </action>
  <verify>
    <automated>node --input-type=module -e "const a=(await import('node:assert')).strict;const m=await import('./src/templates.js');a.deepEqual(Object.keys(m.TEMPLATES),['procedure']);a.deepEqual(m.TEMPLATES.procedure.requiredSections,['process','success_criteria']);a.deepEqual(Object.keys(m.SECTIONS).sort(),['process','success_criteria']);a.ok(typeof m.SECTIONS.process==='string'&&m.SECTIONS.process.length>0);console.log('templates.js OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/templates.js` exports `SECTIONS` and `TEMPLATES` as named exports.
    - `SECTIONS` has exactly the keys `process` and `success_criteria`, each mapping to a non-empty string description matching design spec §1.
    - `TEMPLATES.procedure.requiredSections` deep-equals `["process", "success_criteria"]`.
    - `TEMPLATES.procedure` has no `waives` key.
    - The file contains no `import` statement and defines no exported function (pure data — TMPL-02).
  </acceptance_criteria>
  <done>src/templates.js exists as pure data and the automated import assertion prints "templates.js OK".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exported fence-aware hasClosedSection scanner (TMPL-03)</name>
  <files>src/schema.js, test/schema.test.js</files>
  <read_first>
    - src/schema.js (the `NAME_KEBAB` export at line 33 — the pure exported-helper analog; the T-02-01 / T-Q-01 anchored non-backtracking regex discipline comments at lines 92, 128, 144, 154)
    - .planning/phases/14-template-mechanism/14-PATTERNS.md ("Pattern 2: Fence-aware, line-anchored, bare-tag section scanner")
    - .planning/phases/14-template-mechanism/14-RESEARCH.md (Assumption A2 + Open Question 1 — trailing same-line content decision; Security Domain — ReDoS note)
  </read_first>
  <behavior>
    - Test: body containing line-anchored `<process>` and `</process>` (each opening its own line) → returns true.
    - Test: body with `<process>` but no `</process>` (unclosed) → returns false (matched pair required — D-07).
    - Test: body where both `<process>` and `</process>` appear ONLY inside a triple-backtick fenced block → returns false (fence-excluded — TMPL-03 / roadmap SC3).
    - Test: body where the open tag is fenced but a real closing tag is unfenced (or vice versa) → returns false (both must be present outside fences).
    - Test: tag mentioned mid-sentence ("the <process> tag") not at line start → does not count as an open tag.
    - Test: `~~~` (tilde) fence markers toggle fence state the same as backtick fences.
    - Test: open tag with trailing same-line content (`<process> notes`) still counts as an open (starts-line anchor, per Assumption A2 — permissive `^<tag>` with no end anchor).
    - Test: empty/undefined body → returns false without throwing (D-01).
  </behavior>
  <action>
    Add a new exported function `hasClosedSection(body, tagName)` to `src/schema.js`, placed beside `NAME_KEBAB` as a module-level exported building block. Implement the two-phase structure from PATTERNS.md Pattern 2 to avoid the hot-loop regex pitfall (Pitfall 3): (1) single pass over `body.split("\n")` toggling an `inFence` flag whenever a line matches a fence-open/close marker (a run of 3+ backticks or 3+ tildes at line start with up to 3 leading spaces), collecting only unfenced non-marker lines; (2) join the unfenced lines and test them once with two anchored multiline regexes — an open matcher `^<tagName>` and a close matcher `^</tagName>`, both with the `m` flag — returning true only when both match.

    Guard against a falsy `body` up front (`const bodyStr = body || ""`) so an empty or undefined body returns false without throwing (D-01). `tagName` is always sourced from the trusted internal registry (`TEMPLATES[...].requiredSections`), never from raw `data.template`, so interpolating it into `new RegExp(...)` carries no injection risk — mirror the T-Q-01 precedent and note this in a code comment. Do NOT add an end-of-line anchor to the open matcher: per Assumption A2 the tag "starts the line" and trailing same-line content still counts.

    In `test/schema.test.js`, add a dedicated describe block of `hasClosedSection` unit tests covering every case in the behavior list above. Import `hasClosedSection` from `../src/schema.js` alongside the existing `validateSkill` import.
  </action>
  <verify>
    <automated>node --test test/schema.test.js 2>&1 | grep -Ei "hasClosedSection|fence" ; node --test test/schema.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/schema.js` exports `hasClosedSection`.
    - `hasClosedSection("# t\n<process>\nx\n</process>\n", "process")` returns `true`.
    - A body whose only `<process>`/`</process>` lines are inside a triple-backtick fence returns `false`.
    - A body with `<process>` but no `</process>` returns `false`.
    - `hasClosedSection(undefined, "process")` returns `false` and does not throw.
    - `~~~` fences toggle fence state identically to backtick fences.
    - `node --test test/schema.test.js` passes (all existing tests still green).
  </acceptance_criteria>
  <done>hasClosedSection is exported, all new scanner unit tests pass, and the full schema.test.js suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add template cascade to validateSkill + split B13 + adversarial/waives tests (TMPL-01/04/05)</name>
  <files>src/schema.js, test/schema.test.js</files>
  <read_first>
    - src/schema.js (the NAME cascade at lines 81-108 — the stop-at-first-failure precedent; the `sharedRefs = new Set()` default param at line 74 — the dependency-injection precedent; the Title check at lines 143-150 and Role check at lines 152-156 that must become waives-gated)
    - src/lint.js (the try/catch backstop at lines ~161-164 — proves unexpected throws are caught, but adversarial tests must still prove the typeof guard short-circuits)
    - test/schema.test.js (the current B13 at lines 258-273 asserting `template: "standard"` produces zero errors — this WILL fail once validation ships and MUST be split, not appended-to; Pitfall 1)
    - .planning/phases/14-template-mechanism/14-PATTERNS.md ("src/schema.js" section — exact cascade code, placement-before-body-spine, DI param, B13 split, waives fixture)
    - .planning/phases/14-template-mechanism/14-RESEARCH.md (Pitfall 1, Pitfall 2, Security Domain adversarial requirement)
  </read_first>
  <behavior>
    - Test: `template: procedure` body missing `<success_criteria>` → one error whose message names the template and quotes the SECTIONS description (D-07).
    - Test: `template: procedure` body with both required sections + valid base spine → zero errors.
    - Test: `template: "standard"` (unknown) → one error `unknown template "standard" (available: procedure)`, sorted names, no section errors (cascade stops — TMPL-04).
    - Test: `template: 123` / array / object → error `template must be a string (got number|object)`, no further template checks (cascade stops).
    - Test: `template: ""` (empty string, key present) → treated as unknown template, errors — never silently skipped (D-07 intent-declared).
    - Test: `template: null` (key present, YAML null) → non-string branch errors, never throws (D-01).
    - Test: object with a throwing `toString`/`Symbol.toPrimitive` as `template` → the `typeof !== "string"` guard short-circuits BEFORE any coercion; returns an error, never throws.
    - Test (TMPL-05 regression): a skill with NO `template` key returns the exact same errors array as before (both a valid skill → `[]` and an invalid-body skill → same base-spine errors as v0.0.4).
    - Test (waives via injected fixture): a fixture template declaring `waives: ["role"]` suppresses the `**Role:**` body error for a role-less body; `procedure` (real registry) waives nothing.
  </behavior>
  <action>
    Extend `validateSkill()` in `src/schema.js` (TMPL-01/04/05, matching the D-13 cascade-vs-independent model and D-01 never-throw invariant):

    1. Add `import { SECTIONS, TEMPLATES } from "./templates.js";` near the top of the file.
    2. Add a third parameter `templatesRegistry = { SECTIONS, TEMPLATES }` to `validateSkill(skill, sharedRefs = new Set(), templatesRegistry = { SECTIONS, TEMPLATES })`, and destructure it locally (e.g. `const { SECTIONS: SEC, TEMPLATES: TPL } = templatesRegistry;`). This mirrors the existing `sharedRefs` DI precedent and lets tests inject a waives fixture without polluting `src/templates.js`.
    3. Insert the template cascade — computing `let waivedSections = new Set()` — using `Object.prototype.hasOwnProperty.call(data, "template")` as the key-presence gate (NOT a truthy check, so `template: ""` errors instead of silently skipping). Branch order (stop at first failure): non-string → `err("template must be a string (got <typeof>)")`; else unknown key in `TPL` → `err('unknown template "<value>" (available: <sorted keys>)')`; else known → set `waivedSections = new Set(template.waives ?? [])` and, for each `requiredSections` entry not satisfied by `hasClosedSection(bodyStr, section)`, `err('template "<name>" requires <<section>>…</<section>> section — <SEC[section]>')`.
    4. CRITICAL placement (Pitfall 2): this cascade must run and resolve `waivedSections` BEFORE the existing Title and Role body-spine checks. Reorder so the template block precedes them. Gate the Title check with `if (!waivedSections.has("title"))` and the Role check with `if (!waivedSections.has("role"))`. When the `template` key is absent, `waivedSections` stays empty, so both checks run exactly as v0.0.4 (TMPL-05).

    In `test/schema.test.js`:
    - Replace the old B13 with two tests: `B13a` — a skill with a `dependencies` key (and no `template` key) still produces zero errors (D-14 still applies to `dependencies`, which stays a passthrough until Phase 15); `B13b` — a skill with `template: "standard"` now produces an error matching `unknown template "standard"` (TMPL-04). Do NOT leave the old combined B13 in place.
    - Add the missing-section, valid-procedure, adversarial (number/array/object/null/empty-string/throwing-toString), and TMPL-05 regression tests from the behavior list.
    - Add a `FIXTURE_TEMPLATES` constant (a `no-role-needed` template with `requiredSections: ["process"]` and `waives: ["role"]`) local to the test file and a test that injects it as the third `validateSkill` argument to prove waives suppresses the Role error. Do NOT add this fixture to `src/templates.js`.
  </action>
  <verify>
    <automated>node --test test/schema.test.js 2>&1 | grep -E "B13a|B13b|waives|unknown template" ; node --test</automated>
  </verify>
  <acceptance_criteria>
    - `validateSkill` accepts a third `templatesRegistry` parameter defaulting to `{ SECTIONS, TEMPLATES }`.
    - A `template: procedure` skill missing `<success_criteria>` yields exactly one error whose message contains `template "procedure" requires` and the `success_criteria` registry description.
    - `template: "standard"` yields an error matching `unknown template "standard" (available: procedure)` and no section errors.
    - `template: 123` yields `template must be a string (got number)`; `template` as an object with a throwing `toString` returns an error and does NOT throw.
    - The old combined B13 is gone; `B13a` (dependencies → no errors) and `B13b` (unknown template → error) both pass.
    - A skill with no `template` key returns the identical error set to v0.0.4 (TMPL-05 regression test passes).
    - The injected-fixture waives test passes (role error suppressed); `node --test` (full suite, 131+ tests) is green.
  </acceptance_criteria>
  <done>The template cascade is live, B13 is split, all adversarial + waives + regression tests pass, and the full `node --test` suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill author → linter | Untrusted frontmatter (`data.template`) and body text authored by any contributor cross into `validateSkill()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | `data.template` flowing into pattern construction (`src/schema.js`) | medium | mitigate | `data.template` is used ONLY as an object-key lookup (`hasOwnProperty.call(TPL, tpl)`, `TPL[tpl]`) — never interpolated into a RegExp or eval. Only trusted registry `requiredSections` names reach `new RegExp(...)`. Adversarial test covers a malicious template string. |
| T-14-02 | Denial of Service | fence + section-tag regexes in `hasClosedSection` (`src/schema.js`) | low | mitigate | All regexes anchored (`^`), fixed-length single-class alternation (`{3,}` on one char class, no nesting) — no catastrophic backtracking (T-02-01 precedent). Only trusted internal tag names interpolated. |
| T-14-03 | Denial of Service | exotic `template` value (throwing `toString`/`Symbol.toPrimitive`) crashing the lint run | low | mitigate | `typeof tpl !== "string"` guard short-circuits before any coercion; `src/lint.js` try/catch backstop is a second layer. Adversarial test proves no throw (D-01). |

No `npm/pip/cargo` installs in this phase (zero new dependencies — see RESEARCH Package Legitimacy Audit), so no supply-chain (T-14-SC) threat applies. No threat is rated `high`; plan remains autonomous with no blocking human checkpoint.
</threat_model>

<verification>
- `node --test` full suite green (131+ existing tests plus all new template/scanner tests).
- `src/templates.js` is pure data (no functions, no imports) — the import assertion in Task 1 passes.
- `hasClosedSection` correctly excludes fenced tags and requires a matched pair.
- The template cascade errors on unknown/malformed values and never throws.
- TMPL-05 regression: template-less skills return identical errors to v0.0.4.
- `waives` merge logic proven via injected fixture (no fixture in production `templates.js`).
</verification>

<success_criteria>
- All five roadmap success criteria for Phase 14 are satisfied at the mechanism level: template enforcement (SC1), unknown-template listing (SC2), fence-aware matching (SC3), template-less regression parity (SC4), and data-only extensibility incl. the exercised `waives` path (SC5).
- Requirements TMPL-01, TMPL-02, TMPL-03, TMPL-04, TMPL-05 covered.
</success_criteria>

<output>
Create `.planning/phases/14-template-mechanism/14-01-SUMMARY.md` when done.
</output>
