---
phase: 18-migrate-base-spine-role-to-role-section-tag
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/schema.js
  - skills/release/SKILL.md
  - skills/build-skill/SKILL.md
  - src/init.js
  - shared/references/skill-schema.md
  - test/doc-sync.test.js
  - README.md
  - test/schema.test.js
  - test/lint.test.js
  - test/build.test.js
  - test/frontmatter.test.js
autonomous: true
requirements: [D-01, D-02, D-04, D-07, D-08, D-09]
must_haves:
  truths:
    - "motto lint rejects a body with only the legacy bold-line role convention and no <role> section, emitting the composed missing-role error (D-01 hard break)"
    - "motto lint rejects a body whose <role></role> section is empty/whitespace-only, emitting the distinct empty-role error (D-05, D-08)"
    - "motto lint accepts a body containing a non-empty <role>…</role> section"
    - "Both live skills (release, build-skill), the motto init hello-world starter, README examples, and skill-schema.md all use <role> section tags; no legacy bold-line role convention remains except the frontmatter B8 alias-guard fixture"
    - "The role rule is registry-driven: src/schema.js loops BASE_SPINE and composes the missing-role error from SECTIONS.role (D-03); the hardcoded title check is untouched (D-04)"
    - "The full node --test suite is green in a single atomic commit — dogfood (skills/) and doc-sync guards are mutually consistent only when linter, artifacts, and docs all move together"
  artifacts:
    - "src/schema.js — BASE_SPINE loop replacing the legacy role regex block; two distinct error strings"
    - "skills/release/SKILL.md, skills/build-skill/SKILL.md, src/init.js — migrated to <role>"
    - "shared/references/skill-schema.md, test/doc-sync.test.js, README.md — docs + drift guard updated to the two new error strings"
    - "test/schema.test.js, test/lint.test.js, test/build.test.js, test/frontmatter.test.js — fixture bodies migrated to <role>"
  key_links:
    - "src/schema.js BASE_SPINE loop ↔ BASE_SPINE / SECTIONS.role from src/templates.js"
    - "test/doc-sync.test.js staticSegments ↔ the two new src/schema.js error strings ↔ skill-schema.md error tables"
    - "test/dogfood.test.js (lints skills/) ↔ migrated skills/release + skills/build-skill"
---

<!-- planner-discipline-allow: **Role: -->
<!-- planner-discipline-allow: \*\*Role: -->

<objective>
Perform the atomic hard-break flip: replace the legacy bold-line role check in src/schema.js with a registry-driven BASE_SPINE loop (missing + empty errors), and migrate every live artifact, doc, drift guard, and test fixture to the `<role>…</role>` section tag — all in one green commit.

ATOMIC COMMIT REQUIREMENT (mirror the Phase 16 atomic-swap precedent): husky's pre-commit runs `npm test`, which includes `test/dogfood.test.js` (lints `skills/`) and `test/doc-sync.test.js` (asserts each schema error string is both live source text AND quoted in skill-schema.md). These guards are mutually consistent ONLY when the linter, the live skills, and the docs all use the section tag and the two new error strings simultaneously. Therefore all three tasks below land as a SINGLE commit. Do NOT commit after Task 1 or Task 2 — an intermediate commit leaves the suite red and is blocked by husky. Per-task `<verify>` uses self-contained checks; the full `node --test` green gate is asserted once, at the end of Task 3.

Purpose: Complete the base-spine role migration to a section tag with a clean hard break (D-01), registry-driven enforcement (D-03), and the empty-role gap closed (D-05).
Output: A green suite where `<role>` is the only accepted role declaration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-CONTEXT.md
@.planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-PATTERNS.md
@.planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the legacy role regex with the registry-driven BASE_SPINE loop</name>
  <files>src/schema.js, test/schema.test.js</files>
  <read_first>
    - src/schema.js lines 17-19 (imports from ./templates.js — BASE_SPINE must be added here)
    - src/schema.js lines 178-202 (templatesRegistry param default + destructure at line 202 — BASE_SPINE must be threaded through to stay test-injectable; JSDoc @param at line 178 needs updating)
    - src/schema.js lines 276-329 (TEMPLATE cascade resolving waivedSections, the Title check, and the legacy role check block being replaced; waivedSections is resolved BEFORE these checks)
    - src/schema.js lines 296-306 (the template requiredSections loop — the exact hasClosedSection + composed-error shape to mirror)
    - src/templates.js (BASE_SPINE and SECTIONS.role added in Plan 18-01)
    - src/schema.js lines 43-101 (hasClosedSection) plus the hasNonEmptyClosedSection helper added in Plan 18-01
    - test/schema.test.js (existing role/spine assertions that must move to the new errors and section-tag bodies)
    - .planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-CONTEXT.md (D-01, D-02, D-04, D-07, D-08) and 18-PATTERNS.md (src/schema.js section — user-approved loop shape)
  </read_first>
  <behavior>
    - A body with a non-empty <role>…</role> section produces no role error.
    - A body with neither the section nor anything else produces exactly the missing-role error composed with the SECTIONS.role description.
    - A body containing an empty role section (whitespace/fenced-only content between the tags) produces exactly the distinct empty-role error, and NOT the missing-role error (D-08: two distinct errors, never both).
    - A leftover legacy bold-line role convention with no section tag is treated as inert body text — only the missing-role error fires, with no separate "legacy line detected" check (D-02).
    - The role check is skipped when the resolved template waives "role" (waivedSections cascade preserved unchanged, D-03).
    - The hardcoded H1 Title check and its waives("title") escape are untouched (D-04).
    - validateSkill still never throws for malformed bodies (null/non-string), delegating to the Plan 18-01 helpers which coerce input.
  </behavior>
  <action>
    Import BASE_SPINE alongside SECTIONS and TEMPLATES from ./templates.js (line ~19). Thread it through the injectable registry: add it to the `templatesRegistry` default parameter object (line ~196, so the default becomes SECTIONS/TEMPLATES/BASE_SPINE), destructure it at line ~202 (bind it to a local SPINE alias alongside the existing SEC/TPL aliases), and update the `@param {{ SECTIONS, TEMPLATES }}` JSDoc at line ~178 to include BASE_SPINE.

    Delete the legacy role check block (the anchored multiline regex test on the body plus its explanatory comment and its single error string, currently around lines 322-329) and replace it, at the same location (after waivedSections is resolved and after the Title check), with a loop over BASE_SPINE. For each spine section `s`: if `waivedSections.has(s)` continue; else if `hasClosedSection(bodyStr, s)` is false, push the composed missing-section error and continue; else if `hasNonEmptyClosedSection(bodyStr, s)` is false, push the distinct empty-section error. This ordering yields exactly one of the two errors per section (D-08), never both, and never a "legacy line detected" check (D-02).

    Use these concrete target error strings (wording is Claude's discretion, but keep the shape, the composition from the SECTIONS description per D-03, the `…` ellipsis character matching the existing template error at line ~302, and two distinct strings per D-08):
    - Missing:  the template literal `body must contain <${s}>…</${s}> — ${SEC[s] ?? ""}`  — renders "body must contain <role>…</role> — Behavioral instruction that tells the agent who it is and how to act."
    - Empty:    the template literal `<${s}>…</${s}> section must not be empty — ${SEC[s] ?? ""}`  — renders "<role>…</role> section must not be empty — Behavioral instruction that tells the agent who it is and how to act."

    Do NOT touch the Title check or its waives("title") escape (D-04). Do NOT modify hasClosedSection or hasNonEmptyClosedSection. Do NOT add any check that inspects the body for the old bold-line convention (D-02: it is inert).

    In test/schema.test.js, update the existing role/spine cases: swap legacy-role fixture bodies for section-tag bodies where a clean lint is expected; change the case that expected the old missing-role message to expect the new missing-role string; and ADD two cases — an empty role-section body expecting the empty-role error (asserting the missing-role error is NOT also present), and a body carrying only the legacy bold-line convention expecting the missing-role error (D-01/D-02: the legacy line no longer satisfies the spine). Keep an adversarial case asserting a null/non-string body still returns errors without throwing.

    Do NOT run the full suite or commit from this task — dogfood and doc-sync stay red until Tasks 2 and 3 land. The per-task verify below is self-contained.
  </action>
  <verify>
    <automated>node -e "import('./src/schema.js').then(m=>{const v=(b)=>m.validateSkill({dirName:'x',data:{name:'x',description:'d',audience:'public'},body:b}).map(e=>e.message);const clean=v('# T\n\n<role>\nYou are X.\n</role>').filter(s=>/role/i.test(s)).length===0;const miss=v('# T\n\nplain text').some(s=>s.startsWith('body must contain <role>'));const emptyErr=v('# T\n\n<role>\n</role>').some(s=>s.includes('section must not be empty'));const noMissOnEmpty=!v('# T\n\n<role>\n</role>').some(s=>s.startsWith('body must contain <role>'));if(!(clean&&miss&&emptyErr&&noMissOnEmpty))process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Inline validateSkill on a `<role>\nYou are X.\n</role>` body yields no role-related error.
    - Inline validateSkill on a body with no role section yields an error beginning `body must contain <role>…</role> — ` (composed with the SECTIONS.role description).
    - Inline validateSkill on an empty `<role>\n</role>` body yields the empty-role error containing `section must not be empty` AND does NOT yield the missing-role error (exactly one, D-08).
    - `grep -c "import { SECTIONS, TEMPLATES, BASE_SPINE }" src/schema.js` returns 1.
    - `grep -c 'body must contain a ' src/schema.js` returns 0 (legacy missing-role error string removed).
    - `grep -c 'Role:/m' src/schema.js` returns 0 (legacy role regex removed — the frontmatter.js B8 comment is a different file and stays).
    - `grep -c 'waivedSections.has("title")' src/schema.js` returns 1 (Title check untouched, D-04).
  </acceptance_criteria>
  <done>The BASE_SPINE loop drives the role check with two distinct errors; legacy regex + error string removed; Title check and never-throw behavior preserved. Do not commit yet.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate live skills, init starter, and all test fixtures to the role section tag</name>
  <files>skills/release/SKILL.md, skills/build-skill/SKILL.md, src/init.js, test/schema.test.js, test/lint.test.js, test/build.test.js, test/frontmatter.test.js</files>
  <read_first>
    - skills/release/SKILL.md line ~14 (role line to wrap)
    - skills/build-skill/SKILL.md lines ~16 (role line), ~26 (mentions "Title + Role spine"), ~68 (generation-instruction reference), ~90 (quality-gate reference)
    - src/init.js lines ~38-47 (HELLO_WORLD_SKILL_MD starter template literal with the role line at ~46)
    - test/schema.test.js, test/lint.test.js, test/build.test.js, test/frontmatter.test.js (fixture bodies — enumerate exact occurrences via this task's verify grep)
    - src/frontmatter.js lines ~78, ~87 (the B8 alias-parsing guard — the ONE occurrence NOT to migrate; do not edit this file)
    - .planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-CONTEXT.md (Claude's Discretion: build-skill prose keeps "behavioral, not a bare label"; frontmatter B8 fixture stays) and 18-PATTERNS.md (Test fixtures + live-skill sections, and the do-NOT-migrate B8 exception)
  </read_first>
  <behavior>
    - skills/release/SKILL.md and skills/build-skill/SKILL.md declare their role via a <role>…</role> section (same prose, wrapped) instead of the legacy bold line.
    - build-skill's generation-instruction (~line 68) and quality-gate (~line 90) references name the role section and keep the "behavioral, not a bare label" content-quality wording; the "Title + Role spine" mention (~line 26) reads naturally with the tag form.
    - The motto init hello-world starter emits a <role>…</role> section.
    - Every fixture body across the four test files exercised for a clean/consistent spine uses the section tag instead of the legacy bold line, EXCEPT the frontmatter B8 alias-guard fixture, left byte-for-byte unchanged.
    - motto build still copies SKILL.md verbatim (no build-side change); dist output follows source automatically.
  </behavior>
  <action>
    First enumerate every legacy occurrence using this task's verify grep, then for each replace the legacy bold-line role declaration with a `<role>` section wrapping the SAME prose — opening tag on its own line, the existing sentence(s) on following line(s), closing tag on its own line. Where a fixture body is a compact single-line string literal, a single-line section-tag form is acceptable to avoid gratuitous multi-line diffs; where the source is multi-line prose, use the block form. Do not reword the role prose itself — this is a mechanical 1:1 wrap (D-01 migration; multi-line role content is now permitted since the bold-line form's single-line convention no longer applies).

    In skills/build-skill/SKILL.md, additionally update the two prose references (generation instructions ~line 68 and success_criteria quality gate ~line 90) to name the role section instead of the legacy bold line, preserving the "real, behavioral sentence… not a bare label" content-quality language (this stays build-skill's job; lint now enforces only non-emptiness). Keep DOC-06 invariants — do not paste any live lint error string or regex into the prose. Verify the "Title + Role spine" mention (~line 26) still reads correctly.

    In src/init.js, update the HELLO_WORLD_SKILL_MD template literal so the starter body emits a role section (same example sentence). The init-dogfood test scaffolds and lints this starter, so it must satisfy the new non-empty role rule.

    EXCEPTION — do NOT migrate the frontmatter B8 alias-guard fixture (the legacy `Role:` bold line followed by a `---`) and do NOT edit src/frontmatter.js: that fixture guards a YAML alias-parsing property unrelated to the spine rule (CONTEXT Claude's Discretion). Leave that exact fixture string intact even though it still contains the legacy marker.

    Do NOT commit from this task — doc-sync stays red until Task 3 updates the docs. The verify below is a source-assertion grep only.
  </action>
  <verify>
    <automated>test $(grep -rn '\*\*Role:' skills/ src/init.js | wc -l | tr -d ' ') -eq 0 && test $(grep -c '<role>' skills/release/SKILL.md) -ge 1 && test $(grep -c '<role>' skills/build-skill/SKILL.md) -ge 1 && test $(grep -c '<role>' src/init.js) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '\*\*Role:' skills/ src/init.js` returns nothing (both live skills and the starter fully migrated).
    - `skills/release/SKILL.md`, `skills/build-skill/SKILL.md`, and `src/init.js` each contain at least one `<role>` opening tag.
    - build-skill's two prose references (~line 68, ~line 90) name the role section, not the legacy bold line, and retain the "behavioral, not a bare label" wording.
    - The frontmatter B8 fixture in test/frontmatter.test.js and src/frontmatter.js are unchanged (the one legacy occurrence deliberately left; `git diff --stat src/frontmatter.js` shows no change).
    - No lint error string or regex was pasted into any SKILL.md prose (DOC-06 invariant preserved).
  </acceptance_criteria>
  <done>Live skills, init starter, and all applicable fixtures use the role section tag; the B8 guard fixture and src/frontmatter.js are untouched. Do not commit yet.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs + drift guard, then assert the single green suite</name>
  <files>shared/references/skill-schema.md, test/doc-sync.test.js, README.md</files>
  <read_first>
    - shared/references/skill-schema.md lines 83-108 (§4 Body Spine — Check 2 Role line, the empty-passes caveat to delete, the lint-error line to replace) and lines 295-306 (the frontmatter example block role line at ~305)
    - shared/references/skill-schema.md lines 179-187 (§6 documents hasClosedSection section-tag semantics for <process>/<success_criteria> — mirror this prose for the role tag)
    - test/doc-sync.test.js lines 25-65 (staticSegments array; line ~38 holds the old role error string; lines ~37, ~41-43 show the composed-string / non-interpolation-anchor pattern and the "segment must not span a ${...} boundary" rule)
    - README.md lines 100-116 (spine example block at ~108 and the rule description at ~114)
    - src/schema.js (the two new error strings authored in Task 1 — the doc and staticSegments must quote their exact non-interpolated literal anchors)
    - .planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-CONTEXT.md (D-08 two distinct strings; D-09 no legacy mention) and 17-CONTEXT.md provenance (D-02 exact strings in doc; D-03 doc-sync guards drift; D-04 no version header)
  </read_first>
  <behavior>
    - skill-schema.md §4 Check 2 describes the role tag matched via hasClosedSection semantics (fence-aware, line-anchored, open-before-close) PLUS the new non-empty requirement, and quotes BOTH new lint error strings; the "empty Role passes" caveat is gone (D-05) and there is no legacy/migration mention (D-09).
    - The §4/§6 example and the frontmatter example at ~line 305 show a <role> section instead of the legacy bold line.
    - test/doc-sync.test.js staticSegments no longer contains the old role string and contains two new literal anchors (`body must contain <` and `section must not be empty`), each chosen not to span a ${...} interpolation boundary.
    - README's spine example uses a <role> section and its rule sentence describes "a non-empty <role>…</role> section".
    - The full `node --test` suite passes — every migrated artifact, the linter, and the docs are now mutually consistent (this is the single atomic-commit gate).
  </behavior>
  <action>
    In shared/references/skill-schema.md §4, rewrite "Check 2 — Role line" as a role-section check: describe that the body must contain a `<role>…</role>` section matched by the same hasClosedSection semantics documented in §6 (line-start-anchored open/close tags, fenced code excluded, open index before close index), and that the section must additionally be non-empty (contain non-whitespace content). Replace the example so it shows a `<role>` block. DELETE the "The Role line content after `:` is not validated… An empty passes" caveat entirely (D-05 closes that gap). Replace the single "Lint error if missing" line with the two current lint errors, quoting each new string verbatim: the composed missing-role string (whose non-interpolated literal anchor is `body must contain <`) and the empty-role string (whose literal anchor is `section must not be empty`). Add no legacy or migration note (D-09). Also update the frontmatter example block near line 305 to show a `<role>` section instead of the legacy bold line.

    In test/doc-sync.test.js, remove the old role error string entry (line ~38) and add two new pure-literal staticSegments entries — `body must contain <` and `section must not be empty` — each a substring of the new error strings that does not cross a `${...}` boundary (mirroring how the template error uses `requires <` at line ~43). Keep the array's existing style and comments.

    In README.md, update the spine example block (~line 108) to show a `<role>` section, and change the rule sentence (~line 114) to read that the body contains a non-empty `<role>…</role>` section (wording consistent with skill-schema.md). Do not paste the full lint error strings into README prose (keep it a plain-English rule).

    This is the FINAL task of the atomic flip. After these edits, run the full suite. Everything must now be consistent: schema.js emits the two new strings (Task 1), skill-schema.md quotes them and staticSegments lists them (this task), and skills/fixtures use the section tag (Task 2), so dogfood and doc-sync both pass. This whole plan is committed as ONE commit.
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test` passes fully (all files, including test/dogfood.test.js, test/doc-sync.test.js, test/init-dogfood.test.js, and the four migrated fixture files).
    - `grep -Fc 'body must contain <' shared/references/skill-schema.md` returns ≥1 AND `grep -Fc 'section must not be empty' shared/references/skill-schema.md` returns ≥1.
    - `grep -Fc 'body must contain <' test/doc-sync.test.js` returns ≥1 AND `grep -Fc 'section must not be empty' test/doc-sync.test.js` returns ≥1.
    - `grep -Fc 'body must contain a ' test/doc-sync.test.js` returns 0 (old role staticSegment removed).
    - `grep -c '\*\*Role:' shared/references/skill-schema.md README.md` returns 0 (docs fully migrated; D-09 no legacy mention).
    - The doc-sync test's self-check passes, proving both new strings are live source text in src/schema.js AND quoted in skill-schema.md.
  </acceptance_criteria>
  <done>Docs and drift guard describe only the current section-tag validator with two error strings; README migrated; full suite green; plan lands as one atomic commit.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 18-02)

Changed behavior / symbols:
- `src/schema.js` — legacy `/^\*\*Role:/m` role check and its `body must contain a **Role:** line` error REMOVED; replaced by a `for (const s of BASE_SPINE)` loop that imports BASE_SPINE, threads it through `templatesRegistry`, and emits two distinct errors (D-08).

New error strings (become live source text, quoted in skill-schema.md, and listed in doc-sync staticSegments):
- Missing-role: `body must contain <${s}>…</${s}> — ${SEC[s] ?? ""}` (composes SECTIONS.role per D-03; literal anchor `body must contain <`).
- Empty-role:   `<${s}>…</${s}> section must not be empty — ${SEC[s] ?? ""}` (D-05; literal anchor `section must not be empty`).

Removed strings:
- `body must contain a **Role:** line` — deleted from src/schema.js, shared/references/skill-schema.md, and test/doc-sync.test.js staticSegments.

Migrated files (bold-line role convention → `<role>` section tag):
- skills/release/SKILL.md, skills/build-skill/SKILL.md (+ its two prose references), src/init.js (HELLO_WORLD starter), README.md, shared/references/skill-schema.md, and fixture bodies in test/schema.test.js, test/lint.test.js, test/build.test.js, test/frontmatter.test.js.

Deliberately NOT changed:
- src/frontmatter.js and its B8 alias-guard fixture (guards a YAML parsing property, not the spine rule).
- The hardcoded H1 Title check and its waives("title") escape (D-04).

Decision coverage: D-01 (hard break), D-02 (leftover lines inert), D-03 (registry-driven, from 18-01), D-04 (title untouched), D-05 (non-empty, helper from 18-01), D-06 (role-only helper from 18-01), D-07 (position/multiplicity via hasClosedSection semantics), D-08 (two distinct errors), D-09 (no legacy mention in doc).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md body content → validator | Untrusted skill-author text is scanned by the BASE_SPINE loop (hasClosedSection + hasNonEmptyClosedSection). |
| Registry data (BASE_SPINE / SECTIONS) → RegExp construction | Spine tag names are interpolated into `new RegExp` inside hasClosedSection. |
| src/schema.js error strings → skill-schema.md → doc-sync | The drift guard is the integrity link keeping documented errors equal to emitted errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering / DoS | never-throw boundary of the wired role check | medium | mitigate | validateSkill delegates to the Plan 18-01 helpers which coerce `body \|\| ""`; Task 1 keeps an adversarial null/non-string-body test asserting errors-returned-not-thrown. |
| T-18-05 | Repudiation / Integrity | doc-sync drift between emitted and documented error strings | medium | mitigate | Both new strings added to staticSegments AND skill-schema.md in the same commit; doc-sync self-check fails the suite if either drifts (the atomic-commit gate enforces this). |
| T-18-06 | Injection | spine tag name interpolated into new RegExp | low | accept | Tag names come only from the trusted BASE_SPINE registry (["role"]), never from raw data.template — T-Q-01 precedent. |
| T-18-07 | Tampering | unsafe/underscoped migration leaving a stale legacy fixture | low | mitigate | Hard break makes any missed clean-expecting fixture fail the suite (a signal, not a silent gap); the B8 exception is explicitly scoped and asserted unchanged. |
| T-18-SC | Tampering | dependency supply chain | low | accept | No package installs this phase; runtime deps unchanged (single `yaml` dep). No install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
- Single atomic commit: all three tasks staged together; no intermediate commit (husky would block a red intermediate state).
- `node --test` fully green: dogfood lints migrated skills/ clean; doc-sync self-check passes for both new strings; init-dogfood scaffolds and lints the migrated starter; all four fixture files pass under the new spine rule.
- Registry-driven: src/schema.js loops BASE_SPINE and composes the missing error from SECTIONS.role; Title check untouched (D-04).
- Hard break verified: a legacy-bold-line-only body now yields the missing-role error (D-01/D-02); an empty section yields the distinct empty error (D-05/D-08).
- No legacy/migration mention remains in skill-schema.md or README (D-09); the frontmatter B8 guard and src/frontmatter.js are the only deliberate legacy remnants.
</verification>

<success_criteria>
- `<role>…</role>` is the only accepted role declaration; the legacy bold-line convention no longer passes lint (D-01).
- Two distinct, registry-composed lint errors (missing + empty) are emitted, documented, and drift-guarded (D-03, D-05, D-08).
- Every live artifact, doc, and fixture migrated except the deliberately-retained B8 guard; full `node --test` green in one commit.
</success_criteria>

<output>
Create `.planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-02-SUMMARY.md` when done.
</output>
