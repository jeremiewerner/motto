---
phase: 18-migrate-base-spine-role-to-role-section-tag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates.js
  - src/schema.js
  - test/schema.test.js
autonomous: true
requirements: [D-03, D-05, D-06]
must_haves:
  truths:
    - "src/templates.js exports BASE_SPINE listing role, and SECTIONS.role holds a one-line human description"
    - "src/schema.js exports hasNonEmptyClosedSection: false for a whitespace-only or fenced-only <role>, true for non-empty content, boolean (never throws) for non-string / null / undefined body"
    - "The new helper is NOT yet wired into validateSkill — the legacy role check and all existing lint behavior are unchanged, so the full test suite stays green"
  artifacts:
    - "src/templates.js — BASE_SPINE export + role key in SECTIONS"
    - "src/schema.js — exported hasNonEmptyClosedSection(body, tagName)"
    - "test/schema.test.js — adversarial unit tests for hasNonEmptyClosedSection"
  key_links:
    - "hasNonEmptyClosedSection reuses hasClosedSection's fence-aware two-phase scan — it does not introduce a second regex scanner"
---

<objective>
Land the additive, behavior-neutral building blocks for the role-section-tag migration so the atomic flip (Plan 18-02) is a pure wiring change with the risky new validator path already proven against adversarial input.

This plan adds registry data (`BASE_SPINE`, `SECTIONS.role` — D-03) and a new isolated non-empty scanner (`hasNonEmptyClosedSection` — D-05, D-06) WITHOUT touching `validateSkill`'s spine check. Because the legacy role check is untouched, every existing skill, fixture, and doc still validates exactly as before, so this commit is independently green under husky's `npm test` gate.

Purpose: Isolate the new validator path (never-throw boundary property) into its own reviewable, green commit with mandatory adversarial coverage before the cross-cutting flip.
Output: `BASE_SPINE` export, `SECTIONS.role` description, exported `hasNonEmptyClosedSection`, and its adversarial test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-CONTEXT.md
@.planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BASE_SPINE export and role description to the template registry</name>
  <files>src/templates.js</files>
  <read_first>
    - src/templates.js (full file — the pure-data registry being extended; note the header comment's "pure data — no functions, no filesystem/YAML access, no imports" constraint and the "SECTIONS descriptions double as the schema doc source" note)
    - .planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-CONTEXT.md (D-03: registry-driven base spine)
    - .planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-PATTERNS.md (src/templates.js section — the exact shape to extend)
  </read_first>
  <behavior>
    - SECTIONS gains a `role` key whose value is a one-line human description of the role section.
    - A new `BASE_SPINE` array export exists, equal to ["role"].
    - TEMPLATES is unchanged — role is NOT added to procedure.requiredSections (role is a base-spine tag, not a template-required section — D-03).
    - The file remains pure data: no functions, no imports, no filesystem/YAML access.
  </behavior>
  <action>
    Per D-03, add `role` as a new key in the `SECTIONS` object with a concrete one-line description; use this exact target value unless a clearly better one-liner is warranted: "Behavioral instruction that tells the agent who it is and how to act." This description is consumed two ways downstream — quoted into the composed missing-role lint error (Plan 18-02) and transcribed into skill-schema.md — so keep it a single line with no XML tags and no interpolation braces.

    Add a new sibling export `BASE_SPINE` equal to the array containing the single string role. Place it next to `SECTIONS`/`TEMPLATES` and document it in the file header comment block as "spine tag-sections enforced on every skill body regardless of template". Do NOT modify `TEMPLATES` or add role to `procedure.requiredSections` — role is base-spine data, not a per-template requirement (D-04 keeps the title check separate and hardcoded; BASE_SPINE holds tag-sections only).

    Preserve the file's pure-data invariant: no functions, no imports, no fs/YAML.
  </action>
  <verify>
    <automated>node -e "import('./src/templates.js').then(m=>{const ok=Array.isArray(m.BASE_SPINE)&&m.BASE_SPINE.length===1&&m.BASE_SPINE[0]==='role'&&typeof m.SECTIONS.role==='string'&&m.SECTIONS.role.trim().length>0&&!m.TEMPLATES.procedure.requiredSections.includes('role');if(!ok)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./src/templates.js').then(m=>console.log(JSON.stringify(m.BASE_SPINE)))"` prints `["role"]`.
    - `SECTIONS.role` is a non-empty single-line string containing no `<` or `>` characters and no `${` sequence.
    - `procedure.requiredSections` still equals `["process","success_criteria"]` (role absent).
    - `grep -c "^import\|require(" src/templates.js` returns 0 (file stays import-free / pure data).
  </acceptance_criteria>
  <done>BASE_SPINE and SECTIONS.role exist as pure data; TEMPLATES unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add and adversarially test hasNonEmptyClosedSection (unwired)</name>
  <files>src/schema.js, test/schema.test.js</files>
  <read_first>
    - src/schema.js lines 43-101 (hasClosedSection — the fence-aware two-phase scanner to reuse; note it strips fenced lines and requires open-before-close)
    - src/schema.js lines 309-329 (current Title + legacy role checks — DO NOT modify them in this plan; they stay live so the suite stays green)
    - src/schema.js lines 1-15 (module header / Exports list — add the new export here)
    - test/schema.test.js (existing test structure and how hasClosedSection is currently exercised, to mirror describe/it style and the adversarial-input conventions)
    - .planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-CONTEXT.md (D-05 non-empty required; D-06 role-only via a small isolated helper; never-throw invariant note)
    - $HOME/.claude/projects/-Users-jeremie-Projects-motto/memory/motto-never-throw-invariant.md is not readable here — instead honor the STATE/CONTEXT note: never-throw is a boundary property; adversarial malformed-input tests are mandatory for any new validator path
  </read_first>
  <behavior>
    - hasNonEmptyClosedSection(body, "role") returns true when a matched, fence-aware, line-anchored <role>…</role> pair encloses at least one non-whitespace character on the unfenced lines strictly between the open and close tag.
    - Returns false when: the section is not closed at all; the section is closed but the enclosed unfenced text is empty or whitespace-only; the only content between the tags is inside a fenced code block (fenced lines are excluded from content, consistent with hasClosedSection).
    - Returns a boolean and NEVER throws for adversarial input: body = null, undefined, a number, an object, an array; tagName present but body malformed.
    - Existing lint behavior is unchanged: the helper is exported but NOT called from validateSkill in this plan.
  </behavior>
  <action>
    Add an exported function `hasNonEmptyClosedSection(body, tagName)` immediately after `hasClosedSection` in src/schema.js, and register it in the module header Exports list. Implement it by delegating to `hasClosedSection` first (D-06 keeps this isolated so later generalization to other sections is a one-liner): if `hasClosedSection(body, tagName)` is false, return false. Otherwise, reuse the SAME fence-aware unfenced-line collection strategy that `hasClosedSection` uses (coerce with `body || ""`, split on newlines, track the open-fence char/length, collect only unfenced non-marker lines), locate the first unfenced line matching the line-anchored open tag and the first following unfenced line matching the line-anchored close tag, and return whether the joined text strictly between them contains any non-whitespace character. Do not write a second independent regex scanner and do not modify `hasClosedSection`.

    Guard the never-throw boundary explicitly: coerce non-string `body` via `body || ""` before any string method, exactly as `hasClosedSection` does, so `null`/`undefined`/number/object/array inputs return a boolean rather than throwing (D-05 closes the empty-role gap but must not open a throw gap — the never-throw property is a hard invariant that has silently regressed three milestones running).

    Do NOT wire this helper into `validateSkill` in this plan. The legacy role check at lines ~322-329 stays exactly as-is so this commit remains green; Plan 18-02 performs the wiring and removal atomically with the artifact migration.

    In test/schema.test.js add a dedicated describe block for hasNonEmptyClosedSection with adversarial and boundary cases: non-empty closed section (true); closed-but-whitespace-only section (false); closed section whose only content is a fenced code block (false); unclosed / missing section (false); and a malformed-input group asserting a boolean is returned (never a throw) for body values null, undefined, 123, {}, and []. Use node:assert to assert both the boolean result and, for the malformed group, that calling the function does not throw.
  </action>
  <verify>
    <automated>node --test test/schema.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/schema.test.js` passes, including the new hasNonEmptyClosedSection describe block.
    - `grep -c "export function hasNonEmptyClosedSection" src/schema.js` returns 1.
    - The malformed-input tests assert no throw for body ∈ {null, undefined, 123, {}, []} and a boolean result each time.
    - `node --test` (full suite) is green — confirming validateSkill behavior is unchanged (helper is exported but unwired).
    - `grep -n "hasNonEmptyClosedSection" src/schema.js` shows the definition/export only, with no call site inside validateSkill (lines ~193-460).
  </acceptance_criteria>
  <done>hasNonEmptyClosedSection is exported, adversarially tested, never throws, and unused by validateSkill; full suite green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 18-01)

New exports / symbols:
- `BASE_SPINE` — array export in src/templates.js, value `["role"]` (D-03).
- `SECTIONS.role` — new key in the SECTIONS registry; one-line description string (D-03), target value "Behavioral instruction that tells the agent who it is and how to act."
- `hasNonEmptyClosedSection(body, tagName)` — new exported function in src/schema.js (D-05, D-06), boolean, never-throws, reuses hasClosedSection's fence-aware scan.

New tests:
- test/schema.test.js — a describe block for hasNonEmptyClosedSection (non-empty true; whitespace-only false; fenced-only false; unclosed false; malformed-input never-throw group).

No error strings, file-path moves, or behavior changes to validateSkill in this plan (those are Plan 18-02).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md body content → validator | Untrusted skill-author text is scanned by hasNonEmptyClosedSection (via hasClosedSection). |
| Registry data (BASE_SPINE / SECTIONS keys) → RegExp construction | Tag names are interpolated into `new RegExp` inside hasClosedSection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Denial of Service | hasNonEmptyClosedSection scan | low | mitigate | Reuse hasClosedSection's linear two-phase scan and anchored multiline regexes (no catastrophic backtracking, T-02-01 precedent); no new hot-loop regex. |
| T-18-02 | Tampering / DoS | never-throw boundary of the new validator path | medium | mitigate | Coerce non-string body via `body \|\| ""` before any string op; mandatory adversarial malformed-input tests (null/undefined/number/object/array) asserting boolean-returned-not-thrown. |
| T-18-03 | Injection | tagName interpolated into new RegExp | low | accept | tagName is sourced only from the trusted internal BASE_SPINE registry (["role"]), never from raw data.template — same T-Q-01 precedent as hasClosedSection. |
| T-18-SC | Tampering | dependency supply chain | low | accept | No package installs this phase; runtime deps unchanged (single `yaml` dep). No install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
- `node --test` is fully green (this commit is behavior-neutral: legacy role check still live, all fixtures/skills/docs unchanged).
- `hasNonEmptyClosedSection` and `BASE_SPINE` are importable; `SECTIONS.role` is a non-empty single-line string.
- No call to hasNonEmptyClosedSection exists inside validateSkill yet.
</verification>

<success_criteria>
- BASE_SPINE and SECTIONS.role added as pure data (D-03); TEMPLATES unchanged.
- hasNonEmptyClosedSection exported, isolated (D-06), non-empty semantics correct (D-05), never throws on adversarial input.
- Full test suite green — no existing behavior altered.
</success_criteria>

<output>
Create `.planning/phases/18-migrate-base-spine-role-to-role-section-tag/18-01-SUMMARY.md` when done.
</output>
