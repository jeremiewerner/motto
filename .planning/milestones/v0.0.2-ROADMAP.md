# v0.0.2 — Self-Hosting (Dogfood) — Roadmap Archive

**Shipped:** 2026-07-01 · **Closeout:** verified (3/3 phases passed, audit PASSED, hardened post-review)
**Core Value:** A strict schema + linter that guarantees authored skills conform before they ship, then packages them into self-contained standard Agent Skill plugins.

Prove the v0.0.1 `motto lint` + `motto build` pipeline by having Motto author and validate its *own* skills tree (full schema surface: public + private buckets + a shared reference), fix any gaps real content surfaces, and wire a permanent dogfood regression guard. Zero new dependencies.

## Stats

- **Phases:** 3 (04 Self-Hosted Tree · 05 Dogfood Guard · 06 Schema Strictness) + 2 quick tasks (skill-name standardization · code-review hardening)
- **Commits:** 43 (since v0.0.1) · **Tests:** 71 → **75** · **Deps:** `yaml` only (husky devDep)
- **Requirements:** 10/10 (SELF-01..05, DOG-01..04, STRICT-01)
- **PRs:** #1 (milestone) · #2 (post-review hardening)

## Phases

### Phase 4: Self-Hosted Skill Tree + Gap Fixes
**Goal:** Motto's own skills tree exists in the real source layout and passes the shipped lint + build pipeline clean.
**Requirements:** SELF-01..05, DOG-01, DOG-02 · **Mode:** mvp · **Depends on:** Phase 3
- `motto.yaml` (name/version/description/plugins.public/private) + 3 skills (2 public, 1 private) + shared ref `skill-schema`.
- `motto lint` exit 0 `✓ 3 skills OK`; `motto build` emits public+private buckets, bundled refs, per-bucket `plugin.json` with version.
- Gap fixes surfaced by real content applied to `src/` (frontmatter parser); 53 tests still green.
- Plan: 04-01. Verified 5/5.

### Phase 5: Dogfood Regression Guard
**Goal:** A permanent automated test guards the self-hosted tree on every commit; NAME_KEBAB sync debt closed.
**Requirements:** DOG-03, DOG-04 · **Mode:** mvp · **Depends on:** Phase 4
- `test/dogfood.test.js`: in-place lint of repo root (read-only, `import.meta.url`-anchored) + `mkdtemp` build assertion.
- NAME_KEBAB exported from `schema.js`, parity self-test vs `config.js` (DOG-04). Husky pre-commit runs the suite every commit.
- Plan: 05-01. Verified 4/4.

### Phase 6: Schema Strictness (STRICT-01)
**Goal:** `validateSkill` fully conforms to the CLAUDE.md name/description spec.
**Requirements:** STRICT-01 (added mid-milestone; closes Phase-4 research gap G1) · **Depends on:** Phase 5
- Added name ≤64, description ≤1024, description no-XML checks; spec-literal `>` boundary; never-throw model preserved.
- Plan: 06-01. Verified 5/5. Tests B14–B19.

## Quick Tasks

- **260630-uxc** — standardize skill names verb-first, no `motto-` prefix (`author-skill`, `setup-project`, `release`).
- **260630-vzh** — fix 10 findings from `/code-review high` of the merge, incl. 3 D-01 never-throw restorations (boolean name, frontmatter alias, config alias) + 4 malformed-input guard tests. Shipped as PR #2.

## Carried Debt / Deferred

- Phase 4 SUMMARY frontmatter omits `requirements_completed` (satisfaction confirmed via VERIFICATION).
- Intentional backlog: real `npm publish` flow (`release` skill stub), CLI ergonomics (`--quiet`/`--format json`/`--zip`), optional `[path]` arg, template mechanism (TMPL-01), distribution layer, CI (husky-only for now).

**Audits:** [milestone audit](../v0.0.2-MILESTONE-AUDIT.md) · [integration report](../v0.0.2-MILESTONE-INTEGRATION.md)
