---
phase: 12-docs-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - README.md
  - test/dogfood.test.js
  - skills/setup-project/SKILL.md
autonomous: true
requirements:
  - DOC-05
must_haves:
  truths:
    - "Every section of skills/setup-project/SKILL.md is mapped to a README home or a recorded conscious drop before deletion — no silent doc loss (D-10)"
    - "skills/setup-project/ no longer exists (DOC-05, D-09)"
    - "test/dogfood.test.js asserts lint count=2 and build skillCount=2 and contains no setup-project artifact assertions (D-09)"
    - "The deletion and the dogfood-test count edits land in ONE commit; full `node --test` and `motto lint` pass so main never goes red (ROADMAP SC3, D-09)"
  artifacts:
    - "test/dogfood.test.js (count assertions synced to 2, two setup-project it-blocks removed)"
  key_links:
    - "Delete + test-count edits must be staged and committed together — the pre-commit hook runs the full suite against the working tree, so a split commit produces a red intermediate the hook rejects (SC3)"
    - "shared/references/skill-schema.md stays bundled into author-skill after the delete — no shared-reference cleanup needed"
---

<objective>
Salvage-check the `setup-project` skill against the rewritten README, then delete `skills/setup-project/` and sync `test/dogfood.test.js` in a single atomic commit.

Purpose: the cleanup half of DOC-05. This is Commit 2 of the two-commit sequence locked by ROADMAP SC3 — it runs only after Plan 01's README rewrite has replaced the skill's content. The salvage-check (D-10) guarantees no documentation is lost; the atomic delete-plus-test-sync (D-09) guarantees `main` never goes red.

Output: `skills/setup-project/` removed; `test/dogfood.test.js` count assertions at 2 with the two setup-project artifact tests gone. No source code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-docs-cleanup/12-CONTEXT.md
@.planning/phases/12-docs-cleanup/12-PATTERNS.md
</context>

## Artifacts this phase produces

Plan 02 creates no new symbols. It removes artifacts:

- Deletes `skills/setup-project/SKILL.md` (and the `skills/setup-project/` directory).
- Removes two `it(...)` blocks from `test/dogfood.test.js` (the `dist/public/setup-project/SKILL.md exists` and `setup-project has references/skill-schema.md bundled` assertions).
- Edits two numeric assertions in `test/dogfood.test.js` (lint `count` 3→2, build `skillCount` 3→2).

May fold salvaged content into README.md if the salvage-check finds a gap (Task 1). No new source files, exports, or CLI surface.

<tasks>

<task type="auto">
  <name>Task 1: Salvage-check setup-project SKILL.md against the rewritten README</name>
  <files>README.md</files>
  <read_first>
    - skills/setup-project/SKILL.md (the content being retired — all 6 sections: Directory Layout, motto.yaml Fields, Running Lint, Running Build, What dist/ Contains, Installing the Built Plugin in Claude Code)
    - README.md (the rewritten doc from Plan 01 — the destination the content must be covered by)
    - .planning/phases/12-docs-cleanup/12-PATTERNS.md (the salvage disposition table under "Salvage-check against skills/setup-project/SKILL.md")
  </read_first>
  <action>
    Diff each section of skills/setup-project/SKILL.md against the rewritten README.md and record an explicit disposition per D-10 (no silent doc loss). Expected mapping (confirm each against the actual rewritten README):
    - Directory Layout → README §Scaffold a project tree (must be init's ACTUAL output: hello-world, not the SKILL.md's generic my-skill/another-skill).
    - motto.yaml Fields table → README existing field table.
    - Running Lint / Running Build / What dist/ Contains → README §Validate and build.
    - Installing the Built Plugin in Claude Code (the `claude plugin add` path) → superseded by the new §Ship your plugin flow (`/plugin marketplace add` + `/plugin install`).

    For any section NOT already covered, fold the missing content into the appropriate README section now (this is the only task permitted to touch README in Plan 02). For anything intentionally dropped (e.g. the older `claude plugin add` phrasing superseded by the marketplace flow), record the rationale. Record the salvage findings explicitly in this plan's SUMMARY (D-10; recording mechanism is Claude's discretion but the step must exist). Delete nothing in this task.
  </action>
  <verify>
    <automated>grep -q 'hello-world' README.md && grep -q '^## Ship your plugin' README.md && grep -q 'Validate and build' README.md && grep -q 'Field' README.md</automated>
  </verify>
  <done>
    Each of the 6 setup-project SKILL.md sections has a recorded disposition (covered-in-README / superseded-by-Ship-section / dropped-with-rationale); any gap has been folded into README; the salvage findings are recorded in the SUMMARY.
  </done>
  <acceptance_criteria>
    - The SUMMARY records a disposition line for each of the 6 SKILL.md sections (behavior: explicit salvage-check per D-10).
    - `grep -q 'hello-world' README.md` succeeds (Directory Layout salvaged into the init-accurate scaffold tree).
    - `grep -q '^## Ship your plugin' README.md` succeeds (Installing-the-Built-Plugin content superseded by the Ship section).
    - README retains a motto.yaml field table (behavior: motto.yaml Fields covered — grep for the field-table header).
    - No content from SKILL.md is lost without a recorded rationale (behavior: every dropped item has a note in the SUMMARY).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete skills/setup-project/ and sync test/dogfood.test.js in one atomic commit</name>
  <files>skills/setup-project/SKILL.md, test/dogfood.test.js</files>
  <read_first>
    - test/dogfood.test.js (lint count assertion ~42 `result.count`, build skillCount assertion ~86, the two setup-project it-blocks ~97-99 and ~106-108; and the untouched assertions — bucketCount ~87, author-skill ~92/102, release ~111-121, plugin.json ~124-145)
    - skills/setup-project/SKILL.md (the directory being removed)
    - .planning/phases/12-docs-cleanup/12-PATTERNS.md (the "count sync edit" section with exact line targets and the commit-sequencing constraint)
  </read_first>
  <action>
    Perform the delete and the test edits together and commit as ONE commit (ROADMAP SC3 / D-09). The repo's pre-commit hook runs the full suite against the working tree, so splitting the deletion from the test-count edits produces a red intermediate state the hook rejects — this is a hard ordering constraint, not a style preference.

    Steps in one working-tree change:
    1. Remove the skill directory: `git rm -r skills/setup-project/`.
    2. Edit test/dogfood.test.js:
       - Change the dogfood-lint count assertion from 3 to 2, including the message string (`result.count`, ~line 42): expected value and `expected N skills` message both become 2.
       - Change the build `skillCount` assertion from 3 to 2 (~line 86).
       - Remove the entire `it('dist/public/setup-project/SKILL.md exists', ...)` block (~lines 97-99).
       - Remove the entire `it('setup-project has references/skill-schema.md bundled', ...)` block (~lines 106-108).
    3. Leave everything else untouched: `bucketCount` stays 2 (public+private, unrelated to skill count), and the author-skill, release, and plugin.json assertions are not setup-project-related.

    Stage both the deletion and the test edits together, then create a single commit. Do not touch shared/references/skill-schema.md — it remains bundled into author-skill.
  </action>
  <verify>
    <automated>test ! -d skills/setup-project && node bin/motto.js lint 2>&1 | grep -q '2 skills OK' && node --test test/dogfood.test.js && node --test</automated>
  </verify>
  <done>
    `skills/setup-project/` is gone; test/dogfood.test.js asserts count=2 and skillCount=2 with the two setup-project artifact tests removed; `motto lint` reports 2 skills OK; the full `node --test` suite is green; the deletion and test edits are in a single commit.
  </done>
  <acceptance_criteria>
    - `test ! -d skills/setup-project` succeeds (directory deleted).
    - `node bin/motto.js lint 2>&1 | grep -q '2 skills OK'` succeeds (lint reports exactly 2 skills — proves the count=2 assertion is now truthful).
    - `node --test test/dogfood.test.js` exits 0 (proves the count/skillCount assertions were updated to 2 AND the two setup-project it-blocks were removed — a stale block would fail with ENOENT since setup-project no longer builds).
    - `node --test` (full suite) exits 0 (proves the pre-commit-hook working-tree state is green — main never red).
    - `git log --stat -1` shows both `skills/setup-project/SKILL.md` (deleted) and `test/dogfood.test.js` (modified) in the same single commit (behavior: SC3 atomic-commit constraint).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → main branch (pre-commit hook) | The delete + test edits must pass the full suite before the commit is accepted |

## STRIDE Threat Register

ASVS level 1; blocking threshold: high. No package-manager installs occur in this phase, so the supply-chain threat (T-12-SC) is not applicable.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Denial of Service (main breakage) | Split commit — delete without the dogfood-count sync fails the pre-commit full-suite | high | mitigate | Delete + test edits staged and committed as ONE commit; Task 2 verify runs the full `node --test` suite before commit (SC3 / D-09). |
| T-12-04 | Tampering | Over-deletion — removing shared/references/skill-schema.md or non-setup-project assertions | low | mitigate | Only `skills/setup-project/` is removed; shared/references/skill-schema.md stays and is verified still bundled into author-skill by the surviving dogfood assertion; author-skill / release / plugin.json assertions are left untouched. |
</threat_model>

<verification>
- `test ! -d skills/setup-project` (skill deleted)
- `node bin/motto.js lint 2>&1 | grep -q '2 skills OK'` (DOC-05 count truthful)
- `node --test` exits 0 (full suite green — SC3 main-never-red)
- `git log --stat -1` shows the deletion and test/dogfood.test.js edit in one commit (SC3 atomic constraint)
- Salvage findings recorded in the SUMMARY (D-10)
</verification>

<success_criteria>
- `skills/setup-project/` is deleted in the same commit that updates the dogfood-test count, and every intermediate state passes the full suite (ROADMAP SC3, DOC-05).
- No setup-project content is lost without an explicit recorded disposition (D-10).
- `shared/references/skill-schema.md` and the author-skill / release skills are unaffected.
</success_criteria>

<output>
Create `.planning/phases/12-docs-cleanup/12-02-SUMMARY.md` when done.
</output>
