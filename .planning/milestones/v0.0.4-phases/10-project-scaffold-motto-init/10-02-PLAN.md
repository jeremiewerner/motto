---
phase: 10-project-scaffold-motto-init
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - test/init.test.js
  - test/init-dogfood.test.js
autonomous: true
requirements: [INIT-02, INIT-04, INIT-05]

must_haves:
  truths:
    - "A freshly scaffolded project passes `motto lint` and `motto build` with zero edits, proven by a permanent init-dogfood test that runs scaffoldProject → lintProject → buildProject on every commit"
    - "The unit suite proves the empty-dir guard (allowlist + cap), invalid-name rejection + suggestion, --force overwrite-only-scaffold-paths, and adversarial-name rejection"
    - "The dogfood test asserts every scaffold artifact exists (including shared/references/.gitkeep) and that the scaffolded .gitignore keeps dist/public/ trackable while ignoring dist/private/"
  artifacts:
    - test/init.test.js
    - test/init-dogfood.test.js
  key_links:
    - "test/init-dogfood.test.js calls scaffoldProject(tempDir, {}) then lintProject(tempDir) then buildProject(tempDir) fail-fast in one before() hook"
    - "both test files import scaffoldProject from ../src/init.js (built in plan 10-01)"
---

<objective>
Add the two test files that lock down `motto init`: `test/init.test.js` (unit/integration coverage of the guards, name validation, --force, and template content) and `test/init-dogfood.test.js` (the permanent scaffold-dogfood guard that runs init → lint → build in a temp dir on every commit).

Purpose: INIT-02 requires the scaffolded starter skill to pass `motto lint`/`motto build` with zero edits, guarded permanently. This plan is that guard, plus adversarial unit coverage of INIT-04/INIT-05 behavior.
Output: `test/init.test.js`, `test/init-dogfood.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-project-scaffold-motto-init/10-RESEARCH.md
@.planning/phases/10-project-scaffold-motto-init/10-PATTERNS.md
@.planning/phases/10-project-scaffold-motto-init/10-CONTEXT.md
@.planning/phases/10-project-scaffold-motto-init/10-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces

New symbols created by this plan (exclude from source-drift verification — net-new here):

- `test/init.test.js` — new unit/integration test file for `scaffoldProject`
- `test/init-dogfood.test.js` — new permanent scaffold-dogfood guard (init → lint → build)

Consumes (built in plan 10-01, not new here): `scaffoldProject` from `src/init.js`; `lintProject` from `src/lint.js`; `buildProject` from `src/build.js`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create test/init.test.js — unit/integration coverage of scaffoldProject</name>
  <files>test/init.test.js</files>
  <read_first>
    - src/init.js (the module under test — scaffoldProject signature, return shape, and the helper behaviors from plan 10-01)
    - test/dogfood.test.js (import style, mkdtemp/rm temp-dir lifecycle, node:assert/strict assertion style, describe/it structure)
    - test/config.test.js (pure-function assertion style for a validator module)
    - .planning/phases/10-project-scaffold-motto-init/10-PATTERNS.md ("test/init.test.js" section — the assertion targets to cover)
  </read_first>
  <action>
Create `test/init.test.js` using `node:test` (`describe`/`it`/`before`/`after`) and `node:assert/strict`, mirroring `test/dogfood.test.js` imports. Test `scaffoldProject` directly against `mkdtemp`-created temp dirs (never the repo root); clean each up with `rm(dir, { recursive: true, force: true })`. Cover:

1. Happy path: on an empty temp dir, `scaffoldProject(dir, { name: 'hello-proj' })` returns `ok: true` and all six paths exist via `stat` (skills/hello-world/SKILL.md, shared/references/.gitkeep, motto.yaml, .gitignore, .claude-plugin/marketplace.json — the 5 SC1 artifacts plus the .gitkeep).

2. Default name: `scaffoldProject(dir, {})` on a temp dir whose basename is a valid kebab name returns `ok: true` and the emitted motto.yaml `name`/`plugins.public` equal that basename (INIT-01 default-to-cwd-basename).

3. Invalid name (INIT-05, D-08): `scaffoldProject(dir, { name: 'My Project' })` returns `ok: false`, `reason: 'invalid-name'`, `suggestion: 'my-project'`, and NO files were written (assert the target dir is still empty).

4. Adversarial names (Pitfall 1 / T-10-01): names containing `:`, `"`, `../`, and a leading digit each return `ok: false` reason `invalid-name` and write nothing — proves validation gates before interpolation and blocks path-traversal characters.

5. Non-empty guard (INIT-04, D-05): with an unrelated `foo.txt` present, `scaffoldProject(dir, {})` returns `ok: false`, `reason: 'not-empty'`, `offending` containing `foo.txt`. Separately, a dir containing only `.git` and/or `.DS_Store` returns `ok: true` (allowlist).

6. Offending-list cap (D-07): with 7 unrelated entries present, `offending` is deterministically sorted and the CLI-cap contract holds (assert `offending.length === 7` and it is sorted; the 5-item display cap is exercised at the CLI layer in 10-01, so here assert the raw sorted list the CLI formats from).

7. `--force` (INIT-04, D-06): with an unrelated `keep.txt` present, `scaffoldProject(dir, { name: 'hello-proj', force: true })` returns `ok: true`, the scaffold files exist, AND `keep.txt` still exists (force overwrites scaffold paths only, deletes nothing).

8. Content shape: read the scaffolded `.gitignore` and assert its body contains `dist/private/` and does NOT contain a standalone `dist/` line (INIT-06). Read the scaffolded `marketplace.json`, `JSON.parse` it, and assert `plugins[0].source === './dist/public/'`, `name` equals the effective name, `plugins[0].name` equals the effective name, and there is no `owner.email` key (INIT-03). Read `motto.yaml` and assert it has no `plugins.private` line.

Use `assert.strictEqual` / `assert.deepStrictEqual` / `assert.rejects` matching the dogfood test style. Fail-fast messages should carry the offending JSON so a red test is legible.
  </action>
  <verify>
    <automated>node --test test/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/init.test.js imports `scaffoldProject` from `../src/init.js`
    - `node --test test/init.test.js` exits 0 with all assertions passing
    - Test suite includes an assertion that name 'My Project' yields reason 'invalid-name' and suggestion 'my-project' with nothing written
    - Test suite includes an adversarial-name case with a `../`-containing name that returns ok:false and writes nothing
    - Test suite includes a `--force` case asserting an unrelated pre-existing file survives the scaffold
    - Test suite asserts the scaffolded marketplace.json `plugins[0].source === './dist/public/'` and has no `owner.email`
    - Test suite asserts the scaffolded .gitignore contains `dist/private/` and no standalone `dist/` line
  </acceptance_criteria>
  <done>test/init.test.js green: happy path, default-name, invalid-name+suggestion, adversarial names, non-empty guard + allowlist, offending sort, --force overwrite-only, and template content shape all asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Create test/init-dogfood.test.js — permanent init → lint → build guard</name>
  <files>test/init-dogfood.test.js</files>
  <read_first>
    - test/dogfood.test.js (whole file — the mkdtemp/before/after/fail-fast temp-dir lifecycle and per-artifact `stat` assertion pattern to mirror exactly)
    - src/init.js (scaffoldProject — the entry the guard calls first)
    - src/lint.js (lintProject signature and `{ ok, count, errors }` return shape)
    - src/build.js (buildProject signature and `{ ok, skillCount, bucketCount, outDir, errors }` return shape)
    - .planning/phases/10-project-scaffold-motto-init/10-PATTERNS.md ("test/init-dogfood.test.js" section)
    - .planning/phases/10-project-scaffold-motto-init/10-RESEARCH.md (Open Question 2 — the guard must assert artifacts including .gitkeep, not only lint/build success)
  </read_first>
  <action>
Create `test/init-dogfood.test.js` mirroring `test/dogfood.test.js`'s temp-dir lifecycle. In a single `before()` hook: `tempDir = await mkdtemp(join(tmpdir(), 'motto-init-dogfood-'))`, then `scaffoldResult = await scaffoldProject(tempDir, { name: 'sample-project' })`, then `lintResult = await lintProject(tempDir)`, then `buildResult = await buildProject(tempDir)`. After each step, if not `ok`, `throw new Error(...)` carrying the errors JSON — collapsing the cascade into one legible hook failure (matching the existing dogfood comment convention). `after()`: `if (tempDir) await rm(tempDir, { recursive: true, force: true })`.

Per-`it()` assertions (INIT-02, Open Question 2 — assert BOTH lint/build success AND the full artifact set):
- `scaffoldResult.ok === true`, `lintResult.ok === true` with `count === 1` (one starter skill), `buildResult.ok === true` with `skillCount === 1` and `bucketCount === 1` (public only — scaffold has no private skills).
- `stat` each source scaffold artifact exists: skills/hello-world/SKILL.md, shared/references/.gitkeep (explicit — lint/build ignore it, so only an explicit stat catches a missing .gitkeep per Pitfall 5), motto.yaml, .gitignore, .claude-plugin/marketplace.json.
- `stat` the built output exists: dist/public/hello-world/SKILL.md (proves build produced the plugin from the scaffold with zero edits).
- Read the scaffolded .gitignore and assert it contains `dist/private/` and no standalone `dist/` line (INIT-06 — the ship-flow contract that dist/public/ stays committable).

Do NOT copy the repo tree into tempDir (unlike dogfood.test.js) — the whole point is that scaffoldProject alone produces a buildable project from nothing. Use only `tmpdir()`.
  </action>
  <verify>
    <automated>node --test test/init-dogfood.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/init-dogfood.test.js imports scaffoldProject, lintProject, and buildProject from ../src/
    - `node --test test/init-dogfood.test.js` exits 0
    - The before() hook runs scaffoldProject → lintProject → buildProject and throws with the errors JSON if any step is not ok
    - An assertion checks lintProject count === 1 and buildProject skillCount === 1, bucketCount === 1
    - An explicit `stat` assertion checks shared/references/.gitkeep exists (not merely lint/build success)
    - An assertion checks dist/public/hello-world/SKILL.md exists after build
    - The test does NOT copy the repo tree into the temp dir (uses only tmpdir())
    - `node --test` (full suite) still exits 0 — no regressions to the existing 75 tests
  </acceptance_criteria>
  <done>test/init-dogfood.test.js is a permanent guard: scaffoldProject → lintProject → buildProject all return ok on a from-nothing temp dir, every scaffold artifact (including .gitkeep) and the built dist/public/hello-world plugin are asserted present, and the whole suite runs on every `node --test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → src/init.js | Tests drive scaffoldProject with adversarial `[name]` inputs to prove the plan 10-01 mitigations hold; no new runtime trust boundary is introduced by the test layer |
| test harness → filesystem (mkdtemp temp dirs) | All writes are confined to `tmpdir()` scratch dirs, cleaned up in `after()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering | Regression: a future edit weakens the NAME_KEBAB gate or empty-dir guard | high | mitigate | test/init.test.js's adversarial-name and non-empty-guard cases fail loudly if the plan 10-01 mitigations (T-10-01/03/04) regress — the test layer is the durable enforcement of those mitigations |
| T-10-06 | Tampering | Regression: scaffold output stops linting/building cleanly | high | mitigate | test/init-dogfood.test.js runs init → lint → build on every commit; any drift that makes the starter skill invalid turns the suite red before merge (INIT-02) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed this phase — stdlib + first-party modules only (RESEARCH Package Legitimacy Audit: empty) |
</threat_model>

<verification>
- `node --test test/init.test.js` and `node --test test/init-dogfood.test.js` both exit 0.
- Full suite green: `node --test` (existing 75 tests + the new init tests) exits 0.
- The init-dogfood guard proves INIT-02 end-to-end: a from-nothing temp dir scaffolds, lints, and builds with zero edits.
</verification>

<success_criteria>
- Scaffolded starter skill passes `motto lint` and `motto build` with zero edits, proven by a permanent scaffold-dogfood test (INIT-02).
- Unit tests prove the non-empty guard + allowlist + cap and `--force` overwrite-only semantics (INIT-04).
- Unit tests prove invalid-name rejection with the schema rule + sanitized suggestion, including adversarial path/YAML/JSON-breaking inputs (INIT-05).
- Scaffolded `.gitignore` (dist/private/ ignored, dist/public/ tracked) and marketplace.json (`./dist/public/` relative source, name-by-construction) are asserted in both test files.
</success_criteria>

<output>
Create `.planning/phases/10-project-scaffold-motto-init/10-02-SUMMARY.md` when done.
</output>
