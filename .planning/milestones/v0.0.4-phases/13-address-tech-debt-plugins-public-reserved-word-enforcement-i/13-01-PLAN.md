---
phase: 13-address-tech-debt-plugins-public-reserved-word-enforcement-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/schema.js
  - src/init.js
  - test/init.test.js
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-05]
must_haves:
  truths:
    - "README's motto.yaml field table states the plugins.public/plugins.private name rule as kebab-case only, with no reserved-word restriction (matching Claude Code's plugin.json spec) — DEBT-01"
    - "The src/schema.js RESERVED JSDoc scopes the reserved-word rule to SKILL.md name frontmatter only and forbids reuse for plugins.public/plugins.private — DEBT-01"
    - "A corrupt ~/.gitconfig no longer leaks raw git stderr to the user's terminal during motto init — DEBT-02"
    - "The adversarial-name test fails if a scaffold write ever escapes the target directory into the parent scratch dir — DEBT-03"
    - "No RESERVED-style check is added to plugins.public/plugins.private validation — src/config.js remains unchanged this phase — DEBT-01"
    - "bin/motto.js has zero process.exit() call sites (verified, already resolved in Phase 11 commit d35aba7) — DEBT-05"
  artifacts:
    - "README.md — corrected plugins.public/plugins.private field-table rows"
    - "src/schema.js — corrected RESERVED JSDoc scope"
    - "src/init.js — execFileSync in resolveGitOwnerName with stdio stderr suppression"
    - "test/init.test.js — adversarial-names block asserting the parent scratch dir, not just the target dir"
  key_links:
    - "resolveGitOwnerName's stdio option connects the never-throw/silent-fallback contract to silent stderr handling"
    - "The adversarial test asserts the parent scratch dir contents (only the empty target) to catch path-escape regressions the target-only assertion would miss"
---

<objective>
Close four of the five v0.0.4 tech-debt items with mechanical, low-risk edits, and verify-close the fifth:
- DEBT-01: correct the false "reserved-word" claim for `plugins.public`/`plugins.private` in README.md and the stale scope in the `src/schema.js` RESERVED JSDoc. This is a DOCUMENTATION bug — do NOT add a RESERVED check to `src/config.js` or `src/init.js`.
- DEBT-02: suppress git stderr leakage in `resolveGitOwnerName` (`src/init.js`) via an explicit `stdio` option, preserving the never-throw/silent-fallback contract.
- DEBT-03: strengthen the `../evil` adversarial-name path-escape regression test to assert the parent directory, mirroring the existing default-name suite's scratch/target nesting.
- DEBT-05: verify and close the already-resolved `process.exit()`-in-parseArgs item (fixed Phase 11, commit d35aba7) — no code change.

Purpose: Retire audited tech debt without introducing unwarranted validation strictness (the central finding of 13-RESEARCH.md: over-strictness on plugin names would reject spec-conformant names like `claude-notes-sync`).
Output: Corrected docs/comments, a one-line stdio hardening, a strengthened regression test, and a closed verification item — all edits to existing files, no new symbols.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-RESEARCH.md
@.planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-PATTERNS.md
</context>

## Artifacts this phase produces

No new symbols. Every change edits an existing artifact:
- `README.md` — prose edits to two existing field-table rows (`plugins.public`, `plugins.private`).
- `src/schema.js` — JSDoc-comment edit above the existing `RESERVED` constant (the constant value and all its usages are unchanged).
- `src/init.js` — an added `stdio` property on the existing `execFileSync` call inside the existing `resolveGitOwnerName` function.
- `test/init.test.js` — restructured setup/assertions inside the existing `scaffoldProject adversarial names` describe block.
- Tracking IDs introduced this phase: DEBT-01, DEBT-02, DEBT-03, DEBT-05 (DEBT-04 lives in plan 13-02). These are planning identifiers, not code symbols.

<tasks>

<task type="auto">
  <name>Task 1: Correct the false plugins.public/private reserved-word claim (DEBT-01)</name>
  <files>README.md, src/schema.js</files>
  <read_first>
    - README.md (lines 80-85: the motto.yaml field table, including the `plugins.public` row at line 83 and `plugins.private` row at line 84)
    - src/schema.js (lines 33-95: the RESERVED JSDoc at lines 35-38, the `RESERVED` constant at line 39, and its only usages inside validateSkill's NAME cascade at lines 81 and 92 — confirm these stay untouched)
    - .planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-RESEARCH.md (Pattern 2 and Code Examples Fix 1 + Fix 2 — the exact target prose and the citing source)
    - .planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-PATTERNS.md (README Fix A + src/schema.js pattern-assignment sections)
  </read_first>
  <action>
    Fix the documentation-only DEBT-01 defect in two places. This is a doc/comment correction; per 13-RESEARCH.md the `plugin.json` `name` field has NO reserved-word restriction (only SKILL.md frontmatter `name` does), so NO validation code changes anywhere.

    In README.md, rewrite the `plugins.public` row (line 83) so its Rule column reads exactly: `Letter-start kebab-case (Claude Code plugin \`name\` requirement — no reserved-word restriction, unlike skill \`name\`)`. Leave the `plugins.private` row (line 84) rule as `Same kebab-case rule`. Match the surrounding table column style (terse, no trailing period, no inline footnote/source link — this README has no citation-link convention).

    In src/schema.js, replace the RESERVED JSDoc block (currently a single line scoping the rule to "skill or plugin names") with a multi-line JSDoc that scopes it to the SKILL.md `name` frontmatter field only, keeps the `(LINT-02, D-09)` decision-ID citation, states that Claude Code's plugin.json `name` field has no reserved-word restriction (cite `code.claude.com/docs/en/plugins-reference`, 2026-07-02), instructs not to reuse RESERVED for plugins.public/plugins.private, and retains the `@type {string[]}` tag. Match the multi-line-explanation + parenthetical-citation style used by the NAME_KEBAB JSDoc immediately above (schema.js lines 13-32). The `RESERVED` constant value `["anthropic", "claude"]` and its two usages at lines 81 and 92 must remain byte-identical.

    Do NOT import RESERVED into src/config.js or src/init.js and do NOT add any reserved-word check to plugins.public/plugins.private — this is the explicit anti-pattern the phase must avoid.
  </action>
  <verify>
    <automated>node --test 2>&1 | tail -5 && grep -q 'no reserved-word restriction' README.md && grep -q 'no reserved-word restriction' src/schema.js && ! git diff --name-only | grep -q 'src/config.js'</automated>
  </verify>
  <acceptance_criteria>
    - README.md line 83 (`plugins.public` row) Rule column contains the substring `no reserved-word restriction` and the substring `Letter-start kebab-case`.
    - `grep -c 'no reserved-word restriction' README.md` returns 1.
    - src/schema.js RESERVED JSDoc contains the substring `no reserved-word restriction` and a reference to the SKILL.md `name` frontmatter scope and the citation `code.claude.com/docs/en/plugins-reference`.
    - `grep -c 'const RESERVED = \["anthropic", "claude"\];' src/schema.js` returns 1 (constant value unchanged).
    - `git diff --name-only` does NOT list `src/config.js` (config validation untouched — no reserved-word check added).
    - Full suite `node --test` passes with the same 131 passing tests as before the phase (no test count change from these edits).
  </acceptance_criteria>
  <done>README and the schema.js JSDoc both describe the reserved-word rule as scoped to SKILL.md names only; plugins.public/private are documented as kebab-case with no reserved-word restriction; src/config.js and src/init.js contain no RESERVED check; the suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Suppress git stderr leakage in resolveGitOwnerName (DEBT-02)</name>
  <files>src/init.js</files>
  <read_first>
    - src/init.js (lines 26 import of execFileSync; lines 92-99 the resolveGitOwnerName function; the file's top-of-file never-throw/silent-fallback house-style comment)
    - .planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-RESEARCH.md (Pitfall 2 + Code Examples Fix 3)
    - .planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-PATTERNS.md (src/init.js pattern-assignment section)
  </read_first>
  <action>
    Fix DEBT-02: the `execFileSync('git', ['config', 'user.name'], ...)` call at src/init.js line 94 uses only `{ encoding: 'utf8' }`, so a corrupt `~/.gitconfig` prints raw git error text (e.g. `fatal: bad config line ...`) to the user's terminal, violating the function's never-throw/silent-fallback contract. Add a `stdio` property to the existing options object with the value `['ignore', 'pipe', 'ignore']` (ignore stdin, pipe stdout so the captured value is still returned, ignore stderr). Keep `encoding: 'utf8'`. Do NOT change the enclosing `try { ... } catch { return 'Your Name'; }` control flow, the `.trim()`, or the `name || 'Your Name'` fallback — the fix only redirects the child's stderr, it does not alter the never-throw behavior. This is a fixed-argv `execFileSync` (not `exec`/`execSync` with a string), so the command-injection-safe property is preserved unchanged.
  </action>
  <verify>
    <automated>node --test test/init.test.js 2>&1 | tail -5 && grep -q "stdio: \['ignore', 'pipe', 'ignore'\]" src/init.js</automated>
  </verify>
  <acceptance_criteria>
    - src/init.js `resolveGitOwnerName` passes `stdio: ['ignore', 'pipe', 'ignore']` to `execFileSync` alongside `encoding: 'utf8'`.
    - The `try/catch` in `resolveGitOwnerName` is unchanged: the catch still returns `'Your Name'` and the happy path still returns `name || 'Your Name'`.
    - `node --test test/init.test.js` passes (init behavior, including default-name owner resolution, unchanged).
    - No new throw path is introduced (the function still returns `'Your Name'` rather than propagating any git failure).
  </acceptance_criteria>
  <done>resolveGitOwnerName suppresses git's stderr via an explicit stdio option while preserving the never-throw/silent-fallback contract; init tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen the adversarial-name path-escape regression test (DEBT-03)</name>
  <files>test/init.test.js</files>
  <read_first>
    - test/init.test.js (lines 48-80: the `scaffoldProject default name (INIT-01)` suite — the scratchDir/tempDir nesting pattern to mirror; lines 107-140: the `scaffoldProject adversarial names` block to restructure, including the `../evil` case at line 111)
    - .planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-RESEARCH.md (Pitfall 3 + Code Examples Fix 4)
    - .planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-PATTERNS.md (test/init.test.js pattern-assignment section — the exact analog and imports already present)
  </read_first>
  <action>
    Fix DEBT-03: the adversarial-names block (lines 107-140) currently creates `tempDir` directly under `tmpdir()` via `mkdtemp` and only asserts `readdir(tempDir)` is empty. That would still pass if a regression made a scaffold write escape one level up (e.g. `../evil` landing at `tmpdir()/evil`), because the test never inspects the parent. Restructure each per-name `describe` to mirror the default-name suite's nesting (lines 54-66): in `before`, create `scratchDir` with `mkdtemp(join(tmpdir(), 'motto-init-test-'))`, set `tempDir = join(scratchDir, 'target')`, `await mkdir(tempDir)`, then call `scaffoldProject(tempDir, { name })`; in `after`, `rm` the `scratchDir` recursively. Keep the existing `it('returns ok:false, reason:invalid-name', ...)` assertion. Replace/extend the "writes nothing" assertion so it reads the PARENT: assert `readdir(scratchDir)` deep-equals `['target']` (no writes escaped the target dir) AND `readdir(tempDir)` deep-equals `[]` (target itself untouched). All needed imports (`mkdtemp`, `rm`, `readdir`, `mkdir`, `join`, `tmpdir`) are already present at the top of the file — add none. Do not change the `adversarialNames` array contents.
  </action>
  <verify>
    <automated>node --test test/init.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The `scaffoldProject adversarial names` block nests `tempDir` as `join(scratchDir, 'target')` under a `scratchDir` created by `mkdtemp`, matching the default-name suite's pattern.
    - For every adversarial name (including `../evil`), the test asserts `readdir(scratchDir)` deep-equals `['target']` — proving no scaffold write escaped the target directory.
    - The test still asserts `result.ok === false` and `result.reason === 'invalid-name'` for each adversarial name.
    - `node --test test/init.test.js` passes; the adversarial suite's assertion now inspects the parent scratch dir, not only the target.
    - No new imports were added to test/init.test.js (all required fs/path/os helpers already imported).
  </acceptance_criteria>
  <done>The adversarial-name suite asserts the parent scratch dir contains only the empty target, so a future path-escape regression (write outside the target) fails the test; init tests stay green.</done>
</task>

<task type="auto">
  <name>Task 4: Verify-and-close the process.exit()-in-parseArgs item (DEBT-05)</name>
  <files>bin/motto.js</files>
  <read_first>
    - bin/motto.js (confirm every exit path uses `process.exitCode`, not `process.exit(`)
    - .planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-RESEARCH.md (Pitfall 5 — this item was already resolved by Phase 11 commit d35aba7; no code change required)
  </read_first>
  <action>
    Close DEBT-05 with a verification only — NO code change. The v0.0.4 milestone audit tracked a `process.exit()` call in bin/motto.js's parseArgs catch block as open debt, but per 13-RESEARCH.md Pitfall 5 this was already fixed when Phase 11 rewrote the CLI dispatch (commit d35aba7, "fix(11): WR-04 drop forced process.exit in parseArgs catch"). Current bin/motto.js uses `process.exitCode` on every exit path and has zero `process.exit(` call sites.

    Run the grep confirmation below and record the exact output in the SUMMARY as the DEBT-05 closure evidence. Do NOT edit bin/motto.js — if the grep returns any match, STOP and report it as a real finding rather than modifying the file blindly, since that would contradict the research premise and require re-scoping.
  </action>
  <verify>
    <automated>test "$(grep -c 'process\.exit(' bin/motto.js)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'process\.exit(' bin/motto.js` returns 0 (no forced-exit call sites remain).
    - bin/motto.js is NOT modified by this task — `git diff --name-only` for this plan does not add bin/motto.js.
    - The SUMMARY records the grep output (`0`) as DEBT-05 closure evidence, referencing Phase 11 commit d35aba7 as the actual fix.
  </acceptance_criteria>
  <done>DEBT-05 confirmed already-resolved: bin/motto.js has zero process.exit() call sites; the closure is documented in the SUMMARY with grep evidence and the d35aba7 commit reference; no code change made this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child process → CLI (git stderr) | `resolveGitOwnerName` spawns `git config user.name`; git's stderr can carry attacker-influenced or corrupt `~/.gitconfig` text into the parent's terminal |
| user-supplied project name → filesystem path | `motto init <name>` accepts a name that must never be used as a path segment that escapes the target directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Information Disclosure | resolveGitOwnerName execFileSync (src/init.js) | low | mitigate | Task 2 adds `stdio: ['ignore','pipe','ignore']` so a corrupt/hostile `~/.gitconfig`'s raw git error text is not echoed to the user's terminal outside Motto's own reporting format |
| T-13-02 | Tampering (path traversal) | scaffoldProject name→path (src/init.js) + regression guard (test/init.test.js) | low | mitigate | No live vuln today (name is used only as YAML/JSON content, never a path segment); Task 3 strengthens the regression test to assert the parent dir so any future regression that writes outside the target is caught |
| T-13-03 | Denial of Service (false-positive over-rejection) | plugins.public/private name validation (src/config.js) | low | accept | Do NOT add a RESERVED check to plugins.public/private — over-strict rejection of spec-conformant names (e.g. `claude-notes-sync`) is the defect to avoid; documented in Task 1 and 13-RESEARCH.md Pitfall 1 |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase (zero new dependencies); nothing to audit — supply-chain surface unchanged |
</threat_model>

<verification>
- `node --test` passes with the full pre-phase suite (131 tests) — no test count regression from Tasks 1-2; Task 3 restructures existing tests without adding/removing cases.
- `node bin/motto.js lint` (or the repo's lint script) runs clean against the repo's own skills tree — no schema behavior changed.
- DEBT-05 (verify-and-close, no code change): confirm bin/motto.js has zero `process.exit(` call sites — `grep -c 'process\.exit(' bin/motto.js` returns 0 (all exit paths use `process.exitCode`; already fixed Phase 11 commit d35aba7). Record this closure in the SUMMARY.
- `git diff --name-only` for this plan lists only README.md, src/schema.js, src/init.js, test/init.test.js — src/config.js is NOT listed (no reserved-word check added).
</verification>

<success_criteria>
- DEBT-01: README and src/schema.js JSDoc both scope the reserved-word rule to SKILL.md names only; plugins.public/private documented as kebab-case with no reserved-word restriction; no validation code added.
- DEBT-02: resolveGitOwnerName no longer leaks git stderr; never-throw contract intact.
- DEBT-03: adversarial-name test asserts the parent scratch dir; a path-escape regression would fail it.
- DEBT-05: bin/motto.js confirmed to have zero process.exit() call sites; item closed in SUMMARY as already-resolved.
- Full test suite and lint remain green.
</success_criteria>

<output>
Create `.planning/phases/13-address-tech-debt-plugins-public-reserved-word-enforcement-i/13-01-SUMMARY.md` when done. Record the DEBT-05 verification result (grep output) explicitly so the phase can close it without a separate code change.
</output>
