# Requirements Archive: v0.0.2 — Self-Hosting (Dogfood)

**Shipped:** 2026-07-01 · **Coverage:** 10/10 satisfied + verified
**Core Value:** A strict schema + linter that guarantees authored skills conform before they ship, then packages them into self-contained standard Agent Skill plugins.

## Self-Hosted Skill Tree (SELF)

- [x] **SELF-01**: Root `motto.yaml` declares project identity — name, version, description, plugins.public, plugins.private. — *validated (Phase 4)*
- [x] **SELF-02**: Public skill `author-skill` documents writing a conforming SKILL.md. — *validated (Phase 4; renamed from `authoring-a-skill` via quick 260630-uxc)*
- [x] **SELF-03**: Public skill `setup-project` documents initializing a Motto project. — *validated (Phase 4; renamed from `motto-project-setup`)*
- [x] **SELF-04**: Private skill `release` provides the maintainer release checklist. — *validated (Phase 4; renamed from `motto-release`; npm-publish step is an intentional stub)*
- [x] **SELF-05**: Shared reference `skill-schema` declared via `shared_references` and bundled into each public skill's `references/` at build. — *validated (Phase 4)*

## Dogfood + Regression Guard (DOG)

- [x] **DOG-01**: `motto lint` reports the tree clean — exit 0, `✓ 3 skills OK`. — *validated (Phase 4)*
- [x] **DOG-02**: `motto build` emits expected `dist/` — public+private buckets, verbatim skills, bundled refs, per-bucket `plugin.json` with version. — *validated (Phase 4)*
- [x] **DOG-03**: `node:test` dogfood test — in-place lint + `mkdtemp` build — on every commit via husky. — *validated (Phase 5)*
- [x] **DOG-04**: Self-test enforces NAME_KEBAB identical between `schema.js` and `config.js`. — *validated (Phase 5); hardened (quick 260630-vzh): config now imports the single source, parity test upgraded to identity assertion*

## Schema Strictness (STRICT) — added mid-milestone (Phase 6)

- [x] **STRICT-01**: `validateSkill` enforces the full CLAUDE.md name/description spec — name ≤64, description ≤1024, no XML tags — closing Phase-4 research gap G1; never-throw `errors[]` model preserved. — *validated (Phase 6); XML matcher tightened + never-throw holes closed (quick 260630-vzh)*

## Outcomes / Adjustments

- **Renames:** the three skills were renamed to verb-first, no-`motto-` prefix after Phase 6 (quick 260630-uxc). Names above are the shipped names.
- **Post-ship hardening (quick 260630-vzh):** `/code-review high` surfaced 10 findings incl. 3 D-01 never-throw violations (boolean name, frontmatter/config unresolved-alias throws) that the milestone tests missed. All fixed + guarded; 71→75 tests.

## Deferred to future milestones

- Real `npm publish` flow (packaging decision open); CLI ergonomics (`--quiet`, `--format json`, `--zip`); optional `[path]` arg for lint/build; template mechanism validation (TMPL-01); distribution layer; CI workflow (husky-only this milestone).

**Traceability:** SELF-01..05 + DOG-01..02 → Phase 4 · DOG-03..04 → Phase 5 · STRICT-01 → Phase 6.
