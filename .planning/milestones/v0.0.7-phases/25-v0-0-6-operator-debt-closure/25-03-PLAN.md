---
phase: 25-v0-0-6-operator-debt-closure
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - README.md
  - .planning/phases/25-v0-0-6-operator-debt-closure/25-REWALK.md
autonomous: false
requirements: [DEBT-06]
gap_closure: true
must_haves:
  truths:
    - "README's plugin-cache caveat leads with the installed-plugin refresh command that actually re-pulls tarball content (claude plugin update motto@motto), so a stranger who hits a stale cache follows a working instruction first (DEBT-06, closes 25-REVIEW.md WR-01)"
    - "The caveat keeps a full uninstall+reinstall as the documented fallback and states the restart-required nuance, and no longer presents the marketplace-add command as the primary refresh mechanism (WR-01)"
    - "The reworded guidance has been exercised against a real stale-cache scenario (not a clean install) and the observation is recorded, closing the grep-passes-substance-unverified gap (25-REVIEW.md IN-01)"
  artifacts:
    - "README.md marketplace install section caveat, reworded to lead with the installed-plugin update command"
    - ".planning/phases/25-v0-0-6-operator-debt-closure/25-REWALK.md with a stale-cache validation addendum"
  key_links:
    - "the README caveat's primary command must be the plugin-content refresh command (claude plugin update <plugin>), not the marketplace-manifest command, because the marketplace source is an unpinned npm package whose manifest is byte-identical across releases (WR-01 finding)"
    - "the stale-cache validation exercises the exact command the reworded caveat now leads with, so the recorded observation proves the documented instruction, not a different one (IN-01)"
---

<objective>
Close the single open gap from 25-VERIFICATION.md (DEBT-06, partial): the README plugin-cache caveat at README.md line 241 leads with `/plugin marketplace add jeremiewerner/motto` as its refresh instruction, but for this unpinned npm marketplace source that command operates on the marketplace manifest — which is byte-identical across releases — and cannot re-pull installed plugin content (25-REVIEW.md WR-01). Reword the caveat to lead with the installed-plugin refresh command (`claude plugin update motto@motto`), keep a full uninstall+reinstall as fallback, drop the marketplace-add-as-refresh framing, and add the restart-required nuance. Then validate the reworded guidance against a real stale-cache scenario (not a clean install) and record the observation (25-REVIEW.md IN-01).

Purpose: The phase's automated check (`grep -c 'plugin cache' README.md`) validated presence, not correctness — it passed while the primary instruction was very likely a no-op for the exact scenario the caveat targets. A stranger following the wrong command would see no change and conclude the docs are wrong, defeating the caveat's stated purpose. This plan makes the primary path correct and exercises it once for real.
Output: A reworded README caveat and a stale-cache validation addendum in 25-REWALK.md.

Scope fence: DEBT-07 and DEBT-08 are SATISFIED — do not touch them, no token/publishing-lock work here. The substantive re-walk evidence in 25-REWALK.md (build-skill visible, content-diff match) is verified — this plan appends a new addendum, it does not rewrite the existing record. No `src/` changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-v0-0-6-operator-debt-closure/25-CONTEXT.md
@.planning/phases/25-v0-0-6-operator-debt-closure/25-REVIEW.md
@.planning/phases/25-v0-0-6-operator-debt-closure/25-VERIFICATION.md
@.planning/phases/25-v0-0-6-operator-debt-closure/25-REWALK.md
@README.md
@.claude-plugin/marketplace.json
</context>

<artifacts_this_phase_produces>
Symbols and identifiers created/modified by this plan (new here — exclude from source-drift acknowledgement):

- README.md: the existing plugin-cache caveat blockquote at line 241, reworded so its primary instruction is the installed-plugin update command (`claude plugin update motto@motto`) with uninstall+reinstall as fallback and a restart-required note; the marketplace-add-as-refresh framing is removed. Still a blockquote sibling to the `> **Prerequisite:**` callout; still contains the grep-verifiable literal `plugin cache`.
- .planning/phases/25-v0-0-6-operator-debt-closure/25-REWALK.md: a new "Stale-Cache Validation" addendum section recording the one-time exercise of the reworded refresh guidance against a real stale-cache scenario (append-only; the existing DEBT-06 re-walk record is preserved unchanged).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reword the README plugin-cache caveat to lead with the working refresh command</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 237-262 — "Add the marketplace to Claude Code" and "Install Motto's skills" sections; line 241 is the current caveat blockquote to reword; the `> **Prerequisite:**` blockquote at line 239 is the sibling-callout convention to preserve)
    - .planning/phases/25-v0-0-6-operator-debt-closure/25-REVIEW.md (WR-01 — the exact defect and the reviewer's suggested corrected wording; note the suggested fix text as the target state for the primary command, fallback, and restart nuance)
    - .claude-plugin/marketplace.json (source: npm `@jeremiewerner/motto`, unpinned — the reason marketplace-add cannot refresh installed content; the plugin/marketplace name pair is motto@motto)
  </read_first>
  <action>
    Rewrite ONLY the caveat blockquote at README.md line 241 (the `> **Note:** ...` line whose current text ends with re-pulling the current published tarball). Keep it a single blockquote line beginning with `> **Note:**`, sibling to the `> **Prerequisite:**` callout directly above it. The reworded caveat must:
    1. Keep the accurate opening: Claude Code caches installed plugin content locally, so an already-installed plugin can appear stale (not reflecting the latest published npm version) until the plugin cache is refreshed. Retain the literal string `plugin cache` verbatim so the grep gate stays green.
    2. Lead the remediation with the installed-plugin update command as the primary instruction: `claude plugin update motto@motto` (the command that actually re-pulls installed plugin content for the motto plugin from the motto marketplace).
    3. Keep a full uninstall-then-reinstall of the plugin as the documented fallback (phrase it as the fallback, e.g. "or uninstall and reinstall the plugin").
    4. State the restart-required nuance carried over from the Claude Code CLI's own help text: the update requires restarting Claude Code to apply.
    5. Remove the marketplace-add command from the refresh instruction entirely — the reworded caveat must NOT present re-running the marketplace-add command as a way to force a content refresh. (The marketplace-add command still appears in its own fenced code block at line 244 as the normal install step — leave that fenced block untouched; only the caveat's refresh guidance changes.)
    Keep the edit strictly scoped to this one blockquote — do not reflow, rewrite, or touch the `> **Prerequisite:**` callout, the fenced `/plugin marketplace add` block, the "Install Motto's skills" section, or the skill table.
  </action>
  <verify>
    <automated>cd /Users/jeremie/Projects/motto && grep -q 'plugin cache' README.md && grep -q 'claude plugin update motto@motto' README.md && awk 'NR==241 && /marketplace add/ {found=1} END {exit found}' README.md && echo PASS</automated>
  </verify>
  <done>The README.md line-241 caveat leads with `claude plugin update motto@motto`, documents uninstall+reinstall as fallback, notes restart-required, retains the `plugin cache` literal, and no longer presents the marketplace-add command as a refresh mechanism; the fenced marketplace-add install block and all surrounding prose are unchanged.</done>
  <acceptance_criteria>
    - `grep -c 'plugin cache' README.md` returns at least 1 (grep gate stays green).
    - `grep -c 'claude plugin update motto@motto' README.md` returns at least 1 (the working primary command is now present).
    - The caveat blockquote text contains a restart reference (the word `restart` appears in the reworded blockquote).
    - The caveat blockquote text contains a fallback that reinstalls the plugin (the word `reinstall` appears in the reworded blockquote).
    - The caveat blockquote line no longer contains the marketplace-add command as its refresh instruction: the reworded `> **Note:**` blockquote line does not tell the reader to re-run the marketplace-add command to refresh. (The fenced `/plugin marketplace add jeremiewerner/motto` install block at line ~244 remains, unchanged.)
    - `git diff README.md` shows changes confined to the single caveat blockquote line — no edits to the `> **Prerequisite:**` callout, the fenced install blocks, or the skill table.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Validate the reworded refresh guidance against a real stale-cache scenario and record it</name>
  <files>.planning/phases/25-v0-0-6-operator-debt-closure/25-REWALK.md</files>
  <read_first>
    - README.md (the reworded caveat from Task 1 — the exact command(s) the maintainer must exercise)
    - .planning/phases/25-v0-0-6-operator-debt-closure/25-REVIEW.md (IN-01 — the validation the reviewer asks for: reproduce a stale cache, run the documented command, confirm content refreshes; a one-line addendum to 25-REWALK.md is sufficient)
    - .planning/phases/25-v0-0-6-operator-debt-closure/25-REWALK.md (existing DEBT-06 record — append a new "Stale-Cache Validation" section; do not rewrite prior content)
  </read_first>
  <action>
    Blocking human checkpoint (requires a real stale-cache scenario in a live Claude Code session — there is no agent-reachable CLI surface for Claude Code's local plugin cache from this session, so this is maintainer-attested, mirroring the checkpoint pattern already used in 25-02-PLAN.md Task 2). The maintainer reproduces a genuinely stale installed plugin — i.e. an installed `motto@motto` plugin whose local content lags the current published `@jeremiewerner/motto` npm tarball (for example: with an older version installed and a newer version published, or by installing then observing the next release). The maintainer then runs the caveat's now-primary documented command `claude plugin update motto@motto`, restarts Claude Code as the caveat instructs, and confirms the installed plugin content refreshes to match the current published tarball (for example, a skill that was missing or outdated now appears/updates). If `claude plugin update` does not achieve the refresh, the maintainer exercises the documented uninstall+reinstall fallback and records which path worked. The maintainer appends a "Stale-Cache Validation" section to 25-REWALK.md recording: the stale condition reproduced, the exact command(s) run, whether the content refreshed, and the date — leak-safe (no credential values), matching the existing record style. If the reworded guidance does NOT refresh the content by any documented path, the maintainer notes the failure and the caveat must be corrected in-phase before this gap is considered closed (do not mark the debt closed on a documented-but-broken instruction).
  </action>
  <what-built>
    Task 1 reworded the caveat so its primary instruction (`claude plugin update motto@motto`) is the command that actually re-pulls installed plugin content. This checkpoint closes 25-REVIEW.md IN-01: the phase's original re-walk exercised only a clean install, so the refresh guidance's one behavioral claim was never observed working. This exercises the reworded command against a real stale cache and records the observation.
  </what-built>
  <verify>
    <human-check>25-REWALK.md contains a new "Stale-Cache Validation" section recording a reproduced stale-cache condition, the documented command(s) run (leading with `claude plugin update motto@motto`), and a confirmation that the installed plugin content refreshed (or, if the primary command did not, that the documented reinstall fallback did) — plus the date.</human-check>
  </verify>
  <done>The reworded refresh guidance has been exercised once against a real stale-cache scenario, the installed content was confirmed to refresh via a documented command, and 25-REWALK.md records the stale condition, the command(s) run, the outcome, and the date — closing the IN-01 substance-unverified gap.</done>
  <how-to-verify>
    1. Reproduce a stale installed `motto@motto` plugin: install the plugin at a version older than the current published `@jeremiewerner/motto` npm tarball (or install, then publish/observe a newer release), so the installed content lags npm `latest`.
    2. Confirm the plugin is genuinely stale — a skill or content you expect from the newer published tarball is missing or outdated in the installed plugin.
    3. Run the caveat's primary documented command `claude plugin update motto@motto`, then restart Claude Code as the caveat instructs.
    4. Confirm the installed plugin content now matches the current published tarball (the previously-missing/outdated skill content is present/updated). If it did not refresh, run the documented uninstall+reinstall fallback and confirm which path worked.
    5. Append the observation (stale condition, command(s) run, outcome, date) to 25-REWALK.md as a "Stale-Cache Validation" section.
  </how-to-verify>
  <resume-signal>Type "approved — stale cache refreshed via the documented command, recorded in 25-REWALK.md" once the reworded guidance is confirmed working against a real stale cache, or describe which command was needed / what failed.</resume-signal>
  <acceptance_criteria>
    - `25-REWALK.md` contains a "Stale-Cache Validation" section (new; the prior DEBT-06 record above it is unchanged).
    - That section records: (a) the stale-cache condition reproduced (installed content lagging npm `latest`), (b) the documented command(s) run — leading with `claude plugin update motto@motto`, (c) the outcome (content refreshed to match the published tarball), and (d) the date.
    - The section contains no credential values (leak-safe, matching the existing record style — no `npm_`/`ghp_` token patterns).
    - If the primary command did not refresh the content, the section records that the documented reinstall fallback was needed and that it worked (the caveat's fallback is proven, not just its primary path).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README documented instruction → stranger's actions | A stranger with a stale cache follows the caveat's primary instruction verbatim; if that instruction is a no-op, the caveat's stated purpose fails silently. The rewording + one-time real validation is the check across this boundary. |
| Claude Code plugin cache → installed plugin content | A stale local cache masks the current published tarball; the documented refresh command must actually re-pull content, not just refresh marketplace manifest metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07 | Tampering / Repudiation | README caveat documents a refresh command that does not refresh installed content (the WR-01 defect) | high | mitigate | Task 1 rewords the caveat to lead with `claude plugin update motto@motto` (the command that re-pulls installed content) and removes the marketplace-add-as-refresh framing; Task 2 exercises it once against a real stale cache to prove the documented instruction works |
| T-25-08 | Repudiation | Grep gate passes on caveat presence while the substantive refresh claim is unverified (the IN-01 "tests-green-but-broken" shape) | medium | mitigate | Task 2 records a real stale-cache validation observation in 25-REWALK.md, converting the caveat from presence-only to substance-verified |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages added; the only install exercised is the already-published, provenance-attested `@jeremiewerner/motto` tarball via the documented refresh path |
</threat_model>

<verification>
- `grep -c 'plugin cache' README.md` returns at least 1 (gate stays green after rewording).
- `grep -c 'claude plugin update motto@motto' README.md` returns at least 1 (working primary command present).
- The README caveat blockquote no longer presents the marketplace-add command as a refresh mechanism, contains a `restart` note, and documents a `reinstall` fallback.
- `25-REWALK.md` contains a "Stale-Cache Validation" section recording a reproduced stale cache, the documented command run, the refresh outcome, and the date — leak-safe.
</verification>

<success_criteria>
- The README plugin-cache caveat leads with the installed-plugin refresh command that actually works (`claude plugin update motto@motto`), keeps uninstall+reinstall as fallback, notes restart-required, and no longer presents marketplace-add as the refresh path (closes 25-REVIEW.md WR-01).
- The reworded guidance has been exercised once against a real stale-cache scenario and the observation is recorded in 25-REWALK.md (closes 25-REVIEW.md IN-01).
- DEBT-07 and DEBT-08 are untouched; no `src/` changes; the existing 25-REWALK.md DEBT-06 record is preserved unchanged (append-only).
</success_criteria>

<output>
Create `.planning/phases/25-v0-0-6-operator-debt-closure/25-03-SUMMARY.md` when done (record the README caveat rewording and the stale-cache validation result — never any credential value).
</output>
</content>
</invoke>
