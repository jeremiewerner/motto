---
phase: 23-version-stamping-skew-detection
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/init.js
  - test/init.test.js
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "motto init writes a motto.yaml whose mottoVersion equals the running tool's live version, distinct from the project version field"
    - "If getOwnVersion() returns null, the mottoVersion line is omitted entirely (never mottoVersion: null / empty)"
    - "This repo's own root motto.yaml is NOT modified by this plan (stays unstamped through Phase 23, D-R4)"
  artifacts:
    - src/init.js
    - test/init.test.js
  key_links:
    - "init.js imports getOwnVersion from ./version.js (mirrors NAME_KEBAB import from schema.js)"
---

<objective>
Stamp the running tool's version into newly-scaffolded projects: `src/init.js` imports `getOwnVersion()` and adds a `mottoVersion:` line to the `motto.yaml` scaffold template, distinct from the existing project `version` field. Prove via `test/init.test.js` that a fresh scaffold's `mottoVersion` equals the live tool version (read at test time, never hardcoded) (VER-01).

Purpose: A scaffolded project must carry a verifiable "built with Motto X" signal so later lint/build can detect skew. Only `init` writes the stamp (D-01) — this plan is the sole writer.
Output: modified `src/init.js`, extended `test/init.test.js`, fully green. This repo's own motto.yaml stays unstamped (D-R4).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md
@.planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md
@src/version.js
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 slice)

Modified `src/init.js`:
- New import edge: `getOwnVersion` from `./version.js`
- The scaffold `motto.yaml` template string gains a `mottoVersion: "<version>"` line, emitted only when getOwnVersion() returns a non-null string.

Modified `test/init.test.js`:
- New assertion(s) that a scaffolded project's motto.yaml contains a mottoVersion matching the live package.json version, and that mottoVersion is distinct from the project version field.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp mottoVersion into the init.js scaffold template (VER-01, D-01)</name>
  <files>src/init.js</files>
  <read_first>
    - src/init.js (full — the writeScaffold motto.yaml template string around lines 145-151, the existing `import { NAME_KEBAB } from './schema.js';` around line 30, and the never-throw resolveGitOwnerName pattern around lines 94-104)
    - src/version.js (getOwnVersion signature — the import target)
    - .planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md (architecture diagram scaffold section lines 68-78; null-fallback omission rule)
    - .planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md (src/init.js MODIFIED section lines 98-114 — template to extend, null-fallback omission, D-R4 do-not-touch-own-tree note)
  </read_first>
  <behavior>
    - After scaffoldProject writes motto.yaml, the file contains a `mottoVersion:` line whose value equals getOwnVersion()'s return, on its own line, distinct from the `version:` project-version line.
    - When getOwnVersion() returns null (defensive), the motto.yaml contains NO mottoVersion line at all (not `mottoVersion: null`, not an empty value) — scaffold still succeeds.
    - The existing `version: "0.1.0"` project-version line and all other template lines remain unchanged.
  </behavior>
  <action>
    Add `import { getOwnVersion } from './version.js';` alongside the existing schema import in src/init.js. At scaffold-template-build time, compute the tool version once via getOwnVersion(); build the mottoVersion line conditionally — when the value is a non-null string, include a `mottoVersion: "<value>"` line in the motto.yaml template; when null, omit the line entirely (never write `mottoVersion: null` or an empty string — matches the "absence is valid" contract config.js now enforces). Place the mottoVersion line adjacent to the existing project `version` line but keep the two fields clearly distinct (mottoVersion = tool version, version = project version — never conflate). Do NOT touch this repository's own root motto.yaml; only NEW scaffolds get stamped (D-R4). Keep the write never-throw: getOwnVersion already never throws, so no new try/catch is needed around it.
  </action>
  <verify>
    <automated>node -e "import('node:fs/promises').then(async fs => { const os = await import('node:os'); const path = await import('node:path'); const { scaffoldProject } = await import('./src/init.js'); const { getOwnVersion } = await import('./src/version.js'); const dir = await fs.mkdtemp(path.join(os.tmpdir(), 'motto-init-stamp-')); try { await scaffoldProject(dir, { name: 'stamp-proj' }); const y = await fs.readFile(path.join(dir, 'motto.yaml'), 'utf8'); const v = getOwnVersion(); const ok = y.includes('mottoVersion') && y.includes(v) && /version:/.test(y); process.exit(ok ? 0 : 1); } finally { await fs.rm(dir, { recursive: true, force: true }); } })"</automated>
  </verify>
  <acceptance_criteria>
    - src/init.js imports getOwnVersion from './version.js'.
    - A scaffolded project's motto.yaml contains a `mottoVersion` line whose value equals getOwnVersion() (live, not a literal).
    - The scaffold still contains the distinct project `version:` line.
    - The inline verify command exits 0.
    - This repo's own root motto.yaml is unchanged (no mottoVersion added to /Users/jeremie/Projects/motto/motto.yaml).
  </acceptance_criteria>
  <done>init.js stamps mottoVersion into new scaffolds matching the live tool version; null-fallback omits the line; own tree untouched; verify command exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert init stamp in test/init.test.js (VER-01, live version, no literal)</name>
  <files>test/init.test.js</files>
  <read_first>
    - test/init.test.js (existing mkdtemp scaffold fixture pattern and scaffoldProject usage — mirror it)
    - src/init.js (the just-added stamping behavior)
    - src/version.js (getOwnVersion — used to derive the expected value live)
    - .planning/phases/23-version-stamping-skew-detection/23-RESEARCH.md (Pitfall 6 lines 325-330; init.test.js additions note in the structure diagram)
    - .planning/phases/23-version-stamping-skew-detection/23-PATTERNS.md ("Never hardcode version literals in tests" lines 257-259)
  </read_first>
  <behavior>
    - A scaffolded project's motto.yaml, when read, contains a mottoVersion value equal to getOwnVersion() (or to a live read of package.json's version) — asserted by relationship, never against a literal.
    - The mottoVersion value is distinct from / independent of the scaffold's project `version` field.
  </behavior>
  <action>
    Add a test (or extend an existing scaffold test) in test/init.test.js: scaffold into a mkdtemp dir, read motto.yaml, assert it contains a mottoVersion whose value equals getOwnVersion() (import it, or read package.json live in the test). Assert the mottoVersion value is present and matches the live tool version by relationship — use assert.ok / assert.match / equality against the live value, NEVER assert.strictEqual against '0.0.6'/'0.0.7'. Optionally parse the YAML to assert mottoVersion and version are separate keys. Clean up the temp dir in a finally block per the existing convention.
  </action>
  <verify>
    <automated>node --test test/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/init.test.js` passes with zero failures.
    - The new/extended test derives the expected version live (getOwnVersion() or a package.json read), not a hardcoded string.
    - `grep -nE '"0\.0\.[0-9]"' test/init.test.js` shows no new hardcoded Motto-version literal in assertions.
    - The test asserts mottoVersion is present in the scaffolded motto.yaml and equals the live tool version.
  </acceptance_criteria>
  <done>test/init.test.js green; stamp asserted against the live version by relationship; no version literal introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getOwnVersion() → scaffold write | The tool version (or null) is interpolated into a generated motto.yaml; a null must omit the field cleanly rather than write invalid YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-06 | Tampering (data integrity) | conflating tool version with project version in the scaffold | medium | mitigate | mottoVersion emitted as a distinct field from `version`; test asserts both keys exist independently; RESEARCH Runtime State Inventory confirms mottoVersion must never flow into project version |
| T-23-07 | Denial of Service | null tool version producing invalid YAML | low | mitigate | Conditional emission omits the line entirely when getOwnVersion() is null — never writes `mottoVersion: null` |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages this phase (D-06) — no install task; nothing to audit |
</threat_model>

<verification>
- `node --test test/init.test.js` green.
- Inline scaffold smoke check (Task 1) exits 0.
- This repo's own motto.yaml remains unstamped (`grep -c mottoVersion motto.yaml` returns 0).
</verification>

<success_criteria>
- motto init stamps the running tool's live version as a distinct mottoVersion field (VER-01).
- Null-safe omission preserves valid scaffolds.
- Own tree stays unstamped for the no-stamp fixture in Plan 04 (D-R4).
</success_criteria>

<output>
Create `.planning/phases/23-version-stamping-skew-detection/23-03-SUMMARY.md` when done
</output>
